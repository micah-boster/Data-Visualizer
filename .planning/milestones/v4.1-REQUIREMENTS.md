# Requirements: v4.1 Feedback-Driven Polish

**Defined:** 2026-04-19
**Core Value:** Close the gap between "the app has features" and "the app is daily-useful." First-week feedback surfaced correctness bugs, UX friction, and a structural gap (per-partner customization) that blocks several downstream features.

## Theme

The polish + correctness pass that makes v4.0 actually usable day-to-day, plus the single foundational feature (Partner Config) that unlocks product-type pre-population, sub-cohort analysis (Snap EN/ES, Happy Money banks), and segment-aware projections in v5.0.

## Polish (Phase 38 — small, visible fixes)

- [x] **POL-01**: Bounce logo asset replaces the hardcoded "B" letter-mark in the sidebar header
- [x] **POL-02**: Partners list in the sidebar is collapsible with a chevron toggle and pre-collapsed on first load
- [x] **POL-03**: Column picker no longer locks identity columns (PARTNER_NAME, LENDER_ID, BATCH, BATCH_AGE_IN_MONTHS) — they remain defaults via `getDefaultVisibility()` but users can toggle them off
- [x] **POL-04**: `formatPercentage` uses 2 decimals when `|value| < 10`, 1 decimal when `|value| >= 10` (e.g., 9.72% but 10.3%) — applied wherever percentages render (KPI cards, table cells, chart tooltips)
- [x] **POL-05**: Heatmap toggle has a tooltip explaining that it colors cells by deviation from the partner's norm range
- [x] **POL-06**: Column header labels render with a consistent truncation strategy — no mid-word chops, no jarring multi-line wraps. Long headers truncate with ellipsis at a readable max-width, and the full label is available on hover (native `title` attribute or tooltip). Header row height is stable across columns regardless of label length. Applies to `draggable-header.tsx` (the primary offender at `overflow-hidden` without a text-truncation rule) and any other header surface in the app.

## Chart Correctness (Phase 38)

- [x] **CHT-01**: Chart x-axis domain AND average-series line both clip to the max age of currently-displayed vintages — no empty whitespace extending to month 60 when the oldest visible batch is 4 months old. If the user shows 8 recent vintages aged 0-5 months, the x-axis runs 0-5, and the avg line stops at 5; "show all" extends both to the oldest visible vintage. The chart renders ASAP with whatever data exists — no minimum-age gate before displaying curves.
- [x] **CHT-02**: Chart proximity hover tooltip includes the partner-average line (`__avg__` series), not just individual batch lines
- [x] **CHT-03**: Curve legend has a `max-h-[calc(100vh-Xpx)]` + `overflow-y-auto` so "show all" scrolls within viewport instead of overflowing off-screen
- [x] **CHT-04**: On laptop-height viewports (≤900px), the chart and table both remain usable when the chart panel is expanded — neither collapses to zero height or requires scrolling the entire page to reach the other

## KPI Improvements (Phase 38)

- [x] **KPI-01**: KPI cards render a graduated cascade based on the cohort's max batch age — the app shows something meaningful from day one rather than gating on 12 months of data. Rules:
  - **< 3 months**: one card — "Rate since inception" (collection rate on everything placed so far, regardless of age)
  - **3 to < 6 months**: two cards — "3mo rate" + "Rate since inception"
  - **6 to < 12 months**: two cards — "3mo rate" + "6mo rate"
  - **≥ 12 months**: two cards — "6mo rate" + "12mo rate"

  The cascade is driven by max batch age in the current scope (partner + product + filters), not a global setting. Cards that don't apply at the current age are not rendered — no "insufficient data" placeholder tiles.
- [x] **KPI-02**: KPI delta labels explicitly state the comparison baseline (e.g., "vs 3-batch rolling avg" rather than an unlabeled percentage)
- [ ] **KPI-03** *(Deferred to Phase 41 — requires new Snowflake column `ACCOUNTS_WITH_PLANS_AT_{6,12}_MONTH`; `TOTAL_ACCOUNTS_WITH_PLANS` today is lifetime. See `.planning/phases/38-polish-correctness-pass/38-CONTEXT.md`.)*: 6mo and 12mo rate cards display the matching-horizon commitment rate as a secondary line (shown only when the corresponding rate card is shown per KPI-01's cascade)
- [x] **KPI-04**: Rolling-average deltas are suppressed when the cohort is too young to support a meaningful rolling avg at the card's horizon (e.g., a 3-batch rolling average of 6mo rate requires at least 3 prior batches that have reached 6 months of age). When suppressed, the card shows the rate value with no delta indicator — no misleading "vs N/A" or "0.0%" labels. Once Phase 40 (Projected Curves v1) ships, "vs projected curve" becomes the natural fallback comparison for early batches (PRJ-04).

## Filters (Phase 38)

- [x] **FLT-01**: The batch filter is replaced with a date-range filter (the batch filter was structurally broken because batch names are unique per partner and don't cross-filter)
- [x] **FLT-02**: Each filter (partner, account type, date range) has an inline help tooltip explaining what it filters
- [x] **FLT-03**: When the sidebar is scoped to a single partner, the PARTNER_NAME column in the main table auto-hides (eliminating the duplication between sidebar and table column)

## Metabase Import Polish (Phase 38)

**Context**: Phase 37 shipped a heuristic chart-type inference inside `chart-inference.ts` — the parser guesses (GROUP BY + aggregate → bar, time-series X + numeric Y → line, else no chart). SQL alone cannot carry Metabase's visualization setting because Metabase stores `display` + `visualization_settings` in a separate card JSON, not in the SQL string. First-week feedback: users expected chart type to round-trip from their Metabase card; the heuristic is right often but not always, and there's no user recourse when it's wrong.

- [x] **MBI-01**: The Metabase Import Preview step exposes a chart-type override control in the Chart section. The heuristic-inferred type pre-selects; the user can pick line / scatter / bar / none from a dropdown (mirroring `ChartBuilderToolbar`'s chart-type segmented control from Phase 36 so the affordance is consistent) before clicking Apply. The override also lets the user pick X/Y axes when the heuristic leaves them ambiguous. The Chart section shows the inference reason as helper text ("Inferred from GROUP BY + AVG aggregate") so users know why the default was chosen.

## Partner Config (Phase 39 — foundational feature)

**Principle**: Blending metrics across a partner's product lines (e.g., Happy Money 1st-party + 3rd-party in a single view) produces misleading numbers because the economics, recovery curves, and rates are structurally different. The app must enforce per-product scoping as the default — product-splitting is a correctness fix, not a user preference. Segment-splitting (Snap EN/ES, Happy Money banks within a single product) is the user-configurable layer on top.

- [ ] **PCFG-01**: Product type is derived from Snowflake's `ACCOUNT_TYPE` column — no config record, no editable field. The canonical unit of analysis across the app is the `(PARTNER_NAME, ACCOUNT_TYPE)` pair, not `PARTNER_NAME` alone.
- [ ] **PCFG-02**: The sidebar partner list shows one row per `(partner, product)` pair derived from `SELECT DISTINCT PARTNER_NAME, ACCOUNT_TYPE`. Single-product partners look identical to today; multi-product partners split into N rows (e.g., "Happy Money — 1st Party", "Happy Money — 3rd Party").
- [ ] **PCFG-03**: Partner selection state carries both `partner` and `product`. All downstream scoping — KPIs, charts, table, drill-downs, filters, Partner Lists — keys off the pair. Nothing in the app aggregates across products for a single partner.
- [ ] **PCFG-04**: No UI path produces a cross-product blended view for a single partner. Blending is blocked at the selection layer; it is not a user-controllable toggle. (Cross-*partner* aggregation is unchanged — different partners remain independent, blendable entities.)
- [ ] **PCFG-05**: Each `(partner, product)` pair has an editable segment list (named sub-cohorts like "English", "Spanish", "Bank A", "Bank B"), persisted in localStorage. Partner Setup UI reachable from the sidebar partner context menu edits only segments — product type is not editable because it's data-derived.
- [ ] **PCFG-06**: `PartnerListFilters` is extended with `PRODUCT_TYPE` (auto-derived from `ACCOUNT_TYPE`) and `SEGMENT` (from config) attribute filters. Pre-populated Partner Lists auto-maintain one list per distinct product-type value, derived from data.
- [ ] **PCFG-07**: Charts and KPIs accept an optional segment dimension — when a `(partner, product)` pair has segments configured, the user can choose to split curves/KPIs by segment instead of rolling up. Pairs without segments configured render as the pair-rolled-up view (segment-level zero-regression — but note: pair-level splitting is NOT opt-in, it's enforced).

## Projected Curves v1 (Phase 40)

- [x] **PRJ-01**: Collection curve chart renders a projected-trajectory line alongside actual batch curves, computed from the rolling historical average of that partner's prior batches at each age — *Pivoted by Phase 40 CONTEXT to per-batch modeled curves from `BOUNCE.FINANCE.CURVES_RESULTS.PROJECTED_FRACTIONAL`. Plan 40-01 landed the data pipeline (route + hook + `BatchCurve.projection` merge); Plan 40-02 lands the chart render.*
- [ ] **PRJ-02**: The projection extends from the last actual data point to the max-age of the displayed vintages (same truncation contract as CHT-01)
- [ ] **PRJ-03**: Projection is visually distinct from actuals (dashed line, subdued color, labeled "Projected")
- [ ] **PRJ-04**: KPI cards that currently show "vs 3-batch rolling avg" gain an option to show "vs projected curve" — comparison baseline selectable per card or via a panel-level toggle
- [ ] **PRJ-05**: When a partner has segments configured (PCFG-01), projections compute per segment when the segment split is active

## Future Requirements (deferred to v4.5+ and v5.0)

- **PRJ-06**: Confidence bands on projections — v5.0 Phase 49 (née 42) Dynamic Curve Re-Projection
- **PRJ-07**: Target-anchored projections ("to hit target") — v5.0 Phase 49 (depends on target data from Phase 42 née 39)
- **PRJ-08**: Partner-reported trajectory overlays — v5.0 Phase 49
- **PCFG-08**: Snowflake-backed partner config storage with change history — defer until localStorage-based v1 is proven in daily use
- **PCFG-09**: Bulk partner config import (CSV/Excel) — defer until config schema stabilizes
- **MBI-02**: Accept Metabase card JSON export (paste the full card definition, not just SQL) for literal `display` + `visualization_settings` round-trip. Requires mapping Metabase's visualization schema to our `ChartDefinition` shape and deciding UX for the two paste modes (SQL vs card JSON). Defer until MBI-01's override control proves insufficient in real use — the heuristic + override covers ~90% of the value at ~10% of the scope.
- **MBI-03**: Metabase card URL paste — paste the Metabase card URL, the app fetches the card via a proxy and reads both SQL and display config. Requires auth handling, a network path, and Metabase API understanding. Likely v5.0+ when the external-data pipeline work lands.

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| POL-01 | 38-01 | Complete |
| POL-02 | 38-01 | Complete |
| POL-03 | 38-02 | Complete |
| POL-04 | 38-02 | Complete |
| POL-05 | 38-02 | Complete |
| POL-06 | 38-02 | Complete |
| CHT-01 | 38-03 | Complete |
| CHT-02 | 38-03 | Complete |
| CHT-03 | 38-03 | Complete |
| CHT-04 | 38-03 | Complete |
| KPI-01 | 38 | Complete |
| KPI-02 | 38 | Complete |
| KPI-03 | 41 | Deferred |
| KPI-04 | 38 | Complete |
| FLT-01 | 38-05 | Complete |
| FLT-02 | 38-05 | Complete |
| FLT-03 | 38-05 | Complete |
| MBI-01 | 38-05 | Complete |
| PCFG-01 | 39 | Pending |
| PCFG-02 | 39 | Pending |
| PCFG-03 | 39 | Pending |
| PCFG-04 | 39 | Pending |
| PCFG-05 | 39 | Pending |
| PCFG-06 | 39 | Pending |
| PCFG-07 | 39 | Pending |
| PRJ-01 | 40 | Data pipeline complete (Plan 40-01 → 2026-04-25); chart render + REQUIREMENTS wording re-sync remain in Plans 40-02 + 40-03 |
| PRJ-02 | 40 | Pending |
| PRJ-03 | 40 | Pending |
| PRJ-04 | 40 | Pending |
| PRJ-05 | 40 | Pending |

**Coverage:**
- v4.1 requirements: 30 total
- Mapped to phases: 30
- Unmapped: 0

## Dependency Note

- Phase 38 is independent of 39 and 40 — ships first as a batched polish pass
- Phase 39 (Partner Config) is independent of Phase 40 — either can ship first
- Phase 40 (Projected Curves) is stronger when Phase 39 is done (PRJ-05 segment-aware projections) but degrades gracefully without it
- v5.0 Phase 49 (formerly Phase 42) Dynamic Curve Re-Projection extends Phase 40 with target-based, partner-reported, and confidence-band variants

---
*Requirements defined: 2026-04-19 — Sourced from first-week daily-use feedback after v4.0 partner lists + chart builder shipped*
*Updated: 2026-04-23 — Added MBI-01 (Metabase Import chart-type override) to Phase 38 + MBI-02/MBI-03 deferred; sourced from Phase 37 close-out feedback "are we going to have the metabase query pull in the graph type too?"*
