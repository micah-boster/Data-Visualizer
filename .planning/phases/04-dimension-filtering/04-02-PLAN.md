---
phase: 04-dimension-filtering
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/lib/table/hooks.ts
  - src/components/table/data-table.tsx
  - src/components/data-display.tsx
  - src/app/page.tsx
  - src/components/filters/filter-chips.tsx
  - src/components/filters/filter-empty-state.tsx
autonomous: true
requirements:
  - FILT-04
  - FILT-05
must_haves:
  truths:
    - "When multiple filters are active, only rows matching ALL filters are shown in the table"
    - "Each active filter appears as a chip showing dimension label and value with an X to remove"
    - "Clicking X on a chip removes that filter and the table updates immediately"
    - "A Clear all link appears when any filters are active and removes all filters at once"
    - "When filters produce zero results, a No results match message with Clear filters link is shown"
    - "Filter state survives page refresh via URL params"
  artifacts:
    - path: "src/components/filters/filter-chips.tsx"
      provides: "Active filter chip row with Clear all link"
      exports: ["FilterChips"]
    - path: "src/components/filters/filter-empty-state.tsx"
      provides: "Zero-results message with Clear filters link"
      exports: ["FilterEmptyState"]
    - path: "src/lib/table/hooks.ts"
      provides: "Updated useDataTable with columnFilters and getFilteredRowModel"
      exports: ["useDataTable"]
  key_links:
    - from: "src/components/table/data-table.tsx"
      to: "src/hooks/use-filter-state.ts"
      via: "useFilterState hook providing columnFilters to useDataTable"
      pattern: "useFilterState"
    - from: "src/lib/table/hooks.ts"
      to: "@tanstack/react-table getFilteredRowModel"
      via: "getFilteredRowModel() in table config"
      pattern: "getFilteredRowModel"
    - from: "src/app/page.tsx"
      to: "react Suspense"
      via: "Suspense boundary wrapping DataDisplay for useSearchParams"
      pattern: "Suspense"
---

<objective>
Wire filter state into TanStack Table for AND-composed row filtering, add filter chips for visibility/removal, handle zero-results state, and add Suspense boundary for useSearchParams.

Purpose: Completes the filtering feature by connecting the UI controls (Plan 04-01) to actual table row filtering with proper visual feedback and URL persistence.
Output: Working end-to-end filtering with chips, empty state, and Suspense boundary.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dimension-filtering/04-RESEARCH.md
@.planning/phases/04-dimension-filtering/04-01-SUMMARY.md

@src/lib/table/hooks.ts
@src/components/table/data-table.tsx
@src/components/data-display.tsx
@src/app/page.tsx

<interfaces>
<!-- From Plan 04-01 outputs (expected contracts) -->

From src/hooks/use-filter-state.ts:
```typescript
export function useFilterState(): {
  columnFilters: ColumnFiltersState;
  setFilter: (param: string, value: string | null, data?: Record<string, unknown>[]) => void;
  clearAll: () => void;
  activeFilters: { param: string; columnId: string; label: string; value: string }[];
  searchParams: URLSearchParams; // read-only
};
```

From src/components/filters/filter-bar.tsx:
```typescript
interface FilterBarProps {
  data: Record<string, unknown>[];
  columnFilters: ColumnFiltersState;
  onFilterChange: (param: string, value: string | null) => void;
}
export function FilterBar({ data, columnFilters, onFilterChange }: FilterBarProps) { ... }
```

From @tanstack/react-table (existing):
```typescript
import { getFilteredRowModel, type ColumnFiltersState } from '@tanstack/react-table';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire columnFilters into useDataTable and update DataTable/DataDisplay</name>
  <files>src/lib/table/hooks.ts, src/components/table/data-table.tsx, src/components/data-display.tsx, src/app/page.tsx</files>
  <action>
**Update useDataTable (src/lib/table/hooks.ts):**
- Add `columnFilters` as a parameter: `useDataTable(data, columnFilters?: ColumnFiltersState)`.
- Import `getFilteredRowModel` and `ColumnFiltersState` from `@tanstack/react-table`.
- Add `columnFilters: columnFilters ?? []` to the `state` object in `useReactTable`.
- Add `getFilteredRowModel: getFilteredRowModel()` to the table config.
- Do NOT add `onColumnFiltersChange` — URL is the source of truth, not React state.
- TanStack Table applies all column filters with AND logic by default (locked decision FILT-04).

**Update DataTable (src/components/table/data-table.tsx):**
- Import `useFilterState` from `@/hooks/use-filter-state`.
- Call `useFilterState()` at the top of DataTable to get `columnFilters`, `setFilter`, `clearAll`, `activeFilters`.
- Pass `columnFilters` to `useDataTable(data, columnFilters)`.
- Import and render `FilterBar` from `@/components/filters/filter-bar` between the toolbar (preset tabs row) and the table container. Pass `data`, `columnFilters`, and a bound `onFilterChange` that calls `setFilter` with the data for cascade validation.
- Import and render `FilterChips` (Task 2) below FilterBar, above the table. Pass `activeFilters`, `onRemove` (calls `setFilter(param, null)`), and `onClearAll` (calls `clearAll()`).
- Import and render `FilterEmptyState` (Task 2) in place of the table when `table.getRowModel().rows.length === 0` AND `columnFilters.length > 0`. Pass `onClearFilters` calling `clearAll()`.

**Update DataDisplay (src/components/data-display.tsx):**
- No changes needed — DataDisplay passes `data.data` to DataTable which handles filtering internally.

**Update page.tsx (src/app/page.tsx):**
- Wrap `<DataDisplay />` in `<Suspense fallback={...}>` to satisfy Next.js 16 requirement for `useSearchParams`. Import `Suspense` from `react`.
- Use a simple loading fallback (e.g., the existing `<LoadingState />` component or a div with "Loading...").
- This prevents build failures from the "Missing Suspense boundary with useSearchParams" error (Common Pitfall 1 from research).
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && grep -c "getFilteredRowModel\|columnFilters" src/lib/table/hooks.ts && grep -c "useFilterState\|FilterBar\|FilterChips\|FilterEmptyState" src/components/table/data-table.tsx && grep -c "Suspense" src/app/page.tsx && echo "PASS"</automated>
  </verify>
  <done>useDataTable accepts columnFilters and applies getFilteredRowModel for AND-composed filtering. DataTable renders FilterBar and FilterChips above the table. page.tsx has Suspense boundary for useSearchParams. Zero-results shows FilterEmptyState instead of table.</done>
</task>

<task type="auto">
  <name>Task 2: Create FilterChips and FilterEmptyState components</name>
  <files>src/components/filters/filter-chips.tsx, src/components/filters/filter-empty-state.tsx</files>
  <action>
**FilterChips (src/components/filters/filter-chips.tsx):**

Props:
- `activeFilters: { param: string; columnId: string; label: string; value: string }[]`
- `onRemove: (param: string) => void`
- `onClearAll: () => void`

Render a horizontal flex row of chips when `activeFilters.length > 0`. If no filters active, render nothing (null).

Each chip:
- Shows `{label}: {value}` text (e.g., "Partner: Acme Corp").
- Has an X button (lucide-react `X` icon, size 14) that calls `onRemove(filter.param)`.
- Styled: `rounded-full bg-muted text-sm px-3 py-1 flex items-center gap-1.5` (locked decision).
- X button has `hover:bg-muted-foreground/20 rounded-full p-0.5` for hover feedback.

"Clear all" text link:
- Appears at the end of the chip row when any chips exist (locked decision).
- Styled as a text button: `text-sm text-muted-foreground hover:text-foreground underline-offset-4 hover:underline`.
- Calls `onClearAll()` on click.

Container: `flex items-center gap-2 flex-wrap px-2` — wraps if many chips (future multi-select readiness).

**FilterEmptyState (src/components/filters/filter-empty-state.tsx):**

Props:
- `onClearFilters: () => void`

Render centered message: "No results match your filters" with a "Clear filters" link below it (locked decision from CONTEXT.md).

- Container: `flex flex-col items-center justify-center py-16 text-center`
- Message: `text-muted-foreground text-sm`
- "Clear filters" link: `text-sm text-primary hover:underline cursor-pointer mt-2`, calls `onClearFilters()`.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && test -f src/components/filters/filter-chips.tsx && test -f src/components/filters/filter-empty-state.tsx && grep -c "Clear all\|onClearAll\|onRemove" src/components/filters/filter-chips.tsx && grep -c "No results\|Clear filters\|onClearFilters" src/components/filters/filter-empty-state.tsx && echo "PASS"</automated>
  </verify>
  <done>FilterChips renders a row of removable chips for each active filter with Clear all link. FilterEmptyState shows "No results match your filters" with Clear filters link. Chips show "Label: Value" format with X button (locked decision). Removing a chip resets the corresponding dropdown to placeholder (locked decision).</done>
</task>

</tasks>

<verification>
1. Selecting a partner filter reduces table rows to only that partner's data.
2. Adding an account type filter further narrows to rows matching BOTH partner AND account type (AND logic).
3. Each active filter shows as a chip with "Label: Value" text and X button.
4. Clicking X on a chip removes only that filter; table updates immediately.
5. "Clear all" link removes all active filters, restoring full dataset.
6. Filtering to zero results shows "No results match your filters" with "Clear filters" link.
7. Refreshing the page with filter params in URL restores the filter state.
8. `pnpm build` succeeds (Suspense boundary prevents useSearchParams build failure).
</verification>

<success_criteria>
- AND-composed filtering works via TanStack Table's getFilteredRowModel.
- Active filter chips are visible below filter bar, each removable with X.
- Clear all link removes all filters at once.
- Zero-results state shows friendly message with clear action.
- Suspense boundary around DataDisplay prevents Next.js build failure.
- Full round-trip: select filter -> URL updates -> table filters -> chip appears -> remove chip -> table restores.
</success_criteria>

<output>
After completion, create `.planning/phases/04-dimension-filtering/04-02-SUMMARY.md`
</output>
