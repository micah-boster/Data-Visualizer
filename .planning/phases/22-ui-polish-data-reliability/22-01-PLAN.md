---
phase: 22-ui-polish-data-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/charts/curve-tooltip.tsx
  - src/components/charts/collection-curve-chart.tsx
  - src/components/data-display.tsx
  - src/components/cross-partner/comparison-matrix.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Clicking a batch line on the curve chart shows that batch's data in the tooltip, not the first line's data"
    - "When no batch is soloed, tooltip still shows the first available entry (existing behavior preserved)"
    - "When chart is collapsed, a ~80px sparkline mini-preview shows the trend shape without axes or labels"
    - "Chart starts expanded at all levels; collapse state persists across navigation via localStorage"
    - "Comparison matrix card header has a descriptive title and hover tooltip explaining what it shows"
    - "View mode toggle buttons inside the matrix have clear labels"
  artifacts:
    - path: "src/components/charts/curve-tooltip.tsx"
      provides: "Tooltip filters payload by soloedBatch prop"
    - path: "src/components/charts/collection-curve-chart.tsx"
      provides: "Passes soloedBatch to CurveTooltip"
    - path: "src/components/data-display.tsx"
      provides: "Sparkline mini-preview when charts collapsed"
    - path: "src/components/cross-partner/comparison-matrix.tsx"
      provides: "Descriptive card title and hover tooltip"
  key_links:
    - from: "src/components/charts/collection-curve-chart.tsx"
      to: "src/components/charts/curve-tooltip.tsx"
      via: "soloedBatch prop passed through Tooltip content render"
      pattern: "soloedBatch.*CurveTooltip"
    - from: "src/components/data-display.tsx"
      to: "LineChart (Recharts)"
      via: "Sparkline rendered in collapsed state"
      pattern: "chartsExpanded.*LineChart"
---

<objective>
Fix chart tooltip to track the soloed/clicked batch line, add sparkline mini-preview when charts are collapsed, and improve comparison matrix card labeling.

Purpose: Addresses UI-01 (tooltip always shows first line), UI-02 (no way to collapse chart and still see trend), and UI-03 (comparison button/card unclear).
Output: Updated tooltip, sparkline collapse state, descriptive comparison matrix header.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ui-polish-data-reliability/22-CONTEXT.md
@.planning/phases/22-ui-polish-data-reliability/22-RESEARCH.md

@src/components/charts/curve-tooltip.tsx
@src/components/charts/collection-curve-chart.tsx
@src/components/charts/use-curve-chart-state.ts
@src/components/data-display.tsx
@src/components/cross-partner/comparison-matrix.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/charts/curve-tooltip.tsx:
```typescript
interface CurveTooltipProps {
  active?: boolean;
  payload?: Array<{ dataKey?: string; value?: number; color?: string }>;
  label?: number;
  keyMap: BatchKeyMap;
  metric: "recoveryRate" | "amount";
  batchAnomalies?: BatchAnomaly[];
}
export function CurveTooltip({ active, payload, label, keyMap, metric, batchAnomalies }: CurveTooltipProps)
export { CHART_COLORS };
```

From src/components/charts/use-curve-chart-state.ts:
```typescript
export function useCurveChartState(curves: BatchCurve[], batchAnomalies?: BatchAnomaly[])
// Returns: { soloedBatch, handleLineClick, metric, ... }
// soloedBatch is string | null — the dataKey of the clicked/soloed line
```

From src/components/data-display.tsx:
```typescript
// chartsExpanded state with localStorage persistence:
const [chartsExpanded, setChartsExpanded] = useState(() => {
  if (typeof window === 'undefined') return true;
  return localStorage.getItem('charts-expanded') !== 'false';
});
```

From src/components/cross-partner/comparison-matrix.tsx:
```typescript
type ViewMode = 'heatmap' | 'bar' | 'plain';
const VIEW_MODES: { key: ViewMode; label: string; Icon: typeof Grid3X3 }[] = [...]
export function PartnerComparisonMatrix()
// Card with CardHeader > CardTitle, view mode toggle buttons
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix curve tooltip to filter by soloed batch</name>
  <files>src/components/charts/curve-tooltip.tsx, src/components/charts/collection-curve-chart.tsx</files>
  <action>
  **In curve-tooltip.tsx:**
  1. Add `soloedBatch?: string | null` to `CurveTooltipProps` interface
  2. Replace the current payload selection logic (lines 54-57, `payload.find(p => p.value !== undefined)`) with:
     - When `soloedBatch` is set: filter payload to `payload.filter(p => p.dataKey === soloedBatch)`, then take the first entry with a defined value
     - When `soloedBatch` is null/undefined: keep existing behavior (`payload.find(p => p.value !== undefined)`)
  3. This ensures clicking a batch line updates the tooltip to show that specific batch's data

  **In collection-curve-chart.tsx:**
  1. The component already destructures `useCurveChartState` which returns `soloedBatch`. Find where `<CurveTooltip>` is rendered inside the `<Tooltip content={...}>` prop (around line 167-169).
  2. Pass `soloedBatch={soloedBatch}` to the `<CurveTooltip>` component.
  3. The `soloedBatch` value comes from `useCurveChartState` which is already called in this component — just thread it through.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>When a batch line is clicked (soloed), the tooltip shows only that batch's data. When no batch is soloed, tooltip behavior is unchanged (shows first available entry).</done>
</task>

<task type="auto">
  <name>Task 2: Add sparkline mini-preview to collapsed chart state</name>
  <files>src/components/data-display.tsx</files>
  <action>
  The chart collapse toggle already exists at root and partner levels (controlled by `chartsExpanded` state persisted to localStorage). Currently, when collapsed, the chart area is fully hidden. Add a sparkline mini-preview when collapsed.

  1. When `chartsExpanded` is false, render a minimal Recharts `<LineChart>` inside a ~80px height container instead of hiding charts entirely. The sparkline should:
     - Use `<ResponsiveContainer width="100%" height={80}>` wrapping a `<LineChart>`
     - Render only `<Line>` components — NO `<XAxis>`, `<YAxis>`, `<CartesianGrid>`, `<Tooltip>`, `<Legend>`
     - Use `dot={false}`, `strokeWidth={1}`, `isAnimationActive={false}`
     - Apply `opacity-60` to the container for a subtle preview look
     - Use the same data that the full chart would use (the pivoted curve data at root level, collection curves at partner level)

  2. At root level (when drill.level === 'root'), the sparkline shows the cross-partner trajectory lines (from `CrossPartnerTrajectoryChart` data).
  3. At partner level (when drill.level === 'partner'), the sparkline shows collection curve lines.
  4. Keep the same collapse toggle button (chevron icon) — just change what renders in the "collapsed" state from nothing to the sparkline.
  5. The sparkline is purely visual — no interactivity, no click handlers.

  **Important:** Follow the existing hydration-safe pattern for localStorage: `if (typeof window === 'undefined') return true` in the useState initializer.

  **Implementation approach:** Create a small `<ChartSparkline>` inline component (or extract to a separate file if it exceeds 30 lines) that accepts the chart data and renders the mini LineChart. Render it in the `!chartsExpanded` branch where currently nothing is shown.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>When charts are collapsed, a subtle ~80px sparkline preview shows the trend shape. Clicking the expand toggle restores the full interactive chart. State persists via localStorage across navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Improve comparison matrix card labeling and tooltips</name>
  <files>src/components/cross-partner/comparison-matrix.tsx</files>
  <action>
  Per CONTEXT.md decisions: rename to descriptive label, add hover tooltip, show inline context label when active, toggle button switches between enter/exit states.

  1. **Card title:** Change the `<CardTitle>` from its current text to "Partner Comparison" or "Compare Partners" — a descriptive title that communicates what the card shows before interaction.

  2. **Hover tooltip on card header:** Add a shadcn `<Tooltip>` (from `@/components/ui/tooltip`) wrapping the card title or an info icon next to it. Tooltip content: "Compare key metrics across all partners — view as heatmap, bar ranking, or plain table."

  3. **View mode button labels:** The VIEW_MODES array already has `label` fields ('Heatmap', 'Bar', 'Table'). Ensure the toggle buttons show both the icon AND the label text (not just icons). If they already show labels, leave as-is. If icon-only, add `<span className="ml-1">{label}</span>` next to the icon.

  4. **Inline context label:** When comparison mode is active (the matrix is visible), add a subtle text indicator like `<span className="text-xs text-muted-foreground">Comparing {N} partners</span>` below the title or in the card header area.

  Do NOT add a separate enter/exit toggle — the matrix is always visible in the charts area per the research finding. The "toggle" aspect applies to the chart collapse button (already handled in Task 2).
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Comparison matrix card has descriptive "Partner Comparison" title with info tooltip explaining what it shows. View mode buttons show icon+label. Partner count displayed as subtle context.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Tooltip fix: soloedBatch prop is threaded from useCurveChartState through collection-curve-chart to CurveTooltip
3. Sparkline: collapsed chart state renders a minimal LineChart at ~80px height
4. Comparison matrix: CardTitle text is descriptive, shadcn Tooltip wraps title/info icon
</verification>

<success_criteria>
- Clicking a batch line on the curve chart updates tooltip to show that batch's data (not the first line)
- Collapsing charts shows an 80px sparkline mini-preview instead of empty space
- Comparison matrix card has clear labeling with hover tooltip and partner count context
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/22-ui-polish-data-reliability/22-01-SUMMARY.md`
</output>
