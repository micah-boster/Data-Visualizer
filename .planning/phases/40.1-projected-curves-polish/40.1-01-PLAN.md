---
phase: 40.1-projected-curves-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/use-partner-stats.ts
  - src/hooks/use-baseline-mode.ts
  - src/components/table/modeled-delta-cell.tsx
  - .planning/phases/40.1-projected-curves-polish/40.1-01-AUDIT.md
autonomous: true
requirements:
  - PRJ-09  # cross-lender collision audit gates downstream visibility/table work (key-collision-safe lookups)
  - PRJ-12  # useBaselineMode hook (localStorage persistence primitive)

must_haves:
  truths:
    - "Cross-lender batch-name collision rate is documented (count of (PARTNER_NAME, ACCOUNT_TYPE, BATCH) triples mapping to >1 LENDER_ID); if zero, ship audit doc; if non-zero, defensive fix lands"
    - "useBaselineMode hook reads/writes 'gsd:baselineMode' in localStorage; defaults to 'rolling' on first load and on corrupt/missing values; SSR-safe (no window access during SSR)"
    - "ModeledDeltaCell renders a signed percentage-points value with green/red polarity from getPolarity(metricKey); flat (gray) for |delta| < 0.5pp"
    - "ModeledDeltaCell uses .text-body-numeric (no text-xs/text-sm/font-medium); passes npm run check:tokens"
  artifacts:
    - path: "src/hooks/use-baseline-mode.ts"
      provides: "useBaselineMode() => [BaselineMode, (next: BaselineMode) => void] with localStorage persistence under key 'gsd:baselineMode'"
      exports: ["useBaselineMode"]
    - path: "src/components/table/modeled-delta-cell.tsx"
      provides: "ModeledDeltaCell({deltaPercent, metricKey}) — polarity-colored signed Δ cell"
      exports: ["ModeledDeltaCell"]
    - path: ".planning/phases/40.1-projected-curves-polish/40.1-01-AUDIT.md"
      provides: "Cross-lender batch-name collision audit results — count of collisions found and decision (ship-as-is vs defensive fix)"
      contains: "Collisions found"
    - path: "src/hooks/use-partner-stats.ts"
      provides: "lenderByBatch lookup (modified ONLY if audit found collisions; otherwise unchanged with audit-result comment added)"
      contains: "lenderByBatch"
  key_links:
    - from: "src/hooks/use-baseline-mode.ts"
      to: "localStorage"
      via: "lazy useState initializer + setMode useCallback"
      pattern: "localStorage\\.(getItem|setItem)\\(.+gsd:baselineMode"
    - from: "src/components/table/modeled-delta-cell.tsx"
      to: "src/lib/computation/metric-polarity.ts"
      via: "import { getPolarity }"
      pattern: "getPolarity\\("
---

<objective>
Build the three foundation primitives Phase 40.1's downstream plans (chart polish + table integration) consume:

1. **Cross-lender batch-name collision audit** — investigation gate. The `lenderByBatch` map at `src/hooks/use-partner-stats.ts:55-65` is first-seen-wins. CONTEXT explicitly directs us to audit whether real partner-product pairs ever have a single batch name mapping to multiple `LENDER_ID`s. If zero collisions: document the audit and leave the map as-is. If collisions exist: switch the lookup key to `${batchName}||${lenderId}` (defensive fix) so projection routing is collision-safe.
2. **`useBaselineMode` hook** — localStorage-persisted `BaselineMode` state under key `gsd:baselineMode`, default `'rolling'`. Mirror of `src/hooks/use-heatmap-preference.ts`. SSR-safe.
3. **`ModeledDeltaCell` component** — polarity-colored signed Δ cell for the new table columns; uses `getPolarity` + `.text-body-numeric`; flat-band threshold ±0.5pp.

Purpose: every downstream Phase 40.1 task reuses these primitives. Audit gates whether the chart's "narrowed-to-one" projection lookup can trust the existing map. The hook unblocks the BaselineSelector unification in Plan 02 (`data-display.tsx` swap from `useState` to the persisting setter). The cell component unblocks the Δ column in Plan 03 without the executor reinventing polarity-coloring logic.

Output: 3 new files (`use-baseline-mode.ts`, `modeled-delta-cell.tsx`, `40.1-01-AUDIT.md`), 1 conditionally-modified file (`use-partner-stats.ts` — only if audit finds collisions). Zero consumer-facing change in this plan; downstream plans wire these into `data-display.tsx`, `collection-curve-chart.tsx`, and `definitions.ts`.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40.1-projected-curves-polish/40.1-CONTEXT.md
@.planning/phases/40.1-projected-curves-polish/40.1-RESEARCH.md

@src/hooks/use-heatmap-preference.ts
@src/hooks/use-partner-stats.ts
@src/components/table/trend-indicator.tsx
@src/lib/computation/metric-polarity.ts
@src/lib/computation/compute-projection.ts
@src/components/kpi/baseline-selector.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/kpi/baseline-selector.tsx (BaselineMode is THE source of truth):
```typescript
export type BaselineMode = 'rolling' | 'modeled';
```

From src/hooks/use-heatmap-preference.ts (the canonical pattern to mirror):
```typescript
'use client';
import { useState, useCallback } from 'react';

const STORAGE_KEY = 'bounce-heatmap-enabled';

export function useHeatmapPreference(): [boolean, () => void] {
  const [enabled, setEnabled] = useState<boolean>(() => {
    if (typeof window === 'undefined') return true;
    const stored = localStorage.getItem(STORAGE_KEY);
    return stored === null ? true : stored === 'true';
  });

  const toggle = useCallback(() => {
    setEnabled((prev) => {
      const next = !prev;
      localStorage.setItem(STORAGE_KEY, String(next));
      return next;
    });
  }, []);

  return [enabled, toggle];
}
```

From src/hooks/use-partner-stats.ts:55-65 (the audit/fix site):
```typescript
// Per-batch lenderId lookup (Pitfall 1 — partner may span multiple lenders,
// so a partner-uniform lookup would silently drop projections for batches
// under a non-default lender). Walk partnerRows once.
const lenderByBatch = new Map<string, string>();
for (const r of partnerRows) {
  const batch = getBatchName(r);
  const lender = String(r.LENDER_ID ?? '');
  if (batch && lender && !lenderByBatch.has(batch)) {
    lenderByBatch.set(batch, lender);
  }
}
// ...
const projection = projectionIndex.get(`${lenderId}||${curve.batchName}`);
```

From src/components/table/trend-indicator.tsx:38-51 (polarity-color logic to mirror):
```typescript
const polarity = getPolarity(trend.metric);
const isPositive =
  (trend.direction === 'up' && polarity === 'higher_is_better') ||
  (trend.direction === 'down' && polarity === 'lower_is_better');

let colorClass: string;
if (trend.direction === 'flat') {
  colorClass = 'text-muted-foreground';
} else if (isPositive) {
  colorClass = 'text-success-fg';
} else {
  colorClass = 'text-error-fg';
}
```

From src/lib/computation/metric-polarity.ts:
```typescript
export function getPolarity(metric: string): 'higher_is_better' | 'lower_is_better';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-lender batch-name collision audit (Pitfall 3 gate)</name>
  <files>
    src/hooks/use-partner-stats.ts
    .planning/phases/40.1-projected-curves-polish/40.1-01-AUDIT.md
  </files>
  <action>
RESEARCH § Pitfall 3 / CONTEXT § "Investigate: batch-level coverage gaps" — gate the table-integration work on whether real production data has cross-lender batch-name collisions inside a single `(PARTNER_NAME, ACCOUNT_TYPE)` pair.

**Step A — Write the audit script as a one-shot script in `scripts/audit-batch-name-collisions.mjs`** (delete after running):
- Load `data/static-cache/data.json` (or call `useStaticCacheData` directly via a Node script — read it as JSON).
- Group rows by `pairKey = ${PARTNER_NAME}||${ACCOUNT_TYPE}`.
- For each pair group, build a `Map<batchName, Set<lenderId>>`.
- For each entry where the Set has size > 1, record: `{ pair, batchName, lenderIds: [...] }`.
- Print a summary: total pairs, total batches, collisions found, plus a table of any collisions.
- Print Imprint family check explicitly: filter pairs where partner starts with `bounce_imprint`, dump batchName→lenderId map for visual inspection (CONTEXT Pitfall 1b).

Run the script: `node scripts/audit-batch-name-collisions.mjs > audit.tmp`.

**Step B — Write the audit document** at `.planning/phases/40.1-projected-curves-polish/40.1-01-AUDIT.md` with:
- Date + dataset snapshot (rowCount + fetchedAt from data.json).
- Summary table: total pairs scanned, total distinct batch names, collisions found, Imprint-family verification result.
- Decision: "Collisions found: 0 → ship as-is; no fix needed" OR "Collisions found: N → applied defensive fix in use-partner-stats.ts (see Step C)".
- If zero collisions, also add a one-line comment update at `src/hooks/use-partner-stats.ts:54` referencing the audit (e.g., `// Audit 2026-04-25 (40.1-01-AUDIT.md): zero cross-lender collisions in production data — first-seen-wins is safe.`).

**Step C — IF AND ONLY IF collisions found:** Apply the defensive fix to `src/hooks/use-partner-stats.ts:55-83`:
- Change `lenderByBatch` from `Map<string, string>` to `Map<string, Set<string>>`.
- Update the lookup at line 80-81: instead of `lenderByBatch.get(curve.batchName) ?? ''`, iterate the Set and try each lender; first non-undefined `projectionIndex.get(...)` wins.
- Add JSDoc citing `40.1-01-AUDIT.md` explaining why the Set form is needed.
- Run `npx tsc --noEmit` to verify type safety.

**Step D — Delete `scripts/audit-batch-name-collisions.mjs`** after audit runs (not part of shipped product). Do NOT commit it.

The audit doc IS committed (gates downstream plans).

**Why this matters:** Plan 03 (table integration) builds a `projectionByBatch` lookup keyed only by `batchName`. If cross-lender collisions exist and the defensive fix is missing, the modeled column on collision rows would silently show the wrong batch's modeled value. The audit either confirms safety or forces the fix. Do NOT skip Step C if Step A finds collisions.
  </action>
  <verify>
    <automated>node scripts/audit-batch-name-collisions.mjs &amp;&amp; test -f .planning/phases/40.1-projected-curves-polish/40.1-01-AUDIT.md &amp;&amp; grep -qE "Collisions found: [0-9]+" .planning/phases/40.1-projected-curves-polish/40.1-01-AUDIT.md &amp;&amp; npx tsc --noEmit --project tsconfig.json 2>&amp;1 | grep -v "axe-core" | grep -E "error TS" &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <done>
- 40.1-01-AUDIT.md exists and explicitly states the collision count (zero or non-zero).
- If collisions found: `use-partner-stats.ts` has the defensive `Map<string, Set<string>>` form with a JSDoc reference to the audit doc; `npx tsc --noEmit` shows no new errors.
- If zero collisions: `use-partner-stats.ts` has a one-line audit-reference comment; the file is otherwise unchanged.
- The temporary `scripts/audit-batch-name-collisions.mjs` is deleted (NOT committed).
  </done>
</task>

<task type="auto">
  <name>Task 2: useBaselineMode hook (localStorage-persisted BaselineMode)</name>
  <files>
    src/hooks/use-baseline-mode.ts
  </files>
  <action>
Create `src/hooks/use-baseline-mode.ts` — verbatim shape mirror of `src/hooks/use-heatmap-preference.ts` adapted for the `BaselineMode` union (`'rolling' | 'modeled'`). RESEARCH § Pattern 3 has the full reference implementation.

Requirements:
- `'use client';` directive at top.
- Import `BaselineMode` from `@/components/kpi/baseline-selector` (do NOT redefine the union).
- Storage key: `'gsd:baselineMode'` (CONTEXT lock — exact key name).
- Default value: `'rolling'` when localStorage is empty, when stored value is corrupt/unrecognized, or when `window` is undefined (SSR).
- Validation helper: `isValid(v: unknown): v is BaselineMode` returns true only for exact string match `'rolling'` or `'modeled'`.
- Lazy useState initializer wrapped in try/catch (private-browsing / quota-exceeded safety).
- `setMode` is a `useCallback` that updates state AND writes to localStorage in the same call (write wrapped in try/catch).
- Return tuple `[BaselineMode, (next: BaselineMode) => void]` (NOT an object — match `useHeatmapPreference` shape exactly).
- JSDoc on the exported function citing CONTEXT decision: persistence default is `'rolling'` for zero-regression with users who have never seen the toggle. Note that the previous reset-to-rolling effect (data-display.tsx:298-306) is being REMOVED in Plan 02 per CONTEXT § Persistence — do not add any reset behavior to this hook.

Do NOT add any Phase 40 dependencies (no `BatchCurve`, no `partnerStats` — pure preference state).

Reference: copy the SSR-guard + try/catch + useCallback structure from `src/hooks/use-heatmap-preference.ts` line-for-line; adapt only the type, default, and storage key.
  </action>
  <verify>
    <automated>test -f src/hooks/use-baseline-mode.ts &amp;&amp; grep -q "gsd:baselineMode" src/hooks/use-baseline-mode.ts &amp;&amp; grep -q "useBaselineMode" src/hooks/use-baseline-mode.ts &amp;&amp; grep -q "typeof window" src/hooks/use-baseline-mode.ts &amp;&amp; npx tsc --noEmit --project tsconfig.json 2>&amp;1 | grep -v "axe-core" | grep -E "error TS use-baseline-mode" &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <done>
- `src/hooks/use-baseline-mode.ts` exists; exports `useBaselineMode(): [BaselineMode, (next: BaselineMode) => void]`.
- Default value `'rolling'` returned on first render with empty/corrupt/SSR storage.
- localStorage write happens inside `setMode`; both reads and writes wrapped in try/catch.
- `npx tsc --noEmit` introduces zero new errors specific to this file.
  </done>
</task>

<task type="auto">
  <name>Task 3: ModeledDeltaCell component (polarity-colored signed Δ cell)</name>
  <files>
    src/components/table/modeled-delta-cell.tsx
  </files>
  <action>
Create `src/components/table/modeled-delta-cell.tsx` — a small cell formatter for the modeled-vs-actual delta column. RESEARCH § Pattern 5 has the full reference implementation.

Requirements:
- `'use client';` directive at top.
- Imports: `getPolarity` from `@/lib/computation/metric-polarity`. NO Tooltip wrapper (per RESEARCH anti-pattern: tooltips on every row are visual noise).
- Props interface `ModeledDeltaCellProps`:
  - `deltaPercent: number` — signed percentage-points value (positive = actual exceeds modeled). On the 0..100 scale (NOT 0..1 — recoveryRate is stored 0..100 per Pitfall 5 in RESEARCH).
  - `metricKey: string` — Snowflake column key for `getPolarity` lookup (e.g., `'COLLECTION_AFTER_6_MONTH'`).
- Constant `FLAT_THRESHOLD = 0.5` (percentage points). Deltas with `Math.abs(deltaPercent) < FLAT_THRESHOLD` render flat (gray).
- Direction: `'up'` if `deltaPercent > FLAT_THRESHOLD`, `'down'` if `deltaPercent < -FLAT_THRESHOLD`, else `'flat'`.
- `isPositive` logic mirrors `trend-indicator.tsx:40-42`:
  - `up + higher_is_better` → green (text-success-fg)
  - `down + lower_is_better` → green
  - All other up/down → red (text-error-fg)
  - flat → gray (text-muted-foreground)
- Format: `${sign}${deltaPercent.toFixed(1)}pp` where `sign = deltaPercent > 0 ? '+' : ''`. Examples: `+5.3pp`, `-1.7pp`, `0.0pp` for flat-zero.
- Render: `<span className={`text-body-numeric ${colorClass}`}>{formatted}</span>`.

**Type-token compliance (AGENTS.md):**
- MUST use `.text-body-numeric` (not `text-sm`, not `font-medium`, not `text-xs`).
- The color class is one of: `text-success-fg`, `text-error-fg`, `text-muted-foreground`. No other variants.
- The component must pass `npm run check:tokens` after creation.

JSDoc:
- Document the unit choice ("pp" = percentage points; rationale: actual rate − modeled rate is a difference of two percentages).
- Note the 0..100 scale assumption (consumers must pass deltas already on 0..100, NOT 0..1; the cell does NOT divide by 100).
- Flat threshold 0.5pp rationale: matches recoveryRate display precision (1 decimal) — anything sub-0.1pp would be table noise.

Do NOT add a smoke test — that's outside this plan's scope (the module is small enough that a smoke is optional; if Plan 03 wants one it can add it under `modeled-delta-cell.smoke.ts`).
  </action>
  <verify>
    <automated>test -f src/components/table/modeled-delta-cell.tsx &amp;&amp; grep -q "ModeledDeltaCell" src/components/table/modeled-delta-cell.tsx &amp;&amp; grep -q "text-body-numeric" src/components/table/modeled-delta-cell.tsx &amp;&amp; grep -q "getPolarity" src/components/table/modeled-delta-cell.tsx &amp;&amp; ! grep -E "text-(xs|sm|base|lg|xl|2xl)|font-(semibold|medium|bold)" src/components/table/modeled-delta-cell.tsx &amp;&amp; npm run check:tokens &amp;&amp; npx tsc --noEmit --project tsconfig.json 2>&amp;1 | grep -v "axe-core" | grep -E "error TS modeled-delta-cell" &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <done>
- `src/components/table/modeled-delta-cell.tsx` exists; exports `ModeledDeltaCell({ deltaPercent, metricKey })`.
- Renders signed `+X.Xpp` / `-X.Xpp` / `0.0pp` strings with green / red / gray text classes per polarity rules.
- Uses `.text-body-numeric` (no banned token classes); `npm run check:tokens` clean.
- `npx tsc --noEmit` introduces zero new errors specific to this file.
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. **Audit gate verified**: `40.1-01-AUDIT.md` exists with explicit collision count. If non-zero, the defensive fix in `use-partner-stats.ts` is in place; if zero, the audit comment marker is in place.
2. **Hook verified**: `npx tsc --noEmit | grep use-baseline-mode` is empty; the hook module compiles.
3. **Cell verified**: `npm run check:tokens` clean; `npx tsc --noEmit | grep modeled-delta-cell` empty.
4. **Build clean**: `npx tsc --noEmit` introduces ZERO new errors compared to pre-plan baseline (the only acceptable persistent error is the unchanged `axe-core` test-file error tracked in Phase 40 deferred-items.md).
5. **Type-token guard**: `npm run check:tokens` passes (no banned classes anywhere in `src/`).
6. **No regressions**: `npm run check:surfaces` and `npm run check:motion` clean.
</verification>

<success_criteria>
- [ ] `.planning/phases/40.1-projected-curves-polish/40.1-01-AUDIT.md` exists with explicit "Collisions found: N" line and decision.
- [ ] If audit found collisions: `src/hooks/use-partner-stats.ts` uses `Map<string, Set<string>>` form for `lenderByBatch` with audit-doc reference in JSDoc.
- [ ] If audit found zero collisions: `src/hooks/use-partner-stats.ts:54` has a one-line comment referencing the audit doc.
- [ ] `src/hooks/use-baseline-mode.ts` exists; exports `useBaselineMode` returning `[BaselineMode, setter]`; reads/writes localStorage at key `'gsd:baselineMode'`; SSR-safe; default `'rolling'`.
- [ ] `src/components/table/modeled-delta-cell.tsx` exists; exports `ModeledDeltaCell`; uses `.text-body-numeric` + `text-success-fg` / `text-error-fg` / `text-muted-foreground` exclusively; flat threshold 0.5pp; format `+X.Xpp` / `-X.Xpp`.
- [ ] `npm run check:tokens` clean; `npx tsc --noEmit` introduces no new errors.
- [ ] No consumer-facing change: `data-display.tsx`, `collection-curve-chart.tsx`, `definitions.ts` are NOT modified by this plan (those land in Plans 02 + 03).
- [ ] One commit per task (atomic), final phase commit per execute-plan workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/40.1-projected-curves-polish/40.1-01-SUMMARY.md`
</output>
