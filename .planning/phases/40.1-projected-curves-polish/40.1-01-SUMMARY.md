---
phase: 40.1-projected-curves-polish
plan: 01
subsystem: foundation
tags: [react, hooks, localStorage, type-tokens, audit, projection, baseline-mode]

requires:
  - phase: 40-projected-curves
    provides: BatchCurve.projection per-batch modeled overlay; BaselineMode union; lenderByBatch projection-merge map
provides:
  - Cross-lender batch-name collision audit (40.1-01-AUDIT.md) — empirical proof that lenderByBatch first-seen-wins is safe (0 collisions across 477 (pair, batch) entries)
  - useBaselineMode() hook — localStorage-persisted BaselineMode under key 'gsd:baselineMode', SSR-safe, default 'rolling'
  - ModeledDeltaCell component — polarity-colored signed Δ cell with .text-body-numeric + ±0.5pp flat threshold
affects: [40.1-02-projected-curves-polish, 40.1-03-projected-curves-polish]

tech-stack:
  added: []
  patterns:
    - "Audit-script-then-delete pattern: one-shot tooling under scripts/, run, document results in .planning/, then delete (script body recoverable from git history of PLAN.md)"
    - "Persistence-hook tuple shape mirror: any new persisted-preference hook returns [value, setter] tuple matching useHeatmapPreference for consistency"
    - "Type-guard validator on stored values: defensive against hand-edited localStorage payloads, falls back to default on unrecognized strings"

key-files:
  created:
    - src/hooks/use-baseline-mode.ts
    - src/components/table/modeled-delta-cell.tsx
    - .planning/phases/40.1-projected-curves-polish/40.1-01-AUDIT.md
  modified:
    - src/hooks/use-partner-stats.ts (audit-reference comment only)

key-decisions:
  - "Cross-lender batch-name collisions: 0 found across 477 (pair, batch) entries. Decision — ship lenderByBatch as Map<string, string> (first-seen-wins). The 3-lender Imprint family was the worst-case test and passed cleanly because batch names embed the lender suffix (_DSC, _DP)."
  - "useBaselineMode tuple shape mirrors useHeatmapPreference exactly — single SoT pattern for persisted UI preferences. Storage key 'gsd:baselineMode' is the CONTEXT-locked exact key name; default 'rolling' for zero regression on first load."
  - "isValid() type-guard rejects corrupt/unrecognized stored values, falling back to default 'rolling'. Defense against hand-edited localStorage payloads."
  - "ModeledDeltaCell uses 'pp' (percentage points) unit, not '%' — the difference of two percentages is in pp by convention. Consumers must pass deltaPercent on 0..100 scale (Phase 40 Pitfall 5: recoveryRate is 0..100 throughout the app); the cell does NOT divide by 100."
  - "Flat threshold 0.5pp — matches recoveryRate's 1-decimal display precision; sub-0.1pp would be table noise. Flat renders as '0.0pp' (no sign) regardless of input sign noise."
  - "No tooltip on ModeledDeltaCell — RESEARCH anti-pattern (per-row tooltips = visual noise). Column header carries the explanation."

patterns-established:
  - "Audit-script-then-delete: investigate empirical claims via one-shot scripts/, document results in .planning/, delete script (body recoverable from PLAN.md git history)"
  - "Persistence hook signature: [BaselineMode, (next: BaselineMode) => void] tuple mirror of useHeatmapPreference"
  - "Type-token compliance for new cells: .text-body-numeric + text-success-fg/text-error-fg/text-muted-foreground exclusively"

requirements-completed: [PRJ-12]

duration: 4min
completed: 2026-04-26
---

# Phase 40.1-01: Foundation Primitives Summary

**Cross-lender collision audit (0 found, ship as-is), localStorage-persisted useBaselineMode hook ('gsd:baselineMode', default rolling), and polarity-colored ModeledDeltaCell — three primitives Plans 02 and 03 will consume.**

## Performance

- **Duration:** ~4 min (222s)
- **Started:** 2026-04-26T04:37:13Z
- **Completed:** 2026-04-26T04:41:15Z
- **Tasks:** 3 / 3
- **Files created:** 3
- **Files modified:** 1 (comment-only)
- **Commits:** 3 task commits + final metadata commit

## Accomplishments

- **Audit gate cleared (PRJ-12).** Wrote a one-shot scripts/audit-batch-name-collisions.mjs, scanned the production snapshot (477 (pair, batch) entries across 36 partner-product pairs including the 3-lender Imprint family), found zero cross-lender batch-name collisions inside any pair, documented the result in 40.1-01-AUDIT.md (with re-run instructions if the data model evolves), added a one-line audit-reference comment to `use-partner-stats.ts:54-58`, deleted the script. Plan 03's planned `projectionByBatch` lookup is provably safe to key on batchName alone.
- **`useBaselineMode` hook landed.** SSR-safe localStorage-persisted BaselineMode at key `gsd:baselineMode`, default `'rolling'`. Tuple shape `[BaselineMode, setter]` mirrors `useHeatmapPreference`. try/catch on read+write for private-browsing/quota-exceeded survival. `isValid()` type-guard rejects corrupt/unrecognized stored values. Imports `BaselineMode` from `baseline-selector` (single SoT — no union redefinition).
- **`ModeledDeltaCell` component landed.** Polarity-colored signed Δ cell using `getPolarity(metricKey)` to drive green/red/gray. Format `+X.Xpp` / `-X.Xpp` / `0.0pp` flat. ±0.5pp flat threshold matches recoveryRate display precision. Uses `.text-body-numeric` only — passes `npm run check:tokens`. No tooltip (RESEARCH anti-pattern).

## Task Commits

Each task was committed atomically:

1. **Task 1: Cross-lender batch-name collision audit** — `33629d2` (chore)
2. **Task 2: useBaselineMode hook** — `53b1da4` (feat)
3. **Task 3: ModeledDeltaCell component** — `30768dd` (feat)

**Plan metadata:** (final commit pending after this SUMMARY) (docs)

## Files Created/Modified

- `src/hooks/use-baseline-mode.ts` (created, 59 lines) — Persistence hook for `BaselineMode`. Reads/writes `localStorage['gsd:baselineMode']`. SSR-safe, try/catch wrapped, default `'rolling'`.
- `src/components/table/modeled-delta-cell.tsx` (created, 83 lines) — Polarity-colored signed Δ cell. Uses `getPolarity` + `.text-body-numeric`. Flat threshold ±0.5pp. No tooltip.
- `.planning/phases/40.1-projected-curves-polish/40.1-01-AUDIT.md` (created) — Audit results doc. Records collision count (0), method, dataset snapshot, decision (ship as-is), and re-run instructions.
- `src/hooks/use-partner-stats.ts:54-58` (modified, comment-only) — Adds 4-line audit-reference comment above the `lenderByBatch` map citing the audit doc and the empirical zero-collision finding.

## Decisions Made

See `key-decisions` in frontmatter (6 decisions) — propagated to STATE.md via `state add-decision`.

Headline calls:
1. **Audit verdict: ship `lenderByBatch` as-is.** 477 (pair, batch) entries, 0 collisions. Imprint-family worst case passes because batch names embed lender suffixes (`_DSC`, `_DP`).
2. **`useBaselineMode` mirrors `useHeatmapPreference` shape.** Single pattern for persisted UI preferences in this codebase.
3. **`ModeledDeltaCell` uses "pp" unit.** Difference of two percentages is in percentage points, not percent. Consumers pass deltas on 0..100 scale.

## Deviations from Plan

**None — plan executed exactly as written.**

Audit happened to find zero collisions, so Step C (defensive `Map<string, Set<string>>` fix) was correctly skipped per the plan's conditional gating.

## Issues Encountered

**None.** Snowflake credentials are not configured locally (per project state), but the audit explicitly targets the `src/lib/static-cache/batch-summary.json` snapshot (the production-mirroring dataset embedded in static-mode), so no live warehouse access was needed.

## User Setup Required

None — no external service configuration required. Both new modules are self-contained client code.

## Next Phase Readiness

**Ready for Plans 02 and 03:**
- `useBaselineMode` is importable from `@/hooks/use-baseline-mode`. Plan 02 swaps `data-display.tsx`'s `useState<BaselineMode>('rolling')` to `useBaselineMode()` and removes the reset-to-rolling effect at `data-display.tsx:298-306`.
- `ModeledDeltaCell` is importable from `@/components/table/modeled-delta-cell`. Plan 03 wires it into the new `Δ vs Modeled X mo` column defs in `definitions.ts` (or wherever the milestone columns live).
- Audit doc gates Plan 03's `projectionByBatch` lookup design. Lookup keyed on `batchName` alone is now safe to ship.

**No blockers. No regressions: `check:tokens`, `check:surfaces`, `check:motion` all clean. `npx tsc --noEmit` introduces zero new errors (the unchanged `axe-core` test-file error from Phase 40 deferred-items.md remains, as expected).**

## Self-Check: PASSED

- FOUND: src/hooks/use-baseline-mode.ts
- FOUND: src/components/table/modeled-delta-cell.tsx
- FOUND: .planning/phases/40.1-projected-curves-polish/40.1-01-AUDIT.md
- FOUND: src/hooks/use-partner-stats.ts (audit-reference comment present)
- FOUND: commit 33629d2 (Task 1 — audit)
- FOUND: commit 53b1da4 (Task 2 — useBaselineMode)
- FOUND: commit 30768dd (Task 3 — ModeledDeltaCell)
- FOUND: scripts/audit-batch-name-collisions.mjs deleted (correct — one-shot, not shipped)
- PASS: `npm run check:tokens` clean
- PASS: `npm run check:surfaces` clean
- PASS: `npm run check:motion` clean
- PASS: `npx tsc --noEmit` introduces zero new errors

---
*Phase: 40.1-projected-curves-polish*
*Completed: 2026-04-26*
