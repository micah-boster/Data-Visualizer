---
phase: 36-chart-builder
plan: 05
type: execute
wave: 3
depends_on:
  - "36-01"
  - "36-02"
  - "36-03"
  - "36-04"
files_modified:
  - src/components/charts/chart-panel.tsx
  - src/components/charts/collection-curve-chart.tsx
  - src/components/data-display.tsx
autonomous: false
requirements:
  - CHRT-03
  - CHRT-04
  - CHRT-05
  - CHRT-06
  - CHRT-09
  - CHRT-11
  - CHRT-13

must_haves:
  truths:
    - "When ViewSnapshot.chartState is absent for a partner-level view, a collection-curve preset renders — identical to today's output (CHRT-06 visual parity)"
    - "When the user switches chart type in the builder, the chart re-renders immediately with no save gate"
    - "Applying the Collection Curves preset while on a generic chart switches back to the preset branch and restores its metric toggle + batch visibility state"
    - "Saving a view captures the current chartState (generic or preset) and loading a view restores it"
    - "All 5 check:* guards remain green and npm run build succeeds"
  artifacts:
    - path: "src/components/charts/chart-panel.tsx"
      provides: "Dispatcher: renders CollectionCurveChart (preset) OR GenericChart + ChartBuilderToolbar + PresetMenu (generic). Hides builder toolbar on the preset branch (CONTEXT lock)."
      exports: ["ChartPanel"]
      min_lines: 80
    - path: "src/components/charts/collection-curve-chart.tsx"
      provides: "Accepts an optional presetMenu?: ReactNode prop rendered alongside its metric-toggle actions"
      exports: ["CollectionCurveChart"]
    - path: "src/components/data-display.tsx"
      provides: "Partner + batch chart render sites now use ChartPanel; owns chartDefinition state and the onDefinitionChange dispatcher (Pitfall 9 synchronized with chartLoadRef)"
      contains: "ChartPanel"
  key_links:
    - from: "src/components/data-display.tsx"
      to: "src/components/charts/chart-panel.tsx"
      via: "partner- and batch-level chart render sites replaced with <ChartPanel …>"
      pattern: "ChartPanel"
    - from: "src/components/charts/chart-panel.tsx"
      to: "src/components/charts/generic-chart.tsx"
      via: "renders GenericChart when definition.type !== 'collection-curve'"
      pattern: "GenericChart"
    - from: "src/components/charts/chart-panel.tsx"
      to: "src/components/charts/preset-menu.tsx"
      via: "always renders PresetMenu (both branches) so Collection Curves is reachable from any chart (research Open Q #4)"
      pattern: "PresetMenu"
    - from: "src/components/charts/chart-panel.tsx"
      to: "src/components/charts/collection-curve-chart.tsx"
      via: "renders CollectionCurveChart with a new presetMenu prop; hides ChartBuilderToolbar on the preset branch"
      pattern: "CollectionCurveChart"
---

<objective>
Wire Wave 1 + Wave 2 outputs into the app via `ChartPanel` (the preset-vs-generic dispatcher), extend `CollectionCurveChart` with a `presetMenu` prop, and replace both chart render sites in `data-display.tsx` with `<ChartPanel …>`. Deliver CHRT-06 (visual parity), CHRT-11 (one-click preset load including back-to-preset), CHRT-13 (per-view persistence) end-to-end, and close out CHRT-03/04/05/09 at the call site.

This plan has a human-verify checkpoint at the end — visual parity (CHRT-06) and generic-chart smoke behavior cannot be proven by grep guards alone.
Output: One new dispatcher component, one additive prop on the preset, minimum-invasive edits to `data-display.tsx`, and a human-verify checkpoint.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-chart-builder/36-CONTEXT.md
@.planning/phases/36-chart-builder/36-RESEARCH.md
@CLAUDE.md
@AGENTS.md

@src/components/data-display.tsx
@src/components/charts/collection-curve-chart.tsx
@src/components/charts/generic-chart.tsx
@src/components/charts/chart-builder-toolbar.tsx
@src/components/charts/preset-menu.tsx
@src/components/patterns/data-panel.tsx
@src/lib/views/types.ts
@src/lib/views/migrate-chart.ts

<interfaces>
Consumed (from Plans 36-01 through 36-04):
```typescript
import { GenericChart } from '@/components/charts/generic-chart';
import { ChartBuilderToolbar } from '@/components/charts/chart-builder-toolbar';
import { PresetMenu } from '@/components/charts/preset-menu';
import { DEFAULT_COLLECTION_CURVE } from '@/lib/views/migrate-chart';
import type {
  ChartDefinition,
  CollectionCurveDefinition,
  GenericChartDefinition,
} from '@/lib/views/types';
```

Existing in data-display.tsx (lines 388-491, 660, 666 per 36-RESEARCH):
```typescript
const chartSnapshotRef = useRef<(() => CollectionCurveDefinition) | null>(null);
const chartLoadRef = useRef<((state: CollectionCurveDefinition) => void) | null>(null);
// ...
<CollectionCurveChart curves={partnerStats.curves} chartSnapshotRef={chartSnapshotRef} chartLoadRef={chartLoadRef} />
// ...
<CollectionCurveChart curves={batchCurve} chartSnapshotRef={chartSnapshotRef} chartLoadRef={chartLoadRef} />
```

Produced (downstream — none in this phase; Phase 37 consumes):
```typescript
// @/components/charts/chart-panel
interface ChartPanelProps {
  definition: ChartDefinition;
  onDefinitionChange: (next: ChartDefinition) => void;
  rows: Array<Record<string, unknown>>;       // filteredRawData for generic branch
  curves?: BatchCurve[];                      // used by the preset branch only
  chartSnapshotRef?: MutableRefObject<(() => CollectionCurveDefinition) | null>;
  chartLoadRef?: MutableRefObject<((state: CollectionCurveDefinition) => void) | null>;
}
export function ChartPanel(props): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CollectionCurveChart with a presetMenu prop + build ChartPanel dispatcher</name>
  <files>src/components/charts/collection-curve-chart.tsx, src/components/charts/chart-panel.tsx</files>
  <action>
Edit `src/components/charts/collection-curve-chart.tsx`:
- Extend `CollectionCurveChartProps` with an optional `presetMenu?: React.ReactNode`. JSDoc: "Rendered alongside the metric-toggle cluster in the DataPanel actions slot. Keeps the Presets ▾ menu reachable from the preset branch per 36-RESEARCH Open Q #4."
- Inside the `actions={...}` block of `<DataPanel>`, render the existing metric-toggle cluster PLUS `{presetMenu ?? null}` wrapped so the two clusters sit in one flex row: change the wrapping `<div className="flex gap-1">` to `<div className="flex items-center gap-inline">` so the preset menu sits to the right of the metric toggles with Phase 26 token spacing. Do NOT reorder the existing buttons.
- Do NOT alter `getChartSnapshot` / `restoreChartState` / any internal state hook — CHRT-06 visual-parity lock.

Create `src/components/charts/chart-panel.tsx`:
- Start with `'use client'`.
- Imports per the interfaces block. Also add `import type { BatchCurve } from '@/types/partner-stats'` — referenced by `ChartPanelProps.curves?: BatchCurve[]`.
- Signature per the interfaces block.
- Render logic:
  - If `definition.type === 'collection-curve'`:
    - Render `<CollectionCurveChart curves={curves ?? []} chartSnapshotRef={chartSnapshotRef} chartLoadRef={chartLoadRef} presetMenu={<PresetMenu definition={definition} onDefinitionChange={handlePresetApply} />} />`.
    - The handlePresetApply wrapper handles Pitfall 9: when the next definition is ALSO a `collection-curve` AND `chartLoadRef.current` exists, call `chartLoadRef.current(next)` so the preset's internal `useCurveChartState` re-reads `metric` / `hiddenBatches` / etc. immediately. Then always call `onDefinitionChange(next)`.
  - Else (line/scatter/bar — i.e. GenericChartDefinition):
    - `<DataPanel title={TITLE_BY_TYPE[definition.type]} actions={<div className="flex items-center gap-inline"><ChartBuilderToolbar definition={definition} onChange={onDefinitionChange} /><PresetMenu definition={definition} onDefinitionChange={onDefinitionChange} /></div>}><GenericChart definition={definition} rows={rows} onDefinitionChange={onDefinitionChange} /></DataPanel>`.
    - `TITLE_BY_TYPE = { line: 'Line Chart', scatter: 'Scatter Plot', bar: 'Bar Chart' }`.
- ChartBuilderToolbar is HIDDEN for the preset branch (CONTEXT lock) — that's achieved structurally here because the preset branch never renders ChartBuilderToolbar. The PresetMenu is in both branches so the preset is reachable from generic types too.
- Pitfall 9 wrapper (inlined local function `handlePresetApply(next)`): when both branches exist is the preset branch, check `next.type === 'collection-curve' && chartLoadRef?.current` and synchronize before the parent dispatch.
- Keep ChartPanel a thin dispatcher — do NOT own `definition` state. `data-display.tsx` owns it (Task 2).

Type-token discipline: `text-body`, `text-caption`, `text-label`, `text-heading`, `text-title`, `text-display` + numeric variants only. NO `text-sm` / `font-semibold` pairings. `gap-inline` (Phase 26) for toolbar cluster spacing.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" &amp;&amp; npm run check:tokens &amp;&amp; npm run check:surfaces &amp;&amp; npm run check:components &amp;&amp; npm run check:motion &amp;&amp; npm run check:polish &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>ChartPanel compiles; preset branch renders CollectionCurveChart with `presetMenu={<PresetMenu …>}`; generic branch renders DataPanel + ChartBuilderToolbar + PresetMenu + GenericChart. Preset-branch handlePresetApply wrapper synchronizes chartLoadRef before parent dispatch. No guard regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ChartPanel into data-display.tsx (partner + batch chart render sites)</name>
  <files>src/components/data-display.tsx</files>
  <action>
Edit `src/components/data-display.tsx`:

1. Add imports at the top of the file:
```tsx
import { ChartPanel } from '@/components/charts/chart-panel';
import { DEFAULT_COLLECTION_CURVE } from '@/lib/views/migrate-chart';
import type { ChartDefinition } from '@/lib/views/types';
```

2. Near the existing chart refs (around line 490) declare `chartDefinition` state and a dispatcher:
```tsx
const [chartDefinition, setChartDefinition] = useState<ChartDefinition>(DEFAULT_COLLECTION_CURVE);
```
Initial value is `DEFAULT_COLLECTION_CURVE` — CONTEXT lock: "Default chart for a view with no chartState is the collection-curve preset."

3. Hydrate from the loaded view when a saved view is applied. Find the existing `handleLoadView` / view-load flow in this file (search for `chartLoadRef.current`) and add a sibling call: if `view.snapshot.chartState` is present, `setChartDefinition(view.snapshot.chartState)`. If absent, `setChartDefinition(DEFAULT_COLLECTION_CURVE)`. Keep the existing `chartLoadRef.current?.(view.snapshot.chartState)` call intact — it only runs when the stored chartState is a collection-curve variant (chartLoadRef is narrowly typed to CollectionCurveDefinition per Pitfall 8 / Phase 35-01 decision).

4. Capture on view save: in the existing `handleSaveView` / snapshot-build flow, build the `chartState` field by branching on `chartDefinition.type`:
   - If collection-curve: call `chartSnapshotRef.current?.()` and use that (preserves the preset's domain-specific state: hiddenBatches, showAverage, showAllBatches).
   - Else (line/scatter/bar): use `chartDefinition` directly (full generic state is already in `chartDefinition`).
   - This is Pitfall 8's "two snapshot mechanisms" resolution.

5. Replace both `<CollectionCurveChart curves={…} chartSnapshotRef={chartSnapshotRef} chartLoadRef={chartLoadRef} />` call sites (around lines 660 and 666 per 36-RESEARCH) with:
```tsx
<ChartPanel
  definition={chartDefinition}
  onDefinitionChange={setChartDefinition}
  rows={filteredRawData}
  curves={partnerStats.curves /* or batchCurve for the batch site */}
  chartSnapshotRef={chartSnapshotRef}
  chartLoadRef={chartLoadRef}
/>
```
Preserve surrounding conditionals (drill level gates, curves.length >= 2 guard, SectionDivider). Keep curves prop targeted to the right source at each site.

6. Do NOT change the signature of `chartSnapshotRef` / `chartLoadRef` — they remain `MutableRefObject<(() => CollectionCurveDefinition) | null>` (Pitfall 8 lock from 36-RESEARCH and Phase 35-01 decision log).

7. Search for any accidental `text-sm`/`text-xs`/`font-semibold` introductions in the touched regions — this file is in the guarded scope of `check:tokens`. If any exist from before, do NOT refactor them here (scope discipline); only ensure no new violations land.

Preserve Phase 30-02 drill re-key wrapper (single outer wrapper with composite `drill-${level}-${partner}-${batch}` key) — the chart render sites live inside this wrapper. No changes to that shape.

Preserve Phase 31-05 SectionDivider placement (KPI band ↔ charts ↔ table). The ChartPanel sits where CollectionCurveChart used to sit — no divider rearrangement.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" &amp;&amp; npm run check:tokens &amp;&amp; npm run check:surfaces &amp;&amp; npm run check:components &amp;&amp; npm run check:motion &amp;&amp; npm run check:polish &amp;&amp; npx tsc --noEmit &amp;&amp; npm run smoke:migrate-chart &amp;&amp; npm run smoke:axis-eligibility &amp;&amp; npm run smoke:chart-presets &amp;&amp; npm run smoke:charts &amp;&amp; npm run build</automated>
  </verify>
  <done>data-display.tsx compiles; all 5 check:* guards green; all 4 smoke scripts pass; `npm run build` succeeds. Both chart call sites render `<ChartPanel …>` in place of `<CollectionCurveChart …>`. View save captures the correct chartState branch; view load restores chartDefinition state and (on preset branch) synchronizes via chartLoadRef. Initial render defaults to DEFAULT_COLLECTION_CURVE when no saved chartState exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify chart builder end-to-end (CHRT-06 visual parity + CHRT-03/04/05/09/11/13)</name>
  <files>src/components/data-display.tsx, src/components/charts/chart-panel.tsx, src/components/charts/generic-chart.tsx, src/components/charts/chart-builder-toolbar.tsx, src/components/charts/preset-menu.tsx, src/components/charts/stale-column-warning.tsx</files>
  <action>Pause for the user to verify the six scenarios below in a running browser session. Claude has already completed all automated work (Wave 1 + 2 + 3 Task 1/2); this checkpoint confirms the visual + interactive behavior that guards cannot prove. If a must-fix scenario regresses, capture the delta and spawn a gap-closure plan via `/gsd:plan-phase 36 --gaps`.</action>
  <verify>
    <automated>echo "manual checkpoint — see how-to-verify"</automated>
  </verify>
  <done>User types "approved" OR all must-fix issues captured for gap closure.</done>
  <what-built>
Complete chart builder feature: ChartPanel dispatcher, ChartBuilderToolbar (line/scatter/bar segmented control + X/Y pickers), PresetMenu (Collection Curves built-in + user presets + save-new), StaleColumnWarning banner, and extended ChartDefinition schema with line/scatter/bar variants.

Wave 1: schema + types + axis-eligibility + presets slice + hook.
Wave 2: GenericChart renderer + StaleColumnWarning + ChartBuilderToolbar + PresetMenu + SavePresetPopover.
Wave 3: ChartPanel integration in data-display.tsx, CollectionCurveChart presetMenu prop.

All 5 guards green + 4 smoke scripts pass + build succeeds (verified in Task 2).
  </what-built>
  <how-to-verify>
Run `npm run dev` and open http://localhost:3000 (or the live Vercel URL if Snowflake creds aren't local).

**Scenario A — CHRT-06 visual parity (highest priority):**
1. Drill into any partner with ≥ 2 batches. The default chart should render as Collection Curves identical to main-branch behavior (metric toggle, solo mode on line click, average line, batch visibility pill legend).
2. Compare side-by-side against the pre-Phase-36 screenshot or main branch. Report any visual delta.

**Scenario B — CHRT-11 preset branch toolbar hidden + active-marker:**
1. On the preset branch (Collection Curves active), open browser DevTools → confirm `<ChartBuilderToolbar>` is NOT present in the DOM (only `<PresetMenu>` is visible). This enforces the CONTEXT lock that the generic builder toolbar is hidden on the preset branch.
2. Click `Presets ▾` → confirm Collection Curves has a `✓` marker indicating it's the active definition.

**Scenario C — Switching collection-curve → generic → collection-curve:**
1. (This scenario requires a second preset or the builder toolbar. The builder toolbar is hidden on the preset branch by design.) To exercise the generic branch in v1 without another built-in preset, manually dispatch a definition change via devtools: `localStorage.setItem('bounce-dv-saved-views', …)` with a view whose `chartState` is a line variant, OR open React devtools and set `chartDefinition` state directly to `{ type: 'line', version: 1, x: null, y: null }`. Expect:
   - ChartBuilderToolbar appears with line/scatter/bar icons.
   - EmptyState "Pick axes to render" renders.
   - Open X picker → only eligible columns (time/ordinal/identity) visible. Pick BATCH_AGE_IN_MONTHS.
   - Open Y picker → only numeric columns. Pick TOTAL_COLLECTED_LIFE_TIME.
   - Line chart renders with those axes.
   - Switch to scatter icon → X cleared (BATCH_AGE_IN_MONTHS is numeric, so it may actually carry; test both directions). Switch to bar icon → X is definitely cleared because scatter X was numeric and bar X must be categorical. Empty state re-appears.
2. Open Presets ▾ → click Collection Curves → preset branch renders back, chart appears as in Scenario A.

**Scenario D — CHRT-10 save preset + CHRT-13 view persistence:**
1. From a generic chart with axes picked, open Presets ▾ → "Save current as preset…" → enter a name → Save. Toast "Preset saved" with undo.
2. Reload. Open Presets ▾ — the saved preset is still listed.
3. Save a View (existing Phase 32 mechanic) that includes the generic chart config. Reload. Load the view. Confirm the chart returns to the saved generic state with the picked axes.

**Scenario E — Stale column warning:**
1. Open devtools console. Directly mutate `chartDefinition` state (or localStorage — be aware of the Pitfall 2 hydration-overwrite issue — use devtools React tab to set state directly) to `{ type: 'bar', version: 1, x: { column: 'NONEXISTENT_KEY' }, y: { column: 'TOTAL_COLLECTED_LIFE_TIME' } }`.
2. Expect: StaleColumnWarning banner renders above the chart: "X-axis column NONEXISTENT_KEY not available — using PARTNER_NAME" (or whichever first-eligible categorical). Chart still renders using the fallback.

**Hard fails (must-fix before approval):**
- Collection curves visual delta (CHRT-06 regression).
- Builder toolbar visible on the preset branch.
- Chart type switch fails to re-render.
- EmptyState never appears when axes are null.
- StaleColumnWarning never renders for an invalid column key.
- Saving + loading a view loses the generic chart's axis picks.

**Minor observations can be deferred to a follow-up polish plan.**
  </how-to-verify>
  <resume-signal>Type "approved" if all six scenarios pass (acknowledge observations inline). If a must-fix regresses, describe exactly which scenario and the delta so a gap-closure plan can address it.</resume-signal>
</task>

</tasks>

<verification>
- CHRT-06 visual parity: human-verify Scenario A.
- CHRT-03/04/05: human-verify Scenario C renders line + scatter + bar with user-picked axes.
- CHRT-09: type switch re-renders immediately (Scenario C).
- CHRT-11: one-click preset apply; Scenario B + D.
- CHRT-13: view save + reload restores chart state; Scenario D.
- Stale column banner: Scenario E.
- All 5 `check:*` guards + 4 smoke scripts + build green before checkpoint (enforced in Task 2 verify).
</verification>

<success_criteria>
- All 6 human-verify scenarios pass or have deferrable-noted observations; no must-fix regressions.
- `npm run build` green.
- `npm run smoke:migrate-chart && npm run smoke:axis-eligibility && npm run smoke:chart-presets && npm run smoke:charts` all green.
- 11/11 CHRT-03..CHRT-13 requirements satisfied across Plans 01-05.
</success_criteria>

<output>
After completion, create `.planning/phases/36-chart-builder/36-05-SUMMARY.md` capturing:
- Scenario outcomes (approved / deferred-with-notes).
- Any residual Pitfall-9 sync edge cases observed.
- Recommended follow-ups for Phase 36 close-out (e.g., preset-rename UI if demand surfaces — deferred per CONTEXT).
</output>
