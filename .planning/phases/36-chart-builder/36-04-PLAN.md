---
phase: 36-chart-builder
plan: 04
type: execute
wave: 2
depends_on:
  - "36-01"
  - "36-02"
files_modified:
  - src/components/charts/axis-picker.tsx
  - src/components/charts/chart-builder-toolbar.tsx
  - src/components/charts/preset-menu.tsx
  - src/components/charts/save-preset-popover.tsx
  - src/lib/charts/chart-definition-equals.ts
  - src/lib/charts/transitions.ts
  - src/lib/charts/transitions.smoke.ts
  - package.json
autonomous: true
requirements:
  - CHRT-07
  - CHRT-08
  - CHRT-09
  - CHRT-10
  - CHRT-11
  - CHRT-12

must_haves:
  truths:
    - "ChartBuilderToolbar renders a line/scatter/bar icon segmented control that, on click, dispatches onChange(switchChartType(current, nextType))"
    - "AxisPicker shows ONLY columns eligible for the current (chartType, axis) pair via getEligibleColumns"
    - "PresetMenu lists BUILTIN_PRESETS first (with a Lock icon), then user presets, and a 'Save current as preset…' action at the bottom"
    - "Applying a preset deep-copies its definition before dispatch so subsequent axis edits don't mutate the preset"
    - "SavePresetPopover uses the same dup-name 'Replace?' recipe as SaveViewPopover"
    - "switchChartType honors CONTEXT preset→generic conversion: preset's metric maps to generic Y, X defaults to BATCH_AGE_IN_MONTHS (line) / BATCH (bar); scatter leaves X null for the user to pick"
  artifacts:
    - path: "src/components/charts/axis-picker.tsx"
      provides: "Popover-combobox that picks a column for (chartType, axis)"
      exports: ["AxisPicker"]
    - path: "src/components/charts/chart-builder-toolbar.tsx"
      provides: "Icon segmented control + X/Y pickers + ToolbarDivider separators — rendered inside DataPanel.actions slot"
      exports: ["ChartBuilderToolbar"]
    - path: "src/components/charts/preset-menu.tsx"
      provides: "Single Presets ▾ dropdown (built-ins + user + save action)"
      exports: ["PresetMenu"]
    - path: "src/components/charts/save-preset-popover.tsx"
      provides: "Modal/popover for naming a new preset (mirrors SaveViewPopover)"
      exports: ["SavePresetPopover"]
    - path: "src/lib/charts/chart-definition-equals.ts"
      provides: "isSameDefinition helper for active-preset checkmark in PresetMenu"
      exports: ["isSameDefinition"]
    - path: "src/lib/charts/transitions.ts"
      provides: "switchChartType() + seedGenericFromPreset() pure helpers for the carryover rule and the CONTEXT-locked preset→generic conversion"
      exports: ["switchChartType", "seedGenericFromPreset"]
    - path: "src/lib/charts/transitions.smoke.ts"
      provides: "Node strip-types assertions covering the switchChartType branches + preset→generic seeding"
      min_lines: 30
  key_links:
    - from: "src/components/charts/chart-builder-toolbar.tsx"
      to: "src/lib/charts/transitions.ts"
      via: "chart-type switch → switchChartType(current, nextType) → onChange"
      pattern: "switchChartType"
    - from: "src/lib/charts/transitions.ts"
      to: "src/lib/columns/axis-eligibility.ts"
      via: "uses isColumnEligible for the generic→generic carryover decision (never a hand-rolled list)"
      pattern: "isColumnEligible"
    - from: "src/lib/charts/transitions.ts"
      to: "src/lib/views/migrate-chart.ts"
      via: "references DEFAULT_COLLECTION_CURVE for the generic→preset reset branch"
      pattern: "DEFAULT_COLLECTION_CURVE"
    - from: "src/components/charts/axis-picker.tsx"
      to: "src/lib/columns/axis-eligibility.ts"
      via: "options populated by getEligibleColumns(chartType, axis)"
      pattern: "getEligibleColumns"
    - from: "src/components/charts/preset-menu.tsx"
      to: "src/hooks/use-chart-presets.ts"
      via: "reads presets and invokes savePreset on submit from SavePresetPopover"
      pattern: "useChartPresets"
    - from: "src/components/charts/preset-menu.tsx"
      to: "src/components/charts/save-preset-popover.tsx"
      via: "nested popover opened by the 'Save current as preset…' menu item"
      pattern: "SavePresetPopover"
---

<objective>
Build the four builder-UI components: `AxisPicker`, `ChartBuilderToolbar`, `PresetMenu`, `SavePresetPopover`. Each composes existing patterns (DataPanel.actions slot, ToolbarDivider, Popover, Button, FilterCombobox-style keyboard nav). None of them own chart state — they are pure props-in / onChange-out — so Plan 36-05 can wire them into `ChartPanel` without re-architecting.

Purpose: Separate the toolbar/menu UI from rendering (Plan 36-03) and from integration (Plan 36-05). This plan delivers CHRT-07 + CHRT-08 at the picker UI layer, CHRT-09 via the segmented control, and CHRT-10 + CHRT-11 + CHRT-12 at the menu + save-popover layer.
Output: Four new components + one tiny equality helper. No call-site changes to `data-display.tsx` yet (Plan 36-05 owns that wiring).
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-chart-builder/36-CONTEXT.md
@.planning/phases/36-chart-builder/36-RESEARCH.md
@CLAUDE.md
@AGENTS.md

@src/lib/views/types.ts
@src/lib/columns/config.ts
@src/lib/columns/axis-eligibility.ts
@src/lib/chart-presets/types.ts
@src/lib/chart-presets/defaults.ts
@src/hooks/use-chart-presets.ts
@src/lib/charts/transitions.ts
@src/components/ui/button.tsx
@src/components/ui/popover.tsx
@src/components/patterns/toolbar-divider.tsx
@src/components/toolbar/save-view-popover.tsx
@src/components/toolbar/preset-dropdown.tsx
@src/components/toolbar/filter-combobox.tsx

<interfaces>
Consumed (from Plans 36-01 and 36-02, plus existing code):
```typescript
// @/lib/views/types
type ChartDefinition = ...;
type GenericChartDefinition = ...;

// @/lib/columns/axis-eligibility
function getEligibleColumns(chartType, axis): ColumnConfig[];

// @/lib/chart-presets/types
interface ChartPreset { id; name; locked; createdAt; definition; }

// @/lib/chart-presets/defaults
const BUILTIN_PRESETS: ChartPreset[];

// @/hooks/use-chart-presets
function useChartPresets(): { presets, savePreset, deletePreset, restorePreset, hasPresetWithName, renamePreset, getPreset };
```

Authored by this plan (Plan 04 owns transitions.ts; Plan 03 consumes nothing from it):
```typescript
// @/lib/charts/transitions
export function switchChartType(
  current: ChartDefinition,
  nextType: ChartDefinition['type'],
): ChartDefinition;
export function seedGenericFromPreset(
  preset: CollectionCurveDefinition,
  nextType: 'line' | 'scatter' | 'bar',
): ChartDefinition;
```

Produced (Plan 36-05 consumes):
```typescript
// @/components/charts/chart-builder-toolbar
interface ChartBuilderToolbarProps {
  definition: GenericChartDefinition;   // hidden when the collection-curve preset is active (CONTEXT lock)
  onChange: (next: ChartDefinition) => void;
}
export function ChartBuilderToolbar(props): JSX.Element;

// @/components/charts/axis-picker
interface AxisPickerProps {
  chartType: 'line' | 'scatter' | 'bar';
  axis: 'x' | 'y';
  value: { column: string } | null;
  onChange: (next: { column: string } | null) => void;
  placeholder?: string; // default: 'Pick column'
}
export function AxisPicker(props): JSX.Element;

// @/components/charts/preset-menu
interface PresetMenuProps {
  definition: ChartDefinition;
  onDefinitionChange: (next: ChartDefinition) => void;
}
export function PresetMenu(props): JSX.Element;

// @/components/charts/save-preset-popover
interface SavePresetPopoverProps {
  definition: ChartDefinition;
  onSave: (name: string) => void;   // caller (PresetMenu) invokes savePreset on resolution
  hasPresetWithName: (name: string) => boolean;
}
export function SavePresetPopover(props): JSX.Element;

// @/lib/charts/chart-definition-equals
export function isSameDefinition(a: ChartDefinition, b: ChartDefinition): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: transitions.ts (switchChartType + seedGenericFromPreset) + AxisPicker + ChartBuilderToolbar</name>
  <files>src/lib/charts/transitions.ts, src/lib/charts/transitions.smoke.ts, src/components/charts/axis-picker.tsx, src/components/charts/chart-builder-toolbar.tsx, package.json</files>
  <action>
FIRST, create `src/lib/charts/transitions.ts` (Plan 04 owns this — Plan 03 does NOT author it, so Wave 2 parallel execution has no write race):

Imports:
- `ChartDefinition`, `CollectionCurveDefinition`, `GenericChartDefinition` from `@/lib/views/types`.
- `DEFAULT_COLLECTION_CURVE` from `@/lib/views/migrate-chart`.
- `isColumnEligible` from `@/lib/columns/axis-eligibility`.

Export `seedGenericFromPreset(preset, nextType)`:
- Honors the CONTEXT-locked conversion rule (CONTEXT.md:33): "Switching from collection-curve preset → generic type: convert with sensible defaults. Preset's `metric` field maps to generic Y-axis. X defaults to batch/time."
- Map preset.metric → generic Y column:
  - `preset.metric === 'recoveryRate'` → `y = { column: 'RECOVERY_RATE' }` (or whichever COLUMN_CONFIGS key encodes recovery rate; if the exact key differs, pick the closest numeric column that represents the running recovery-rate metric — Claude discretion within the numeric-only Y constraint).
  - `preset.metric === 'amount'` → `y = { column: 'TOTAL_COLLECTED_LIFE_TIME' }` (the cumulative collected amount).
- X defaults by chart type:
  - `nextType === 'line'` → `x = { column: 'BATCH_AGE_IN_MONTHS' }` (the ordinal time axis the preset uses today).
  - `nextType === 'bar'` → `x = { column: 'BATCH' }` (categorical default — one bar per batch).
  - `nextType === 'scatter'` → `x = null` (no meaningful default for two-numeric; user picks).
- Return `{ type: nextType, version: 1, x, y }`.

Export `switchChartType(current, nextType)` implementing the carryover rules exactly as in 36-RESEARCH §Pattern 5:
- `current.type === nextType` → return `current` unchanged (reference-equal).
- `nextType === 'collection-curve'` → return `DEFAULT_COLLECTION_CURVE` (reset to the canonical preset).
- `current.type === 'collection-curve'` and next is generic → delegate to `seedGenericFromPreset(current, nextType)` (CONTEXT-locked preset→generic conversion).
- Generic → generic → carry x/y refs only when `isColumnEligible(nextType, 'x', current.x?.column)` (same for y). Otherwise set to null. Return fresh `{ type: nextType, version: 1, x, y }`.

Then create `src/lib/charts/transitions.smoke.ts`:
- Node `--experimental-strip-types` + `node:assert/strict` (explicit `.ts` imports).
- Assertions:
  1. Same-type no-op: `switchChartType(definition, definition.type)` returns reference-equal input.
  2. Generic → 'collection-curve': returns `DEFAULT_COLLECTION_CURVE` (reference equality).
  3. 'collection-curve' → 'line' (via seedGenericFromPreset): result has `type === 'line'`, `version === 1`, `x.column === 'BATCH_AGE_IN_MONTHS'`, `y.column` is a valid numeric column key from COLUMN_CONFIGS.
  4. 'collection-curve' → 'bar' with `metric: 'amount'`: `x.column === 'BATCH'`, `y.column === 'TOTAL_COLLECTED_LIFE_TIME'`.
  5. 'collection-curve' → 'scatter': `x === null`, `y` is a numeric column.
  6. Generic line → bar with X numeric (`{ column: 'TOTAL_COLLECTED_LIFE_TIME' }`) clears X to null (bar X must be categorical).
  7. Generic line → scatter with both axes numeric-eligible carries them unchanged.
- End with `console.log('✓ transitions smoke test passed (N assertions)')`.

Then wire `"smoke:transitions": "node --experimental-strip-types src/lib/charts/transitions.smoke.ts"` into `package.json`. APPEND at the end of the scripts block — do NOT rely on sibling order. If `npm run smoke:transitions` fails because package.json conflicts with a sibling plan's write (Plan 03 is also appending `smoke:charts` in parallel), re-apply your edit on top of the merged file.

---

SECOND, create `src/components/charts/axis-picker.tsx`:
- Start with `'use client'`.
- Imports: `useState` from `react`; `ChevronDown` from `lucide-react`; `Popover`, `PopoverTrigger`, `PopoverContent` from `@/components/ui/popover`; `Button` from `@/components/ui/button`; `getEligibleColumns` from `@/lib/columns/axis-eligibility`; `ColumnConfig` from `@/lib/columns/config`.
- Render a Button (variant="outline", size="sm", className="h-7") as the PopoverTrigger showing either `value?.column ? columnConfig.label : placeholder` + a trailing `ChevronDown`. Content: the eligible column list (up to ~60 entries) as a simple list of buttons. Keyboard nav is handled by the underlying `@base-ui/react` popover; a basic down/up selection with Enter-to-pick is optional — if the existing `src/components/toolbar/filter-combobox.tsx` pattern is straightforward to reuse, compose it; otherwise build a simple scrollable list (add `thin-scrollbar` from Phase 31 on the content panel).
- On option click → `onChange({ column: col.key })`.
- "Clear" item at the top of the list → `onChange(null)`.
- Each option renders `col.label` in `text-body` and `col.key` in `text-caption text-muted-foreground` below it (so power users see the Snowflake key — CONTEXT "density over hand-holding").

Type-token compliance: `text-body`, `text-caption`, `text-label`, `text-label-numeric` only. NO `text-sm`/`font-semibold`. Popover content uses `focus-glow` for a:focus states (Phase 31 DS-31 utility).

Create `src/components/charts/chart-builder-toolbar.tsx`:
- Start with `'use client'`.
- Imports: icons `LineChart`, `ScatterChart` (scatter icon from lucide-react is `ScatterChart` — confirm), `BarChart3` from `lucide-react`; `Button`; `ToolbarDivider` from `@/components/patterns/toolbar-divider`; `AxisPicker`; `switchChartType` from `@/lib/charts/transitions`; types `GenericChartDefinition`, `ChartDefinition`.
- Segmented control: three icon buttons (`LineChart`, `ScatterChart`, `BarChart3`). Active variant is highlighted via `variant="default"` vs `variant="ghost"`. `aria-label` / `aria-pressed` wired for each.
- Click handler: `onChange(switchChartType(definition, nextType))`. `nextType` is one of `'line' | 'scatter' | 'bar'`.
- ToolbarDivider between segmented control and X picker.
- Label copy is semantic (uppercase overline per Phase 27) and lives OUTSIDE the picker to avoid doubling. Render `<span className="text-label">X</span>` before the X picker; the picker's internal placeholder is plain `"Pick column"` (no `X:` prefix).
- X picker: `<AxisPicker chartType={definition.type} axis="x" value={definition.x} onChange={(next) => onChange({ ...definition, x: next })} placeholder="Pick column" />`.
- Y picker equivalent with its own external `<span className="text-label">Y</span>` label: `<AxisPicker axis="y" value={definition.y} onChange={(next) => onChange({ ...definition, y: next })} placeholder="Pick column" />`.
- No ToolbarDivider after Y — the PresetMenu (rendered by Plan 36-05's ChartPanel alongside this toolbar) owns the trailing divider + menu.
- Layout: `className="flex items-center gap-inline"` (Phase 26 semantic gap token).

Do NOT render a Presets button in ChartBuilderToolbar itself — PresetMenu is a separate component rendered by ChartPanel (Plan 36-05). Keeps responsibilities clean.

Type-token + surfaces guard: no `bg-border` hand-rolled dividers (use ToolbarDivider). No `text-sm`/`font-semibold`.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" &amp;&amp; npm run smoke:transitions &amp;&amp; npm run check:tokens &amp;&amp; npm run check:surfaces &amp;&amp; npm run check:components &amp;&amp; npx tsc --noEmit</automated>
    <note>If `npm run smoke:transitions` fails because package.json conflicts with a sibling plan's write, re-apply your edit on top of the merged file.</note>
  </verify>
  <done>All files compile; all three guards + the new smoke green. `transitions.ts` exports `switchChartType` + `seedGenericFromPreset`. `AxisPicker` shows only eligible columns for the current (chartType, axis). `ChartBuilderToolbar` dispatches `switchChartType` on type click, `{ ...definition, x/y: next }` on axis pick. ToolbarDivider used (no hand-rolled `bg-border` divider). External `text-label` X/Y spans; no `X:`/`Y:` prefixes inside the picker placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: isSameDefinition helper + SavePresetPopover (dup-name "Replace?" recipe)</name>
  <files>src/lib/charts/chart-definition-equals.ts, src/components/charts/save-preset-popover.tsx</files>
  <action>
Create `src/lib/charts/chart-definition-equals.ts`:
- Export `isSameDefinition(a, b)` — a small discriminator-aware deep-equals for `ChartDefinition`.
- If `a.type !== b.type` → false.
- For `'collection-curve'`: compare `metric`, `showAverage`, `showAllBatches`, and `hiddenBatches` as a sorted-array equality (`JSON.stringify([...a.hiddenBatches].sort())`). Version differences between 2/2 are always equal — variants are version-locked.
- For `'line' | 'scatter' | 'bar'`: compare `x?.column ?? null === b.x?.column ?? null` and same for y.
- Pure helper; no imports from `/components`.

Create `src/components/charts/save-preset-popover.tsx`:
- Mirror `src/components/toolbar/save-view-popover.tsx` 1:1 in shape. Simpler — no `includeDrill` checkbox.
- `'use client'`.
- Imports: `useState`, `useRef`, `useEffect` from `react`; `Plus` or `Save` icon from `lucide-react`; `Button`; `Popover`, `PopoverTrigger`, `PopoverContent`, `PopoverHeader`, `PopoverTitle` from `@/components/ui/popover`.
- Props: `definition` (unused directly by the popover — the parent menu passes it to `onSave` once the user confirms the name), `onSave(name)`, `hasPresetWithName(name)`.
- State: `name`, `showReplace`, `open`.
- On open: `requestAnimationFrame(() => inputRef.current?.focus())`.
- On close: reset state.
- On submit (Enter key or Save button): if `trimmed === ''` no-op; if duplicate && !showReplace set showReplace true; else call `onSave(trimmed)` and close.
- Render recipe: popover trigger = ghost button with `Plus` icon and text label "Save current as preset…" (matches the research sketch of the last menu item, but this popover is opened FROM the PresetMenu — the trigger here is how it mounts; PresetMenu may render the trigger inside its DropdownMenu, and pass `render={<MenuItem>…</MenuItem>}` if needed, otherwise this popover provides the trigger and PresetMenu just composes it). Simplest: this component IS a popover with a button trigger labeled "Save current as preset…"; PresetMenu renders it at the bottom of its menu content. If the @base-ui/react PopoverRoot from `preset-dropdown.tsx` supports nested popovers, compose; otherwise close the PresetMenu on trigger click and open this popover immediately (same pattern as SaveViewPopover's own trigger).
- Input className per the SaveViewPopover template: `flex-1 rounded-md border bg-transparent px-2 py-1.5 text-body focus:outline-none focus-glow`.
- Button copy: `showReplace ? 'Replace?' : 'Save'`. Button variant flips to destructive when replacing.

Type-token rule: `text-heading` for PopoverTitle; `text-body` for input and hint text. No font-weight pairings.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" &amp;&amp; npm run check:tokens &amp;&amp; npm run check:surfaces &amp;&amp; npm run check:components &amp;&amp; npm run check:polish &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Both files compile. All 4 guards green. `isSameDefinition({ type: 'line', version: 1, x: null, y: null }, { type: 'line', version: 1, x: null, y: null })` is `true`. Dup-name handling mirrors SaveViewPopover exactly (Enter submits, Esc closes, Replace? flips destructive).</done>
</task>

<task type="auto">
  <name>Task 3: PresetMenu (built-ins + user + save action)</name>
  <files>src/components/charts/preset-menu.tsx</files>
  <action>
Create `src/components/charts/preset-menu.tsx`. Start with `'use client'`.

Reference template: `src/components/toolbar/preset-dropdown.tsx` (structural template for the Popover + grouped list + `✓`/Lock badges + delete-on-hover recipe).

Imports:
- `useState` from `react`.
- Icons: `ChevronDown`, `Lock`, `Check`, `Trash2` from `lucide-react`.
- `Popover`, `PopoverTrigger`, `PopoverContent` (and header bits as needed) from `@/components/ui/popover`.
- `Button` from `@/components/ui/button`.
- `SavePresetPopover` from `./save-preset-popover`.
- `useChartPresets` from `@/hooks/use-chart-presets`.
- `isSameDefinition` from `@/lib/charts/chart-definition-equals`.
- Types: `ChartDefinition`, `ChartPreset`.
- `toast` from `sonner` for save/delete toasts with undo (mirror `handleSaveView` / `handleDeleteView` in `src/components/data-display.tsx:411-480`).

Props (interfaces block):
```tsx
interface PresetMenuProps {
  definition: ChartDefinition;
  onDefinitionChange: (next: ChartDefinition) => void;
}
```

Render:
1. Trigger: `<Button variant="outline" size="sm" className="h-7">Presets <ChevronDown className="ml-1 h-3.5 w-3.5" /></Button>`.
2. PopoverContent: two grouped sections.
   - Built-ins section heading `.text-label text-muted-foreground uppercase` "Built-in".
   - For each `p of presets.filter(p => p.locked)`: a row with `Lock` icon, preset `p.name`, and a trailing `<Check />` if `isSameDefinition(definition, p.definition)`. Click → `onDefinitionChange(structuredClone(p.definition))` (Pitfall 4 deep-copy lock) and close popover.
   - Horizontal divider (use the `.divider-horizontal-fade` utility from Phase 31, or a simple `border-t`).
   - User presets section heading `.text-label text-muted-foreground uppercase` "Your presets". Empty state: muted italic `text-caption` "No saved presets yet."
   - For each user preset: same row shape. Trailing `Trash2` icon button (shown on row hover via `group-hover:opacity-100 opacity-0` or similar — Phase 26 motion-friendly). Click → `deletePreset(p.id)`, toast with undo that calls `restorePreset` on action click.
   - Footer divider + SavePresetPopover trigger (the "Save current as preset…" action). On submit via SavePresetPopover's `onSave(name)` callback: call `savePreset(name, definition)`, show sonner toast "Preset saved" with undo (restore → `deletePreset(newId)`).
3. Close popover on any preset-apply click. SavePresetPopover is responsible for its own open/close state.

Active-preset detection: compute `const activeId = presets.find(p => isSameDefinition(definition, p.definition))?.id` once inside the component. Render `<Check />` only on the row whose `p.id === activeId`.

Type-token compliance:
- `text-label` (uppercase) for section headings, `text-body` for preset names, `text-caption` for empty-state message. No `text-sm`/`text-xs`.
- No `font-semibold`/`font-medium`/`font-bold` paired with a type token.

Surface recipe: PopoverContent is a shadcn primitive — its surface-overlay elevation is inherited from the token system (Phase 28-06). No hand-rolled `bg-white`/`shadow-lg` here.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" &amp;&amp; npm run check:tokens &amp;&amp; npm run check:surfaces &amp;&amp; npm run check:components &amp;&amp; npm run check:motion &amp;&amp; npm run check:polish &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <done>PresetMenu compiles and builds. All 5 guards green. Menu shows BUILTIN_PRESETS (with Lock badge + Check when active) above user presets (with delete-on-hover + Check when active) above a "Save current as preset…" trigger that opens SavePresetPopover. Preset apply deep-copies the definition (structuredClone) — grep asserts `structuredClone` appears at least once inside this file.</done>
</task>

</tasks>

<verification>
- CHRT-09 (type switch re-render): `ChartBuilderToolbar` dispatches `switchChartType(definition, nextType)` → parent state changes → renderer re-mounts the right Recharts primitive.
- CHRT-07/08: AxisPicker options = `getEligibleColumns(chartType, axis)` only. Changing `chartType` flushes to a different option list automatically.
- CHRT-10: SavePresetPopover onSave → caller invokes `savePreset(name, definition)`. PresetMenu wires this end-to-end.
- CHRT-11: Clicking a preset row → `onDefinitionChange(structuredClone(preset.definition))`. One-click apply.
- CHRT-12: BUILTIN_PRESETS[0] (Collection Curves) renders with Lock icon at the top of the menu.
- Pitfall 4 lock: `structuredClone` present in the preset-apply code path.
- Type-token discipline: no raw Tailwind text sizes; `check:tokens` green; `check:components` green (uses `ToolbarDivider` + `DataPanel`-compatible children).
</verification>

<success_criteria>
- `npm run build` green.
- All 5 `check:*` guards green.
- Four new components export per the interfaces block.
- No changes to `data-display.tsx` yet (integration is Plan 36-05's responsibility).
- `isSameDefinition` correctly identifies the active preset for the checkmark.
</success_criteria>

<output>
After completion, create `.planning/phases/36-chart-builder/36-04-SUMMARY.md`.
</output>
