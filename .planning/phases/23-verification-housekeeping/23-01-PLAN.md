---
phase: 23-verification-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-claude-query-infrastructure/17-VERIFICATION.md
  - .planning/phases/18-claude-query-ui/18-VERIFICATION.md
autonomous: true
requirements:
  - NLQ-01
  - NLQ-02
  - NLQ-05
  - NLQ-06
  - NLQ-07
  - NLQ-08
  - NLQ-10

must_haves:
  truths:
    - "Phase 17 has a VERIFICATION.md with Pass/Partial/Fail rating for each of NLQ-01 through NLQ-05"
    - "Phase 18 has a VERIFICATION.md with Pass/Partial/Fail rating for each of NLQ-06 through NLQ-10"
    - "Each verification entry includes specific file paths, line references, and observable behavior as evidence"
    - "Snowflake-dependent requirements are marked Partial with environment dependency note"
  artifacts:
    - path: ".planning/phases/17-claude-query-infrastructure/17-VERIFICATION.md"
      provides: "Formal verification of NLQ infrastructure requirements"
      contains: "NLQ-01"
    - path: ".planning/phases/18-claude-query-ui/18-VERIFICATION.md"
      provides: "Formal verification of NLQ UI requirements"
      contains: "NLQ-06"
  key_links:
    - from: "17-VERIFICATION.md"
      to: "src/app/api/query/route.ts"
      via: "evidence references to actual source code"
      pattern: "route\\.ts|system-prompt|context-builder"
    - from: "18-VERIFICATION.md"
      to: "src/components/query/"
      via: "evidence references to actual UI components"
      pattern: "QuerySearchBar|useChat|suggested"
---

<objective>
Generate formal VERIFICATION.md files for Phase 17 (Claude Query Infrastructure) and Phase 18 (Claude Query UI), the only two completed phases missing verification documentation.

Purpose: Close the verification gap for NLQ requirements so REQUIREMENTS.md can accurately reflect verified status for all 28 v3.0 requirements.
Output: Two VERIFICATION.md files following the established format (YAML frontmatter, goal achievement, success criteria table, requirement traceability, build verification, artifacts).
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-verification-housekeeping/23-CONTEXT.md

# Source material — executor MUST read these to write accurate verification evidence
@.planning/phases/17-claude-query-infrastructure/17-01-SUMMARY.md
@.planning/phases/18-claude-query-ui/18-01-SUMMARY.md
@.planning/phases/21-critical-bug-fixes/21-VERIFICATION.md

# Template — follow this format exactly
@.planning/phases/15-anomaly-detection-engine/15-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Phase 17 VERIFICATION.md (NLQ Infrastructure)</name>
  <files>.planning/phases/17-claude-query-infrastructure/17-VERIFICATION.md</files>
  <action>
Read the actual source code that Phase 17 built to gather verification evidence. Key files to read:
- `src/app/api/query/route.ts` — the POST handler, streamText usage, error handling
- `src/lib/ai/system-prompt.ts` — system prompt construction, data context injection
- `src/lib/ai/context-builder.ts` — data summarization, token budget management
- Check `package.json` for `ai` and `@ai-sdk/anthropic` dependencies
- Check for ANTHROPIC_API_KEY usage (server-only, not in client bundle)

Also read `21-VERIFICATION.md` to check which NLQ requirements Phase 21 already verified (NLQ-03, NLQ-04 were bug-fixed there).

Write `.planning/phases/17-claude-query-infrastructure/17-VERIFICATION.md` following the exact format from `15-VERIFICATION.md`:

1. **YAML frontmatter**: phase, status (passed/partial), verified date (2026-04-14), verifier: phase-23-verification
2. **Goal Achievement**: Restate Phase 17 goal from ROADMAP.md, give overall result
3. **Success Criteria Verification**: Table with all 5 success criteria from ROADMAP Phase 17, each with Status and Evidence citing specific file paths and line numbers
4. **Requirement Traceability**: Table for NLQ-01 through NLQ-05, each with Plan, Status (VERIFIED/PARTIAL), and Evidence
   - NLQ-01 (POST /api/query route): Verify route.ts accepts question + context, returns streaming response
   - NLQ-02 (AI SDK streamText + useChat): Verify ai and @ai-sdk/anthropic in package.json, streamText in route.ts, no custom SSE
   - NLQ-03 (System prompt with data context): Reference Phase 21 fix if applicable, verify context-builder summarizes data
   - NLQ-04 (Claude constrained to context): Reference Phase 21 fix if applicable, verify system prompt instructions
   - NLQ-05 (ANTHROPIC_API_KEY server-only): Verify env var usage, confirm no client exposure
   - Mark any requirement depending on live Snowflake as PARTIAL with note: "Works with static cache; live Snowflake verification pending"
5. **Build Verification**: Run `npx tsc --noEmit` and `npx next build`, record results
6. **Artifacts Created**: Table of files and purposes from Phase 17

Each requirement gets a deep walkthrough — not just "file exists" but what the code does, how it satisfies the requirement, and edge cases covered. Professional SWE standard per CONTEXT.md decisions.
  </action>
  <verify>
    <automated>test -f ".planning/phases/17-claude-query-infrastructure/17-VERIFICATION.md" && grep -c "NLQ-0[1-5]" ".planning/phases/17-claude-query-infrastructure/17-VERIFICATION.md" | grep -q "5" && echo "PASS: All 5 NLQ requirements referenced" || echo "FAIL: Missing NLQ requirement references"</automated>
  </verify>
  <done>17-VERIFICATION.md exists with YAML frontmatter, all 5 success criteria evaluated, all 5 NLQ requirements (01-05) have VERIFIED or PARTIAL status with specific code evidence</done>
</task>

<task type="auto">
  <name>Task 2: Generate Phase 18 VERIFICATION.md (NLQ UI)</name>
  <files>.planning/phases/18-claude-query-ui/18-VERIFICATION.md</files>
  <action>
Read the actual source code that Phase 18 built to gather verification evidence. Key files to read:
- `src/components/query/QuerySearchBar.tsx` or similar — the search bar component
- Any suggested prompts component/config — drill-level-aware prompt suggestions
- Response rendering component — streaming display, narrative format
- Loading/error state handling — timeout, retry, graceful errors
- Check how useChat is wired to `/api/query`

Also check `21-VERIFICATION.md` for NLQ-09 (context scoping bug fix).

Write `.planning/phases/18-claude-query-ui/18-VERIFICATION.md` following the exact format from `15-VERIFICATION.md`:

1. **YAML frontmatter**: phase, status (passed/partial), verified date (2026-04-14), verifier: phase-23-verification
2. **Goal Achievement**: Restate Phase 18 goal from ROADMAP.md, give overall result
3. **Success Criteria Verification**: Table with all 5 success criteria from ROADMAP Phase 18, each with Status and Evidence
4. **Requirement Traceability**: Table for NLQ-06 through NLQ-10, each with Plan, Status (VERIFIED/PARTIAL), and Evidence
   - NLQ-06 (Search bar input): Verify search bar component exists, is not a chat interface
   - NLQ-07 (Suggested prompts): Verify 3-5 prompts, drill-level awareness (root/partner/batch)
   - NLQ-08 (Streaming narrative): Verify response renders as paragraph with data points, token-by-token streaming
   - NLQ-09 (Context scoping): Reference Phase 21 fix, verify questions scope to current drill context
   - NLQ-10 (Loading/error/timeout): Verify loading indicator, error messages, 30-second timeout, retry option
   - Mark any requirement depending on live Snowflake as PARTIAL with note
5. **Build Verification**: Reference build results from Task 1 (same build)
6. **Artifacts Created**: Table of files and purposes from Phase 18

Same depth standard as Task 1 — deep walkthrough with specific file paths, line numbers, and observable behavior descriptions.
  </action>
  <verify>
    <automated>test -f ".planning/phases/18-claude-query-ui/18-VERIFICATION.md" && grep -c "NLQ-\(0[6-9]\|10\)" ".planning/phases/18-claude-query-ui/18-VERIFICATION.md" | grep -q "5" && echo "PASS: All 5 NLQ requirements referenced" || echo "FAIL: Missing NLQ requirement references"</automated>
  </verify>
  <done>18-VERIFICATION.md exists with YAML frontmatter, all 5 success criteria evaluated, all 5 NLQ requirements (06-10) have VERIFIED or PARTIAL status with specific code evidence</done>
</task>

</tasks>

<verification>
- Both VERIFICATION.md files exist and follow the established format (matching 15-VERIFICATION.md structure)
- Every NLQ requirement (01-10) appears in exactly one VERIFICATION.md with a status rating
- Evidence references actual source file paths, not generic claims
- Snowflake-dependent items marked PARTIAL with environment note
- YAML frontmatter is valid with phase, status, verified, verifier fields
</verification>

<success_criteria>
- 17-VERIFICATION.md covers NLQ-01, NLQ-02, NLQ-03, NLQ-04, NLQ-05 with evidence
- 18-VERIFICATION.md covers NLQ-06, NLQ-07, NLQ-08, NLQ-09, NLQ-10 with evidence
- Each requirement has Pass/Partial/Fail with explanation
- Professional SWE quality — a senior engineer would sign off on the evidence depth
</success_criteria>

<output>
After completion, create `.planning/phases/23-verification-housekeeping/23-01-SUMMARY.md`
</output>
