---
phase: 16-anomaly-detection-ui
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/components/anomaly/anomaly-summary-panel.tsx
  - src/components/data-display.tsx
  - src/components/charts/collection-curve-chart.tsx
  - src/components/charts/use-curve-chart-state.ts
  - src/components/charts/curve-tooltip.tsx
autonomous: true
requirements: [AD-09, AD-10]

must_haves:
  truths:
    - "A collapsible panel at the top of root view shows how many anomalies are detected and lists the top 5 flagged partners by severity"
    - "Clicking a flagged entry in the summary panel drills into that partner"
    - "The summary panel is collapsed by default showing only 'X anomalies detected'"
    - "Anomalous batch curves on the collection curve chart are rendered in red/amber with thicker strokes"
    - "Non-anomalous curves get reduced opacity when any anomalous batches exist"
    - "Hovering an anomalous curve shows anomaly info in the tooltip"
  artifacts:
    - path: "src/components/anomaly/anomaly-summary-panel.tsx"
      provides: "Collapsible panel listing top flagged partners with drill navigation"
    - path: "src/components/charts/collection-curve-chart.tsx"
      provides: "Anomaly-aware line styling (color, stroke, opacity)"
    - path: "src/components/charts/curve-tooltip.tsx"
      provides: "Extended tooltip showing anomaly info for flagged batches"
  key_links:
    - from: "src/components/anomaly/anomaly-summary-panel.tsx"
      to: "AnomalyProvider context"
      via: "useAnomalyContext()"
      pattern: "useAnomalyContext"
    - from: "src/components/anomaly/anomaly-summary-panel.tsx"
      to: "src/components/data-display.tsx"
      via: "onDrillToPartner callback prop"
      pattern: "onDrillToPartner"
    - from: "src/components/charts/collection-curve-chart.tsx"
      to: "anomaly batch data"
      via: "batchAnomalies prop from DataDisplay"
      pattern: "batchAnomalies"
---

<objective>
Add the anomaly summary panel at root level and chart highlighting for anomalous batches on collection curve charts.

Purpose: Users can quickly scan top anomalies from a summary banner and visually identify flagged batches on charts without needing to read individual table rows.
Output: Collapsible summary panel with drill navigation, and anomaly-aware chart styling.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-anomaly-detection-ui/16-RESEARCH.md
@.planning/phases/16-anomaly-detection-ui/16-01-SUMMARY.md

@src/types/partner-stats.ts
@src/contexts/anomaly-provider.tsx
@src/components/data-display.tsx
@src/components/charts/collection-curve-chart.tsx
@src/components/charts/use-curve-chart-state.ts
@src/components/charts/curve-tooltip.tsx
@src/components/charts/pivot-curve-data.ts
@src/lib/formatting/anomaly-labels.ts

<interfaces>
<!-- Key types and contracts from Plan 16-01 outputs -->

From src/lib/formatting/anomaly-labels.ts (created in 16-01):
```typescript
export function getMetricLabel(metric: string): string;
export function classifySeverity(anomaly: { flags: MetricAnomaly[]; severityScore: number }): 'warning' | 'critical';
export const SEVERITY_COLORS: { warning: string; critical: string };
```

From src/contexts/anomaly-provider.tsx:
```typescript
export function useAnomalyContext(): { partnerAnomalies: Map<string, PartnerAnomaly> };
```

From src/types/partner-stats.ts:
```typescript
export interface PartnerAnomaly {
  isFlagged: boolean;
  severityScore: number;
  flaggedBatchCount: number;
  totalBatchCount: number;
  latestBatch: BatchAnomaly | null;
  batches: BatchAnomaly[];
  usedPortfolioFallback: boolean;
}
export interface BatchAnomaly {
  batchName: string;
  isFlagged: boolean;
  flags: MetricAnomaly[];
  groups: AnomalyGroup[];
  severityScore: number;
}
```

From src/components/charts/collection-curve-chart.tsx:
```typescript
interface CollectionCurveChartProps {
  curves: BatchCurve[];
}
```

From src/components/charts/use-curve-chart-state.ts:
```typescript
export function useCurveChartState(curves: BatchCurve[]): {
  metric, showAverage, showAllBatches, sortedCurves, newestBatchKey,
  visibleBatchKeys, defaultVisibleKeys, hiddenBatches,
  toggleMetric, handleLineClick, toggleBatchVisibility, toggleAverage,
  toggleShowAll, getLineOpacity, getLineStrokeWidth,
};
```

From src/components/charts/pivot-curve-data.ts:
```typescript
export const BATCH_KEY_PREFIX: string;
export type BatchKeyMap = Map<string, { batchName: string; totalPlaced: number; ageInMonths: number }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create anomaly summary panel for root view</name>
  <files>
    src/components/anomaly/anomaly-summary-panel.tsx
    src/components/data-display.tsx
  </files>
  <action>
**1. Create `src/components/anomaly/anomaly-summary-panel.tsx`:**

- Props: `onDrillToPartner: (name: string) => void`.
- Call `useAnomalyContext()` to get `partnerAnomalies`.
- Compute `flaggedPartners`: filter the map for `isFlagged === true`, sort descending by `severityScore`, take top 5.
- If no flagged partners, return `null` (no panel rendered).

**Collapsed state (default):**
- A compact bar (full width, rounded, `bg-amber-50 dark:bg-amber-950/30 border border-amber-200 dark:border-amber-800` or similar subtle warning styling).
- Content: An AlertTriangle icon (from lucide-react) + text "N anomalies detected" + a ChevronDown toggle button on the right.
- Use `useState(false)` for `isExpanded`.

**Expanded state:**
- Below the collapsed bar, show a list of up to 5 entries.
- Each entry is a clickable row showing:
  - Severity dot (reuse `SEVERITY_COLORS` from anomaly-labels or inline the same amber-500/red-500 classes)
  - Partner name (bold)
  - Severity label ("Warning" or "Critical")
  - Top anomalous metric from the partner's latestBatch: `getMetricLabel(latestBatch.flags[0].metric)` + formatted deviation (e.g., "2.4 SD below")
- Clicking an entry calls `onDrillToPartner(partnerName)`.
- Use a smooth height transition for expand/collapse. Can use `overflow-hidden` with `max-h-0`/`max-h-[300px]` transition, or Tailwind's `transition-all duration-200`.
- Add a "Collapse" chevron button (ChevronUp) when expanded.

**2. Modify `src/components/data-display.tsx`:**
- Import `AnomalySummaryPanel`.
- Render `<AnomalySummaryPanel onDrillToPartner={drillToPartner} />` inside the AnomalyProvider tree, at root level only.
- Place it ABOVE the schema warning alert and ABOVE the KPI cards, as the first item in the flex column -- but only when `drillState.level === 'root'`.
- Add: `{drillState.level === 'root' && <AnomalySummaryPanel onDrillToPartner={drillToPartner} />}` as the first child after `<AnomalyProvider>` opens the `<div className="flex h-[calc(...)] flex-col gap-2">`.
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    Summary panel appears at root level as a collapsed bar.
    Expanding shows top 5 flagged partners.
    Clicking an entry drills into that partner.
    Panel does not appear at partner or batch drill-down levels.
  </verify>
  <done>
    Collapsible anomaly summary panel appears at top of root view.
    Collapsed state shows "N anomalies detected" with expand toggle.
    Expanded state lists top 5 partners sorted by severity with name, severity level, and top anomalous metric.
    Clicking an entry drills into that partner's view.
    Panel hidden at non-root drill levels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add anomaly highlighting to collection curve charts</name>
  <files>
    src/components/charts/collection-curve-chart.tsx
    src/components/charts/use-curve-chart-state.ts
    src/components/charts/curve-tooltip.tsx
    src/components/data-display.tsx
  </files>
  <action>
**1. Modify `src/components/charts/collection-curve-chart.tsx`:**
- Add prop `batchAnomalies?: BatchAnomaly[]` to `CollectionCurveChartProps`.
- Pass `batchAnomalies` to `useCurveChartState` (new optional parameter).
- In the Line rendering loop, determine if a batch is anomalous by looking up `batchAnomalies?.find(b => b.batchName === keyMap.get(key)?.batchName)?.isFlagged`.
- For anomalous batches:
  - Override stroke color to `hsl(0, 72%, 51%)` (Tailwind red-500 equivalent) for critical, `hsl(38, 92%, 50%)` (amber-500) for warning. Use `classifySeverity` to determine level.
  - Override strokeWidth to 3 (vs. default 2 or 1.5).
- For non-anomalous batches when ANY anomalous batch exists:
  - Reduce opacity to 0.3 (dimmed).
- When no anomalous batches exist, render everything normally (no opacity changes).

**2. Modify `src/components/charts/use-curve-chart-state.ts`:**
- Add optional `batchAnomalies?: BatchAnomaly[]` parameter to `useCurveChartState`.
- Add derived boolean `hasAnyAnomalies = batchAnomalies?.some(b => b.isFlagged) ?? false`.
- Create helper `isBatchAnomalous(batchKey: string): boolean` that maps batchKey to sortedCurves index, then checks batchAnomalies.
- Modify `getLineOpacity(key: string)`: if `hasAnyAnomalies && !isBatchAnomalous(key)`, return 0.3. Otherwise use existing logic.
- Modify `getLineStrokeWidth(key: string)`: if `isBatchAnomalous(key)`, return 3. Otherwise use existing logic.
- Export `hasAnyAnomalies` and `isBatchAnomalous` from the hook return.
- Add a new function `getAnomalyLineColor(key: string): string | null` that returns the anomaly color if flagged, null otherwise. Consumer uses this to override the default CHART_COLORS.

**3. Modify `src/components/charts/curve-tooltip.tsx`:**
- Add optional `batchAnomalies?: BatchAnomaly[]` prop to `CurveTooltipProps`.
- When rendering tooltip entries, if the batch is anomalous (look up by batchName from keyMap), append anomaly info below the value:
  - A small red/amber dot + text like "Anomaly: Penetration Rate 2.4 SD below" (show the top flag from the batch's flags array).
  - Use `getMetricLabel` and format the deviation.
- Keep it compact -- one line of anomaly info per flagged batch entry in the tooltip.

**4. Modify `src/components/data-display.tsx`:**
- At the partner drill-down level where CollectionCurveChart is rendered, pass the batch anomaly data.
- Get batch anomalies from: `partnerStats?.anomalies?.partner.batches` (from usePartnerStats) OR fall back to the AnomalyProvider context for partners with <3 batches.
- For the root-level provider access (partners <3 batches), use `useAnomalyContext()` -- but this needs to be called inside the AnomalyProvider. Since DataDisplay already renders inside AnomalyProvider, add `useAnomalyContext()` at the top of the returned JSX's scope (or in a wrapper component).
- The simplest approach: Extract a small `useAnomalyData` call at the component level. Since DataDisplay wraps everything in AnomalyProvider, but the hook must be called inside the provider, and currently the DataDisplay function body is outside the provider's render...
- **Resolution:** The `useAnomalyContext()` call should go inside a child component. Since DataTable already calls it (from Plan 16-01), and CollectionCurveChart is separate, the cleanest approach is to create a thin wrapper:
  - In DataDisplay, wrap the partner-level chart section in a component that can access the context. OR simply import `useAnomalyContext` directly in CollectionCurveChart and access the context there.
  - **Best approach:** Import `useAnomalyContext` directly in DataDisplay after moving the entire content inside AnomalyProvider. Check if DataDisplay already has hooks called before the provider render -- if so, create an inner `DataDisplayContent` component.
  - Actually, looking at the existing code: DataDisplay returns `<AnomalyProvider allRows={data.data}><div>...</div></AnomalyProvider>`. So the div and everything inside IS a child of AnomalyProvider. But React hooks in DataDisplay are called BEFORE the return statement, outside the Provider's render scope. Calling `useAnomalyContext()` in DataDisplay itself would fail because DataDisplay is NOT inside an AnomalyProvider (it IS the AnomalyProvider).
  - **Solution:** Pass `batchAnomalies` from the `partnerStats?.anomalies?.partner.batches` directly. For partners with <3 batches where `partnerStats.anomalies` is undefined, look up from the root context. BUT DataDisplay can't call `useAnomalyContext()`.
  - **Best solution:** Create a small component `AnomalyAwareChart` that wraps CollectionCurveChart and calls `useAnomalyContext()` inside the provider tree. It computes the correct batchAnomalies array and passes it down. OR just have CollectionCurveChart call `useAnomalyContext()` directly (chart is always rendered inside AnomalyProvider).
  - **Go with:** Have CollectionCurveChart optionally call `useAnomalyContext()` if available (try/catch or conditional). Actually -- the chart IS always inside AnomalyProvider in DataDisplay, so just call `useAnomalyContext()` directly in CollectionCurveChart. Look up the current partner's batch anomalies by matching curve batchNames. This is the simplest approach with no prop-drilling.
- In CollectionCurveChart: import `useAnomalyContext`, get `partnerAnomalies`, find the partner whose batches match the curves prop (match by batchName overlap), extract batch anomalies, pass to `useCurveChartState` and `CurveTooltip`.
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    `npm run build` succeeds.
    At partner drill-down with anomalous batches, flagged batch curves appear in red/amber with thicker strokes.
    Non-anomalous curves are dimmed when anomalies present.
    Chart tooltip shows anomaly info when hovering a flagged batch.
  </verify>
  <done>
    Anomalous batch curves rendered in warning/critical color with thicker stroke (3px vs default).
    Non-anomalous curves dimmed to 0.3 opacity when any anomalies present.
    Hovering an anomalous curve shows anomaly info (flagged metric + deviation) in tooltip.
    Chart renders normally when no anomalies detected.
  </done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` succeeds with no errors
- [ ] Summary panel appears at root level, collapsed by default
- [ ] Expanding panel shows top 5 flagged partners sorted by severity
- [ ] Each entry shows partner name, severity, top anomalous metric
- [ ] Clicking entry drills to that partner
- [ ] Panel hidden at partner and batch levels
- [ ] Anomalous batch curves highlighted with different color and thicker stroke
- [ ] Non-anomalous curves dimmed when anomalies present
- [ ] Chart tooltip includes anomaly info for flagged batches
- [ ] Chart renders normally when no anomalies exist
</verification>

<success_criteria>
AD-09: Collapsible anomaly summary panel at top of root view lists top flagged partners/batches by severity with click-to-drill navigation.
AD-10: Anomalous batches visually distinguished on collection curve charts with bold colored lines and enhanced tooltips.
</success_criteria>

<output>
After completion, create `.planning/phases/16-anomaly-detection-ui/16-02-SUMMARY.md`
</output>
