---
phase: 16-anomaly-detection-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/anomaly/anomaly-badge.tsx
  - src/components/anomaly/anomaly-detail.tsx
  - src/lib/columns/anomaly-column.tsx
  - src/lib/columns/definitions.ts
  - src/lib/table/hooks.ts
  - src/lib/formatting/anomaly-labels.ts
autonomous: true
requirements: [AD-07, AD-08]

must_haves:
  truths:
    - "Anomaly badges (colored dots) appear as the leftmost column on partner rows at root level and batch rows at partner drill-down without user action"
    - "Clicking an anomaly badge opens a popover showing all anomalous metrics with actual value, expected range, and deviation magnitude"
    - "Non-anomalous rows show an empty Status cell (no green dot, no icon)"
    - "Popover stays open until user dismisses it (click-triggered, not hover)"
  artifacts:
    - path: "src/components/anomaly/anomaly-badge.tsx"
      provides: "Colored dot component (yellow=warning, red=critical) with click handler"
    - path: "src/components/anomaly/anomaly-detail.tsx"
      provides: "Popover content listing all anomalous metrics with values and ranges"
    - path: "src/lib/columns/anomaly-column.tsx"
      provides: "TanStack ColumnDef for Status column that reads anomaly data from table meta"
    - path: "src/lib/formatting/anomaly-labels.ts"
      provides: "Metric display name mapping and severity classification utility"
  key_links:
    - from: "src/lib/columns/anomaly-column.tsx"
      to: "AnomalyProvider context"
      via: "table.options.meta.anomalyMap"
      pattern: "meta\\.anomalyMap"
    - from: "src/lib/columns/definitions.ts"
      to: "src/lib/columns/anomaly-column.tsx"
      via: "prepending anomaly column to columnDefs array"
      pattern: "anomalyStatusColumn"
    - from: "src/components/anomaly/anomaly-badge.tsx"
      to: "src/components/anomaly/anomaly-detail.tsx"
      via: "Popover trigger wrapping badge, content rendering detail"
      pattern: "PopoverTrigger.*PopoverContent"
---

<objective>
Create the anomaly Status column with badge indicators and detail popovers for the data table.

Purpose: Users see at a glance which partners/batches are flagged, and can click to understand exactly which metrics are anomalous and by how much.
Output: Status column with colored dot badges at root and partner drill-down levels, click-triggered popovers with full metric detail.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-anomaly-detection-ui/16-RESEARCH.md

@src/types/partner-stats.ts
@src/contexts/anomaly-provider.tsx
@src/lib/columns/definitions.ts
@src/lib/columns/config.ts
@src/lib/table/hooks.ts
@src/components/data-display.tsx
@src/components/ui/popover.tsx

<interfaces>
<!-- Key types the executor needs -->

From src/types/partner-stats.ts:
```typescript
export interface MetricAnomaly {
  metric: string;
  value: number;
  zScore: number;
  direction: 'above' | 'below';
}
export interface AnomalyGroup {
  groupKey: string;
  label: string;
  flags: MetricAnomaly[];
  avgDeviation: number;
}
export interface BatchAnomaly {
  batchName: string;
  isFlagged: boolean;
  flags: MetricAnomaly[];
  groups: AnomalyGroup[];
  severityScore: number;
}
export interface PartnerAnomaly {
  isFlagged: boolean;
  severityScore: number;
  flaggedBatchCount: number;
  totalBatchCount: number;
  latestBatch: BatchAnomaly | null;
  batches: BatchAnomaly[];
  usedPortfolioFallback: boolean;
}
export interface MetricNorm {
  mean: number;
  stddev: number;
  count: number;
}
```

From src/contexts/anomaly-provider.tsx:
```typescript
export function useAnomalyContext(): { partnerAnomalies: Map<string, PartnerAnomaly> };
```

From src/lib/columns/definitions.ts:
```typescript
export interface TableDrillMeta {
  onDrillToPartner?: (name: string) => void;
  onDrillToBatch?: (name: string, partnerName?: string) => void;
  drillLevel?: DrillLevel;
  trending?: TrendingData;
  norms?: Record<string, MetricNorm> | null;
  heatmapEnabled?: boolean;
}
```

From src/lib/computation/compute-anomalies.ts:
```typescript
export const ANOMALY_METRICS: readonly string[];
```

From src/lib/computation/compute-norms.ts:
```typescript
export function computeNorms(rows: Record<string, unknown>[]): Record<string, MetricNorm>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create anomaly formatting utilities, badge component, and detail popover</name>
  <files>
    src/lib/formatting/anomaly-labels.ts
    src/components/anomaly/anomaly-badge.tsx
    src/components/anomaly/anomaly-detail.tsx
  </files>
  <action>
**1. Create `src/lib/formatting/anomaly-labels.ts`:**
- Export a `METRIC_LABELS` record mapping ANOMALY_METRICS keys to human-readable names. Examples: `PENETRATION_RATE_POSSIBLE_AND_CONFIRMED` -> "Penetration Rate", `COLLECTION_AFTER_6_MONTH` -> "6-Month Collection", `AVG_EXPERIAN_CA_SCORE` -> "Avg Credit Score", etc.
- Export `getMetricLabel(metric: string): string` that looks up the label or falls back to title-casing the key.
- Export `classifySeverity(anomaly: { flags: MetricAnomaly[]; severityScore: number }): 'warning' | 'critical'` -- returns `'critical'` if `flags.length >= 4`, otherwise `'warning'`. This is the single source of truth for severity classification.
- Export `formatExpectedRange(mean: number, stddev: number, metric: string): string` that returns a formatted range string like "8.1% - 14.3%" using the existing `getFormatter` from `src/lib/formatting/` for the metric's column type (look up type from COLUMN_CONFIGS).

**2. Create `src/components/anomaly/anomaly-badge.tsx`:**
- `AnomalyBadge` component accepting `severity: 'warning' | 'critical'` and `onClick: () => void`.
- Renders a small circle (w-3 h-3 rounded-full) with: warning = `bg-amber-500`, critical = `bg-red-500`.
- Add a subtle pulse animation on the critical dot using Tailwind's `animate-pulse` class.
- The dot is a button with `aria-label` describing the severity (e.g., "Warning: anomaly detected").
- Export the color constants for reuse: `SEVERITY_COLORS = { warning: 'text-amber-500', critical: 'text-red-500' }`.

**3. Create `src/components/anomaly/anomaly-detail.tsx`:**
- `AnomalyDetail` component accepting `anomaly: BatchAnomaly | PartnerAnomaly`, `norms: Record<string, MetricNorm> | null`, and `entityName: string`.
- Header: severity label + metric count. E.g., "Warning -- 3 anomalous metrics" using the `classifySeverity` utility.
- For each `AnomalyGroup` in the anomaly's groups array, render the group label as a subheading.
- Under each group, list each `MetricAnomaly` flag showing: metric label (from `getMetricLabel`), actual value (formatted by type), expected range (from `formatExpectedRange` using norms), and deviation (e.g., "2.4 SD below mean" using `Math.abs(zScore).toFixed(1)` + direction).
- If anomaly has no groups (edge case), list flags directly without grouping.
- Use Tailwind prose-like styling. Severity colors for the header badge.
- Max width of ~320px for the popover content. No scrolling needed (bounded by 2+ metric threshold, typically 2-6 items).
  </action>
  <verify>
    TypeScript compilation: `npx tsc --noEmit` passes with no errors in the new files.
    Confirm the three files exist and export expected symbols.
  </verify>
  <done>
    anomaly-labels.ts exports getMetricLabel, classifySeverity, formatExpectedRange.
    AnomalyBadge renders colored dot with click handler.
    AnomalyDetail renders grouped metric list with values, ranges, and deviations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Status column definition and wire into table</name>
  <files>
    src/lib/columns/anomaly-column.tsx
    src/lib/columns/definitions.ts
    src/lib/table/hooks.ts
    src/components/data-display.tsx
  </files>
  <action>
**1. Create `src/lib/columns/anomaly-column.tsx`:**
- Export `anomalyStatusColumn: ColumnDef<Record<string, unknown>>` -- a TanStack Table column definition.
- Column id: `'__anomaly_status'` (prefixed to avoid collision with data columns).
- Header: empty string (no header text -- the colored dots are self-explanatory).
- Size: 40px fixed width. `enableSorting: false`, `enableHiding: false`, `enableResizing: false`.
- Cell renderer logic:
  - Read `meta` from `ctx.table.options.meta as TableDrillMeta & { anomalyMap?: Map<string, PartnerAnomaly> }`.
  - At root level (`meta.drillLevel === 'root'` or undefined): look up `meta.anomalyMap?.get(String(ctx.row.original.PARTNER_NAME ?? ''))`. If `isFlagged`, get severity via `classifySeverity`, render Popover with AnomalyBadge as trigger and AnomalyDetail as content.
  - At partner level (`meta.drillLevel === 'partner'`): look up partner from anomalyMap, then find batch via `partner.batches.find(b => b.batchName === String(ctx.row.original.BATCH ?? ''))`. If batch `isFlagged`, render Popover with badge and detail.
  - At batch level (`meta.drillLevel === 'batch'`): return null (no anomaly badges at account level).
  - If not flagged or no anomaly data: return null (empty cell).
  - Pass norms from meta to AnomalyDetail for range formatting.
- Use `createElement` for JSX (matching existing pattern in definitions.ts) OR use .tsx extension.
- Import Popover, PopoverTrigger, PopoverContent from `@/components/ui/popover`.

**2. Modify `src/lib/columns/definitions.ts`:**
- Import `anomalyStatusColumn` from `./anomaly-column`.
- In `buildColumnDefs()`, prepend the anomaly column to the returned array: `return [anomalyStatusColumn, ...COLUMN_CONFIGS.map(...)];`.

**3. Modify `src/lib/table/hooks.ts`:**
- Extend the `TableDrillMeta` type (or its usage in meta) to include `anomalyMap?: Map<string, PartnerAnomaly>`.
- Actually -- `TableDrillMeta` is defined in `definitions.ts`. Add `anomalyMap?: Map<string, PartnerAnomaly>` to the `TableDrillMeta` interface there.
- In `useDataTable`, accept `anomalyMap` in options and pass it through to table meta alongside existing drillLevel, trending, norms, heatmapEnabled.

**4. Modify `src/components/data-display.tsx`:**
- Import `useAnomalyContext` from `@/contexts/anomaly-provider`.
- Call `const { partnerAnomalies } = useAnomalyContext();` inside the DataDisplay component (after the AnomalyProvider wrapper -- it's inside the provider already).
- Wait -- DataDisplay renders AnomalyProvider at the top level and the table inside it. The `useAnomalyContext()` call must be in a child of AnomalyProvider. The DataTable component is inside the provider. So either:
  a) Pass `partnerAnomalies` as a prop to DataTable, OR
  b) Call `useAnomalyContext()` inside DataTable itself.
- Option (a) is cleaner since DataTable already receives many props. Add `anomalyMap?: Map<string, PartnerAnomaly>` to DataTableProps.
- In DataDisplay, extract anomalyContext inside the AnomalyProvider render tree. To do this, extract the inner content into a separate component (e.g., `DataDisplayContent`) that uses `useAnomalyContext()`, OR simply move the useAnomalyContext call to DataTable.
- **Simplest approach:** In `src/components/table/data-table.tsx`, import and call `useAnomalyContext()` directly. It's already inside the AnomalyProvider tree. Then pass `partnerAnomalies` into useDataTable options as `anomalyMap`.
- Update `useDataTable` to pass `anomalyMap` from options into table meta.

Also ensure the Status column:
- Does NOT appear in the column picker sidebar (since enableHiding is false, TanStack Table should exclude it automatically).
- Is pinned left alongside identity columns -- add `'__anomaly_status'` to the columnPinning.left array in useDataTable's initial state.
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    `npm run build` succeeds.
    At root level, the Status column appears as the first column. Flagged partners show colored dots, non-flagged show empty cells. Clicking a dot opens a popover with metric details.
  </verify>
  <done>
    Status column is the leftmost column at root and partner drill-down levels.
    Anomaly badges show yellow (warning) or red (critical) dots for flagged entities.
    Clicking a badge opens a popover showing all anomalous metrics with actual values, expected ranges, and deviation magnitudes.
    Non-anomalous rows have empty Status cells.
    Status column does not appear in column picker.
  </done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` succeeds with no errors
- [ ] Status column visible as first column at root and partner levels
- [ ] Status column absent at batch (account) level
- [ ] Flagged partners show colored badge (yellow or red)
- [ ] Non-flagged rows show empty cell
- [ ] Clicking badge opens popover with metric details
- [ ] Popover shows: metric name, actual value, expected range, deviation
- [ ] Popover closes on click outside
</verification>

<success_criteria>
AD-07: Anomaly badge displayed in Status column on partner rows (root) and batch rows (partner drill-down) without user configuration.
AD-08: Clicking badge shows popover with all anomalous metrics, actual values, expected ranges, and deviation magnitudes.
</success_criteria>

<output>
After completion, create `.planning/phases/16-anomaly-detection-ui/16-01-SUMMARY.md`
</output>
