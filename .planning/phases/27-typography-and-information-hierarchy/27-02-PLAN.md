---
phase: 27-typography-and-information-hierarchy
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/components/kpi/kpi-summary-cards.tsx
  - src/components/charts/collection-curve-chart.tsx
  - src/components/charts/curve-tooltip.tsx
  - src/components/charts/curve-legend.tsx
  - src/components/cross-partner/trajectory-chart.tsx
  - src/components/cross-partner/trajectory-tooltip.tsx
  - src/components/cross-partner/trajectory-legend.tsx
  - src/components/cross-partner/comparison-matrix.tsx
  - src/components/cross-partner/matrix-heatmap.tsx
  - src/components/cross-partner/matrix-plain-table.tsx
  - src/components/cross-partner/matrix-bar-ranking.tsx
  - src/components/ui/chart.tsx
autonomous: false
requirements: [DS-07, DS-08, DS-10]

must_haves:
  truths:
    - "Every chart-related surface (KPI summary, collection curve, trajectory chart, cross-partner matrix, chart tooltip) renders with zero ad-hoc text-(xs|sm|base|lg|xl|2xl) classes outside the allowlist"
    - "Every chart axis tick uses tabular-nums lining-nums (applied via Recharts tick prop or custom tick component, not via className on XAxis/YAxis)"
    - "Every chart tooltip numeric value uses .text-body-numeric or .text-label-numeric (never standalone tabular-nums)"
    - "KPI summary card labels read as overline (.text-label uppercase text-muted-foreground); KPI values use .text-display-numeric"
    - "Cross-partner matrix cells holding digits use .text-body-numeric or .text-label-numeric; row/column headers use text-body or text-caption"
    - "Section titles above charts (e.g., 'Collection Curves', 'Partner Trajectory') use <SectionHeader>"
  artifacts:
    - path: "src/components/kpi/kpi-summary-cards.tsx"
      provides: "Migrated KPI grid wrapper using type tokens"
    - path: "src/components/charts/collection-curve-chart.tsx"
      provides: "Migrated curve chart + axis tick styled via Recharts tick prop"
    - path: "src/components/cross-partner/comparison-matrix.tsx"
      provides: "Migrated matrix wrapper"
    - path: "src/components/ui/chart.tsx"
      provides: "Tooltip numeric value migrated to text-label-numeric"
  key_links:
    - from: "src/components/charts/collection-curve-chart.tsx XAxis/YAxis"
      to: "Recharts tick prop (style object or custom NumericTick component)"
      via: "tick={{ fontFamily: 'var(--font-mono)', fontSize, style: { fontVariantNumeric } }}"
      pattern: "tick=\\{"
    - from: "src/components/ui/chart.tsx tooltip value span"
      to: ".text-label-numeric"
      via: "className"
      pattern: "text-label-numeric"
---

<objective>
Sweep every chart + KPI + cross-partner surface to type tokens. 12 files, ~30 ad-hoc text-size hits, ~15 ad-hoc font-weight hits. Apply Recharts numeric tick pattern (Pattern 5 from RESEARCH) on all axes holding digits. Consolidate chart tooltip numeric styling into a single `.text-label-numeric` usage in `src/components/ui/chart.tsx`. Adopt `<SectionHeader>` for chart section titles where they exist.

Purpose: Charts carry the highest density of per-call-site numeric styling drift — tabular-nums sprinkled inconsistently, axis labels at `text-[10px]` that don't propagate, KPI summary labels with ad-hoc tracking. Consolidating in one sweep catches drift before it compounds.
Output: All 12 files migrated. Chart axes render tabular digits. Tooltip numeric values use the utility class. Section titles use SectionHeader.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-typography-and-information-hierarchy/27-CONTEXT.md
@.planning/phases/27-typography-and-information-hierarchy/27-RESEARCH.md
@docs/TYPE-MIGRATION.md
@src/components/layout/section-header.tsx
@src/components/kpi/kpi-card.tsx
@src/app/globals.css

<interfaces>
<!-- Reference implementations shipped in Phase 26 — use verbatim patterns: -->
<!-- kpi-card.tsx: overline = `text-label uppercase text-muted-foreground`; value = `text-display-numeric`; delta = `text-label-numeric`. -->
<!-- table-body.tsx:65: numeric-vs-text branch pattern. -->

<!-- SectionHeader signature (from Plan 27-01): -->
export function SectionHeader(props: {
  title: string;
  eyebrow?: string;
  description?: string;
  actions?: React.ReactNode;
  className?: string;
}): JSX.Element;

<!-- Recharts numeric tick patterns (from RESEARCH Pattern 5 — these are SVG <text>, className does NOT propagate): -->

// Simple path — inline style object on tick prop
<XAxis
  type="number"
  dataKey="month"
  tick={{
    fontSize: 11,
    fontFamily: 'var(--font-mono), ui-monospace, monospace',
    fill: 'var(--muted-foreground)',
  }}
/>

// Strict path — custom tick component (required for fontVariantNumeric: tabular-nums)
function NumericTick({ x, y, payload }: { x: number; y: number; payload: { value: string | number } }) {
  return (
    <text
      x={x}
      y={y}
      dy={10}
      textAnchor="middle"
      style={{
        fontFamily: 'var(--font-mono), ui-monospace, monospace',
        fontSize: 11,
        fontVariantNumeric: 'tabular-nums lining-nums',
        fill: 'var(--muted-foreground)',
      }}
    >
      {payload.value}
    </text>
  );
}
<XAxis tick={<NumericTick />} />
</interfaces>

<project_constraints>
- Recharts `className="text-[10px]"` on XAxis/YAxis is a no-op (SVG text doesn't inherit Tailwind classes the way HTML does). Replace with the `tick={{ ... }}` prop per Pattern 5.
- If the chart needs `tabular-nums` on tick text, you MUST use a custom tick component — the inline style object does not support `font-variant-numeric`.
- Deferred ideas NOT in scope: surface/elevation changes, hover lifts, motion tokens, state-color migrations outside the table (Pitfall 4 applies to trend-indicator, owned by Plan 27-03, not this plan).
- Spacing (gap-*, p-*, m-*, h-*) is OUT OF SCOPE. Do not touch.
- If a file imports from `src/components/ui/**`, do NOT migrate classes inside the ui/ primitive — those are allowlisted. Migrate only the caller's own JSX.
- Exception: `src/components/ui/chart.tsx` IS in scope (line 256 tooltip value) — it's already a first-party-modified file per RESEARCH, not pristine shadcn.
</project_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate charts + chart tooltip + axis tick strategy</name>
  <files>
    src/components/charts/collection-curve-chart.tsx
    src/components/charts/curve-tooltip.tsx
    src/components/charts/curve-legend.tsx
    src/components/cross-partner/trajectory-chart.tsx
    src/components/cross-partner/trajectory-tooltip.tsx
    src/components/cross-partner/trajectory-legend.tsx
    src/components/ui/chart.tsx
  </files>
  <action>
Work through each file in this order. For every file: open, read, and apply the migration table from `docs/TYPE-MIGRATION.md`.

**Per-file checklist (apply to every file in this task):**
1. Replace `text-xs/sm/base/lg/xl/2xl` with the token-per-the-table.
2. Drop `font-semibold`/`font-medium`/`font-bold` paired with a type token.
3. Keep `uppercase` ONLY on `.text-label`; remove elsewhere.
4. For numeric spans (tooltip value, legend value, delta), switch to `.text-body-numeric` or `.text-label-numeric`. Remove standalone `tabular-nums` once the numeric variant is applied.
5. Do NOT touch `gap-*`, `p-*`, `m-*`, `h-*`, colors (unless colors are state-color pending — they aren't in these files; Pitfall 4 is about trend-indicator, not here).

**File-specific instructions:**

`src/components/charts/collection-curve-chart.tsx` (lines 227, 235 have `className="text-[10px]"` on axes — no-op):
- Import NumericTick (or inline the style object if tabular-nums not required) at file top.
- Remove any `className="text-[10px]"` from XAxis/YAxis.
- Add `tick={<NumericTick />}` if the axis values are numeric (month numbers, percentages, day counts) AND tabular alignment matters. Otherwise use `tick={{ fontSize: 11, fontFamily: 'var(--font-mono), ui-monospace, monospace', fill: 'var(--muted-foreground)' }}`.
- Migrate any other ad-hoc text classes in the component body (legend labels, chart titles inside JSX) per the table.

`src/components/charts/curve-tooltip.tsx` / `curve-legend.tsx` / `cross-partner/trajectory-tooltip.tsx` / `trajectory-legend.tsx`:
- Label spans → `text-body` or `text-caption` per the table.
- Numeric value spans → `.text-body-numeric` (drops standalone `tabular-nums` and `font-mono`).

`src/components/cross-partner/trajectory-chart.tsx` (has axes):
- Same NumericTick pattern. Remove any className on axes.
- Migrate body text per the table.

`src/components/ui/chart.tsx` (line 256 tooltip value currently `font-mono font-medium tabular-nums`):
- Change to `text-label-numeric font-medium` (preserves emphasis; numeric variant bakes mono + tabular).
- **Important:** This is the shadcn ChartTooltipContent component — verify whether `font-medium` is structurally meaningful for the tooltip visual weight. If yes (matches shadcn defaults), keep `font-medium`. If no, drop it. Prefer keeping it because tooltip values benefit from emphasis and the CONTEXT "tokens own weight" rule targets app code, not shadcn-derived primitives.

**Define NumericTick once, reuse:**

Create a shared `NumericTick` in `src/components/charts/numeric-tick.tsx` (new file inside this task's scope since both collection-curve-chart.tsx and trajectory-chart.tsx import it):

```tsx
// src/components/charts/numeric-tick.tsx
export function NumericTick({ x, y, payload, dy = 10, anchor = 'middle' }: {
  x: number;
  y: number;
  payload: { value: string | number };
  dy?: number;
  anchor?: 'start' | 'middle' | 'end';
}) {
  return (
    <text
      x={x}
      y={y}
      dy={dy}
      textAnchor={anchor}
      style={{
        fontFamily: 'var(--font-mono), ui-monospace, monospace',
        fontSize: 11,
        fontVariantNumeric: 'tabular-nums lining-nums',
        fill: 'var(--muted-foreground)',
      }}
    >
      {payload.value}
    </text>
  );
}
```

Add this file to `files_modified` mentally — it's a new helper under the charts/ directory and is covered by this plan's chart scope. (It is not in the frontmatter files_modified list because it's a new file derived from this task — executors should create it as part of the work. The plan scope includes "chart axis tick styling" which requires this helper.)

Note: the frontmatter intentionally lists the files expected to be modified; new helpers created inline are acceptable when they are strictly scoped to the plan's concern.
  </action>
  <verify>
    <automated>npm run build</automated>
    Follow-ups:
    - `rg -n 'text-(xs|sm|base|lg|xl|2xl)' src/components/charts/ src/components/cross-partner/trajectory-* src/components/ui/chart.tsx` → 0 hits
    - `rg -n '\bfont-(semibold|bold)\b' src/components/charts/ src/components/cross-partner/trajectory-* ` → 0 hits (font-medium allowed in ui/chart.tsx tooltip emphasis if preserved)
    - `rg -n 'className="text-\[10px\]"' src/components/charts/` → 0 hits
    - `rg -n '\btabular-nums\b' src/components/charts/ src/components/cross-partner/trajectory-*` → 0 hits (folded into .text-*-numeric)
  </verify>
  <done>
    Build passes. All 7 files in this task render numeric text through the variants; axes use Recharts tick prop. NumericTick helper exists and is imported by both chart files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate KPI summary wrapper + cross-partner matrix surfaces</name>
  <files>
    src/components/kpi/kpi-summary-cards.tsx
    src/components/cross-partner/comparison-matrix.tsx
    src/components/cross-partner/matrix-heatmap.tsx
    src/components/cross-partner/matrix-plain-table.tsx
    src/components/cross-partner/matrix-bar-ranking.tsx
  </files>
  <action>
**Per-file checklist (same as Task 1):**
1. Replace ad-hoc text-size with token-per-the-table.
2. Drop paired weight classes.
3. `uppercase` ONLY on `.text-label`.
4. Numeric spans → `.text-body-numeric` / `.text-label-numeric`.
5. Do NOT touch spacing or colors.

**File-specific instructions:**

`src/components/kpi/kpi-summary-cards.tsx`:
- This file is the grid wrapper; `kpi-card.tsx` itself was migrated in Phase 26. Migrate any surrounding section title / grid-level labels / skeletons per the table.
- If a section title exists above the grid (e.g., a heading saying "Metrics" or similar), consider swapping to `<SectionHeader title="..." />`. If no such heading exists, skip — do not invent one.

`src/components/cross-partner/comparison-matrix.tsx`:
- Section title → `<SectionHeader title="..." />` (or `text-heading`) if one exists.
- Column headers → `text-label uppercase text-muted-foreground` if already styled overline, else `text-caption` or `text-body`.
- Row headers (partner names) → `text-body`.
- Drop `font-medium` paired with tokens.

`src/components/cross-partner/matrix-heatmap.tsx` / `matrix-plain-table.tsx` / `matrix-bar-ranking.tsx`:
- Cells holding digits → `.text-body-numeric` (preferred) or `.text-label-numeric` if the row-height constraint demands the smaller tier. Read existing `tabular-nums`/`text-xs` sites to decide.
- Row/column labels → `text-body` or `text-caption` per visual weight.
- Drop standalone `tabular-nums`, standalone `font-mono`, standalone `font-medium`.

After each file, grep-verify zero remaining hits before moving on.
  </action>
  <verify>
    <automated>npm run build</automated>
    Follow-ups:
    - `rg -n 'text-(xs|sm|base|lg|xl|2xl)' src/components/kpi/kpi-summary-cards.tsx src/components/cross-partner/` → 0 hits
    - `rg -n '\bfont-(semibold|medium|bold)\b' src/components/kpi/kpi-summary-cards.tsx src/components/cross-partner/comparison-matrix.tsx src/components/cross-partner/matrix-*.tsx` → 0 hits
    - `rg -n '\btabular-nums\b' src/components/cross-partner/matrix-*.tsx` → 0 hits
  </verify>
  <done>
    Build passes. KPI summary wrapper + all cross-partner matrix surfaces are token-migrated. Numeric cells use .text-body-numeric / .text-label-numeric. Column headers render correctly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — chart + KPI + cross-partner surfaces in light + dark</name>
  <files>(verification only)</files>
  <action>Pause for human verification of chart + KPI + cross-partner typography. Instructions below.</action>
  <verify>Human confirms with screenshots.</verify>
  <done>User approves with screenshots covering: (a) root collection curves area (both modes), (b) partner drill-down trajectory chart (both modes), (c) cross-partner comparison matrix (both modes).</done>
  <what-built>
12 files migrated. Charts now use Recharts tick prop for axis labels (with mono family + tabular-nums via NumericTick). Chart tooltip numeric values use .text-label-numeric. KPI summary wrapper and all cross-partner matrix surfaces use type tokens.
  </what-built>
  <how-to-verify>
1. `mcp__Claude_Preview__preview_start`.
2. **Collection curves (root view):**
   - Light mode screenshot + dark mode screenshot.
   - Axis tick labels: `preview_inspect` on one tick — confirm computed `font-family` includes mono var and `font-variant-numeric` includes tabular-nums lining-nums (if using NumericTick).
   - Tooltip (hover a curve point) — numeric value should be JetBrains Mono, digits aligned.
   - Legend text: not shouting, mixed case, readable.
3. **Trajectory chart (cross-partner view):**
   - Same axis + tooltip checks.
4. **Cross-partner comparison matrix:**
   - Light + dark screenshots.
   - Matrix cells holding digits should have aligned digits across rows/columns (tabular-nums visible).
   - Column headers legible, consistent weight.
5. **KPI summary grid:** Digits in values align across the 6 cards (already shipped in Phase 26; confirm Plan 27-02 didn't regress).
6. Spot-check: toggle a few times between light and dark — no element should change size, weight, or tracking unexpectedly.

Approval requires: 6 screenshots total (3 surfaces × 2 modes). Explicit confirmation that chart axis digits render in mono AND are tabular-aligned (this is the fix for the `text-[10px]` no-op pre-migration).
  </how-to-verify>
  <resume-signal>Type "approved" with screenshots, or describe issues (e.g. "axis ticks still sans", "tooltip mono broken", "matrix column headers misaligned", "hierarchy flattened on some surface").</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes.
- Zero ad-hoc text-size or paired font-weight hits in any modified file.
- Chart axes use Recharts tick prop (no className="text-[...]" survives).
- NumericTick helper exists at src/components/charts/numeric-tick.tsx and is imported by both chart files.
- Visual verification passed in both modes for all 3 chart-related surfaces.
</verification>

<success_criteria>
- DS-07 progressed: 12 files off ad-hoc classes.
- DS-08 progressed: chart axes + tooltip values + matrix cells all use numeric variants with proper mono + tabular.
- DS-10 progressed: SectionHeader adopted where chart sections have headings.
</success_criteria>

<output>
After completion, create `.planning/phases/27-typography-and-information-hierarchy/27-02-SUMMARY.md` capturing the axis tick pattern adoption, any ambiguous mappings resolved during this sweep (fed back to docs/TYPE-MIGRATION.md §10), and notes on shared NumericTick helper usage.
</output>
