---
phase: 27-typography-and-information-hierarchy
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/components/table/table-footer.tsx
  - src/components/table/trend-indicator.tsx
  - src/components/table/sort-dialog.tsx
  - src/components/table/sort-indicator.tsx
  - src/components/table/draggable-header.tsx
  - src/components/table/text-column-filter.tsx
  - src/components/table/numeric-column-filter.tsx
  - src/components/table/heatmap-toggle.tsx
  - src/components/table/column-preset-tabs.tsx
  - src/components/cross-partner/percentile-cell.tsx
autonomous: false
requirements: [DS-07, DS-08]

must_haves:
  truths:
    - "Every file under src/components/table/* (except table-body.tsx shipped in Phase 26) renders with zero ad-hoc text-(xs|sm|base|lg|xl|2xl) hits"
    - "trend-indicator.tsx numeric delta uses .text-label-numeric; emerald/red Tailwind classes replaced with text-success-fg / text-error-fg state tokens (Pitfall 4 scope expansion)"
    - "table-footer.tsx aggregate totals use .text-label-numeric (single class — no duplicate text-xs + tabular-nums + font-mono combo)"
    - "Column filter dialogs (text + numeric) use text-body for inputs/labels and text-caption for helper text"
    - "Sort dialog + sort indicator + draggable header + preset tabs all use type tokens"
    - "cross-partner/percentile-cell.tsx (numeric cell) uses .text-body-numeric"
  artifacts:
    - path: "src/components/table/trend-indicator.tsx"
      provides: "Trend delta using text-label-numeric AND state-color tokens"
      contains: "text-success-fg"
    - path: "src/components/table/table-footer.tsx"
      provides: "Aggregate row using text-label-numeric only"
  key_links:
    - from: "src/components/table/trend-indicator.tsx"
      to: "state-color tokens (text-success-fg, text-error-fg)"
      via: "className"
      pattern: "text-(success|error)-fg"
    - from: "src/components/table/table-footer.tsx aggregate cell"
      to: ".text-label-numeric"
      via: "className"
      pattern: "text-label-numeric"
---

<objective>
Sweep all table-related surfaces (except table-body.tsx, shipped in Phase 26) plus the cross-partner percentile cell. 10 files, ~28 ad-hoc text-size hits, ~6 ad-hoc font-weight hits.

**Scope expansion per RESEARCH Pitfall 4:** `trend-indicator.tsx` currently uses Tailwind `text-emerald-*` / `text-red-*` classes (lines 48–50) for positive/negative delta colors. Phase 26-02 migrated KPI card to `text-success-fg` / `text-error-fg`. The app ships two different "positive green" values. Since this plan touches the file for typography, migrate the state colors in the same pass (one-to-one swap, file is ~110 lines, scope is justified). State-color migrations in OTHER files (e.g., header amber, KPI card) are NOT in scope.

Purpose: Tables are the highest-density data surface — any inconsistency here is visible on every page. Consolidating table-footer's triple class `text-xs font-mono tabular-nums` into a single `.text-label-numeric` is a canonical drift-removal win.
Output: All 10 files migrated. Trend indicator uses state-color tokens. Zero ad-hoc hits.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-typography-and-information-hierarchy/27-CONTEXT.md
@.planning/phases/27-typography-and-information-hierarchy/27-RESEARCH.md
@docs/TYPE-MIGRATION.md
@src/components/table/table-body.tsx
@src/components/kpi/kpi-card.tsx
@src/app/globals.css

<interfaces>
<!-- Already-shipped reference patterns: -->

<!-- table-body.tsx:65 (Phase 26-04 pilot) — numeric-vs-text branch: -->
<td className={`... ${isNumeric ? 'text-body-numeric text-right' : 'text-body'}`} />

<!-- kpi-card.tsx (Phase 26-02 pilot) — state-color tokens for success/error trend: -->
// positive trend: className="... text-success-fg"
// negative trend: className="... text-error-fg"

<!-- Available state-color tokens (shipped 26-01): -->
// text-success-fg   Positive trends
// text-error-fg     Negative trends
// text-warning-fg   (reserved — do NOT use here; header amber stays for Phase 28+)

<!-- Numeric variants: -->
// .text-body-numeric    JetBrains Mono + tabular-nums + lining-nums
// .text-label-numeric   JetBrains Mono + tabular-nums + lining-nums + 0.04em
</interfaces>

<project_constraints>
- Pitfall 4 expansion: migrate emerald/red ONLY in trend-indicator.tsx. Other files in this plan (table-footer, sort-dialog, etc.) may have state colors but are NOT in scope for the color swap — migrate only typography in those files.
- table-body.tsx was shipped in Phase 26-04 — do NOT touch it here. If grep shows remaining hits in table-body.tsx, flag for the execution SUMMARY but do not modify.
- shadcn primitives under src/components/ui/** are allowlisted. Several of the column-filter dialogs likely import from ui/popover, ui/input, ui/label — migrate the caller's classes, leave the ui/ primitives alone.
- Spacing/gap tokens are OUT OF SCOPE.
- Animation / hover / focus-ring work is OUT OF SCOPE — Phase 30.
</project_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate trend-indicator (with state-color expansion) + table-footer + percentile-cell</name>
  <files>
    src/components/table/trend-indicator.tsx
    src/components/table/table-footer.tsx
    src/components/cross-partner/percentile-cell.tsx
  </files>
  <action>
**trend-indicator.tsx** (~110 lines, most intricate file in this plan):

1. Read the full file.
2. Locate the positive/negative color logic (around lines 48–50 per RESEARCH). Replace:
   - `text-emerald-600 dark:text-emerald-400` → `text-success-fg`
   - `text-red-500 dark:text-red-400` → `text-error-fg`
   - Any neutral/zero-delta class → `text-muted-foreground`
3. Locate line 83: `text-xs font-medium ${colorClass}` → replace with `text-label-numeric ${colorClass}` IF the element holds a digit (e.g., the delta percentage value). If the element is arrow-only (a unicode arrow character, no digits), use `text-caption font-medium ${colorClass}` — non-numeric.
4. Walk the rest of the file for any other ad-hoc text or weight classes and migrate per the table.
5. Confirm no `text-emerald|text-red|text-green|dark:text-emerald|dark:text-red|dark:text-green` remain (use Grep after edit).
6. Do NOT touch amber/yellow anywhere — if an amber class appears, it's out of this plan's scope. Leave it.

**table-footer.tsx** (line 82: `tabular-nums` + `text-xs`):

1. Read the file.
2. Find the aggregate cell styling. Replace the `text-xs font-mono tabular-nums` triple (or similar combo) with `text-label-numeric` — the utility bakes all three.
3. Walk the file for any other ad-hoc text-size / font-weight hits and migrate.

**percentile-cell.tsx** (1 text-size hit + 1 font-weight hit):

1. Read the file.
2. Numeric value → `.text-body-numeric` (or `.text-label-numeric` depending on visual tier — use `.text-body-numeric` unless row-height demands smaller).
3. Drop any `tabular-nums` or `font-mono` once numeric variant applied.
  </action>
  <verify>
    <automated>npm run build</automated>
    Follow-ups:
    - `rg -n 'text-(xs|sm|base|lg|xl|2xl)' src/components/table/trend-indicator.tsx src/components/table/table-footer.tsx src/components/cross-partner/percentile-cell.tsx` → 0 hits
    - `rg -n '\bfont-(semibold|bold)\b' src/components/table/trend-indicator.tsx src/components/table/table-footer.tsx src/components/cross-partner/percentile-cell.tsx` → 0 hits (font-medium may remain on the arrow span if no digit)
    - `rg -n 'text-emerald|text-red|text-green|dark:text-emerald|dark:text-red|dark:text-green' src/components/table/trend-indicator.tsx` → 0 hits
    - `rg -n '\btabular-nums\b' src/components/table/table-footer.tsx src/components/cross-partner/percentile-cell.tsx` → 0 hits
  </verify>
  <done>
    Build passes. trend-indicator uses state-color tokens. table-footer uses .text-label-numeric single class. percentile-cell uses .text-body-numeric.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining table surfaces (filters, sort, preset, header, heatmap toggle)</name>
  <files>
    src/components/table/sort-dialog.tsx
    src/components/table/sort-indicator.tsx
    src/components/table/draggable-header.tsx
    src/components/table/text-column-filter.tsx
    src/components/table/numeric-column-filter.tsx
    src/components/table/heatmap-toggle.tsx
    src/components/table/column-preset-tabs.tsx
  </files>
  <action>
Per-file migration. For each file:
1. Read the full file.
2. Apply the migration table from `docs/TYPE-MIGRATION.md`.
3. Drop paired `font-semibold`/`font-medium`/`font-bold` with type tokens.
4. `uppercase` only on `.text-label` (most unlikely to be needed in these files).
5. Column headers and sort labels typically map: `text-sm` → `text-body`; `text-xs` → `text-caption`.
6. Dialog titles (sort-dialog) per CONTEXT lock: dialog titles use `text-heading`.
7. Input placeholders, helper text in filters: `text-caption text-muted-foreground`.
8. Preset-tab labels: `text-body` if they're the primary tab label.
9. Do NOT touch classes in ui/ primitives imported by these files.

**file-specific notes:**
- `text-column-filter.tsx` and `numeric-column-filter.tsx` have 6 hits each — read carefully, these are dense. Map each hit individually.
- `draggable-header.tsx` (1 hit, 1 weight hit) is likely the small label text — `text-caption` or `text-body`.
- `sort-indicator.tsx` (1 weight hit, no size hit) — likely just `font-medium` — drop it if paired with a token, keep it on a plain element only if weight is semantic.
- `heatmap-toggle.tsx` (1 hit) — single label, likely `text-caption`.
  </action>
  <verify>
    <automated>npm run build</automated>
    Follow-ups:
    - `rg -n 'text-(xs|sm|base|lg|xl|2xl)' src/components/table/sort-dialog.tsx src/components/table/sort-indicator.tsx src/components/table/draggable-header.tsx src/components/table/text-column-filter.tsx src/components/table/numeric-column-filter.tsx src/components/table/heatmap-toggle.tsx src/components/table/column-preset-tabs.tsx` → 0 hits
    - `rg -n '\bfont-(semibold|medium|bold)\b' src/components/table/sort-dialog.tsx src/components/table/sort-indicator.tsx src/components/table/draggable-header.tsx src/components/table/text-column-filter.tsx src/components/table/numeric-column-filter.tsx src/components/table/heatmap-toggle.tsx src/components/table/column-preset-tabs.tsx` → 0 hits
  </verify>
  <done>
    Build passes. All 7 files migrated. Tokens applied consistently across filters, sort UI, preset tabs, heatmap toggle, draggable header.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — table surfaces + state-color migration in trend-indicator</name>
  <files>(verification only)</files>
  <action>Pause for human verification. Instructions below.</action>
  <verify>Human confirms with screenshots.</verify>
  <done>User approves table surface screenshots in both modes; explicitly confirms positive trend green matches KPI card success green (proving state-color migration worked).</done>
  <what-built>
10 table-related files migrated to type tokens. trend-indicator.tsx additionally swapped emerald/red to state-color tokens (text-success-fg / text-error-fg), aligning with KPI card from Phase 26-02. table-footer aggregate cells now use a single .text-label-numeric class instead of the triple `text-xs font-mono tabular-nums`.
  </what-built>
  <how-to-verify>
1. `mcp__Claude_Preview__preview_start`.
2. **Table body + footer:**
   - Light mode screenshot. Confirm footer aggregate row digits are JetBrains Mono, aligned tabular across columns.
   - Dark mode screenshot. Same confirmation.
3. **Trend indicator (state-color proof):**
   - In the table, find a column with a trend indicator (batch-over-batch delta column). Verify: positive trend color matches the positive trend color in a KPI card delta (same green). Negative matches negative.
   - `preview_inspect` on a positive trend — computed `color` should resolve to `--success-fg` (same CSS var the KPI card uses).
4. **Column filter dialogs:**
   - Open a numeric column filter. Input labels use `text-body`; helper text uses `text-caption`.
   - Open a text column filter. Same check.
5. **Sort dialog:**
   - Open sort dialog. Title reads as heading (text-heading, 18px, weight 600).
6. **Preset tabs + heatmap toggle + draggable header:** visual smoke test — nothing looks wildly different from pre-migration. If anything looks dramatically smaller/larger, flag.

Approval requires: (a) 2 screenshots of the table (light + dark) with visible footer aggregate row, (b) explicit confirmation that positive-trend green matches KPI positive-trend green, (c) acknowledgement that no unexpected hierarchy shifts occurred.
  </how-to-verify>
  <resume-signal>Type "approved" with screenshots, or describe issues (e.g. "footer digits not aligned", "trend green looks different from KPI green", "filter dialog helper text too small", "sort dialog title wrong size").</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes.
- Zero ad-hoc text-size hits in any of the 10 files.
- trend-indicator.tsx has zero `text-(emerald|red|green)` and uses `text-success-fg` + `text-error-fg`.
- Visual verification passed: footer digits aligned, trend state colors match KPI card.
</verification>

<success_criteria>
- DS-07 progressed: 10 files off ad-hoc classes.
- DS-08 progressed: footer + percentile + trend delta all use numeric variants.
- Pitfall 4 (RESEARCH) addressed: trend-indicator now shares the state-color system with the KPI card shipped in Phase 26-02.
</success_criteria>

<output>
After completion, create `.planning/phases/27-typography-and-information-hierarchy/27-03-SUMMARY.md` noting the state-color expansion boundary (trend-indicator yes, other files no) and any migration-table resolutions fed back to docs/TYPE-MIGRATION.md §10.
</output>
