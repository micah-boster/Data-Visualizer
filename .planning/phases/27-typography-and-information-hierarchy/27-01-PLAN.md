---
phase: 27-typography-and-information-hierarchy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/TYPE-MIGRATION.md
  - src/components/layout/section-header.tsx
  - src/components/anomaly/anomaly-detail.tsx
  - AGENTS.md
autonomous: false
requirements: [DS-07, DS-08, DS-09, DS-10]

must_haves:
  truths:
    - "docs/TYPE-MIGRATION.md exists and resolves every ad-hoc text-size class to a type token (text-xs/sm/base/lg/xl/2xl) with explicit mapping + ambiguous-case resolutions"
    - "SectionHeader component exists at src/components/layout/section-header.tsx with { title, eyebrow?, description?, actions? } props and renders title via text-heading, eyebrow via .text-label uppercase text-muted-foreground"
    - "Pilot surface (anomaly-detail.tsx) renders with zero ad-hoc text-(xs|sm|base|lg|xl|2xl) classes and zero ad-hoc font-(semibold|medium|bold) classes"
    - "Every numeric element in the pilot uses text-body-numeric / text-display-numeric / text-label-numeric"
    - "Visual parity preserved in both light and dark mode (hierarchy unchanged, overline reads as overline, digits align)"
  artifacts:
    - path: "docs/TYPE-MIGRATION.md"
      provides: "Canonical mapping from Tailwind text/weight classes to Phase 26 type tokens"
      min_lines: 40
      contains: "text-xs"
    - path: "src/components/layout/section-header.tsx"
      provides: "Shared SectionHeader component (DS-10)"
      min_lines: 20
      exports: ["SectionHeader"]
    - path: "src/components/anomaly/anomaly-detail.tsx"
      provides: "Pilot surface migrated to type tokens"
      min_lines: 30
  key_links:
    - from: "src/components/anomaly/anomaly-detail.tsx"
      to: "token utilities (text-heading, text-body, text-caption, text-label, text-body-numeric)"
      via: "Tailwind v4 utility classes"
      pattern: "text-(heading|body|caption|label|title|display)(-numeric)?"
    - from: "src/components/layout/section-header.tsx"
      to: "text-heading + .text-label uppercase"
      via: "JSX className"
      pattern: "text-heading|text-label"
---

<objective>
Ship the Phase 27 foundation: publish the canonical ad-hoc-to-token migration table, create the shared SectionHeader component (DS-10), and prove the full migration recipe on one dense pilot surface (`anomaly-detail.tsx` — 6 text-xs + 4 font-medium hits). Every downstream sweep plan (27-02..27-05) will reference this table. Matches Phase 26's Plan 01 cadence (foundation + one pilot).

Purpose: Lock the mapping once so the four parallel sweep plans don't drift on ambiguous cases (`text-base` → `text-title` vs `text-body`). Prove `SectionHeader` works in a real surface before sweeps consume it. Validate the migration recipe end-to-end (text-size → token, font-weight removed, uppercase+label composition, numeric variants) on a surface representative of the 50+ files still to migrate.
Output: Migration table doc, SectionHeader component, migrated anomaly-detail.tsx, AGENTS.md entry flagging the rule and the ui/ allowlist.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-typography-and-information-hierarchy/27-CONTEXT.md
@.planning/phases/27-typography-and-information-hierarchy/27-RESEARCH.md
@src/app/globals.css
@src/components/kpi/kpi-card.tsx
@src/components/layout/header.tsx
@src/components/table/table-body.tsx
@src/components/anomaly/anomaly-detail.tsx

<interfaces>
<!-- Shipped in Phase 26 and available as Tailwind v4 utilities. Use directly — no exploration needed. -->

Type scale (size / line-height / weight / letter-spacing baked in via --text-*--* sub-tokens):
- text-display   1.5rem / 1.2  / 600 / —       Page/KPI level
- text-heading   1.125rem / 1.35 / 600 / —     Section titles, dialog titles
- text-title     0.9375rem / 1.4 / 500 / —     Sub-section titles, emphasized labels
- text-body      0.875rem / 1.5 / inherit / —  Default body, table text cells, menu items
- text-label     0.75rem / 1.4 / 500 / 0.04em  Overline (pair with `uppercase` + `text-muted-foreground`)
- text-caption   0.75rem / 1.4 / inherit / —   Helper text, timestamps, fine print

Numeric variants (defined in globals.css @layer utilities lines 488-508):
- .text-display-numeric   Inter + tabular-nums + lining-nums   KPI hero values
- .text-body-numeric      JetBrains Mono + tabular-nums + lining-nums   Table cells, inline metrics
- .text-label-numeric     JetBrains Mono + tabular-nums + lining-nums + 0.04em   Chart ticks, footer totals

Canonical shipped pilots (reference implementations):
- src/components/kpi/kpi-card.tsx — overline `text-label uppercase text-muted-foreground`; value `text-display-numeric`
- src/components/layout/header.tsx — freshness indicator `text-caption`
- src/components/table/table-body.tsx:65 — cell branch `isNumeric ? 'text-body-numeric text-right' : 'text-body'`

SectionHeader props contract (locked in CONTEXT.md):
```tsx
interface SectionHeaderProps {
  title: string;
  eyebrow?: string;
  description?: string;
  actions?: React.ReactNode;
  className?: string;
}
```
</interfaces>

<project_constraints>
- AGENTS.md requires reading `node_modules/next/dist/docs/` before writing Next.js code. SectionHeader is a pure client component with no Next.js APIs — flag only if the file needs 'use client'.
- `feedback_testing` memory: never push CSS/type changes blind. The Task 3 checkpoint is mandatory and covers light + dark.
- CONTEXT lock: `uppercase` lives ONLY on `.text-label`. Do not apply it to other tokens.
- CONTEXT lock: tokens own weight. Drop `font-semibold` / `font-medium` when migrating; do not pair them with type tokens.
- CONTEXT lock: `text-metric` outlier is conditional — add the token only if the migration audit (Task 1) finds ≥3 off-scale display uses. If only 0–2, do NOT add.
- The migration table is the single source of truth — Plans 27-02..05 will reference docs/TYPE-MIGRATION.md verbatim. If it is ambiguous, sweeps drift.
</project_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the migration table + SectionHeader + pilot migration</name>
  <files>
    docs/TYPE-MIGRATION.md
    src/components/layout/section-header.tsx
    src/components/anomaly/anomaly-detail.tsx
    AGENTS.md
  </files>
  <action>
**Step A — Author `docs/TYPE-MIGRATION.md`.**

Create the doc with these sections (exact headings):

1. `# Type Token Migration Table (Phase 27)`
2. `## The 6 tokens` — bullet list of text-display/heading/title/body/label/caption with size/line-height/weight/letter-spacing, quoting `src/app/globals.css:48-71`.
3. `## The 3 numeric variants` — .text-display-numeric / .text-body-numeric / .text-label-numeric with family + features, quoting `src/app/globals.css:488-508`.
4. `## Ad-hoc → token` (a table):

| Current class(es) | → Token | Notes |
|---|---|---|
| `text-2xl font-semibold` | `text-display` | Weight baked; drop `font-semibold`. Page titles. |
| `text-xl font-semibold` | `text-heading` | Section titles. Prefer `<SectionHeader>`. |
| `text-lg font-medium` | `text-heading` | Resolved: anchoring-a-section size = heading tier. |
| `text-lg` (no weight) | `text-title` | Emphasized label without section anchor. |
| `text-base` / `text-base font-medium` | `text-title` | **Resolved on pilot:** default to `text-title`. Read-through prose → `text-body`. |
| `text-sm` | `text-body` | Most common migration. |
| `text-sm font-medium` | `text-body` (drop weight) or `text-title` if emphasized | Weight dropped by default. Escalate to `text-title` only if visual hierarchy demands. |
| `text-xs` (plain) | `text-caption` | Helper/secondary text. |
| `text-xs font-medium uppercase` | `text-label uppercase` | Overline. Weight + tracking baked. |
| `text-xs tabular-nums` | `text-label-numeric` (context-dependent) | Use `.text-label-numeric` for chart ticks / footer totals; `.text-caption` + font-mono otherwise. |
| `text-[10px]` | `text-caption` OR custom axis tick | On Recharts XAxis/YAxis use `tick={{ fontSize, fontFamily }}` or custom tick component — className on axis does not propagate. |
| standalone `tabular-nums` on a digit element | swap parent class to `text-body-numeric`/`text-label-numeric`/`text-display-numeric` | Numeric variants bake tabular + lining. Remove standalone `tabular-nums`. |

5. `## Weight policy` — "Tokens own weight. Never pair `font-semibold`/`font-medium`/`font-bold` with a type token. Exception: NONE."
6. `## Uppercase policy` — "`uppercase` only on `.text-label`. Other tokens render mixed-case."
7. `## Numeric policy` — apply at shared component level (table cell branch, KPI value, chart tick, tooltip). Never global on `<body>`.
8. `## Allowlist` — `src/components/ui/**` (shadcn primitives), `src/app/tokens/**`, `src/components/tokens/**`. Files outside the allowlist must pass the Plan 27-06 grep check.
9. `## Outlier token audit (run on pilot)` — state explicit answer: is `text-metric` needed? Record the answer (`yes: promoted to --text-metric, see globals.css line X` OR `no: all hero values fit text-display-numeric`) after Step C audit.
10. `## Ambiguous cases resolved during pilot` — append as resolutions are discovered in Step C.

**Step B — Create `src/components/layout/section-header.tsx`.**

```tsx
import { cn } from '@/lib/utils';

interface SectionHeaderProps {
  title: string;
  eyebrow?: string;
  description?: string;
  actions?: React.ReactNode;
  className?: string;
}

export function SectionHeader({ title, eyebrow, description, actions, className }: SectionHeaderProps) {
  return (
    <div className={cn('flex items-start justify-between gap-stack', className)}>
      <div className="flex flex-col gap-1">
        {eyebrow && (
          <span className="text-label uppercase text-muted-foreground">{eyebrow}</span>
        )}
        <h2 className="text-heading text-foreground">{title}</h2>
        {description && (
          <p className="text-caption text-muted-foreground">{description}</p>
        )}
      </div>
      {actions && <div className="shrink-0 flex items-center gap-inline">{actions}</div>}
    </div>
  );
}
```

Do NOT add `'use client'` — the component has no hooks, no event handlers, no state. Server-renderable.

Do NOT wire the component into any existing surface in this plan. Consumer plans in Wave 2 (especially 27-02 for chart sections and 27-04 for toolbar group titles) will adopt it.

**Step C — Audit + migrate the pilot (`src/components/anomaly/anomaly-detail.tsx`).**

1. Read the full file.
2. Record every ad-hoc text-size + font-weight occurrence (6 text-xs + 4 font-medium per grep survey).
3. For each occurrence, apply the migration table from Step A. Resolve ambiguous `text-base` / `text-sm font-medium` cases by choosing the semantically correct token and appending the resolution to `docs/TYPE-MIGRATION.md` section 10.
4. If any numeric display (anomaly confidence percent, threshold values, deltas) appears: swap to the appropriate numeric variant.
5. While auditing, note any off-scale larger-than-display size (e.g., an inline `text-[2rem]` hero) — if the grep over `src/` finds ≥3 such sites, create `--text-metric` in `src/app/globals.css` (add under @theme inline: `--text-metric: 2rem;` with matching `--text-metric--line-height: 1.15;` `--text-metric--font-weight: 600;`) and paired `.text-metric-numeric` utility. Record the answer in docs/TYPE-MIGRATION.md §9. **Do NOT add the token if the audit finds < 3 sites** — this is decision-locked.
6. After migration, grep the pilot file: zero hits on `text-(xs|sm|base|lg|xl|2xl)` and zero on `\bfont-(semibold|medium|bold)\b`.

**Step D — Update `AGENTS.md`.**

Append a brief section at the end (keep the existing Next.js advisory intact):

```
## Type tokens (Phase 27)

- App code uses the 6 named type tokens only: text-display, text-heading, text-title, text-body, text-label, text-caption. Do NOT use Tailwind's default text-xs/sm/base/lg/xl/2xl in src/ outside the allowlist.
- Tokens own weight. Do not pair font-semibold/font-medium/font-bold with a type token.
- `uppercase` is for .text-label overline only.
- Numeric displays use .text-display-numeric / .text-body-numeric / .text-label-numeric (never standalone tabular-nums).
- Allowlist: src/components/ui/**, src/app/tokens/**, src/components/tokens/**.
- Full mapping: docs/TYPE-MIGRATION.md.
- SectionHeader: src/components/layout/section-header.tsx. Use for section titles instead of ad-hoc `<h2 className="text-xl">`.
```

Do NOT edit other AGENTS.md sections. Do NOT delete the Next.js advisory.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx tsc --noEmit</automated>
    Follow-ups (run these after build succeeds):
    - `rg -n 'text-(xs|sm|base|lg|xl|2xl)' src/components/anomaly/anomaly-detail.tsx` → exit with 0 hits
    - `rg -n '\bfont-(semibold|medium|bold)\b' src/components/anomaly/anomaly-detail.tsx` → exit with 0 hits
    - `test -f docs/TYPE-MIGRATION.md` → exists
    - `test -f src/components/layout/section-header.tsx` → exists
    - `grep -q 'Type tokens (Phase 27)' AGENTS.md` → matches
  </verify>
  <done>
    Build + typecheck pass. anomaly-detail.tsx has zero ad-hoc text-size/font-weight hits. docs/TYPE-MIGRATION.md published with filled-in ambiguous-case resolutions and text-metric audit answer. SectionHeader component exports correctly. AGENTS.md flags the rule + allowlist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adopt SectionHeader at one call site inside the pilot</name>
  <files>
    src/components/anomaly/anomaly-detail.tsx
  </files>
  <action>
Find the primary heading inside `anomaly-detail.tsx` — the section title that the user sees first when the anomaly detail opens (likely an `<h2>` or `<div>` with the largest text class before Task 1's migration). If the file has a clear section header + optional metadata (e.g., anomaly category + title + description), replace that block with `<SectionHeader>`:

```tsx
import { SectionHeader } from '@/components/layout/section-header';

<SectionHeader
  eyebrow={/* e.g., anomaly.category ? anomaly.category.toUpperCase() : undefined */}
  title={/* e.g., anomaly title */}
  description={/* optional short explanation */}
  actions={/* optional existing right-aligned buttons if present */}
/>
```

Guidelines:
- If the pilot does NOT have a clear section-header block (no eyebrow-worthy meta), pick one plausible heading and wrap with `<SectionHeader title={...} />` (no eyebrow / no description / no actions). The goal is to prove the component renders inside a real surface, not to force structure.
- Preserve all surrounding layout — only the heading block changes.
- Do NOT change existing data props or handler wiring.
- After swapping, re-run grep to confirm no ad-hoc text classes reappeared in anomaly-detail.tsx.
  </action>
  <verify>
    <automated>npm run build</automated>
    Follow-ups:
    - `rg -n 'SectionHeader' src/components/anomaly/anomaly-detail.tsx` → at least 1 import + 1 usage
    - `rg -n 'text-(xs|sm|base|lg|xl|2xl)' src/components/anomaly/anomaly-detail.tsx` → 0 hits
  </verify>
  <done>
    SectionHeader imported and used at one site in anomaly-detail.tsx. Build passes. No ad-hoc text classes remain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — pilot surface + SectionHeader render correctly in light + dark</name>
  <files>src/components/anomaly/anomaly-detail.tsx (verification only, no edits)</files>
  <action>Pause for human verification. Instructions below.</action>
  <verify>Human confirms by typing "approved" with attached screenshots.</verify>
  <done>User approves both light-mode and dark-mode screenshots of the anomaly detail panel with SectionHeader; confirms hierarchy reads correctly; confirms overline (if eyebrow used) reads as overline.</done>
  <what-built>
Foundation for Phase 27: docs/TYPE-MIGRATION.md (canonical mapping), src/components/layout/section-header.tsx (DS-10 component), anomaly-detail.tsx fully migrated to type tokens, AGENTS.md rule entry. The pilot surface proves the recipe — every remaining surface in Plans 27-02..27-05 will apply the same pattern.
  </what-built>
  <how-to-verify>
1. `mcp__Claude_Preview__preview_start` (dev server) and navigate to an anomaly detail view (drill into a partner with an active anomaly, then open the anomaly detail panel/drawer).
2. Light mode: `preview_screenshot`. Verify:
   - Primary section heading reads as a heading (~18px, weight 600) — NOT a body-sized line.
   - If eyebrow rendered: uppercase, tracked, muted (12px, 0.04em).
   - Any numeric values (confidence %, delta, threshold) have aligned digits — eyeball or inspect `font-variant-numeric`.
   - No element feels too small/too big relative to pre-migration. Heading hierarchy preserved.
3. `preview_inspect` on the section heading — confirm computed `font-size: 18px`, `line-height: ~24px`, `font-weight: 600`.
4. If eyebrow present, `preview_inspect` on the eyebrow — confirm `text-transform: uppercase`, `letter-spacing: 0.04em`, `font-size: 12px`, `font-weight: 500`, `color` resolves to `--muted-foreground`.
5. Toggle to dark mode. `preview_screenshot`. Verify: same hierarchy, same overline treatment, no contrast regressions.
6. (Sanity) Open `docs/TYPE-MIGRATION.md` and confirm §9 (outlier audit) has a clear yes/no + section 10 (ambiguous cases) has at least one resolution entry from the pilot.

Approval requires: light + dark screenshots of the pilot surface, explicit confirmation the hierarchy did not flatten, confirmation the overline (if used) reads as overline.
  </how-to-verify>
  <resume-signal>Type "approved" with screenshots, or describe issues (e.g. "heading feels smaller", "overline looks like body text", "digits don't align", "dark mode contrast dropped").</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` and `npx tsc --noEmit` pass.
- `docs/TYPE-MIGRATION.md` exists with all 10 sections filled in.
- `src/components/layout/section-header.tsx` exports `SectionHeader`.
- `src/components/anomaly/anomaly-detail.tsx` has zero hits on `text-(xs|sm|base|lg|xl|2xl)` and zero on `font-(semibold|medium|bold)`.
- AGENTS.md contains the "Type tokens (Phase 27)" section.
- Visual verification passed in both modes.
</verification>

<success_criteria>
- DS-07 foundation: migration table published, one full file migrated, AGENTS.md rule in place.
- DS-08 foundation: numeric variants documented and applied on the pilot where digits appear.
- DS-09 foundation: overline policy documented; `text-label uppercase text-muted-foreground` pattern re-confirmed on the pilot.
- DS-10 shipped: `SectionHeader` component exists with locked prop contract and is adopted at one real call site.
</success_criteria>

<output>
After completion, create `.planning/phases/27-typography-and-information-hierarchy/27-01-SUMMARY.md` capturing: (a) the final migration table with resolved ambiguous cases, (b) text-metric audit answer, (c) SectionHeader adoption notes, (d) any patterns discovered on the pilot that downstream sweeps should replicate or avoid.
</output>
