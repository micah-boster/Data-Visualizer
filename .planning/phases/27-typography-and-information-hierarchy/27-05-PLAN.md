---
phase: 27-typography-and-information-hierarchy
plan: 05
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/components/layout/app-sidebar.tsx
  - src/components/navigation/breadcrumb-trail.tsx
  - src/components/query/query-search-bar.tsx
  - src/components/query/query-scope-pill.tsx
  - src/components/query/query-suggested-prompts.tsx
  - src/components/query/query-response.tsx
  - src/components/anomaly/anomaly-summary-panel.tsx
  - src/components/anomaly/anomaly-toolbar-trigger.tsx
  - src/components/section-error-boundary.tsx
  - src/components/error-state.tsx
  - src/components/loading-state.tsx
  - src/components/empty-state.tsx
  - src/components/data-display.tsx
autonomous: false
requirements: [DS-07, DS-10]

must_haves:
  truths:
    - "App-sidebar (pill counts, section labels, navigation labels) fully migrated to type tokens — no text-[10px], no text-xs font-semibold"
    - "Breadcrumb trail uses type tokens (text-body or text-caption per tier)"
    - "Claude query (NLQ) UI — search bar, scope pill, suggested prompts, response prose — uses type tokens; prose response uses text-body as the canonical reading tier"
    - "Anomaly summary panel + toolbar trigger use type tokens (and if the trigger count is numeric, .text-label-numeric)"
    - "All empty/error/loading state surfaces (section-error-boundary, error-state, loading-state, empty-state) use type tokens"
    - "data-display.tsx (the 1 font-weight hit) is resolved"
  artifacts:
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Sidebar migrated (pill counts use .text-label-numeric or similar)"
    - path: "src/components/query/query-response.tsx"
      provides: "NLQ response surface migrated — prose on text-body canonical"
  key_links:
    - from: "src/components/query/query-response.tsx"
      to: "text-body (canonical reading tier for AI responses)"
      via: "className"
      pattern: "text-body"
    - from: "src/components/layout/app-sidebar.tsx pill counts"
      to: ".text-label-numeric"
      via: "className"
      pattern: "text-label-numeric"
---

<objective>
Sweep everything remaining outside tables/charts/toolbar: the app-sidebar, navigation breadcrumb, Claude query UI, anomaly panels, and the empty/error/loading state surfaces. 13 files, ~28 ad-hoc text-size hits, ~11 ad-hoc font-weight hits. Closes out DS-07 coverage for all non-allowlisted surfaces.

**Query prose decision:** Per RESEARCH "What I might have missed", `query/*` prose (Claude chat responses) may want a distinct reading-prose treatment. Decision: use `text-body` as the canonical prose tier — consistent with the rest of the app. If the response surface feels cramped (`text-body` is 14px / 1.5 line-height), we accept that and revisit in a dedicated prose phase only if user feedback demands. No new prose-tier token in this plan.

Purpose: Finish the sweep. After this plan, every non-allowlisted file in `src/` is token-migrated — Plan 27-06 can then switch on the grep guard without surprises.
Output: 13 files migrated. Sidebar pill counts use numeric variant. Query response prose on text-body. Empty/error/loading states consistent.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-typography-and-information-hierarchy/27-CONTEXT.md
@.planning/phases/27-typography-and-information-hierarchy/27-RESEARCH.md
@docs/TYPE-MIGRATION.md
@src/components/layout/section-header.tsx
@src/components/kpi/kpi-card.tsx

<interfaces>
<!-- Relevant reference patterns: -->

<!-- Pill-count pattern (per RESEARCH Pattern 4): -->
<!-- app-sidebar.tsx:59, 119 currently: text-[10px] tabular-nums -->
<!-- Migrate to: text-label-numeric (mono, tracked, tabular) -->

<!-- Empty-state convention: -->
<!-- Headline: text-heading (anchoring section) OR text-title (sub-section empty state) -->
<!-- Description: text-body text-muted-foreground (canonical) OR text-caption text-muted-foreground (small hint) -->
</interfaces>

<project_constraints>
- `app-sidebar.tsx` imports from `src/components/ui/sidebar.tsx` (a shadcn primitive). Migrate only app-sidebar's own JSX; leave the ui/sidebar.tsx primitive alone.
- RESEARCH Pitfall 4 (state colors) applies ONLY to trend-indicator.tsx (handled in Plan 27-03). Other state colors (amber stale indicators, etc.) are cross-cutting and stay deferred.
- Query response prose uses `text-body` — no new prose tier in this plan.
- Sidebar "subtle depth separation" (DS-16) is Phase 28, not here. Touch typography only.
- Anomaly-detail.tsx was migrated in Plan 27-01 (pilot) — do NOT touch it here. If grep flags remaining hits there, record in the SUMMARY but do not edit.
- Spacing / gap / motion all OUT OF SCOPE.
</project_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate sidebar + breadcrumb + query UI</name>
  <files>
    src/components/layout/app-sidebar.tsx
    src/components/navigation/breadcrumb-trail.tsx
    src/components/query/query-search-bar.tsx
    src/components/query/query-scope-pill.tsx
    src/components/query/query-suggested-prompts.tsx
    src/components/query/query-response.tsx
  </files>
  <action>
Per-file migration. For each file:
1. Read the full file.
2. Apply `docs/TYPE-MIGRATION.md`.
3. Drop paired weights.

**File-specific:**

`app-sidebar.tsx` (5 size hits, 2 weight hits — including lines 59/119 with `text-[10px] tabular-nums` pill counts):
- Pill counts at lines 59 / 119: replace `text-[10px] tabular-nums` with `.text-label-numeric` (mono + tabular + tracked). If the 12px size feels too large in the pill context during the Task 3 visual check, flag for follow-up — do NOT introduce a new smaller token in this plan (violates CONTEXT "keep the scale tight" lock).
- Section labels (e.g., nav group titles): `.text-label uppercase text-muted-foreground`.
- Nav item labels: `text-body`.
- Active-nav item may keep its own color/weight treatment — drop ad-hoc `font-semibold` ONLY if paired with a type token.

`breadcrumb-trail.tsx` (1 size hit):
- Breadcrumb segments: `text-caption` or `text-body` — decide on visual weight (breadcrumbs are typically quieter than body, so `text-caption text-muted-foreground` is the default).

`query-search-bar.tsx` (1 size hit):
- Input placeholder / label: `text-body` for the primary input; `text-caption` for any helper.

`query-scope-pill.tsx` (1 size hit, 1 weight hit):
- Pill label: `.text-label uppercase text-muted-foreground` (it's a categorical pill).

`query-suggested-prompts.tsx` (1 size hit):
- Prompt chip label: `text-body` (or `text-caption` if chips are quiet meta — decide on visual inspection).

`query-response.tsx` (3 size hits):
- Prose body: `text-body` (canonical).
- Code/inline-code within prose: leave any `font-mono` usage that is structurally semantic (code spans); drop `font-mono` only if paired with a type token purely for font-family override — use `.text-body-numeric` for inline numeric callouts.
- Response title/meta (if any): `text-heading` or `text-caption` per tier.
  </action>
  <verify>
    <automated>npm run build</automated>
    Follow-ups:
    - `rg -n 'text-(xs|sm|base|lg|xl|2xl)' src/components/layout/app-sidebar.tsx src/components/navigation/breadcrumb-trail.tsx src/components/query/` → 0 hits
    - `rg -n 'text-\[10px\]' src/components/layout/app-sidebar.tsx` → 0 hits
    - `rg -n '\bfont-(semibold|medium|bold)\b' src/components/layout/app-sidebar.tsx src/components/navigation/breadcrumb-trail.tsx src/components/query/` → 0 hits
  </verify>
  <done>
    Build passes. 6 files migrated. Sidebar pill counts use numeric variant. Query response prose on text-body.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate anomaly panels + empty/error/loading states + data-display</name>
  <files>
    src/components/anomaly/anomaly-summary-panel.tsx
    src/components/anomaly/anomaly-toolbar-trigger.tsx
    src/components/section-error-boundary.tsx
    src/components/error-state.tsx
    src/components/loading-state.tsx
    src/components/empty-state.tsx
    src/components/data-display.tsx
  </files>
  <action>
Per-file migration. For each file:
1. Read the full file.
2. Apply `docs/TYPE-MIGRATION.md`.
3. Drop paired weights.

**File-specific:**

`anomaly-summary-panel.tsx` (2 size hits, 2 weight hits):
- Panel title: `text-heading` OR `<SectionHeader title="Anomalies" />` if the panel has a title anchoring a distinct section.
- Summary metrics (counts, severity): `text-body-numeric` for the numeric value, `text-caption` for the label.
- Drop paired weights.

`anomaly-toolbar-trigger.tsx` (1 size hit, 2 weight hits):
- Trigger button label: `text-body` (or keep shadcn button's own text styling if the button primitive owns it — migrate only the caller's supplemental text).
- Count badge (if present): `.text-label-numeric`.

`section-error-boundary.tsx` (1 size hit, 1 weight hit):
- Error headline: `text-heading` (anchoring the errored section).
- Error description: `text-body text-muted-foreground`.
- Retry button text: let shadcn Button primitive own its text.

`error-state.tsx` (1 size hit):
- Error message: `text-body text-muted-foreground` (or `text-heading` if anchoring a full-page error — decide based on current hierarchy).

`loading-state.tsx` (1 size hit):
- Loading message: `text-body text-muted-foreground` OR `text-caption` for skeleton captions.

`empty-state.tsx` (2 size hits, 1 weight hit):
- Headline: `text-heading`.
- Description: `text-body text-muted-foreground`.
- Drop paired `font-medium` / `font-semibold`.

`data-display.tsx` (1 weight hit, no size hit per grep):
- Find the single `font-medium`. If paired with a type token on the same element, drop. If standalone on a plain element, use judgment: prefer replacing the element's size class with a token AND dropping font-medium. If the site has NO size class at all, the font-medium may be semantic on a native `<strong>`-like element — leave it and document in the file.
  </action>
  <verify>
    <automated>npm run build</automated>
    Follow-ups:
    - `rg -n 'text-(xs|sm|base|lg|xl|2xl)' src/components/anomaly/anomaly-summary-panel.tsx src/components/anomaly/anomaly-toolbar-trigger.tsx src/components/section-error-boundary.tsx src/components/error-state.tsx src/components/loading-state.tsx src/components/empty-state.tsx src/components/data-display.tsx` → 0 hits
    - `rg -n '\bfont-(semibold|medium|bold)\b' src/components/anomaly/anomaly-summary-panel.tsx src/components/anomaly/anomaly-toolbar-trigger.tsx src/components/section-error-boundary.tsx src/components/error-state.tsx src/components/loading-state.tsx src/components/empty-state.tsx src/components/data-display.tsx` → 0 hits (unless explicitly documented exception in data-display.tsx)
  </verify>
  <done>
    Build passes. 7 files migrated. Anomaly panels, empty/error/loading states, data-display wrapper all token-clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — sidebar, breadcrumb, query UI, anomaly, empty/error/loading states</name>
  <files>(verification only)</files>
  <action>Pause for human verification. Instructions below.</action>
  <verify>Human confirms with screenshots of the key surfaces.</verify>
  <done>User approves with screenshots covering: sidebar (pill counts visible), breadcrumb + drill, Claude query surface with a response, anomaly summary panel, at least one empty or error state.</done>
  <what-built>
13 files migrated. Sidebar pill counts now use .text-label-numeric (fixing the text-[10px] no-canonical pattern). Claude query response prose consolidated on text-body. Anomaly panels, error/loading/empty states fully on type tokens. data-display.tsx weight hit resolved.
  </what-built>
  <how-to-verify>
1. `mcp__Claude_Preview__preview_start`.
2. **Sidebar:**
   - Light + dark screenshots. Pill counts should be small, mono, tabular-aligned. If 12px feels too large visually, note it in the approval — we will NOT add a new token, we will accept as-is.
3. **Breadcrumb + drill:**
   - Drill into a partner. Screenshot of breadcrumb — segments legible, consistent tier.
4. **Claude query surface:**
   - Submit a query that returns a reasonable-length response. Screenshot light + dark.
   - Prose reads at text-body (14px / 1.5 line-height). Confirm readability.
   - Any inline numeric callouts render mono + tabular (if applicable).
5. **Anomaly summary panel:**
   - Open the anomaly panel. Screenshot.
   - Title reads as heading. Counts render with mono digits.
6. **Empty / error state:**
   - Apply a filter combination that produces zero results. Screenshot of empty state.
   - If reachable without destructive action, trigger an error boundary (e.g., via an error-state route). Screenshot.

Approval requires: 6+ screenshots covering the surfaces above; explicit confirmation that sidebar pill counts are small + tabular-aligned; explicit confirmation that Claude response prose is readable at text-body.
  </how-to-verify>
  <resume-signal>Type "approved" with screenshots, or describe issues (e.g. "sidebar pills too large", "response prose too tight", "empty state headline wrong tier").</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes.
- Zero ad-hoc text-size hits in any of the 13 files.
- Zero paired font-weight hits (exception permitted in data-display.tsx if documented).
- Visual verification passed across sidebar, breadcrumb, query, anomaly, empty/error.
</verification>

<success_criteria>
- DS-07 essentially closed: every non-allowlisted file under src/ has been swept.
- DS-10 progressed: SectionHeader available for anomaly panel / error section if adopted.
</success_criteria>

<output>
After completion, create `.planning/phases/27-typography-and-information-hierarchy/27-05-SUMMARY.md` noting sidebar pill resolution (accepted 12px), data-display weight decision, query prose decision, and any final migration-table resolutions fed to docs/TYPE-MIGRATION.md §10.
</output>
