---
phase: 27-typography-and-information-hierarchy
plan: 04
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/components/toolbar/filter-popover.tsx
  - src/components/toolbar/save-view-popover.tsx
  - src/components/toolbar/preset-dropdown.tsx
  - src/components/toolbar/unified-toolbar.tsx
  - src/components/filters/filter-chips.tsx
  - src/components/filters/filter-combobox.tsx
  - src/components/filters/filter-empty-state.tsx
  - src/components/columns/column-picker-sidebar.tsx
  - src/components/columns/column-group.tsx
  - src/components/columns/column-search.tsx
  - src/components/views/views-sidebar.tsx
  - src/components/views/view-item.tsx
  - src/components/views/save-view-input.tsx
autonomous: false
requirements: [DS-07, DS-10]

must_haves:
  truths:
    - "Every toolbar + popover + filter + column-picker + saved-views surface renders with zero ad-hoc text-(xs|sm|base|lg|xl|2xl) hits"
    - "Every font-(semibold|medium|bold) paired with a type token is removed"
    - "Popover/dropdown section titles (e.g., 'Filter by column', 'Saved views') use text-heading OR SectionHeader"
    - "Popover menu items use text-body; helper text uses text-caption"
    - "Filter chip labels use text-label or text-caption (per visual weight)"
    - "Saved views list items use text-body for the view name, text-caption for meta (timestamp, filter count)"
  artifacts:
    - path: "src/components/toolbar/filter-popover.tsx"
      provides: "Filter popover migrated to type tokens"
    - path: "src/components/views/views-sidebar.tsx"
      provides: "Saved views sidebar migrated to type tokens"
  key_links:
    - from: "src/components/toolbar/**"
      to: "token utilities + SectionHeader (where headings exist)"
      via: "className + import"
      pattern: "text-(heading|title|body|caption|label)"
---

<objective>
Sweep all toolbar, popover, filter, column-picker, and saved-views surfaces. 13 files, ~36 ad-hoc text-size hits, ~8 ad-hoc font-weight hits. This is the "controls" layer — the surfaces users interact with between data views. No numeric-heavy content here, so DS-08 is not a primary target; DS-07 (no ad-hoc sizes) and DS-10 (consistent headings) dominate.

Purpose: These surfaces appear across every page (toolbar, filter popover, saved-views sidebar) — any inconsistency is visible globally. Adopting SectionHeader for popover titles + nested filter sections locks in a consistent visual rhythm for all popups.
Output: All 13 files migrated. SectionHeader adopted where section titles exist inside popovers/sidebars.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-typography-and-information-hierarchy/27-CONTEXT.md
@.planning/phases/27-typography-and-information-hierarchy/27-RESEARCH.md
@docs/TYPE-MIGRATION.md
@src/components/layout/section-header.tsx
@src/components/kpi/kpi-card.tsx

<interfaces>
<!-- SectionHeader signature (from Plan 27-01): -->
export function SectionHeader(props: {
  title: string;
  eyebrow?: string;
  description?: string;
  actions?: React.ReactNode;
  className?: string;
}): JSX.Element;

<!-- Common mappings for toolbar/popover surfaces: -->
// Popover/dropdown title            → text-heading (or SectionHeader if + actions)
// Menu item / list item text        → text-body
// Muted helper under list item      → text-caption text-muted-foreground
// Chip / badge label                → text-label uppercase text-muted-foreground (if categorical pill) OR text-caption
// Small meta (timestamp, count)     → text-caption text-muted-foreground
// Input label                       → text-body (label is semantic, sizing is body tier)
// Section eyebrow in long popover   → .text-label uppercase text-muted-foreground via SectionHeader eyebrow prop
</interfaces>

<project_constraints>
- Every file here imports from `src/components/ui/**` (popover, dropdown-menu, button, input, etc.). Do NOT migrate classes inside the ui/ primitives — they are allowlisted. Migrate only the caller's own JSX in these 13 files.
- `uppercase` only on `.text-label`.
- Tokens own weight — drop paired `font-semibold`/`font-medium`/`font-bold`.
- If `filter-chips.tsx` uses `uppercase` without `.text-label`, either add `.text-label` (if categorical pill) or remove `uppercase`. Do not leave orphan uppercase.
- Spacing, colors, motion all OUT OF SCOPE.
- `unified-toolbar.tsx` has 1 font-weight hit (no size hit per grep). If the only touch is a `font-medium` next to a type token, drop it. If no type token exists at that site, the font-medium may be semantically meaningful on a plain element — use judgment.
- For popover titles: prefer `<SectionHeader title="..." />` when the title is the only heading-tier element. If the popover is a small flat menu (e.g., preset-dropdown with just a list), do NOT wrap a single word in SectionHeader — it's overkill.
</project_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate toolbar + filter surfaces (popovers, chips, combobox, empty state)</name>
  <files>
    src/components/toolbar/filter-popover.tsx
    src/components/toolbar/save-view-popover.tsx
    src/components/toolbar/preset-dropdown.tsx
    src/components/toolbar/unified-toolbar.tsx
    src/components/filters/filter-chips.tsx
    src/components/filters/filter-combobox.tsx
    src/components/filters/filter-empty-state.tsx
  </files>
  <action>
Per-file migration. For each file:
1. Read the full file.
2. Apply `docs/TYPE-MIGRATION.md`.
3. Drop paired weight classes.
4. `uppercase` only on `.text-label`.

**File-specific:**

`filter-popover.tsx` (5 text-size hits, 5 font-weight hits — densest in this plan):
- Popover title (e.g., "Filters" / "Filter by column"): `text-heading` OR adopt `<SectionHeader title="Filters" />` if there's no title/metadata ambiguity.
- Column-group headings inside the popover: `.text-label uppercase text-muted-foreground` (acts as section eyebrow inside the popover).
- Filter value rows: `text-body`.
- Helper text / "no filters active" placeholder: `text-caption text-muted-foreground`.

`save-view-popover.tsx` (3 text-size hits):
- Popover title "Save view": `text-heading`.
- Input label: `text-body`.
- Helper / validation text: `text-caption text-muted-foreground`.
- Any existing "Include drill state" checkbox label (from Phase 32-02): `text-body`.

`preset-dropdown.tsx` (2 hits):
- Dropdown items: `text-body`.
- Any small meta (e.g., preset count, last used): `text-caption text-muted-foreground`.

`unified-toolbar.tsx` (1 weight hit, no size hit):
- Locate the single `font-medium`. If paired with a type token — drop. If standalone on a plain element — use judgment; prefer replacing the element's size class with a token AND dropping font-medium.

`filter-chips.tsx` (3 size hits, no weight hit per grep):
- Chip labels: `text-label` (pairs with uppercase if chips are categorical pills) OR `text-caption` (if chips read as inline meta). Decide on visual inspection during execution.
- Count badges inside chips (if any): `.text-label-numeric` (preserves mono + tabular for tiny counts).

`filter-combobox.tsx` (3 size hits, 1 weight hit):
- Combobox item text: `text-body`.
- Selected-value display: `text-body`.
- Placeholder / empty state: `text-caption text-muted-foreground`.

`filter-empty-state.tsx` (2 size hits):
- Empty-state headline: `text-heading` (or `text-title` if it's not anchoring a section).
- Empty-state description: `text-body text-muted-foreground` (or `text-caption` for very small hint text).
  </action>
  <verify>
    <automated>npm run build</automated>
    Follow-ups:
    - `rg -n 'text-(xs|sm|base|lg|xl|2xl)' src/components/toolbar/ src/components/filters/` → 0 hits
    - `rg -n '\bfont-(semibold|medium|bold)\b' src/components/toolbar/ src/components/filters/` → 0 hits (unless a standalone weight on a non-token element in unified-toolbar is judged semantic — if so, document in the file and in the plan SUMMARY)
  </verify>
  <done>
    Build passes. 7 files migrated. Popover titles consistent. Filter chip labels resolved per visual inspection.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate column-picker + saved-views surfaces</name>
  <files>
    src/components/columns/column-picker-sidebar.tsx
    src/components/columns/column-group.tsx
    src/components/columns/column-search.tsx
    src/components/views/views-sidebar.tsx
    src/components/views/view-item.tsx
    src/components/views/save-view-input.tsx
  </files>
  <action>
Per-file migration. For each file:
1. Read the full file.
2. Apply `docs/TYPE-MIGRATION.md`.
3. Drop paired weight classes.

**File-specific:**

`column-picker-sidebar.tsx` (4 size hits):
- Sidebar section header (e.g., "Visible columns", "Hidden columns"): adopt `<SectionHeader title="..." />` per section, or use `.text-label uppercase text-muted-foreground` if these are eyebrow-sized section dividers inside a single sidebar.
- Column name items: `text-body`.
- Meta (column type indicator, data type badge): `text-caption text-muted-foreground`.

`column-group.tsx` (3 size hits, 1 weight hit):
- Group header: `.text-label uppercase text-muted-foreground` (classic eyebrow pattern for a list group).
- Group item: `text-body`.

`column-search.tsx` (1 size hit):
- Likely the search input placeholder or empty-results text: `text-caption text-muted-foreground`.

`views-sidebar.tsx` (4 size hits):
- Sidebar title "Saved views": `<SectionHeader title="Saved views" />` OR `text-heading`.
- Empty state: `text-body text-muted-foreground`.
- Section dividers: `.text-label uppercase text-muted-foreground`.

`view-item.tsx` (1 size hit):
- View name: `text-body`.
- Meta (timestamp / filter count / drill badge): `text-caption text-muted-foreground`.

`save-view-input.tsx` (2 size hits):
- Input label: `text-body`.
- Validation / hint text: `text-caption text-muted-foreground`.
  </action>
  <verify>
    <automated>npm run build</automated>
    Follow-ups:
    - `rg -n 'text-(xs|sm|base|lg|xl|2xl)' src/components/columns/ src/components/views/` → 0 hits
    - `rg -n '\bfont-(semibold|medium|bold)\b' src/components/columns/ src/components/views/` → 0 hits
  </verify>
  <done>
    Build passes. 6 files migrated. Column picker and saved-views both use SectionHeader for their top-level titles (where applicable).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — toolbar, popovers, filters, column picker, saved views</name>
  <files>(verification only)</files>
  <action>Pause for human verification. Instructions below.</action>
  <verify>Human confirms with screenshots of the key popovers and sidebars.</verify>
  <done>User approves screenshots in both modes covering: filter popover, save-view popover, column picker sidebar, saved-views sidebar.</done>
  <what-built>
13 files migrated: toolbar (filter popover, save-view popover, preset dropdown, unified toolbar), filters (chips, combobox, empty state), columns (picker sidebar, group, search), views (sidebar, item, save input). Section titles inside popovers/sidebars use SectionHeader where they anchor distinct sections; eyebrow-style list-group headers use .text-label uppercase text-muted-foreground.
  </what-built>
  <how-to-verify>
1. `mcp__Claude_Preview__preview_start`.
2. **Filter popover:**
   - Open the filter popover from the toolbar. Light mode screenshot + dark mode screenshot.
   - Title reads as heading (18px, weight 600).
   - Column-group headings inside read as overline (12px uppercase tracked muted).
   - Filter rows read as body.
3. **Save-view popover:**
   - Click Save view. Light + dark screenshots.
   - Title, input label, helper text render at correct tiers.
4. **Column picker sidebar:**
   - Open column picker. Light + dark screenshots.
   - "Visible columns" / "Hidden columns" sections read consistently.
5. **Saved views sidebar:**
   - Open saved-views sidebar. Light + dark screenshots.
   - View names (text-body) vs. meta (text-caption) clearly differentiated.
6. **Filter chips:**
   - Apply one filter. Confirm the chip at the top of the table renders with the chosen type (text-label or text-caption — per Task 1 decision).
7. **Toolbar:**
   - Smoke test — buttons and labels look consistent with pre-migration.

Approval requires: 8 screenshots total (4 surfaces × 2 modes). Explicit confirmation that the "overline" eyebrow inside the filter popover reads as uppercase tracked muted (DS-09 proof in a non-KPI context).
  </how-to-verify>
  <resume-signal>Type "approved" with screenshots, or describe issues (e.g. "popover title too small", "chip labels inconsistent", "sidebar heading flat").</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes.
- Zero ad-hoc hits in any of the 13 files.
- SectionHeader adopted where applicable (popover/sidebar titles).
- Visual verification passed in both modes for all 4 major surfaces.
</verification>

<success_criteria>
- DS-07 progressed: 13 files off ad-hoc classes.
- DS-10 progressed: SectionHeader adopted in popovers/sidebars where section titles exist.
</success_criteria>

<output>
After completion, create `.planning/phases/27-typography-and-information-hierarchy/27-04-SUMMARY.md` noting SectionHeader adoption sites, filter-chip tier decision (label vs caption), and any migration-table resolutions fed to docs/TYPE-MIGRATION.md §10.
</output>
