---
phase: 31-visual-polish-pass
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
autonomous: true
requirements: [DS-33]
must_haves:
  truths:
    - "Table row hover uses a very soft neutral bg tint (no accent tint, no geometry shift, no chevron) with --duration-quick + --ease-default motion wiring"
    - "Hover tint feels closer to Linear/Notion than to the current Phase 26 baseline — validated in-browser in both light and dark mode before any value change"
    - "If retune is needed, --hover-bg is softened (e.g., light 8%→5%, dark 6%→4%) — otherwise the plan ships as validate-only and documents the no-op"
  artifacts:
    - path: src/app/globals.css
      provides: "--hover-bg token either unchanged (validate-only) or retuned in :root + .dark"
      contains: "--hover-bg"
  key_links:
    - from: "table-body.tsx:48"
      to: "--hover-bg token"
      via: "hover:bg-hover-bg existing class + transition-colors duration-quick ease-default"
      pattern: "hover:bg-hover-bg"
---

<objective>
DS-33 is a validate-first requirement. Phase 30-03 already shipped the row-hover wiring (`transition-colors duration-quick ease-default hover:bg-hover-bg` at `src/components/table/table-body.tsx:48`). The only question Phase 31 must answer is: does the current `--hover-bg` intensity (light 8% / dark 6%) already hit CONTEXT's "~muted/4" target, or does it need to soften?

Purpose: Do not blindly change a token. Validate current intensity in-browser first; retune only if the tint reads as too loud vs. Linear/Notion reference. Per 31-RESEARCH §DS-33, current values are believed to be in-the-neighborhood correct — this plan is sized accordingly.

Output: Either a one-hunk globals.css edit softening `--hover-bg` (if retune is needed) or a no-op ship with a summary noting the current values pass the bar.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-visual-polish-pass/31-CONTEXT.md
@.planning/phases/31-visual-polish-pass/31-RESEARCH.md
@src/app/globals.css
@src/components/table/table-body.tsx

<interfaces>
<!-- Current token values (globals.css ground truth) -->
:root  — line 291: --hover-bg: color-mix(in oklch, var(--neutral-500) 8%, transparent);
.dark  — line 404: --hover-bg: color-mix(in oklch, oklch(1 0 0)      6%, transparent);

<!-- Wiring already live (Phase 30-03, do NOT touch) -->
table-body.tsx:48: `h-[var(--row-height)] transition-colors duration-quick ease-default hover:bg-hover-bg`

<!-- Optional retune (if browser check flags tint as too loud) -->
Light: 8% → 5%  (closer to ~muted/4 target)
Dark:  6% → 4%
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: In-browser validation of current --hover-bg intensity</name>
  <files>(no file changes — browser verification)</files>
  <action>
Start `npm run dev`. Navigate to the main data-display page with the table visible. Hover over table rows in:

1. Light mode — does the row tint read as "very soft neutral ~muted/4" per 31-CONTEXT, or does it read as a clearly-visible background change?
2. Dark mode — same question.

Reference: Linear and Notion row-hover intensity. If the current tint is "noticeable but gentle" and matches that feel, the token is correct and Task 2 is a no-op. If the tint reads as clearly louder than Linear/Notion, proceed to Task 2 and soften.

Document the in-browser call in the plan SUMMARY.md with one of two findings:
- **A (no retune needed):** "Current --hover-bg values (light 8% / dark 6%) pass the ~muted/4 bar; no token change shipping."
- **B (retune needed):** "Current --hover-bg values read too loud; softening to 5% / 4%."

If Snowflake creds are unavailable (recurring issue per STATE.md session notes), fall back to the /tokens page + a mocked table surface for the visual check, OR defer the final call to the Phase 31 human-verify checkpoint in 31-06 and ship this plan as no-op-pending-validation.
  </action>
  <verify>
    <automated>echo "Validate-first task — verification criterion is the recorded finding in 31-04-SUMMARY.md (A or B)"; test -f src/components/table/table-body.tsx && grep -q "hover:bg-hover-bg" src/components/table/table-body.tsx</automated>
    (Automated sanity: wiring from Phase 30-03 still present.)
  </verify>
  <done>In-browser finding recorded. The executor commits SUMMARY.md stating A (no change) or B (retune ships in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: [Conditional] Retune --hover-bg if Task 1 flagged tint as too loud</name>
  <files>src/app/globals.css</files>
  <action>
**This task runs only if Task 1's finding was B (retune needed).** If finding was A, skip this task and note in SUMMARY that Task 2 was a no-op.

If retuning:

1. Line 291 (`:root` block):
   - Change FROM: `--hover-bg: color-mix(in oklch, var(--neutral-500) 8%, transparent);`
   - Change TO:   `--hover-bg: color-mix(in oklch, var(--neutral-500) 5%, transparent);`

2. Line 404 (`.dark` block):
   - Change FROM: `--hover-bg: color-mix(in oklch, oklch(1 0 0) 6%, transparent);`
   - Change TO:   `--hover-bg: color-mix(in oklch, oklch(1 0 0) 4%, transparent);`

Add a rationale comment directly above the :root --hover-bg declaration:

    /* Phase 31 DS-33: Softened to ~muted/4 target per CONTEXT (was light 8% / dark 6%). */

Do NOT touch the wiring at table-body.tsx:48 — that's Phase 30-03 output and correctly consumes --hover-bg via `hover:bg-hover-bg` + `transition-colors duration-quick ease-default`. Do NOT expand the hover to other tables (matrix, comparison) — CONTEXT scopes DS-33 to main table rows only.

Pitfall 6 guard (31-RESEARCH): do NOT add `.hover-lift` to table rows. The rows are not interactive cards; `transition-colors` on the row would fight `transition: transform, box-shadow` on `.hover-lift`. Table rows stay on transition-colors.
  </action>
  <verify>
    <automated>grep -nE "^\\s*--hover-bg:" src/app/globals.css | wc -l | grep -q "^\\s*2$"</automated>
    (Manual: if retuning, confirm both declarations show the new percentages and the intent comment is present. If no retune, confirm the file is unchanged at these two lines.)
  </verify>
  <done>Either --hover-bg retuned to softer percentages in both :root and .dark (with rationale comment) OR file left unchanged per Task 1 finding A.</done>
</task>

</tasks>

<verification>
- Decision recorded: validate-only or retune
- If retune: grep shows new 5%/4% values; rationale comment present; no consumer file edits
- Phase 30 row-hover wiring (table-body.tsx:48 + transition-colors + hover:bg-hover-bg) is UNCHANGED
- All existing check:* guards exit 0
</verification>

<success_criteria>
- DS-33 has a decision: validate-only (current values pass bar) OR validate-then-retune (softer values ship)
- Row hover remains tint-only (no geometry shift, no accent bar, no chevron) per CONTEXT
- Phase 30 motion wiring preserved — DS-33 is intensity only, not rewiring
- Plan SUMMARY documents the in-browser finding so the audit trail is explicit
</success_criteria>

<output>
After completion, create `.planning/phases/31-visual-polish-pass/31-04-SUMMARY.md` stating finding A or B and, if B, the exact before/after percentages + the wall-clock duration of the browser check.
</output>
