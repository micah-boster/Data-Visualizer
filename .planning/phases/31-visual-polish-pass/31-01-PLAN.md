---
phase: 31-visual-polish-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
autonomous: true
requirements: [DS-30, DS-32]
must_haves:
  truths:
    - "Cards, panels, and the table read as near-borderless in both light and dark mode (~8% border opacity)"
    - "Dark-mode surface-raised (KPI cards, chart cards, query cards) carries a visibly softer top-edge highlight (~7% white) than before"
    - "No consumer file changes are required — every border-border / bg-border / shadow-elevation-raised site automatically picks up the retuned values"
  artifacts:
    - path: src/app/globals.css
      provides: "Retuned --border (8%) in :root + .dark and bumped inset glass highlight layer inside --shadow-elevation-raised (.dark)"
      contains: "--border: color-mix(in oklch"
  key_links:
    - from: ":root { --border }"
      to: ".dark { --border }"
      via: "same 8% opacity in both blocks"
      pattern: "--border:\\s*color-mix.*8%"
    - from: ".dark { --shadow-elevation-raised }"
      to: "inset 0 1px 0 0 oklch(1 0 0 / 0.07)"
      via: "bumped from 0.05 to 0.07"
      pattern: "inset 0 1px 0 0 oklch\\(1 0 0 / 0\\.07\\)"
---

<objective>
Land the two token-level changes that unblock the rest of Phase 31:

1. **DS-32 border retune** — drop `--border` from 15% (light) / 10% (dark) to a single 8% standard in both modes, so cards/panels/table read as near-borderless and surface + shadow (Phase 28) carry the shape. `--input` stays at 15% for form-control affordance (intentional divergence — documented inline).
2. **DS-30 glass highlight** — bump the existing inset layer in `--shadow-elevation-raised` (`.dark`) from `0.05` → `0.07` so dark-mode `surface-raised` consumers (KPI cards, chart cards, query cards) gain a faint top-edge highlight. Path A from 31-RESEARCH: zero consumer edits, avoids the Tailwind shadow-composition pitfall (Pitfall 1).

Purpose: Both requirements are globals.css-only token retunes and naturally live in the same plan — they touch the same file, share the "single token source" discipline, and independently unblock downstream work (gradient divider consumes `--border`; no other plan depends on the shadow bump).

Output: Two-hunk edit to `src/app/globals.css` with a documentation comment block noting the Phase 31 intent + the `--input` divergence.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-visual-polish-pass/31-CONTEXT.md
@.planning/phases/31-visual-polish-pass/31-RESEARCH.md
@src/app/globals.css

<interfaces>
<!-- Current token values (from globals.css ground-truth read) -->

:root (light mode) — line 291, 323-324:
  --hover-bg: color-mix(in oklch, var(--neutral-500) 8%, transparent);  /* DS-33 baseline, not touched here */
  --border:   color-mix(in oklch, var(--neutral-500) 15%, transparent); /* DS-32 RETUNE → 8% */
  --input:    color-mix(in oklch, var(--neutral-500) 15%, transparent); /* LEAVE at 15% per Pitfall 5 */

.dark (dark mode) — line 404, 440, 469-470:
  --hover-bg: color-mix(in oklch, oklch(1 0 0) 6%, transparent);  /* DS-33 baseline, not touched here */
  --shadow-elevation-raised:
    0 1px 2px 0 oklch(0 0 0 / 0.45),
    0 4px 12px -2px oklch(0 0 0 / 0.40),
    inset 0 1px 0 0 oklch(1 0 0 / 0.05);  /* DS-30 BUMP → 0.07 */
  --border:   color-mix(in oklch, oklch(1 0 0) 10%, transparent);  /* DS-32 RETUNE → 8% */
  --input:    color-mix(in oklch, oklch(1 0 0) 15%, transparent);  /* LEAVE at 15% */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retune --border to single 8% standard in :root and .dark</name>
  <files>src/app/globals.css</files>
  <action>
Edit two lines in globals.css:

1. Line 323 (inside `:root { ... }` block):
   - Change FROM: `--border: color-mix(in oklch, var(--neutral-500) 15%, transparent);`
   - Change TO:   `--border: color-mix(in oklch, var(--neutral-500) 8%, transparent);`

2. Line 469 (inside `.dark { ... }` block):
   - Change FROM: `--border: color-mix(in oklch, oklch(1 0 0) 10%, transparent);`
   - Change TO:   `--border: color-mix(in oklch, oklch(1 0 0) 8%, transparent);`

Leave `--input` (lines 324 and 470) UNCHANGED at 15%. This is intentional per 31-RESEARCH Pitfall 5 — form controls benefit from a slightly stronger frame for affordance; documented as an intentional divergence.

Add a short rationale comment directly above the `:root --border` line (one comment block covers both blocks):

    /*
     * Phase 31 DS-32: Single border-opacity standard at ~8% in both light and dark.
     * Cards, panels, and the table are meant to read as almost borderless and lean
     * on surface + shadow (Phase 28) for shape. --input stays at 15% for form-control
     * affordance (intentional divergence).
     */

Do NOT touch --sidebar-border (it already aliases --border via cascade — grep confirms if in doubt) or any other token. Do NOT introduce a new `--border-opacity` variable — retune in place per 31-RESEARCH Path A recommendation.
  </action>
  <verify>
    <automated>grep -n "^\s*--border:" src/app/globals.css | grep -cE "8%, transparent" | grep -q "^2$"</automated>
    Manual check: both `--border` declarations should show the 8% opacity and the intent comment should sit above the :root declaration.
  </verify>
  <done>`--border` in both :root and .dark uses `8%` opacity; `--input` unchanged at 15%; rationale comment in place; grep `grep -n 'color-mix.*15%.*transparent' src/app/globals.css` still returns the two --input lines only.</done>
</task>

<task type="auto">
  <name>Task 2: Bump --shadow-elevation-raised inset glass highlight (dark mode) from 0.05 to 0.07</name>
  <files>src/app/globals.css</files>
  <action>
Edit the multi-layer `--shadow-elevation-raised` declaration in the `.dark { ... }` block (starts around line 440). The declaration is a comma-separated three-layer `box-shadow` value. The third layer is the inset top-edge highlight and currently reads:

    inset 0 1px 0 0 oklch(1 0 0 / 0.05)

Change ONLY that third layer:

    inset 0 1px 0 0 oklch(1 0 0 / 0.07)

Do NOT touch the other two layers (the ambient + diffuse drop shadows) and do NOT touch the `:root` (light mode) `--shadow-elevation-raised` — per 31-CONTEXT the light-mode analog is explicitly declined.

Add a one-line rationale comment immediately above the dark-mode `--shadow-elevation-raised` declaration:

    /* Phase 31 DS-30: Dark-mode surface-raised carries a ~7% white inset top-edge highlight (glass effect). */

Critical Pitfall 1 guard (from 31-RESEARCH): do NOT attempt to compose a second shadow utility on consumers (`shadow-[inset_...]`) — Tailwind's shadow utilities REPLACE rather than compose. This plan's entire correctness depends on the fact that bumping the inset value inside the CSS custom property propagates transparently to every `shadow-elevation-raised` consumer with zero file edits.
  </action>
  <verify>
    <automated>grep -n "inset 0 1px 0 0 oklch(1 0 0 / 0.07)" src/app/globals.css | grep -q "^[0-9]"</automated>
    Also: `grep -n "inset 0 1px 0 0 oklch(1 0 0 / 0.05)" src/app/globals.css` should return zero matches (old value removed).
  </verify>
  <done>Dark-mode `--shadow-elevation-raised` third layer reads `inset 0 1px 0 0 oklch(1 0 0 / 0.07)`; light-mode shadow-elevation-raised (at line 96) UNCHANGED; rationale comment present.</done>
</task>

<task type="auto">
  <name>Task 3: Verify build + run existing guards + visual sanity</name>
  <files>(no file changes; verification only)</files>
  <action>
Run in sequence:

1. `npm run build` — confirm Next.js build completes (globals.css is imported into the App Router root layout; Tailwind v4 picks up CSS changes at build time).
2. `npm run check:tokens` — should exit 0 (Phase 27 guard; typography unaffected).
3. `npm run check:surfaces` — should exit 0 (Phase 28 guard; surface-raised consumers unaffected — no consumer file edits).
4. `npm run check:components` — should exit 0 (Phase 29 guard).
5. `npm run check:motion` — should exit 0 (Phase 30 guard).

Then visually sanity-check by grepping that consumer sites haven't changed:

    git diff --stat src/components/ src/app/ -- ':(exclude)src/app/globals.css'

Should report zero files changed (only globals.css touched).

No manual browser verification required in this plan — /tokens Visual Polish tab in 31-06 handles the visible sign-off. This plan's DS-32 + DS-30 outcomes will be verified downstream when 31-06 adds the side-by-side "old 15% vs new 8%" border-standard specimen and the dark-mode glass-highlight specimen.
  </action>
  <verify>
    <automated>npm run build && npm run check:tokens && npm run check:surfaces && npm run check:components && npm run check:motion</automated>
  </verify>
  <done>Build passes; all four existing guards exit 0; git diff shows only src/app/globals.css modified.</done>
</task>

</tasks>

<verification>
**Phase-level checks that land here:**
- `grep -c "8%, transparent" src/app/globals.css` includes two new hits for --border (light + dark both at 8%)
- `grep "inset 0 1px 0 0 oklch(1 0 0 / 0.07)" src/app/globals.css` returns exactly one match (the dark-mode glass highlight)
- No consumer file changes in src/ outside globals.css
- All four existing check:* guards exit 0
- Build passes
</verification>

<success_criteria>
- DS-32 token retune shipped: --border is 8% in both :root and .dark; --input untouched at 15%; rationale comment documents the intentional divergence
- DS-30 glass highlight refinement shipped: .dark --shadow-elevation-raised third layer bumped to 0.07; light-mode analog explicitly NOT added per CONTEXT lock
- Zero consumer file edits — the Path A approach (token-level retune) from 31-RESEARCH is preserved
- Downstream plans (31-05 gradient divider) can now rely on the retuned --border as the canonical divider color source
</success_criteria>

<output>
After completion, create `.planning/phases/31-visual-polish-pass/31-01-SUMMARY.md` describing the two token edits, any surprises (e.g., build-time Tailwind recalc warnings), and observed visual outcome when the executor spot-checks in `npm run dev`.
</output>
