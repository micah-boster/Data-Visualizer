---
phase: 26-design-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: false
requirements: [DS-01, DS-02, DS-03, DS-04, DS-05, DS-06]

must_haves:
  truths:
    - "Tailwind v4 build emits utilities for all new token namespaces without errors (next build succeeds)"
    - "Inter + JetBrains Mono load via next/font/google and drive --font-sans / --font-mono"
    - "Light and dark modes both render with warm-paper surfaces; dark-mode surface-raised is LIGHTER than surface-base"
    - "Every shadcn-dependent component (header, sidebar, cards, popovers, table) still renders correctly after shadcn var re-map"
    - "Spacing, typography, shadow, motion, radius, surface, neutral, state-color, interaction, chart-palette tokens are all defined in globals.css"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Full token system: @theme namespaces + :root / .dark raw values + shadcn re-map"
      contains: "--spacing-1"
    - path: "src/app/layout.tsx"
      provides: "Inter + JetBrains_Mono font loaders producing --font-inter and --font-jetbrains-mono CSS vars"
      contains: "next/font/google"
  key_links:
    - from: "src/app/globals.css @theme inline"
      to: "CSS custom properties in :root / .dark"
      via: "semantic alias pattern (--color-surface-raised: var(--surface-raised))"
      pattern: "var\\(--surface-"
    - from: "shadcn utilities (bg-background, bg-card, border-border, etc.)"
      to: "new surface / neutral tokens"
      via: "re-mapped values inside existing :root / .dark blocks"
      pattern: "--background:\\s*var\\(--surface-base\\)"
    - from: "src/app/layout.tsx font variable classes"
      to: "@theme inline --font-sans / --font-mono"
      via: "next/font variable option"
      pattern: "variable:\\s*['\"]--font-(inter|jetbrains-mono)"
---

<objective>
Establish the foundational design token infrastructure: define spacing, typography, shadow, motion, radius, surface, neutral, state-color, interaction, and chart palette tokens as CSS custom properties + Tailwind v4 `@theme` entries in `src/app/globals.css`. Swap fonts Geist → Inter + JetBrains Mono in `src/app/layout.tsx`. Re-map shadcn variables onto the new surface/neutral tokens without renaming them so existing shadcn consumers keep working. Finish with a human-verify checkpoint to confirm light + dark modes render correctly before Wave 2 pilots migrate to the new system.

Purpose: Every downstream phase (27-31) and the Wave 2 pilots of this phase consume these tokens. Without correct infrastructure, nothing else works.
Output: Extended globals.css (all token categories present, light + dark values) and updated layout.tsx (new fonts). App continues to render; no visual breakage in existing shadcn-driven UI.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-design-tokens/26-CONTEXT.md
@.planning/phases/26-design-tokens/26-RESEARCH.md
@src/app/globals.css
@src/app/layout.tsx

<interfaces>
<!-- Current state of globals.css entry block + tokens that MUST continue working after this plan. -->

From src/app/globals.css (existing — must not break):
- `@import "tailwindcss"; @import "tw-animate-css"; @import "shadcn/tailwind.css";`
- `@custom-variant dark (&:is(.dark *));`
- `@theme inline { --color-background: var(--background); --font-sans: var(--font-sans); --font-mono: var(--font-geist-mono); --color-sidebar-*: ...; --color-chart-1..8: var(--chart-1..8); --color-ring: var(--ring); --color-input: ...; }`
- `:root { --radius: 0.625rem; --background: oklch(...); --foreground: ...; --card: ...; --popover: ...; --primary: ...; --secondary: ...; --muted: ...; --accent: ...; --destructive: ...; --border: ...; --input: ...; --ring: ...; --chart-1..8: ...; --sidebar*: ...; --cell-zero: ...; --cell-tint-low: ...; --cell-tint-high: ...; }`
- `.dark { same keys with dark values, including --border: oklch(1 0 0 / 10%); }`

From src/app/layout.tsx (current — swap away from Geist):
```tsx
import { Geist, Geist_Mono } from "next/font/google";
const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });
// <html className={`${geistSans.variable} ${geistMono.variable} ...`}>
```

Next.js font pattern (from node_modules/next/dist/docs/01-app/01-getting-started/13-fonts.md):
```tsx
import { Inter, JetBrains_Mono } from 'next/font/google';
const inter = Inter({ variable: '--font-inter', subsets: ['latin'] });
const jetbrainsMono = JetBrains_Mono({ variable: '--font-jetbrains-mono', subsets: ['latin'] });
```

Tailwind v4 namespace contract (verified in RESEARCH.md):
- `--spacing-*` → generates `p-*`, `gap-*`, `m-*`, etc.
- `--text-*` + optional `--text-*--line-height` / `--text-*--letter-spacing` / `--text-*--font-weight` → `text-*` utilities
- `--shadow-*` → `shadow-*`
- `--duration-*` → `duration-*`
- `--ease-*` → `ease-*`
- `--radius-*` → `rounded-*`
- `--color-*` → `bg-*`, `text-*`, `border-*`, `ring-*`, etc.
- `@theme inline { --color-surface-raised: var(--surface-raised); }` resolves at build time (use for theme-varying surfaces — light vs dark define raw `--surface-raised` in `:root`/`.dark`).
</interfaces>

<project_constraints>
- Next.js 16.2.3 is NOT the Next.js in training data. Always check node_modules/next/dist/docs/ before Next-specific code. font-loading pattern is confirmed above.
- Tailwind v4.2.2: NO tailwind.config.* file exists; all config is CSS-first in globals.css. Do not reintroduce a JS config.
- shadcn vars MUST be re-mapped, NOT renamed. Existing utilities (bg-background, bg-card, border-border, bg-popover, bg-primary, text-muted-foreground, bg-sidebar, bg-sidebar-accent, ring-ring, etc.) must keep working.
- Dark mode surface-raised MUST be LIGHTER than surface-base (CONTEXT locked — inverts shadcn convention).
- No ESLint rule for token enforcement (deferred). No new dependencies.
- OKLCH values for warm neutrals / warm accent / chart palette / shadow tints are Claude's Discretion per CONTEXT; aim for warm (hue ~55-85) paper in light, warm near-black with brown undertone in dark.
</project_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define full token system in globals.css (extend @theme + :root + .dark with shadcn re-map)</name>
  <files>src/app/globals.css</files>
  <action>
Extend `src/app/globals.css` to define the complete Phase 26 token system. Preserve all existing shadcn + project tokens (see interfaces block) and ADD new tokens. Use clearly commented sections (`/* === SPACING === */`, `/* === TYPOGRAPHY === */`, `/* === SHADOWS === */`, `/* === MOTION === */`, `/* === RADIUS === */`, `/* === NEUTRAL SCALE === */`, `/* === SURFACES === */`, `/* === ACCENT / STATE / INTERACTION === */`, `/* === CHART PALETTE === */`).

**IMPORTANT — Tailwind v4 namespace naming:** Above the spacing aliases AND above the motion easing tokens, add a CSS comment block:

```css
/* NOTE: naming
   Tailwind v4 auto-generates utility classes from specific token namespaces:
   - `--spacing-*`  → emits p-*, m-*, gap-*, w-*, h-*, etc.
   - `--ease-*`     → emits ease-* utilities
   - `--duration-*` → emits duration-* utilities
   - `--text-*`     → emits text-* utilities
   - `--shadow-*`   → emits shadow-* utilities
   - `--radius-*`   → emits rounded-* utilities
   - `--color-*`    → emits bg-*, text-*, border-*, ring-* utilities

   Therefore semantic spacing aliases use `--spacing-inline`, `--spacing-stack`, `--spacing-section`,
   `--spacing-card-padding`, `--spacing-page-gutter` (NOT `--space-*` as loosely referenced in CONTEXT).
   The numeric scale `--spacing-1..12` is the canonical source; semantic aliases reference those numerics.

   Easing tokens use `--ease-default`, `--ease-spring`, `--ease-decelerate` (NOT `easing-*` as loosely
   referenced in CONTEXT) so they emit `ease-default` etc. utilities.

   Renames are namespace-driven, not stylistic. Do not "fix" them back to `--space-*` / `easing-*`. */
```

Organize into:

1. **`@theme` block** — primitive numeric tokens that must emit utilities:
   - `--spacing-1..12` = 4, 8, 12, 16, 20, 24, 32, 40, 48, 64, 80, 96px (per CONTEXT)
   - `--text-display` 1.5rem / `--text-display--line-height` 1.2 / `--text-display--font-weight` 600
   - `--text-heading` 1.125rem / line-height 1.35 / font-weight 600
   - `--text-title` 0.9375rem / line-height 1.4 / font-weight 500
   - `--text-body` 0.875rem / line-height 1.5
   - `--text-label` 0.75rem / line-height 1.4 / letter-spacing 0.04em / font-weight 500 (uppercase applied per-utility when used)
   - `--text-caption` 0.75rem / line-height 1.4
   - `--shadow-xs`, `--shadow-sm`, `--shadow-md`, `--shadow-lg` — each multi-layer (tight inner + wider soft blur). Use warm-tinted shadow color in light mode, e.g. `oklch(0.2 0.015 70 / 0.05)` for tight layer, `oklch(0.2 0.015 70 / 0.08)` for wider. Pick layered radii like: xs = `0 1px 2px 0 ... / 0.04`; sm = `0 1px 2px 0 + 0 2px 6px -2px`; md = `0 2px 4px 0 + 0 6px 12px -4px`; lg = `0 4px 8px 0 + 0 12px 24px -8px`.
   - `--duration-quick` 120ms, `--duration-normal` 200ms, `--duration-slow` 320ms
   - `--ease-default` cubic-bezier(0.4, 0, 0.2, 1), `--ease-spring` cubic-bezier(0.34, 1.56, 0.64, 1), `--ease-decelerate` cubic-bezier(0, 0, 0.2, 1)
   - `--radius-sm` 4px, `--radius-md` 8px, `--radius-lg` 12px (replace existing computed ladder; keep `--radius: 0.625rem` as legacy alias if removing it would break shadcn primitives — verify via grep for `var(--radius)` usage first)
   - Density pairs: `--table-row-height-dense` 32px, `--table-row-height-sparse` 40px (plan 04 will consume these)

2. **`@theme inline` block** — semantic aliases + theme-varying colors exposed as Tailwind utilities:
   - Semantic spacing: `--spacing-inline: var(--spacing-2)`, `--spacing-stack: var(--spacing-3)`, `--spacing-section: var(--spacing-6)`, `--spacing-card-padding: var(--spacing-4)`, `--spacing-page-gutter: var(--spacing-4)`
   - Color aliases (surface): `--color-surface-base: var(--surface-base)`, `--color-surface-raised: var(--surface-raised)`, `--color-surface-inset: var(--surface-inset)`, `--color-surface-overlay: var(--surface-overlay)`, `--color-surface-floating: var(--surface-floating)`
   - Neutral scale: `--color-neutral-50: var(--neutral-50)` through `--color-neutral-950: var(--neutral-950)`
   - Accent: `--color-accent-warm: var(--accent-warm)`
   - State colors: `--color-success-bg`, `--color-success-border`, `--color-success-fg`, same for warning/error/info (each referencing its raw `--state-*-*` var)
   - Interaction: `--color-focus-ring: var(--focus-ring)`, `--color-selection-bg: var(--selection-bg)`, `--color-hover-bg: var(--hover-bg)`
   - Chart palette: `--color-chart-categorical-1..8`, `--color-chart-diverging-neg`, `--color-chart-diverging-neu`, `--color-chart-diverging-pos`
   - Font var retarget: `--font-sans: var(--font-inter)`, `--font-mono: var(--font-jetbrains-mono)`, `--font-heading: var(--font-inter)` (REPLACES current `--font-mono: var(--font-geist-mono)` line)

3. **`:root` block** — light-mode raw values (warm paper):
   - 11-step warm neutral scale `--neutral-50..950` using OKLCH with hue ~75 (stone/khaki). Example ladder: 0.985 / 0.97 / 0.93 / 0.87 / 0.76 / 0.62 / 0.50 / 0.38 / 0.28 / 0.20 / 0.14 lightness, chroma ~0.004-0.014.
   - Surfaces: `--surface-base: oklch(0.985 0.008 85)` (cream), `--surface-raised: oklch(1 0.004 85)` (paper-white), `--surface-inset: oklch(0.965 0.012 85)` (recessed beige), `--surface-overlay: oklch(1 0 0)`, `--surface-floating: oklch(1 0 0)`.
   - Warm accent: `--accent-warm: oklch(0.68 0.15 55)` (ochre/amber; verify does not clash with chart palette visually — Claude's Discretion).
   - State colors (each -bg / -border / -fg triplet). Example: success `oklch(0.95 0.05 140)` / `oklch(0.72 0.12 140)` / `oklch(0.35 0.10 140)`; warning hue ~70; error hue ~25; info hue ~230. Fine-tune visually.
   - Interaction: `--focus-ring: var(--accent-warm)`, `--selection-bg: color-mix(in oklch, var(--accent-warm) 18%, transparent)`, `--hover-bg: color-mix(in oklch, var(--neutral-500) 8%, transparent)`.
   - Chart categorical 8-color palette (warm-anchored, distinct hues — Claude's Discretion; target hues around 55, 150, 210, 295, 25, 110, 250, 335). Chart diverging triplet: neg (red hue ~25), neu (neutral warm), pos (green hue ~145).
   - **shadcn re-map (edit existing keys, do NOT rename):** `--background: var(--surface-base)`; `--foreground: var(--neutral-900)`; `--card: var(--surface-raised)`; `--card-foreground: var(--neutral-900)`; `--popover: var(--surface-overlay)`; `--popover-foreground: var(--neutral-900)`; `--primary: var(--accent-warm)`; `--primary-foreground: var(--neutral-50)`; `--secondary: var(--surface-inset)`; `--secondary-foreground: var(--neutral-800)`; `--muted: var(--surface-inset)`; `--muted-foreground: var(--neutral-600)`; `--accent: var(--surface-inset)`; `--accent-foreground: var(--neutral-900)`; `--destructive: var(--state-error-fg)`; `--border: color-mix(in oklch, var(--neutral-500) 15%, transparent)`; `--input: color-mix(in oklch, var(--neutral-500) 15%, transparent)`; `--ring: var(--accent-warm)`. Re-map `--chart-1..8` to reference `--chart-categorical-1..8`. Re-map `--sidebar: var(--surface-base)`, `--sidebar-foreground: var(--neutral-800)`, `--sidebar-accent: var(--surface-inset)`, `--sidebar-accent-foreground: var(--neutral-900)`, `--sidebar-border: var(--border)`, `--sidebar-ring: var(--accent-warm)`, `--sidebar-primary: var(--accent-warm)`, `--sidebar-primary-foreground: var(--neutral-50)`.

4. **`.dark` block** — dark-mode overrides (warm dark; raised LIGHTER than base):
   - Neutral ladder inverted (50 is now near-black, 950 is near-white), hue ~60 (warm brown undertone).
   - `--surface-base: oklch(0.16 0.012 60)` (warm near-black)
   - `--surface-raised: oklch(0.22 0.012 60)` (LIGHTER than base — CONTEXT locked)
   - `--surface-inset: oklch(0.12 0.010 60)` (darker recessed)
   - `--surface-overlay: oklch(0.24 0.012 60)`, `--surface-floating: oklch(0.26 0.012 60)`
   - `--accent-warm: oklch(0.74 0.13 55)` (slightly brighter in dark to maintain contrast)
   - State colors rebalanced for dark (fg lighter, bg darker-tinted).
   - Shadows re-tinted darker / more opaque: `--shadow-xs: 0 1px 2px 0 oklch(0 0 0 / 0.35)`, etc.
   - shadcn re-map lines REPEAT inside `.dark` block so the dark `--surface-*` values cascade: `--background: var(--surface-base);` etc. (Same structure as `:root` re-map, but .dark's raw surface values propagate.)
   - Keep existing `--cell-zero`, `--cell-tint-low`, `--cell-tint-high` but re-map to reference chart diverging palette (`--cell-zero: var(--chart-diverging-neu)` etc.) so heatmap cells adapt without separate tuning.

Use oklch() throughout — do not mix hsl/rgb. Add CSS comments in each section noting which DS requirement it addresses (DS-01 spacing, DS-02 typography, DS-03 shadows, DS-04 motion, DS-05 surfaces, DS-06 both modes verified in task 3).

Do not introduce `--table-row-height-dense` overrides scoped by data-attr here — that belongs in plan 04 (table row pilot). Define only the raw tokens.

Preserve:
- Existing `@import` order (tailwindcss, tw-animate-css, shadcn/tailwind.css).
- `@custom-variant dark (&:is(.dark *))` declaration.
- `--radius: 0.625rem` if removing it breaks shadcn primitives (grep first: `grep -rn "var(--radius)" src/components/ui/`). If used, leave the alias but add the explicit `--radius-sm/md/lg` values separately.
- All existing non-shadcn vars that this plan isn't renaming.
  </action>
  <verify>
    <automated>npm run build</automated>
    After build succeeds, also spot-check with: `grep -E '^\s*--(spacing-1|text-display|shadow-md|duration-quick|ease-spring|radius-md|surface-raised|neutral-500|accent-warm|focus-ring|chart-categorical-1):' src/app/globals.css` returns matches in both `@theme`/`:root`/`.dark` where appropriate.
  </verify>
  <done>
    `npm run build` succeeds with zero Tailwind/CSS errors. All required token names exist per grep check. Both `:root` and `.dark` blocks define `--surface-base`, `--surface-raised` (lighter in dark), `--surface-inset`, surface-overlay/floating, and re-mapped shadcn vars. `@theme inline` exposes `--color-surface-*`, `--color-neutral-*`, `--color-chart-categorical-*`, `--font-sans: var(--font-inter)`, `--font-mono: var(--font-jetbrains-mono)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Swap Geist → Inter + JetBrains Mono in layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <action>
Modify `src/app/layout.tsx`:

1. Replace the import:
   - OLD: `import { Geist, Geist_Mono } from "next/font/google";`
   - NEW: `import { Inter, JetBrains_Mono } from "next/font/google";`

2. Replace the font loader calls:
   - OLD: `const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });` and the matching `geistMono = Geist_Mono({ variable: "--font-geist-mono", ... })`.
   - NEW:
     ```tsx
     const inter = Inter({ variable: "--font-inter", subsets: ["latin"], display: "swap" });
     const jetbrainsMono = JetBrains_Mono({ variable: "--font-jetbrains-mono", subsets: ["latin"], display: "swap" });
     ```

3. Replace the className on `<html>` / `<body>` references: every instance of `${geistSans.variable}` becomes `${inter.variable}` and every `${geistMono.variable}` becomes `${jetbrainsMono.variable}`.

4. Keep everything else (suppressHydrationWarning, ThemeProvider wiring, SidebarProvider, Toaster) exactly as-is.

5. Grep the project for any other reference to `--font-geist-sans` / `--font-geist-mono`:
   ```bash
   grep -rn "font-geist" src/
   ```
   If any remain outside globals.css (which the previous task already retargets via `@theme inline`) and layout.tsx, update them to reference `--font-inter` / `--font-jetbrains-mono`.

Do not modify shadcn components or any consumer class. The @theme inline alias (`--font-sans: var(--font-inter)`) from Task 1 is what drives utility classes like `font-sans` to resolve correctly — downstream components do not need edits.
  </action>
  <verify>
    <automated>npm run build</automated>
    Additional sanity: `grep -rn "Geist\|font-geist" src/` should return zero results (or only historical comments — confirm none are live code paths).
  </verify>
  <done>
    `npm run build` succeeds. Inter and JetBrains_Mono loaders instantiated with `--font-inter` / `--font-jetbrains-mono` CSS vars. No live `Geist` / `Geist_Mono` imports remain. `font-sans` and `font-mono` Tailwind utilities resolve through the new font vars via `@theme inline` from Task 1.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — app renders correctly in light + dark modes after token + font swap</name>
  <files>src/app/globals.css, src/app/layout.tsx (no edits — verification only)</files>
  <action>Pause for human verification of Task 1 + Task 2 outputs. Instructions below.</action>
  <verify>Human confirms by typing "approved" with attached screenshots.</verify>
  <done>User types "approved" after visual review of light + dark modes; raised-lighter-than-base inversion confirmed in dark; no shadcn component regressed.</done>
  <what-built>
Token infrastructure complete: full Tailwind v4 token system (spacing, type, shadow, motion, radius, surface, neutral, state, interaction, chart) in globals.css; shadcn vars re-mapped onto surface/neutral tokens; fonts swapped to Inter + JetBrains Mono. The build passes, but visual regressions from the shadcn re-map and font swap can only be caught by eyes.
  </what-built>
  <how-to-verify>
1. Start dev server: `mcp__Claude_Preview__preview_start` (launch server named `dev` — create `.claude/launch.json` if missing with `npm run dev`, port 3000).
2. Navigate to `/` (root). Take `preview_screenshot` in light mode.
3. Open DevTools via `preview_inspect` — confirm `<html>` has the Inter and JetBrains Mono font variable classes applied; confirm computed `font-family` on body is Inter.
4. Inspect a shadcn-driven element (e.g., the sidebar, a Card, a Popover trigger, a Button) with `preview_inspect`. Verify computed `background-color` matches the surface token (light: creamy off-white; raised cards slightly whiter than the page background).
5. Toggle dark mode via the ThemeToggle button; `preview_screenshot` dark.
6. In dark mode, explicitly verify: raised cards (e.g., KPI cards or a shadcn Card) are LIGHTER than the page background (surface-raised > surface-base in dark). Sidebar right-edge still separates from main content. Popover/Dropdown (open any) uses surface-overlay.
7. Check the table (batch performance view) — borders are still visible but subtle in both modes (hairlines didn't disappear after `--border` re-map to color-mix alpha).
8. Drill into a partner — confirm KPI cards, header, table all render without layout shifts, missing shadows, invisible borders, or inverted text contrast.
9. Open /api/... routes shouldn't be affected but confirm no server errors: `preview_console_logs` should be clean.

Approval requires: screenshots attached of light + dark (root + drilled-in state), and explicit confirmation that dark-mode raised cards are lighter than base and that no shadcn component visually regressed.
  </how-to-verify>
  <resume-signal>Type "approved" with screenshots, or describe specific visual issues (e.g., "sidebar too dark in light mode", "card shadow invisible in dark", "focus ring wrong color").</resume-signal>
</task>

</tasks>

<verification>
- Build passes (`npm run build`) — all `@theme` tokens compile; no syntax errors.
- `grep -rn "Geist\|font-geist" src/` returns zero live references.
- `grep -c "^\s*--" src/app/globals.css` shows a significant increase (adds ~60+ new token lines across the sections).
- Light + dark mode both render; dark-mode surface-raised is LIGHTER than surface-base (verified via preview_inspect computed styles on a raised card vs body background).
- All existing shadcn utilities (bg-background, bg-card, border-border, bg-popover, text-muted-foreground, bg-sidebar, bg-sidebar-accent, ring-ring) still produce valid colors — no "invalid property value" warnings in console.
- Pilot components (KPI card, header, table row) not yet modified — Wave 2 does that.
</verification>

<success_criteria>
- DS-01 foundation: `--spacing-1..12` + semantic aliases (`--spacing-inline`, `--spacing-stack`, `--spacing-section`, `--spacing-card-padding`, `--spacing-page-gutter`) present.
- DS-02 foundation: `--text-display/heading/title/label/body/caption` + per-token `--line-height`, `--letter-spacing`, `--font-weight` present.
- DS-03 foundation: `--shadow-xs/sm/md/lg` multi-layer, with `.dark`-specific tint overrides.
- DS-04 foundation: `--duration-quick/normal/slow` + `--ease-default/spring/decelerate` present.
- DS-05 foundation: `--surface-base/raised/inset/overlay/floating` defined; shadcn re-map in place; `.dark` inverts (raised > base).
- DS-06: Light AND dark mode both visually verified via preview_screenshot + preview_inspect.
- Fonts Inter + JetBrains Mono active.
- No shadcn visual regression in existing UI.
</success_criteria>

<output>
After completion, create `.planning/phases/26-design-tokens/26-01-SUMMARY.md`.
</output>
