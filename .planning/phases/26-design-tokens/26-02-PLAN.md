---
phase: 26-design-tokens
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/components/kpi/kpi-card.tsx
autonomous: false
requirements: [DS-01, DS-02, DS-03, DS-05, DS-06]

must_haves:
  truths:
    - "KPI card renders on surface-raised (clearly distinguishable from page background in both light and dark mode)"
    - "KPI card uses spacing tokens — no hardcoded p-4 / gap-* with arbitrary values"
    - "KPI value renders in text-body-numeric (tabular-nums + lining-nums) so digits align vertically"
    - "KPI label uses text-label (12px uppercase tracked) style"
    - "KPI card has shadow-xs or shadow-sm + radius-lg"
    - "In dark mode, KPI card is LIGHTER than page background (surface-raised > surface-base)"
  artifacts:
    - path: "src/components/kpi/kpi-card.tsx"
      provides: "Pilot token-migrated KPI card component"
      min_lines: 30
  key_links:
    - from: "src/components/kpi/kpi-card.tsx className"
      to: "token utilities (bg-surface-raised, p-card-padding, shadow-sm, rounded-lg, text-label, text-body-numeric, text-display)"
      via: "Tailwind v4 utility classes"
      pattern: "bg-surface-raised|p-card-padding|text-display|text-label"
---

<objective>
Migrate `src/components/kpi/kpi-card.tsx` to consume Phase 26 design tokens: surface-raised background, semantic card-padding, shadow, radius, type scale for label/value, and tabular numeric variant. This pilot proves the token system works end-to-end in a real component used in the app's most visible area (the 6-card grid above the performance table).

Purpose: Validate tokens → utilities → component pipeline before broader migration in Phases 27-31. Prove dark-mode raised-lighter inversion is correct on a concrete component.
Output: KPI card using only Phase 26 tokens for surface, spacing, shadow, radius, and type. No hardcoded colors or spacing values.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-design-tokens/26-CONTEXT.md
@.planning/phases/26-design-tokens/26-RESEARCH.md
@.planning/phases/26-design-tokens/26-01-SUMMARY.md
@src/components/kpi/kpi-card.tsx

<interfaces>
<!-- Current KPI card styling (from RESEARCH.md inspection): -->
<!-- Uses: `rounded-xl border border-border/50 bg-transparent p-4` + text sizing via hardcoded classes. -->
<!-- Exports: KpiCard component (props: label, value, delta?, className?, icon?, etc. — read actual props from the source during execution). -->

Token utilities available (from Plan 26-01):
- Surface: `bg-surface-base`, `bg-surface-raised`, `bg-surface-inset`, `bg-surface-overlay`, `bg-surface-floating`
- Spacing: `p-1` through `p-12` (numeric), `p-card-padding` (semantic alias = 16px), `gap-stack` (12px), `gap-inline` (8px)
- Type: `text-display`, `text-heading`, `text-title`, `text-body`, `text-label`, `text-caption` (line-height / letter-spacing / weight baked in via `--text-*--*` suffixes)
- Numeric variant: `text-body-numeric` and `text-label-numeric` utilities with tabular-nums + lining-nums + mono family baked in. These are defined in globals.css @layer utilities per Plan 26-01 / RESEARCH Pattern 2 — verify they exist after 26-01 execution; if not, this plan adds them (see Task 1).
- Shadow: `shadow-xs`, `shadow-sm`, `shadow-md`, `shadow-lg`
- Radius: `rounded-sm` (4px), `rounded-md` (8px), `rounded-lg` (12px)
- Motion: `transition-colors duration-quick ease-default`, etc.
- Accent: `ring-accent-warm` or custom CSS `var(--accent-warm)`
</interfaces>

<project_constraints>
- Silence on data — do NOT tween KPI values. Card container may animate on hover (e.g. subtle shadow bump + scale via duration-quick ease-spring), but numeric value is always instant.
- "Colored focus rings using warm accent" — any focusable element inside the KPI card uses `--focus-ring` / `ring-accent-warm` on focus.
- shadcn Card primitive exists (src/components/ui/card.tsx) — the KPI card may either wrap shadcn Card or render its own div. Preserve whichever pattern exists; only change classNames, not structure.
- Verify text-body-numeric / text-label-numeric utilities exist in globals.css @layer utilities. Plan 26-01 was scoped to token definitions; if these utility classes were not added there, add them in Task 1 of this plan (they live in @layer utilities since Tailwind v4's --text-* namespace doesn't set font-variant-numeric).
</project_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure text-body-numeric / text-label-numeric utility classes exist; migrate KPI card</name>
  <files>
    src/app/globals.css
    src/components/kpi/kpi-card.tsx
  </files>
  <action>
**Step A — Verify / add numeric utility classes.**

Check `src/app/globals.css` for an `@layer utilities` block containing `.text-body-numeric` and `.text-label-numeric`. If absent, append after the existing @theme / :root / .dark blocks:

```css
@layer utilities {
  .text-body-numeric {
    font-size: var(--text-body);
    line-height: var(--text-body--line-height);
    font-variant-numeric: tabular-nums lining-nums;
    font-family: var(--font-mono), ui-monospace, monospace;
  }
  .text-label-numeric {
    font-size: var(--text-label);
    line-height: var(--text-label--line-height);
    letter-spacing: var(--text-label--letter-spacing);
    font-variant-numeric: tabular-nums lining-nums;
  }
  .text-display-numeric {
    font-size: var(--text-display);
    line-height: var(--text-display--line-height);
    font-weight: var(--text-display--font-weight);
    font-variant-numeric: tabular-nums lining-nums;
  }
}
```

`text-display-numeric` is added because KPI values are display-sized, not body-sized. All three utilities behave like `text-*` tokens but bake numeric features.

**Step B — Migrate kpi-card.tsx.**

Open `src/components/kpi/kpi-card.tsx` and read its full source first. Map each hardcoded class to a token utility. The current surface+shell classes are (per RESEARCH): `rounded-xl border border-border/50 bg-transparent p-4`. Replace with:

- `rounded-lg` (token; replaces `rounded-xl` — CONTEXT locked radius ladder at 3 steps, 12px is --radius-lg)
- `bg-surface-raised` (replaces `bg-transparent` — KPI cards are raised containers per CONTEXT surface map)
- `border border-border/50` → keep shadcn `border border-border` (shadcn var now resolves to color-mix with neutral alpha from Plan 26-01 re-map — the /50 modifier is likely unnecessary now; drop it or keep as-is if visual feedback in Task 2 shows border is too heavy)
- `p-4` → `p-card-padding` (semantic token alias to 16px)
- Add `shadow-sm` (subtle multi-layer shadow consistent with "raised but quiet" per CONTEXT)
- Add `transition-colors duration-quick ease-default` for any hover state that already exists

For the label text (the uppercase section-header-like text at top of card):
- Replace whatever `text-xs / font-medium / uppercase / tracking-*` combo exists with `text-label uppercase` (the `--text-label--letter-spacing: 0.04em` + `--text-label--font-weight: 500` are baked in; apply `uppercase` Tailwind utility manually since the `@theme` text tokens don't set text-transform).
- Use `text-muted-foreground` (shadcn var, re-mapped to `--neutral-600`).

For the KPI value (the big number):
- Replace the current size class (likely `text-2xl font-semibold`) with `text-display-numeric` (baked-in tabular-nums; mono feel avoided since display-numeric keeps the sans font. Actually — display-numeric uses var(--font-mono) per step A. Reconsider: the heading/display tier is typically sans; the NUMERIC part of the display needs tabular-nums but the font family stays sans. Adjust the utility definition if appropriate — for display-numeric, drop the `font-family` line so it inherits sans. ONLY text-body-numeric and text-label-numeric use mono. Update step A to remove `font-family` from `.text-display-numeric`.)

For any sub-label (e.g. "vs last batch") or delta indicator:
- Use `text-caption` + appropriate color (state-success-fg / state-error-fg / text-muted-foreground for neutral).

For the delta percentage value itself (a small number):
- Use `text-label-numeric` (mono, tabular-nums, tracked).

Do NOT:
- Change component structure or props.
- Change the JSX tree beyond className / minor wrapper changes to achieve proper semantic grouping.
- Add framer-motion or any motion lib.
- Animate the numeric value.

After editing, re-check the file for any remaining hardcoded spacing/color not covered by tokens (e.g., an inline `style={{ padding: 12 }}`). Replace with token utilities.
  </action>
  <verify>
    <automated>npm run build</automated>
    Follow up: `grep -n "p-4\|rounded-xl\|bg-transparent\|tabular-nums\"" src/components/kpi/kpi-card.tsx` should return zero hits (p-4 → p-card-padding, rounded-xl → rounded-lg, bg-transparent → bg-surface-raised, standalone tabular-nums → folded into text-*-numeric).
  </verify>
  <done>
    Build succeeds. KPI card uses bg-surface-raised, p-card-padding, rounded-lg, shadow-sm, text-label (for top label), text-display-numeric (for value), text-label-numeric or text-caption (for delta). `@layer utilities` in globals.css contains text-body-numeric, text-label-numeric, text-display-numeric classes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — KPI card surface, type, shadow correct in light + dark</name>
  <files>src/components/kpi/kpi-card.tsx (no edits — verification only)</files>
  <action>Pause for human verification of Task 1 output. Instructions below.</action>
  <verify>Human confirms by typing "approved" with attached screenshots.</verify>
  <done>User types "approved" with screenshots of light + dark KPI grid; dark-mode card confirmed lighter than body background; computed styles confirm tokens resolve.</done>
  <what-built>
KPI card migrated to Phase 26 tokens. surface-raised background, semantic spacing, shadow, radius, type scale, tabular numerics. Needs visual confirmation that (a) raised cards read as raised above the page in both modes, (b) dark-mode raised IS lighter than base (not darker), (c) tabular numerics align, (d) label uses the new 12px uppercase tracked style, (e) shadow is visible but quiet (not harsh).
  </what-built>
  <how-to-verify>
1. `mcp__Claude_Preview__preview_start` (dev server).
2. Navigate in-app to a partner drill-down where the 6 KPI cards render above the performance table.
3. Light mode: `preview_screenshot`. Cards should visibly float above the cream page background — subtle but perceptible. Card corners are ~12px (--radius-lg). Values are large, digits align when stacked vertically (take two screenshots of different KPI values and eyeball digit alignment — or inspect a column of KPI values with `preview_inspect` to confirm `font-variant-numeric: tabular-nums lining-nums` is applied).
4. Inspect one KPI card with `preview_inspect` with selector matching the card root. Confirm:
   - computed `background-color` matches `--surface-raised` OKLCH (lighter than body background).
   - computed `padding` is 16px (--spacing-card-padding).
   - computed `border-radius` is 12px.
   - computed `box-shadow` shows two layers (the multi-layer shadow from --shadow-sm).
5. Inspect the KPI value element. Confirm `font-variant-numeric` includes `tabular-nums lining-nums`.
6. Inspect the KPI label element. Confirm `text-transform: uppercase`, `letter-spacing: 0.04em`, `font-size: 0.75rem`, `font-weight: 500`.
7. Toggle to dark mode. `preview_screenshot`. Verify: KPI card is LIGHTER than the page background (critical — this is the inversion from shadcn default). Shadow still visible but darker-tinted. Label and value legible with proper contrast.
8. Optionally hover over a card — if it has a hover state, the transition should be quick (120ms) and gentle.

Approval requires: screenshots of light + dark mode KPI grid attached, explicit confirmation that dark-mode card is lighter than page bg.
  </how-to-verify>
  <resume-signal>Type "approved" with screenshots, or describe issues (e.g. "dark card still darker than page", "shadow too harsh", "label not uppercase", "digits don't align").</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes.
- No `p-4`, `rounded-xl`, `text-2xl` (or any arbitrary size), or bare `tabular-nums` remains in kpi-card.tsx.
- Preview screenshots attached for both modes.
- Computed styles confirm tokens resolve (checked via preview_inspect in Task 2).
</verification>

<success_criteria>
- DS-01 proven: KPI card consumes `--spacing-card-padding` and other spacing tokens — no ad-hoc spacing.
- DS-02 proven: KPI card uses `text-display-numeric` (value), `text-label` (label), `text-caption` / `text-label-numeric` (delta). tabular-nums baked in for numeric contexts.
- DS-03 proven: KPI card uses `shadow-sm` multi-layer shadow, visible in both modes with appropriate tint.
- DS-05 proven: KPI card consumes `bg-surface-raised`; dark mode renders raised LIGHTER than base.
- DS-06 proven: both modes visually verified.
</success_criteria>

<output>
After completion, create `.planning/phases/26-design-tokens/26-02-SUMMARY.md`.
</output>
