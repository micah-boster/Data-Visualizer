---
phase: 26-design-tokens
plan: 04
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/components/table/table-body.tsx
  - src/app/globals.css
autonomous: false
requirements: [DS-01, DS-02, DS-05, DS-06]

must_haves:
  truths:
    - "Table area renders on surface-inset background (recessed below page base in light mode; even more recessed in dark)"
    - "Table row height is driven by --table-row-height-dense token (32px default)"
    - "Sparse variant exists: applying data-density='sparse' on the table container switches row height to --table-row-height-sparse (40px)"
    - "Table cells use text-body for text columns and text-body-numeric for numeric columns"
    - "Row padding uses spacing tokens (no hardcoded px-3 py-2)"
    - "Light and dark mode both render correctly"
  artifacts:
    - path: "src/components/table/table-body.tsx"
      provides: "Pilot token-migrated table body with density variant support"
      min_lines: 30
    - path: "src/app/globals.css"
      provides: "Density selector block ([data-density=...]) mapping density to --row-height / --row-padding-y"
      contains: "data-density"
  key_links:
    - from: "src/components/table/table-body.tsx"
      to: "CSS variables --row-height / --row-padding-y (density-controlled)"
      via: "inline style or className consuming var()"
      pattern: "var\\(--row-height\\)|var\\(--row-padding-y\\)"
    - from: "src/app/globals.css [data-density='dense'|'sparse']"
      to: "--table-row-height-dense / --table-row-height-sparse tokens"
      via: "--row-height / --row-padding-y variable assignment inside [data-density=...] blocks"
      pattern: "\\[data-density="
---

<objective>
Migrate `src/components/table/table-body.tsx` to Phase 26 tokens: surface-inset background for the table area, density-driven row height + padding via `data-density` attribute + token pair, and type scale for cells (text-body / text-body-numeric). Ship dense as the default; add the sparse variant plumbing (no UI toggle — deferred per CONTEXT).

Purpose: Validate the density-variant pattern (Pattern 3 from RESEARCH) end-to-end in the component where it matters most. Prove surface-inset recedes correctly in both modes. Lock in how numeric columns get tabular figures.
Output: Table body consuming density tokens, surface-inset background, and type-scale utilities. globals.css extended with `[data-density=...]` selectors that assign `--row-height` / `--row-padding-y`.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-design-tokens/26-CONTEXT.md
@.planning/phases/26-design-tokens/26-RESEARCH.md
@.planning/phases/26-design-tokens/26-01-SUMMARY.md
@src/components/table/table-body.tsx

<interfaces>
<!-- Current table-body row styling (from RESEARCH.md inspection): -->
<!-- Row: `transition-colors hover:bg-muted/50`, cell: `px-3 py-2 text-sm`, estimated row height 42px via TanStack Virtual `estimateSize: 42`. -->

Token utilities available (from Plan 26-01):
- Surfaces: `bg-surface-inset` (recessed — table area per CONTEXT surface map)
- Spacing: `px-*`, `py-*` numeric scale, or consume `var(--row-padding-y)` inline
- Type: `text-body` (14px), `text-body-numeric` (14px + tabular-nums + mono)
- Density tokens (defined in @theme by Plan 26-01): `--table-row-height-dense` 32px, `--table-row-height-sparse` 40px
- Hover: `bg-hover-bg` (or `hover:bg-hover-bg` via interaction token from Plan 26-01)
- Transition: `transition-colors duration-quick ease-default`
</interfaces>

<project_constraints>
- Dense is app default; sparse is opt-in via `data-density="sparse"` on the table container. No UI toggle in Phase 26 — deferred to Phase 29 Component Patterns or a dedicated phase.
- Density selector scoped to the table container, NOT `<body>` or `<html>` — per RESEARCH Open Question #3 recommendation. Root-level density toggle can be added later without breaking the table pattern.
- TanStack Virtual's `estimateSize` is a JS number, not a CSS var. The row-height token is consumed via CSS on `<tr>` / `<td>` styling; `estimateSize: 42` may need to become `estimateSize: 32` to match dense default, OR be wired to read the CSS var. Simpler: update the `estimateSize` hardcoded number to 32 (dense default); document that sparse mode will need separate handling in a future phase. This plan locks the visual row height; TanStack estimate stays in sync for dense-only.
- "Silence on data" — no transitions on numeric values. Row hover transition is on background-color only (already correct).
- Numeric vs text columns: Table is a heatmap-like visualization with many numeric cells. Apply `text-body-numeric` to numeric cells; `text-body` to textual cells (e.g., partner name column). Determine numeric vs text from the column metadata — read accessorFn / cell render logic. If unclear, default to `text-body-numeric` only for cells where the data type is number.
</project_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-density selector block to globals.css; migrate table-body.tsx</name>
  <files>
    src/app/globals.css
    src/components/table/table-body.tsx
  </files>
  <action>
**Step A — globals.css density selector block.**

Append to `src/app/globals.css` (inside `@layer base` if one exists, else create one after the existing @theme / :root / .dark blocks):

```css
@layer base {
  [data-density="dense"] {
    --row-height: var(--table-row-height-dense);
    --row-padding-y: var(--spacing-2); /* 8px */
    --row-padding-x: var(--spacing-3); /* 12px */
  }
  [data-density="sparse"] {
    --row-height: var(--table-row-height-sparse);
    --row-padding-y: var(--spacing-3); /* 12px */
    --row-padding-x: var(--spacing-4); /* 16px */
  }
}
```

This means any element with `data-density="dense"` (or "sparse") propagates `--row-height`, `--row-padding-y`, `--row-padding-x` to its descendants. The table container gets `data-density="dense"` by default in Task B.

**Step B — Migrate table-body.tsx.**

Read `src/components/table/table-body.tsx` fully first.

1. **Wrapper / outer container** (if the component renders a `<tbody>` or a virtualized div container):
   - Add `data-density="dense"` attribute. Do NOT wrap in a new container purely for this — add to whatever the current root of the table-body is, OR to the parent `<table>` wrapper if that's higher up. Density cascades to descendants, so the attribute location is flexible.
   - Add `bg-surface-inset` on the wrapper or the parent element that currently has the table's background (may be a parent `<div>` in a sibling file — if so, leave a comment in table-body.tsx noting the upstream change is needed; otherwise apply here).

2. **Row styling** (`<tr>` or equivalent virtual row element):
   - Current: `transition-colors hover:bg-muted/50`. Replace with `transition-colors duration-quick ease-default hover:bg-hover-bg` (if `bg-hover-bg` isn't generated as a utility — depending on whether Plan 26-01 exposed `--color-hover-bg` in `@theme inline`, which it does — use the Tailwind utility; otherwise use `hover:[background-color:var(--hover-bg)]` arbitrary-value syntax).
   - Row height: add `style={{ height: 'var(--row-height)' }}` OR set `className` to include `h-[var(--row-height)]` (arbitrary Tailwind variant) — either works. Prefer the arbitrary Tailwind class.

3. **Cell styling** (`<td>` or virtualized cell):
   - Current: `px-3 py-2 text-sm`. Replace:
     - `px-3` → `px-[var(--row-padding-x)]` (or if `px-3` still matches dense default exactly, keep `px-3` with a comment; the token approach is preferred for sparse support).
     - `py-2` → `py-[var(--row-padding-y)]`.
     - `text-sm` → For NUMERIC cells (detected via column meta / cell data type): `text-body-numeric`. For TEXT cells: `text-body`. Split the class assignment based on the column — if the component uses `cell.column.columnDef.meta.type` or similar, switch on it; otherwise use a heuristic (e.g., `typeof cellValue === 'number'`).
   - If the text-body-numeric utility class name isn't available (check globals.css after Plan 26-02 may have added it), either add it here or use `text-body tabular-nums font-mono` as a fallback — but prefer the canonical utility.

4. **Virtualization estimateSize**:
   - Search for `estimateSize` in the file. If it returns `42` or similar, update to `32` (dense default, matching `--table-row-height-dense`). Leave a code comment: `// Dense default — sparse (40px) would need dynamic estimate via data-density prop`.

5. **Remove any `dark:` utility on row/cell backgrounds** (e.g., `dark:hover:bg-muted/30`) — the token-based `hover:bg-hover-bg` handles both modes via the re-mapped `--hover-bg` var from Plan 26-01.

6. Preserve all functional code unchanged: row click handlers, virtualization logic, conditional formatting (heatmap cell tints consuming `--cell-zero` / `--cell-tint-low` / `--cell-tint-high`) which after Plan 26-01 already reference the diverging chart palette via the re-map — these cells' color logic remains the same.

Do not rename props, change virtualization library, add motion libs, or tween anything data-related.
  </action>
  <verify>
    <automated>npm run build</automated>
    Follow-up greps on src/components/table/table-body.tsx:
    - `grep -n "px-3\|py-2\|text-sm\|bg-muted/50" src/components/table/table-body.tsx` — should return zero (or with explicit inline comments justifying any residual).
    - `grep -n "data-density" src/components/table/table-body.tsx` — should return at least one match.
    - `grep -n "data-density" src/app/globals.css` — should return matches in the new [data-density=...] blocks.
  </verify>
  <done>
    Build passes. Table container has `data-density="dense"`. Row consumes `h-[var(--row-height)]` + `py-[var(--row-padding-y)]` + `px-[var(--row-padding-x)]`. Numeric cells use `text-body-numeric`; text cells use `text-body`. Hover uses tokenized `--hover-bg`. globals.css has `[data-density=...]` selectors that assign --row-height + padding vars. TanStack virtual estimateSize updated to 32 (dense default).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — table rows dense in both modes; sparse variant functional when toggled manually</name>
  <files>src/components/table/table-body.tsx, src/app/globals.css (no edits — verification only)</files>
  <action>Pause for human verification of Task 1 output. Instructions below.</action>
  <verify>Human confirms by typing "approved" with attached screenshots.</verify>
  <done>User types "approved" with screenshots; row height 32px dense / 40px sparse confirmed via inspect; dark-mode table confirmed darker than surface-base.</done>
  <what-built>
Table body migrated to surface-inset + density-token row height + padding + type-tokened cells. Dense is default. Sparse works if `data-density="sparse"` is manually set. Needs visual confirmation that: (a) table area reads as recessed below page in both modes, (b) rows are 32px tall dense, (c) numeric cells have tabular-nums (digits align), (d) hover bg-change is quick and subtle, (e) manually flipping to sparse makes rows 40px.
  </what-built>
  <how-to-verify>
1. `mcp__Claude_Preview__preview_start` → dev server.
2. Navigate to default view (batch performance table). `preview_screenshot` light mode.
3. Table area should read as slightly RECESSED compared to the header/KPI cards above. `preview_inspect` the table container — confirm `background-color` matches `--surface-inset` (light: slightly darker beige than surface-base).
4. `preview_inspect` a table row. Confirm computed `height` is 32px.
5. Inspect a numeric cell. Confirm `font-variant-numeric: tabular-nums lining-nums`. Eyeball a column of numbers — digits should align.
6. Hover over rows — hover color change is subtle (neutral tint, not muted/50) and transition is 120ms.
7. Toggle dark mode. `preview_screenshot`. Table area should STILL be the most recessed surface (darker than page background in dark; light-to-dark scale: inset < base < raised). Rows legible; numeric alignment preserved.
8. **Sparse variant check**: in dev tools, manually set `data-density="sparse"` on the table container (use `preview_eval` to run: `document.querySelector('[data-density]').setAttribute('data-density', 'sparse')`). Confirm rows expand to 40px row height and cell padding increases proportionally. Revert to dense.
9. `preview_console_logs` — no errors. Virtualization still works (scroll the table — rows render correctly at new height).

Approval requires: screenshots (light + dark with table visible), computed inspect of row height = 32px dense / 40px sparse, confirmation that dark-mode table is darker than surface-base.
  </how-to-verify>
  <resume-signal>Type "approved" with screenshots, or describe issues ("rows too tall", "digits don't align", "sparse mode breaks virtualization", "table bg not recessed in dark").</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes.
- No `px-3`, `py-2`, `text-sm`, `bg-muted/50` remaining in table-body.tsx.
- `[data-density="dense"]` and `[data-density="sparse"]` selectors present in globals.css.
- Preview screenshots attached for both modes.
- Sparse variant functional when manually flipped (preview_eval / devtools).
</verification>

<success_criteria>
- DS-01 proven: Row padding + container spacing driven by tokens (--row-padding-y, --row-padding-x).
- DS-02 proven: Numeric cells use text-body-numeric; text cells use text-body; digits align vertically via tabular-nums.
- DS-05 proven: Table area uses bg-surface-inset; recedes below surface-base in light mode and darker still in dark.
- DS-06 proven: Both modes visually verified; sparse variant functional.
</success_criteria>

<output>
After completion, create `.planning/phases/26-design-tokens/26-04-SUMMARY.md`.
</output>
