---
phase: 28-surfaces-and-elevation
plan: 05
type: execute
wave: 3
depends_on: ["28-01"]
files_modified:
  - src/components/charts/collection-curve-chart.tsx
  - src/components/charts/root-sparkline.tsx
  - src/components/charts/partner-sparkline.tsx
  - src/components/cross-partner/trajectory-chart.tsx
  - src/components/cross-partner/comparison-matrix.tsx
  - src/components/query/query-response.tsx
  - src/components/query/query-search-bar.tsx
  - src/components/anomaly/anomaly-summary-panel.tsx
autonomous: false
requirements: [DS-14]

must_haves:
  truths:
    - "Each chart in the app sits inside a raised shell with consistent padding and corner radius"
    - "Query response + search-bar cards match the same raised recipe"
    - "Anomaly summary panel container uses semantic-elevation + (opportunistically) warning-bg state tokens"
  artifacts:
    - path: "src/components/charts/collection-curve-chart.tsx"
      provides: "Raised shell wrapping chart content"
      contains: "shadow-elevation-raised"
    - path: "src/components/cross-partner/trajectory-chart.tsx"
      provides: "Raised shell wrapping chart content"
      contains: "shadow-elevation-raised"
    - path: "src/components/cross-partner/comparison-matrix.tsx"
      provides: "Raised shell wrapping matrix tabs"
      contains: "shadow-elevation-raised"
    - path: "src/components/charts/root-sparkline.tsx"
      provides: "Raised shell (pilot-driven: may stay bare if collapsed-state reads better)"
    - path: "src/components/charts/partner-sparkline.tsx"
      provides: "Raised shell (pilot-driven same as root)"
    - path: "src/components/query/query-response.tsx"
      provides: "Response cards (default + error + pre/code) on shadow-elevation-raised"
      contains: "shadow-elevation-raised"
    - path: "src/components/query/query-search-bar.tsx"
      provides: "Search-bar card on shadow-elevation-raised"
      contains: "shadow-elevation-raised"
    - path: "src/components/anomaly/anomaly-summary-panel.tsx"
      provides: "Panel container on shadow-elevation-raised (state-color migrated opportunistically)"
  key_links:
    - from: "chart components"
      to: "shadow-elevation-raised + bg-surface-raised + rounded-lg + p-card-padding"
      via: "outer wrapping div"
      pattern: "bg-surface-raised.*shadow-elevation-raised"
    - from: "src/components/query/*.tsx"
      to: "same recipe as chart shells"
      via: "className swap on existing card wrappers"
      pattern: "shadow-elevation-raised"
---

<objective>
Wrap every chart in the app with a semantic raised shell (bg-surface-raised + shadow-elevation-raised + rounded-lg + p-card-padding) and migrate the query response + search-bar + anomaly-summary panel cards to the same recipe. Closes DS-14 across the chart family and the adjacent card-tier surfaces.

Purpose: 28-RESEARCH inventory shows charts currently render inside a bare `<div className="shrink-0 px-2 pt-2 space-y-2">` wrapper in data-display.tsx:497 — individual charts have NO raised container. Query response (`rounded-lg border bg-card p-4`) and search-bar (`rounded-lg border bg-card p-3 shadow-sm`) are already card-shaped but on ad-hoc `bg-card` + raw shadow; they need to align to the semantic recipe. Anomaly-summary-panel uses a hand-rolled amber palette — fold into the sweep opportunistically (per 27-03 Pitfall 4 pattern) since this plan touches its surface.

Output: Every visible chart and every card-tier non-KPI surface uses the same recipe. No ad-hoc `bg-card` or raw `shadow-sm` on primary containers.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-surfaces-and-elevation/28-CONTEXT.md
@.planning/phases/28-surfaces-and-elevation/28-RESEARCH.md
@.planning/phases/28-surfaces-and-elevation/28-01-PLAN.md

@src/components/charts/collection-curve-chart.tsx
@src/components/charts/root-sparkline.tsx
@src/components/charts/partner-sparkline.tsx
@src/components/cross-partner/trajectory-chart.tsx
@src/components/cross-partner/comparison-matrix.tsx
@src/components/query/query-response.tsx
@src/components/query/query-search-bar.tsx
@src/components/anomaly/anomaly-summary-panel.tsx

<interfaces>
Canonical chart shell recipe (28-RESEARCH Example 5):
  <div className="rounded-lg bg-surface-raised shadow-elevation-raised p-card-padding">
    {/* existing chart content untouched */}
  </div>

Query card recipe (28-RESEARCH:282-285):
  Current: rounded-lg border bg-card p-4  (and rounded-lg border bg-card p-4 max-h-[200px] overflow-y-auto for pre/code block)
  Target:  rounded-lg bg-surface-raised shadow-elevation-raised p-card-padding

Query search-bar (28-RESEARCH:286):
  Current: rounded-lg border bg-card p-3 shadow-sm
  Target:  rounded-lg bg-surface-raised shadow-elevation-raised p-card-padding

Query error variant (28-RESEARCH:283):
  Current: rounded-lg border border-destructive/20 bg-card p-4
  Target:  rounded-lg bg-surface-raised shadow-elevation-raised p-card-padding border border-destructive/20
  (keep destructive border accent — it's a semantic error cue, not a surface border)

Anomaly-summary-panel (28-RESEARCH:281):
  Current: rounded-lg border border-amber-200 bg-amber-50 dark:border-amber-800 dark:bg-amber-950/30
  Target options:
    (a) rounded-lg bg-warning-bg border border-warning-border shadow-elevation-raised
    (b) Keep amber but wrap in shadow-elevation-raised
  Recommend (a) IF `--color-warning-bg` / `--color-warning-border` tokens exist in globals.css
  (check first). If they don't, use (b) and flag warning-bg token creation for a future phase.

Sparklines (root + partner) — pilot-driven per 28-RESEARCH. The collapsed-state sparklines sit
inline in sidebar list items and a raised shell might over-frame them. Implementer decides:
wrap or don't wrap. If not wrapped, document in SUMMARY Deviations.

Pitfall 1 applies: ensure the raised shell is NOT a descendant of an overflow-hidden ancestor
that would clip its shadow. If a parent has overflow-hidden (check data-display.tsx), leave
the shell in place and add a visual observation note — the inset pane of the table will not
contain charts so this is unlikely to bite, but verify.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap 5 chart components in the raised shell recipe</name>
  <files>src/components/charts/collection-curve-chart.tsx, src/components/charts/root-sparkline.tsx, src/components/charts/partner-sparkline.tsx, src/components/cross-partner/trajectory-chart.tsx, src/components/cross-partner/comparison-matrix.tsx</files>
  <action>
Read each file first to understand its existing outer JSX structure.

For collection-curve-chart.tsx, trajectory-chart.tsx, comparison-matrix.tsx: wrap the top-level chart JSX with a new outer div:
  <div className="rounded-lg bg-surface-raised shadow-elevation-raised p-card-padding">
    {/* existing root JSX, untouched */}
  </div>

If the component already has a top-level div with layout classes (e.g., flex, h-[40vh], w-full), layer the shell OUTSIDE that. Do not merge classes into the existing layout div — shell responsibilities (surface, shadow, padding, radius) are separate from chart sizing.

For root-sparkline.tsx and partner-sparkline.tsx: these render inline in sidebar list items (collapsed state per research). Pilot decision:
  - If wrapping with the full raised shell makes the inline sparkline feel over-framed relative to its sidebar-row context, leave unwrapped and note as Deviation.
  - If the app ALSO renders a standalone (non-collapsed) version of the sparkline in a card-tier location, wrap only that variant.
  - Default: wrap both, visual-check, and demote to unwrapped in SUMMARY if reading wrong.

If comparison-matrix.tsx has per-tab content that already has raised-feeling inner panels, wrap the outer matrix (the tabbed container), NOT each tab's content — single raised layer per surface per DS-17.

Do not modify any chart internals — Recharts config, ChartContainer from ui/chart, tooltip components, legends, data bindings. The shell is a presentation-only outer wrapper.

Pitfall 1 check: if any component's parent in data-display.tsx has overflow-hidden that would clip the new shell's shadow, note it — do not yet restructure data-display.tsx (that's out of scope; 28-08 cleanup may address).
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && for f in src/components/charts/collection-curve-chart.tsx src/components/cross-partner/trajectory-chart.tsx src/components/cross-partner/comparison-matrix.tsx; do grep -q 'shadow-elevation-raised' "$f" || { echo "MISSING: $f"; exit 1; }; done && echo OK</automated>
Sparklines: grep check skipped because pilot-driven (decision documented in SUMMARY instead). Then `npm run build` passes.
  </verify>
  <done>
- 3 of 5 chart components confirmed wrapped (collection-curve, trajectory, matrix)
- 2 sparklines' decision documented (wrapped or deferred) in SUMMARY
- No chart internals modified
- Build green
  </done>
</task>

<task type="auto">
  <name>Task 2: Align query-response + query-search-bar cards to the semantic recipe</name>
  <files>src/components/query/query-response.tsx, src/components/query/query-search-bar.tsx</files>
  <action>
Read both files.

In query-response.tsx, locate the three card-tier className strings (research flags lines 44, 62, 94 — line numbers may drift, locate by `rounded-lg border bg-card`):
- Default response card: `rounded-lg border bg-card p-4` -> `rounded-lg bg-surface-raised shadow-elevation-raised p-card-padding`
- Error variant: `rounded-lg border border-destructive/20 bg-card p-4` -> `rounded-lg bg-surface-raised shadow-elevation-raised p-card-padding border border-destructive/20` (keep destructive border — it's a semantic accent, not a surface border)
- Pre/code block: `rounded-lg border bg-card p-4 max-h-[200px] overflow-y-auto` -> `rounded-lg bg-surface-raised shadow-elevation-raised p-card-padding max-h-[200px] overflow-y-auto`

In query-search-bar.tsx, locate `rounded-lg border bg-card p-3 shadow-sm` and swap to `rounded-lg bg-surface-raised shadow-elevation-raised p-card-padding`. Note: p-3 -> p-card-padding changes the padding token (consistency over literal); if visual check shows the search bar is now too padded, swap to a tighter padding token or keep p-3 and document in Deviations.

Do not modify the response content rendering, markdown parser, copy-to-clipboard button, or search-bar input internals.

For the pre/code block: overflow-y-auto on the same element as the shadow creates Pitfall 1 (shadow clipping by the scroll wrapper). Accept this — the shadow clips at the code block's bounds, which is the correct visual (the block itself is a raised recessed thing). If it reads off, note for 28-08 cleanup.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && grep -c 'shadow-elevation-raised' src/components/query/query-response.tsx && grep -q 'shadow-elevation-raised' src/components/query/query-search-bar.tsx && echo OK</automated>
Expected: query-response.tsx count >= 3 (default + error + pre/code). Then `npm run build` passes.
  </verify>
  <done>
- 3 card-tier classNames in query-response.tsx migrated to semantic recipe
- query-search-bar.tsx card migrated
- Error variant retains destructive border accent
- Content / interactions untouched
- Build green
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate anomaly-summary-panel container to elevation-raised + (opportunistic) state tokens</name>
  <files>src/components/anomaly/anomaly-summary-panel.tsx</files>
  <action>
Read the file. Locate the container className currently using the hand-rolled amber palette per 28-RESEARCH:281:
  rounded-lg border border-amber-200 bg-amber-50 dark:border-amber-800 dark:bg-amber-950/30

Step 1: Check src/app/globals.css for state-color tokens. Grep for `--color-warning-bg`, `--color-warning-border`, or similar (`warning-`, `state-warning`). Also acceptable: `--warning-*` in :root/.dark.

Step 2: Based on findings:
  Option A (tokens exist): swap to `rounded-lg bg-warning-bg border border-warning-border shadow-elevation-raised` (or the equivalent named utility that Tailwind emits for the tokens present). This is the canonical fix.
  Option B (tokens do NOT exist): keep the amber palette AS-IS for now, but ADD `shadow-elevation-raised` to the className. Flag "warning state-color token migration" in SUMMARY as a future-phase Deviation — do NOT invent new tokens in this plan (tokens are 28-01's responsibility and that plan didn't cover state colors).

Do not modify the panel's inner content (severity badges, flag counts, expandable list, etc.).

Precedent: Phase 27-03 Pitfall 4 established the "opportunistic state-color migration" pattern — when a surface sweep touches a file with a pending state-color todo, fold it in. That's exactly what Option A does.

If the component's amber palette spans beyond the root container (e.g., inner header band also uses bg-amber-*), scope this plan to the OUTER container only — inner state-color migration is tracked separately.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && grep -q 'shadow-elevation-raised' src/components/anomaly/anomaly-summary-panel.tsx && echo OK</automated>
Then `npm run build` passes.
  </verify>
  <done>
- Outer container of anomaly-summary-panel consumes shadow-elevation-raised
- Amber palette either migrated to warning-bg tokens (Option A) or retained with shadow added (Option B, flagged for follow-up)
- Inner content untouched
- Build green
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification — chart + query + anomaly surfaces</name>
  <action>Verify every chart, query card, and the anomaly summary panel share the raised recipe in both modes. Check for any Pitfall 1 shadow-clipping. See how-to-verify below.</action>
  <what-built>Every visible chart + query response/search cards + anomaly summary panel now use the same semantic raised shell recipe. No more mix of `bg-card` + raw `shadow-sm` on primary non-KPI containers.</what-built>
  <how-to-verify>
1. `npm run dev`. Navigate to the dashboard.
2. Confirm each chart (collection-curve, trajectory, comparison-matrix) sits inside a visible raised card with rounded corners, padding, and multi-layer shadow.
3. Toggle dark mode; confirm the same charts still read as raised (surface-color lift carries; shadow reinforces).
4. Run a Claude query (if the query panel is visible). Confirm the response default card, any error state if reachable, and the pre/code block all share the same raised recipe visually.
5. If an anomaly is detected on the current data, confirm anomaly-summary-panel renders with the raised shell. State color (amber vs. warning-bg) is pilot's call.
6. Sparklines: note whether the wrapped/unwrapped decision looks right for the sidebar collapsed state.
7. Look for any chart whose shadow is visibly clipped (Pitfall 1) — shadow cut off at an ancestor's edge. Note any occurrence.

If anything reads off, describe it.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues observed.</resume-signal>
</task>

</tasks>

<verification>
- grep 'shadow-elevation-raised' src/components/charts/collection-curve-chart.tsx src/components/cross-partner/trajectory-chart.tsx src/components/cross-partner/comparison-matrix.tsx src/components/query/query-response.tsx src/components/query/query-search-bar.tsx src/components/anomaly/anomaly-summary-panel.tsx returns >= 1 per file (except sparklines, pilot-driven)
- grep 'bg-card' src/components/query/query-response.tsx src/components/query/query-search-bar.tsx returns 0 (migrated away)
- npm run build passes
- User-verified in both modes
</verification>

<success_criteria>
- DS-14 satisfied: every chart container + query card + summary panel uses the raised shell recipe
- No ad-hoc `bg-card` or raw `shadow-sm`/`shadow-md` on primary containers in these files
- Chart internals (Recharts, legends, tooltips, axes) preserved
- Sparkline wrapping decision documented
- Anomaly-panel state-color migration decision documented
- No shadow-clipping regressions (Pitfall 1)
</success_criteria>

<output>
Create `.planning/phases/28-surfaces-and-elevation/28-05-SUMMARY.md` documenting:
- For each of the 5 chart components: wrapped or not, outer className chosen
- For sparklines: pilot verdict (wrapped vs. unwrapped), rationale
- For query-response: 3 className strings migrated (default, error, pre/code)
- For query-search-bar: final className, any padding-token adjustment
- For anomaly-summary-panel: Option A or Option B, and whether a warning state-color token migration is flagged for a future phase
- Any Pitfall 1 shadow-clipping instances observed and whether flagged for 28-08
- Both-mode visual verification notes
</output>
