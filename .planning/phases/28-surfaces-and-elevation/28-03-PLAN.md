---
phase: 28-surfaces-and-elevation
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/components/kpi/kpi-card.tsx
  - src/components/kpi/kpi-summary-cards.tsx
autonomous: false
requirements: [DS-12]

must_haves:
  truths:
    - "KPI cards render with shadow-elevation-raised in place of shadow-sm + border"
    - "KPI skeleton cells (loading state) and zero-batch empty state match the real card's surface recipe"
    - "KPI cards pop off the background with visible 3-layer shadow in both modes"
  artifacts:
    - path: "src/components/kpi/kpi-card.tsx"
      provides: "KpiCard consuming bg-surface-raised + shadow-elevation-raised (border dropped if rim carries)"
      contains: "shadow-elevation-raised"
    - path: "src/components/kpi/kpi-summary-cards.tsx"
      provides: "Skeleton + zero-batch empty cells matching real KpiCard surface"
      contains: "shadow-elevation-raised"
  key_links:
    - from: "src/components/kpi/kpi-card.tsx"
      to: "shadow-elevation-raised + bg-surface-raised"
      via: "cardClasses string composition"
      pattern: "shadow-elevation-raised"
    - from: "src/components/kpi/kpi-summary-cards.tsx"
      to: "same surface recipe as KpiCard"
      via: "skeleton + empty-state className"
      pattern: "shadow-elevation-raised"
---

<objective>
Retune the KPI card family to Phase 28's semantic elevation. Real card (kpi-card.tsx) swaps `border border-border` + `shadow-sm` for `shadow-elevation-raised`; skeleton and zero-batch empty cells in kpi-summary-cards.tsx (currently `rounded-xl border border-border/50 bg-transparent p-4` per 28-RESEARCH:279-280) align to match.

Purpose: 26-02 pilot shipped KpiCard on `shadow-sm`; Phase 28 semantic tier raises it a notch to the compound recipe with rim highlight for top-edge separation. Research flagged the skeleton/empty-state mismatch as a direct DS-12 gap — skeleton tiles look nothing like the real cards. Single plan closes the whole KPI surface story.

Output: Three KPI visual states (real card, loading skeleton, zero-batch empty) all using the same semantic recipe — rounded-lg + bg-surface-raised + shadow-elevation-raised + p-card-padding.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-surfaces-and-elevation/28-CONTEXT.md
@.planning/phases/28-surfaces-and-elevation/28-RESEARCH.md
@.planning/phases/28-surfaces-and-elevation/28-01-PLAN.md

@src/components/kpi/kpi-card.tsx
@src/components/kpi/kpi-summary-cards.tsx

<interfaces>
Tailwind utilities available after 28-01:
- shadow-elevation-raised  (3-layer key + ambient + rim highlight, per-mode)

Existing KpiCard state (28-RESEARCH:266, from 26-02 pilot):
  cardClasses =
    'rounded-lg border border-border bg-surface-raised p-card-padding shadow-sm transition-colors duration-quick ease-default';

Existing KpiSummaryCards gap (28-RESEARCH:279-280):
  Line 59 skeleton: 'rounded-xl border border-border/50 bg-transparent p-4'
  Line 71 zero-batch empty: 'rounded-xl border border-border/50 bg-transparent'

Transition timing (transition-colors duration-quick ease-default) is retained per 28-RESEARCH
Example 3 — Phase 30 will layer hover:shadow-elevation-overlay on top using this timing.
This plan defines rest state only.

Border decision: drop `border border-border` on the real card. 28-RESEARCH Example 3 states the
rim highlight layer inside shadow-elevation-raised carries the edge. If pilot visual check shows
the card lacks definition without the border (e.g., light mode low-contrast backgrounds), note
as a Deviation and add `border border-border/40` (lighter than original) back — do NOT add a
second shadow.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retune kpi-card.tsx to shadow-elevation-raised</name>
  <files>src/components/kpi/kpi-card.tsx</files>
  <action>
Read src/components/kpi/kpi-card.tsx first; confirm the cardClasses string and any other surface-affecting className strings.

Update the existing cardClasses constant (or inline className) from:
  rounded-lg border border-border bg-surface-raised p-card-padding shadow-sm transition-colors duration-quick ease-default
to:
  rounded-lg bg-surface-raised p-card-padding shadow-elevation-raised transition-colors duration-quick ease-default

Changes:
- Drop `border border-border` — rim highlight layer in shadow-elevation-raised carries top-edge definition. If pilot fails (see Interfaces fallback), add `border border-border/40` back.
- `shadow-sm` -> `shadow-elevation-raised` — semantic tier. Do NOT stack both; semantic elevation replaces raw.
- Retain `transition-colors duration-quick ease-default` — Phase 30 will extend this with hover:shadow-elevation-overlay; preserving the timing now avoids a re-pass.
- Retain `rounded-lg p-card-padding bg-surface-raised` — Phase 26 tokens unchanged.

Do NOT modify any children (value, label, trend delta, drill-affordance chevron). Only the root container className.

If the component exports additional variant strings (e.g., `interactiveCardClasses` with hover states), align each to the same elevation base — drop shadow-sm, use shadow-elevation-raised. Hover shadow stays out of this plan (Phase 30 territory).
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && grep -q 'shadow-elevation-raised' src/components/kpi/kpi-card.tsx && ! grep -q 'shadow-sm' src/components/kpi/kpi-card.tsx && echo OK</automated>
Then `npm run build` passes.
  </verify>
  <done>
- KpiCard cardClasses now uses shadow-elevation-raised, not shadow-sm
- border border-border either dropped or demoted to border-border/40 (record which in SUMMARY)
- Transition retained; Phase 30 has its anchor
- Build green
  </done>
</task>

<task type="auto">
  <name>Task 2: Align kpi-summary-cards.tsx skeleton + empty-state to elevation-raised recipe</name>
  <files>src/components/kpi/kpi-summary-cards.tsx</files>
  <action>
Read src/components/kpi/kpi-summary-cards.tsx; confirm current skeleton cells (line 59) and zero-batch empty state (line 71) className strings. Line numbers from 28-RESEARCH may have drifted; locate by grep for `rounded-xl border border-border/50 bg-transparent`.

Update both instances from:
  rounded-xl border border-border/50 bg-transparent p-4
(or without p-4 on line 71)
to the KpiCard-matching recipe:
  rounded-lg bg-surface-raised p-card-padding shadow-elevation-raised

Rationale:
- rounded-xl -> rounded-lg — Pitfall 5 consistency, matches real KpiCard.
- border border-border/50 -> dropped — match KpiCard's decision (rim carries).
- bg-transparent -> bg-surface-raised — skeletons should look like the real card with a grayed interior; transparency against surface-base made them blend.
- p-4 -> p-card-padding — use the token, not the raw utility; KpiCard already uses p-card-padding.
- Add shadow-elevation-raised — same elevation recipe.

For the skeleton's internal content (Skeleton or animated placeholder bars), preserve what's there. This plan swaps the outer surface only.

For the zero-batch empty state (line 71), preserve the inner messaging content; if the muted text treatment feels too faint on the new lighter surface, that's a Deviation to note — do NOT change text classes in this plan (typography tokens are Phase 27's domain and were settled last sprint).

If the file has MORE than two instances of this pattern (e.g., a third loading-error variant), align them too — the rule is "every KPI-tier surface uses the same recipe."
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && ! grep -q 'rounded-xl border border-border/50 bg-transparent' src/components/kpi/kpi-summary-cards.tsx && grep -q 'shadow-elevation-raised' src/components/kpi/kpi-summary-cards.tsx && echo OK</automated>
Then `npm run build` passes.
  </verify>
  <done>
- No remaining `rounded-xl border border-border/50 bg-transparent` in kpi-summary-cards.tsx
- Skeleton + zero-batch empty cells now match KpiCard recipe visually
- Build green
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — KPI family surface consistency</name>
  <action>Verify KPI cards, skeletons, and empty-state cells share the same raised recipe in both modes. See how-to-verify below.</action>
  <what-built>KPI card family unified on shadow-elevation-raised. Real cards, loading skeletons, and zero-batch empty cells now share the same surface recipe.</what-built>
  <how-to-verify>
1. Run `npm run dev`. Navigate to the main dashboard (root view with KPI cards at top).
2. Confirm the KPI cards render with a visible 3-layer shadow (key + ambient + rim highlight on top edge) in light mode.
3. Toggle dark mode. Confirm cards still read as raised (surface-raised color lift does the primary lifting; shadow reinforces).
4. Trigger the loading state (soft reload, throttle network in DevTools). Skeleton cells should match the real cards' surface treatment — same radius, same shadow, same padding.
5. Trigger a zero-batch filter state if possible (partner with no data, or apply a filter with zero matches). Confirm the empty-state cell matches the card recipe.
6. Confirm KPI cards are visually distinct from the surrounding surface-base/surface-translucent header.

If any state looks off (border missing and card reads as floating prose, or shadow looks muddy in dark mode), describe it. Otherwise approve.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues observed.</resume-signal>
</task>

</tasks>

<verification>
- grep 'shadow-elevation-raised' src/components/kpi/kpi-card.tsx returns >= 1
- grep 'shadow-sm' src/components/kpi/kpi-card.tsx returns 0
- grep 'shadow-elevation-raised' src/components/kpi/kpi-summary-cards.tsx returns >= 2 (skeleton + empty)
- grep 'rounded-xl border border-border/50 bg-transparent' src/components/kpi/kpi-summary-cards.tsx returns 0
- npm run build passes
- User-verified in both modes across real/loading/empty states
</verification>

<success_criteria>
- DS-12 fully satisfied: KPI cards use surface-raised + semantic elevation, pop off background
- Skeleton + empty-state cells match real card visually (no mismatched outer rectangles during loading)
- Phase 30's hover-lift story has a clean anchor (transition-colors duration-quick ease-default retained)
- No regression on the value/label/trend internals
</success_criteria>

<output>
Create `.planning/phases/28-surfaces-and-elevation/28-03-SUMMARY.md` documenting:
- Final KpiCard cardClasses string
- Whether `border border-border` was dropped entirely or demoted to border-border/40 (with rationale based on pilot visual check)
- Final kpi-summary-cards skeleton + empty-state className strings
- Any Deviations (e.g., empty-state text looked too faint — flagged for typography follow-up)
- Visual verification notes from both modes across real/loading/empty states
</output>
