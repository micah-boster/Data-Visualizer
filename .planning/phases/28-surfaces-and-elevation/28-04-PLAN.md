---
phase: 28-surfaces-and-elevation
plan: 04
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/components/table/data-table.tsx
  - src/components/table/table-header.tsx
  - src/components/table/table-body.tsx
autonomous: false
requirements: [DS-13]

must_haves:
  truths:
    - "Table area reads as a recessed pane — no outer border/card frame wrapping the scroll container"
    - "Sticky column header visibly lifts above the body when content scrolls underneath"
    - "Row backgrounds are transparent over the surface-inset bg — no zebra stripes"
    - "Hover on a row applies a subtle bg tint (color change, not elevation change)"
  artifacts:
    - path: "src/components/table/data-table.tsx"
      provides: "Table scroll wrapper on surface-inset without outer border-card frame"
      contains: "bg-surface-inset"
    - path: "src/components/table/table-header.tsx"
      provides: "Sticky column header with a gentle lift (shadow-xs or surface-raised) inside the inset pane"
    - path: "src/components/table/table-body.tsx"
      provides: "Row backgrounds transparent; zebra stripe (bg-muted/30) removed; hover-tint preserved"
  key_links:
    - from: "src/components/table/data-table.tsx"
      to: "bg-surface-inset"
      via: "scroll wrapper className"
      pattern: "bg-surface-inset"
    - from: "src/components/table/table-header.tsx"
      to: "sticky top-0 lift classes"
      via: "thead sticky + surface-base or surface-raised background"
      pattern: "sticky.*bg-surface-(base|raised)"
    - from: "src/components/table/table-body.tsx"
      to: "removed zebra stripe"
      via: "deletion of bg-muted/30"
      pattern: "bg-muted/30"
---

<objective>
Complete the table's "data lives in a recessed pane" treatment. The Phase 26-04 pilot already shipped `bg-surface-inset` on the scroll wrapper; Phase 28 finishes the story by (1) dropping any outer border/card frame wrapping the DataTable, (2) adding a sticky column-header lift so it visibly floats above scrolling body content, and (3) removing zebra striping per CONTEXT lock (transparent rows over inset bg).

Purpose: CONTEXT locks table recession as "inset bg only — no inner box-shadow, no dashed top border, no extra effects. The beige-recessed color contrast does the work." It also locks "drop the outer border / card wrapper" and "row backgrounds transparent, no zebra." The sticky column header is the one affordance added here — marks "this stays put" per the hover-lift-on-affordance philosophy.

Output: Table reads as a recessed pane inside surface-raised content. No nested picture-frame. Column header marks its stickiness. Rows pick up inset bg transparently. Hover remains a color tint.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-surfaces-and-elevation/28-CONTEXT.md
@.planning/phases/28-surfaces-and-elevation/28-RESEARCH.md
@.planning/phases/28-surfaces-and-elevation/28-01-PLAN.md

@src/components/table/data-table.tsx
@src/components/table/table-header.tsx
@src/components/table/table-body.tsx

<interfaces>
Existing state (28-RESEARCH):
- data-table.tsx:347 — scroll wrapper already `bg-surface-inset overflow-auto` (shipped 26-04)
- data-table.tsx — CONTEXT says drop OUTER border wrapper around the DataTable. Check for an
  outer <div className="rounded-*.border.bg-card"> or similar wrapping the scroll container.
- table-body.tsx:50 — `bg-muted/30` zebra stripe on alternate rows. CONTEXT lock: REMOVE.
- table-header.tsx — currently NO sticky or lift treatment documented in research; this plan adds one.

Sticky header lift options (CONTEXT "pilot will pick"):
- Option A: thead `sticky top-0 bg-surface-base shadow-xs` — lighter treatment inside the inset pane
- Option B: thead `sticky top-0 bg-surface-raised shadow-xs` — more contrast vs inset body
- Option C: `data-[stuck]:shadow-elevation-overlay` via IntersectionObserver — only shows lift when actually stuck

Recommend Option A to start (lightest touch, matches CONTEXT's "not elevation-chrome-heavy —
a lighter treatment inside the inset"). Option C is Phase 30 territory if it comes up. Document
decision in SUMMARY. Pitfall 4 applies: if the shadow reads unconditionally as a weird glowy
bar, demote to just `bg-surface-base` with a thin border-b instead of shadow.

Existing hover-tint on rows: preserve. It's a color affordance, not a surface-elevation change,
so it's out of scope for Phase 28's surface/shadow discipline.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop outer border/card wrapper around DataTable; keep inset scroll wrapper</name>
  <files>src/components/table/data-table.tsx</files>
  <action>
Read src/components/table/data-table.tsx. Identify any outer container wrapping the scroll `<div className="bg-surface-inset overflow-auto ...">`.

Per 28-RESEARCH and CONTEXT: "drop the outer border / card wrapper around the DataTable. The inset bg + padding + rounded corners defines the boundary."

Action:
- If an outer wrapper exists with `rounded-* border* bg-card` or similar card-frame classes, remove those classes from that wrapper's className (keep the wrapper itself if it has layout duties — flex, overflow, h-full, etc.).
- Confirm the scroll wrapper keeps `bg-surface-inset overflow-auto` and add a `rounded-lg` on the scroll wrapper itself (so the recessed pane has corners per Pitfall 5 radius consistency).
- If the DataTable returns JSX like:
    <div className="rounded-lg border bg-card">
      <div className="bg-surface-inset overflow-auto">...</div>
    </div>
  Flatten to:
    <div className="rounded-lg bg-surface-inset overflow-auto">...</div>
  Preserve ref forwarding, data-density attribute, and TanStack Virtual wiring.
- If the outer frame carries an aria role, keep the element and strip only the visual classes.

Do NOT add shadow-elevation-* to the inset pane. Inset is the RECESSION cue; shadow would contradict recession.

If footer row treatments (table-footer.tsx) ship with `border-t bg-card` that assumes the outer card frame, that's a cleanup candidate for 28-08 (DS-17 sweep) — do not fix here to keep plan scope tight. Note in SUMMARY Deviations if observed.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && grep -q 'bg-surface-inset' src/components/table/data-table.tsx && echo OK</automated>
Then `npm run build` passes. Visual check: table reads as ONE recessed pane, not a card containing a table.
  </verify>
  <done>
- Outer card-frame classes removed from any wrapper above the scroll container
- Scroll wrapper retains bg-surface-inset + overflow-auto and gains rounded-lg
- No shadow-* on the inset pane
- Build green
  </done>
</task>

<task type="auto">
  <name>Task 2: Add sticky lift to column header; remove zebra stripe from row bodies</name>
  <files>src/components/table/table-header.tsx, src/components/table/table-body.tsx</files>
  <action>
Read both files first.

In table-header.tsx (thead element):
- Add sticky positioning: `sticky top-0 z-10` (z-10 sits above body rows, below popovers)
- Add a lift treatment: `bg-surface-base shadow-xs` — Option A from Interfaces (lighter than elevation-chrome; signals "stays put" inside the inset pane)
- Preserve existing column header cell styling (text-label overline, draggable handles, sort indicators, etc.)
- If thead is not the correct sticky anchor (e.g., headers are rendered inside each <th> with position: sticky on individual cells), apply the same lift to whichever element owns stickiness — the goal is a gentle visible lift when body scrolls underneath.

Pitfall 4 mitigation: the shadow-xs applied unconditionally is a light touch — not `shadow-elevation-overlay` which would over-signal. If it still reads wrong at rest (random glowy bar when no scroll has happened), note as Deviation and demote to `border-b border-border/40` only. IntersectionObserver-gated conditional lift is Phase 30 territory.

In table-body.tsx:
- Find the zebra-stripe class (`bg-muted/30` on alternate rows per 28-RESEARCH line 323). Remove it.
- Preserve the hover treatment (`hover:bg-muted`, `hover:bg-accent/50`, etc.) — CONTEXT locks "hover = subtle bg tint (color, not elevation change)." That affordance stays.
- Row rest state now = transparent over bg-surface-inset. Do not add any bg-* class to rest state.
- If there's conditional selected-row styling, preserve it.

Do not modify row heights, padding, or density tokens — 26-04 locked those and Phase 28 is surface/elevation only.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && grep -E 'sticky top-0' src/components/table/table-header.tsx && grep -E 'bg-surface-(base|raised)' src/components/table/table-header.tsx && ! grep -q 'bg-muted/30' src/components/table/table-body.tsx && echo OK</automated>
Then `npm run build` passes.
  </verify>
  <done>
- table-header.tsx thead (or its sticky anchor) has `sticky top-0 z-10 bg-surface-base shadow-xs`
- table-body.tsx no longer contains bg-muted/30 zebra stripe
- Hover tint preserved (hover:bg-* still present)
- Build green
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — table recessed pane</name>
  <action>Confirm the table reads as a single recessed pane (no nested card frame), column header lifts when scrolled, zebra stripe is gone, hover tint is preserved. See how-to-verify below.</action>
  <what-built>Table now reads as a recessed pane inside surface-raised content. Outer card frame dropped; scroll wrapper retains bg-surface-inset + rounded-lg. Column header is sticky and gently lifted. Zebra stripe removed; rows pick up inset bg transparently; hover-tint preserved.</what-built>
  <how-to-verify>
1. `npm run dev`. Navigate to the main dashboard, view the DataTable.
2. Confirm the table area reads as ONE recessed pane — no visible nested frame, no "card containing a table" impression.
3. Scroll the table body. Column header should visibly lift above body content — shadow-xs ambient halo appears against the inset pane below.
4. Confirm row bodies have no alternating zebra stripe. Rows should all have the same (transparent over inset) bg.
5. Hover a row. Subtle bg tint should appear (hover:bg-muted or similar) — color change only, no row lifting/shadow.
6. Toggle dark mode. Confirm:
   - Inset pane reads as recessed (lighter-than-base per Phase 26-01 dark-mode inversion)
   - Sticky header lift doesn't smudge (Pitfall 3)
   - Zebra absence doesn't make dense rows hard to read — if it does, that's a legitimate concern to raise; CONTEXT explicitly rejected striping but if data-density demands it, the choice can be revisited in a future table-polish phase (not 28).
7. Confirm nested popovers (filter menus, column menus) still render above the table.

If anything reads wrong (sticky header bar too loud, inset pane boundary unclear), describe it.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues observed.</resume-signal>
</task>

</tasks>

<verification>
- grep 'bg-surface-inset' src/components/table/data-table.tsx >= 1
- grep 'bg-card' src/components/table/data-table.tsx == 0 (card frame removed)
- grep 'sticky top-0' src/components/table/table-header.tsx >= 1
- grep 'bg-muted/30' src/components/table/table-body.tsx == 0 (zebra removed)
- hover:bg-* still present in table-body.tsx (hover-tint preserved)
- npm run build passes
- User-verified in both modes
</verification>

<success_criteria>
- DS-13 fully satisfied: table area is a recessed pane, data is the focus
- Sticky column header reads "stays put" during scroll (lift visible, not overwhelming)
- No zebra stripe; no nested card frame
- Hover-tint row affordance preserved
- Pitfall 4 (always-on sticky-header shadow) addressed via light-touch shadow-xs or documented demotion
</success_criteria>

<output>
Create `.planning/phases/28-surfaces-and-elevation/28-04-SUMMARY.md` documenting:
- Whether an outer card frame was present and what classes were removed
- Sticky header recipe chosen (Option A bg-surface-base+shadow-xs, Option B, or demoted to border-b)
- Final table-header.tsx thead className
- Before/after table-body row className (demonstrating zebra removal, hover preservation)
- Any Deviations (e.g., table-footer.tsx still relied on the outer card frame — flagged for 28-08 cleanup)
- Both-mode visual verification notes
</output>
