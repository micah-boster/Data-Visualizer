---
phase: 28-surfaces-and-elevation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/tokens/shadow-sample.tsx
  - src/components/tokens/token-browser.tsx
autonomous: true
requirements: [DS-11, DS-15, DS-17]

must_haves:
  truths:
    - "CSS custom properties --shadow-elevation-chrome/raised/overlay/floating exist in :root and .dark blocks"
    - "CSS custom property --surface-translucent exists in :root and .dark blocks"
    - "Tailwind v4 @theme emits shadow-elevation-chrome/raised/overlay/floating utility classes"
    - "Tailwind v4 @theme inline emits bg-surface-translucent utility class"
    - "/tokens page renders an Elevation tab/section with one sample swatch per semantic level in both light and dark mode"
  artifacts:
    - path: "src/app/globals.css"
      provides: "4 semantic elevation tokens (chrome/raised/overlay/floating) + surface-translucent, per-mode"
      contains: "--shadow-elevation-chrome"
    - path: "src/components/tokens/shadow-sample.tsx"
      provides: "Reusable elevation sample component (or extended sample) used by the browser"
      min_lines: 10
    - path: "src/components/tokens/token-browser.tsx"
      provides: "/tokens page now lists the 4 semantic elevation recipes + translucent variant"
  key_links:
    - from: "src/app/globals.css"
      to: "Tailwind v4 @theme"
      via: "--shadow-elevation-* namespace"
      pattern: "--shadow-elevation-(chrome|raised|overlay|floating)"
    - from: "src/components/tokens/token-browser.tsx"
      to: "shadow-elevation-* utility classes"
      via: "className consumption"
      pattern: "shadow-elevation-(chrome|raised|overlay|floating)"
---

<objective>
Establish Phase 28's token foundation: add 4 semantic elevation recipes (3-layer compounds) and 1 translucent surface variant to globals.css, then extend the unlisted /tokens reference page so every downstream plan has a live preview + copy-to-clipboard surface for these recipes.

Purpose: Phase 26 built the raw --shadow-xs/sm/md/lg primitives and the --surface-base/raised/inset/overlay/floating surfaces. Phase 28 adds the SEMANTIC layer (elevation-chrome/raised/overlay/floating) that every container sweep plan consumes — call sites never reach for `shadow-sm` directly for primary containers again. The translucent variant lights up the header sweep (28-02).

Output: Extended globals.css with new tokens in :root + .dark (per-mode), updated /tokens browser with an Elevation section, and automatic Tailwind utility emission for `shadow-elevation-*` + `bg-surface-translucent`.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-surfaces-and-elevation/28-CONTEXT.md
@.planning/phases/28-surfaces-and-elevation/28-RESEARCH.md

# Token system precedent (Phase 26 additive pattern):
@src/app/globals.css

# /tokens consumer:
@src/components/tokens/token-browser.tsx
@src/components/tokens/shadow-sample.tsx

<interfaces>
<!-- Tailwind v4 namespace-driven utility emission (confirmed from 26-01 SUMMARY) -->
<!-- - `--shadow-*` under @theme auto-emits shadow-* utilities -->
<!-- - `--color-*` under @theme (or aliased via @theme inline) auto-emits bg-*, text-*, border-* -->
<!-- - Semantic elevations MUST be named `--shadow-elevation-chrome/raised/overlay/floating` -->
<!--   so Tailwind emits `shadow-elevation-chrome` etc. automatically. -->

<!-- Existing token file layout (verify with Read before edits):
  globals.css @theme block:    raw --shadow-xs/sm/md/lg live here
  globals.css :root block:     --surface-base/raised/inset/overlay/floating (light)
  globals.css .dark block:     same keys re-tinted for dark
  globals.css @theme inline:   --color-surface-*: var(--surface-*) aliases that emit bg-surface-* utilities
-->

<!-- /tokens browser (token-browser.tsx) already has a tabbed structure from 26-05.
  Existing tabs include Spacing, Type, Shadow, Surface, Motion, Colors.
  This plan extends the Shadow tab OR adds an Elevation tab — implementer picks based
  on current structure. Shadow tab extension preferred (raw primitives + semantic recipes together). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 semantic elevation tokens + translucent surface to globals.css</name>
  <files>src/app/globals.css</files>
  <action>
Read src/app/globals.css first to understand its current structure (do NOT skip — Phase 26 established a specific layout with @theme, @theme inline, :root, and .dark blocks).

Add four semantic elevation tokens to the EXISTING @theme block (do not create a new @theme block):

```
--shadow-elevation-chrome:
  0 1px 2px 0 oklch(0.2 0.015 70 / 0.04),
  0 2px 8px -2px oklch(0.2 0.015 70 / 0.05),
  inset 0 1px 0 0 oklch(1 0 0 / 0.06);
--shadow-elevation-raised:
  0 1px 2px 0 oklch(0.2 0.015 70 / 0.06),
  0 4px 12px -2px oklch(0.2 0.015 70 / 0.08),
  inset 0 1px 0 0 oklch(1 0 0 / 0.08);
--shadow-elevation-overlay:
  0 2px 4px 0 oklch(0.2 0.015 70 / 0.08),
  0 8px 24px -4px oklch(0.2 0.015 70 / 0.12),
  inset 0 1px 0 0 oklch(1 0 0 / 0.10);
--shadow-elevation-floating:
  0 4px 8px 0 oklch(0.2 0.015 70 / 0.10),
  0 16px 40px -8px oklch(0.2 0.015 70 / 0.16),
  inset 0 1px 0 0 oklch(1 0 0 / 0.12);
```

Mirror these in the `.dark` block using hand-tuned DARKER compound values per Pitfall 3 (CONTEXT locks: dark mode shadows use pure black at higher opacity; DO NOT derive via calc/color-mix from light values). Use 28-RESEARCH Example 1 as the starting recipe. Verify the existing `.dark { --shadow-xs: 0 1px 2px 0 oklch(0 0 0 / 0.35); ... }` block (Phase 26) as the precedent — match its tinting philosophy.

Add `--surface-translucent` to :root block (after existing surface-base/raised/inset/overlay/floating):
```
--surface-translucent: color-mix(in oklch, var(--surface-raised) 80%, transparent);
```
Mirror in .dark block at 82% opacity per research recipe (dark mode needs slightly more opaque to remain legible).

Add alias in the existing @theme inline block (alongside other `--color-surface-*` aliases):
```
--color-surface-translucent: var(--surface-translucent);
```
This triggers Tailwind v4 to emit `bg-surface-translucent` utility.

Add an inline comment block above the new elevation tokens documenting:
- 3-layer compound structure (key + ambient + rim)
- Semantic-over-raw ethos (raw `--shadow-*` primitives remain for edge cases)
- CONTEXT lock: dark-mode shadows hand-tuned, not derived
- Consumer guidance: primary containers use `shadow-elevation-*`, badges/chips stay on raw `shadow-xs`

DO NOT rename, move, or remove any existing token. Additive only.

Exact opacity values above are a starting recipe per CONTEXT's "Claude's Discretion" allowance on shadow color/opacity. If pilot plans (28-02 through 28-04) surface that the values read too heavy or too light, fine-tuning happens in those plans' gap closures, not here.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && grep -cE '^\s*--shadow-elevation-(chrome|raised|overlay|floating):' src/app/globals.css</automated>
Expected count: 8 (4 tokens × 2 modes). Also run `npm run build` — Tailwind v4 must compile without error; failure means the @theme block syntax is wrong.
  </verify>
  <done>
- 4 `--shadow-elevation-*` tokens defined in @theme block (light-mode primary values)
- 4 `--shadow-elevation-*` tokens re-tinted in `.dark` block (pure-black-higher-opacity philosophy)
- `--surface-translucent` defined in :root + .dark
- `--color-surface-translucent: var(--surface-translucent)` added to @theme inline
- npm run build succeeds (Tailwind compiles; utilities emit)
- Temporary `<div className="shadow-elevation-raised">test</div>` smoke test in any component renders with a visible multi-layer shadow (remove after verification)
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend /tokens page with semantic elevation demos</name>
  <files>src/components/tokens/shadow-sample.tsx, src/components/tokens/token-browser.tsx</files>
  <action>
First Read both files to understand the current structure (the Phase 26-05 SUMMARY establishes a tabbed browser with a Shadow tab that already lists raw --shadow-xs/sm/md/lg samples).

Update `shadow-sample.tsx` (or add a sibling `elevation-sample.tsx` if the existing ShadowSample's props are too tight) to support rendering a named semantic elevation level. The sample should render a card with:
- `bg-surface-raised` base (for elevation-chrome, elevation-raised, elevation-floating)
- `bg-surface-overlay` base (for elevation-overlay, to match the real consumer)
- Generous padding and a rounded corner so the multi-layer shadow is not clipped by the parent (Pitfall 1)
- The semantic elevation class applied (`shadow-elevation-chrome`, etc.)
- A label ("elevation-chrome", "elevation-raised", etc.) and the compound recipe shown inline as code (similar to existing token samples)
- A copy-to-clipboard button matching the existing pattern in the tokens browser

Update `token-browser.tsx` to append a new Elevation section — preferably inside the existing Shadow tab, rendered below the raw primitives so users see "raw tokens" and "semantic recipes" side by side. Also add ONE sample for `bg-surface-translucent` in the Surface tab (alongside existing surface samples), labeled "surface-translucent (header-only)".

Each sample must render in both light and dark mode — the browser already has a theme toggle; verify samples read correctly in both (the dark-mode shadow values must look DEPTH not smudge per Pitfall 3).

DO NOT break existing samples. Additive only. Follow the 26-05 locked pattern: template-literal class names are NOT scanned by Tailwind's content scanner — use inline `className={'shadow-elevation-' + name}` ONLY if `name` is a literal string union the compiler can see, otherwise spell each className explicitly per sample instance.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && grep -cE 'shadow-elevation-(chrome|raised|overlay|floating)' src/components/tokens/token-browser.tsx src/components/tokens/shadow-sample.tsx 2>/dev/null</automated>
Expected: count ≥ 4 (at least one reference per elevation level across the two files).
Then `npm run build` — page must compile.
  </verify>
  <done>
- /tokens page renders 4 semantic elevation samples (chrome/raised/overlay/floating), each with visible multi-layer shadow
- One surface-translucent sample renders in Surface tab showing the blur/transparency effect against a visible background
- Samples render correctly in both light and dark mode (user will spot-check visually)
- Existing Spacing/Type/Shadow-raw/Surface/Motion/Colors samples remain untouched and rendering
- npm run build passes
  </done>
</task>

</tasks>

<verification>
- `grep -c 'shadow-elevation-' src/app/globals.css` returns ≥ 8 (4 tokens × light + dark)
- `grep -c 'surface-translucent' src/app/globals.css` returns ≥ 3 (:root, .dark, @theme inline)
- `npm run build` compiles without error
- /tokens page loads at `/tokens` and shows new samples in both modes
- Smoke-test className `shadow-elevation-raised` on any div renders a visible 3-layer shadow
- No existing token renamed, moved, or removed (verify via git diff — only additions)
</verification>

<success_criteria>
- 4 semantic elevation tokens emit as Tailwind utilities (shadow-elevation-chrome/raised/overlay/floating)
- bg-surface-translucent utility emits and renders a color-mix semi-opaque backdrop
- /tokens reference page exposes every new token for copy-to-clipboard and visual verification
- Foundation is in place for Wave 2 pilot plans (28-02, 28-03, 28-04) to consume without any further token work
- Build green, both modes verified on /tokens
</success_criteria>

<output>
After completion, create `.planning/phases/28-surfaces-and-elevation/28-01-SUMMARY.md` documenting:
- Exact opacity values landed per level (both modes) — future pilots will reference these when tuning
- Any opacity adjustments made relative to the 28-RESEARCH Example 1 starting recipe (deviations, with rationale)
- Whether the Elevation samples were added to the existing Shadow tab or a new Elevation tab
- Surface-translucent placement and blend percentage per mode
- Confirmation that both modes pass visual verification on /tokens
</output>
