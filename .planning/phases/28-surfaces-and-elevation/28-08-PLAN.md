---
phase: 28-surfaces-and-elevation
plan: 08
type: execute
wave: 4
depends_on: ["28-02", "28-03", "28-04", "28-05", "28-06", "28-07"]
files_modified:
  - scripts/check-surfaces.sh
  - package.json
autonomous: true
requirements: [DS-17]

must_haves:
  truths:
    - "scripts/check-surfaces.sh exits 0 on a clean tree"
    - "npm run check:surfaces invokes the guard"
    - "Guard forbids ad-hoc rounded+border+bg-(card|popover) container combos outside the allowlist"
    - "Guard forbids raw shadow-xs/sm/md/lg/xl/2xl on primary containers outside the allowlist"
    - "Any gaps found by the guard during initial run are closed (stragglers from Waves 2-3 not caught by plan-level verifies)"
  artifacts:
    - path: "scripts/check-surfaces.sh"
      provides: "POSIX grep guard analogous to check-type-tokens.sh"
      min_lines: 50
      contains: "shadow-(xs|sm|md|lg|xl|2xl)"
    - path: "package.json"
      provides: "npm run check:surfaces script alias"
      contains: "check:surfaces"
  key_links:
    - from: "scripts/check-surfaces.sh"
      to: "src/ tree scan via find + grep -rE"
      via: "POSIX find with EXCLUDE_DIRS allowlist"
      pattern: "find src -type f"
    - from: "package.json"
      to: "scripts/check-surfaces.sh"
      via: "npm scripts entry"
      pattern: "\"check:surfaces\""
---

<objective>
Close Phase 28 with enforcement: a POSIX grep guard (`scripts/check-surfaces.sh`) that exits 0 when every primary container outside the allowlist uses semantic-elevation tokens (shadow-elevation-*) and rejects ad-hoc combos (rounded-*/border/bg-card + raw shadow-sm/md/lg). Mirrors Phase 27's `check-type-tokens.sh` pattern that closed the typography sweep. Any stragglers surfaced by the guard's initial run are closed inline (DS-17 cleanup) so the guard passes before this plan ends.

Purpose: Semantic-token discipline only survives with a machine check. Prior phases (26, 27) consistently flipped to grep-guard enforcement at phase end; 28 must too or DS-17 regresses within weeks. POSIX grep chosen over ripgrep/ESLint per research + Phase 27-06 locked precedent (universal availability, no install footprint, <1s runtime).

Output: scripts/check-surfaces.sh that passes on a clean tree. npm run check:surfaces alias. All DS-17 stragglers closed (table-footer card-frame coupling, data-display.tsx overflow-hidden issues, any sort-dialog or view-item container that slipped through, etc.).
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-surfaces-and-elevation/28-CONTEXT.md
@.planning/phases/28-surfaces-and-elevation/28-RESEARCH.md
@.planning/phases/28-surfaces-and-elevation/28-01-PLAN.md
@.planning/phases/28-surfaces-and-elevation/28-04-PLAN.md
@.planning/phases/28-surfaces-and-elevation/28-05-PLAN.md
@.planning/phases/28-surfaces-and-elevation/28-06-PLAN.md

@scripts/check-type-tokens.sh
@package.json

<interfaces>
Template (scripts/check-type-tokens.sh):
- POSIX bash (#!/usr/bin/env bash, set -euo pipefail)
- EXCLUDE_DIRS allowlist: src/components/ui, src/app/tokens, src/components/tokens
- find src -type f ... -not -path '<allowed>/*'
- xargs grep -nE <pattern> || true  (|| true so pipeline doesn't fail on zero hits)
- Report failures with file:line output + remediation hint
- Exit 1 on any violation; exit 0 otherwise

Patterns to forbid (primary containers only, outside allowlist):
1. Raw shadow utilities that should be semantic:
     grep -nE 'shadow-(xs|sm|md|lg|xl|2xl)([^a-zA-Z0-9-]|$)'
   BUT exclude hits that are FOLLOWED BY something qualifying them out. Simpler:
   this pattern catches all raw shadows. Legitimate raw-shadow consumers AFTER Phase 28:
     - table-header.tsx sticky header lift (shadow-xs) — ALLOW explicitly via comment-based allowlist OR move to elevation-chrome if visually safe
     - Any badge/chip using shadow-xs for inline emphasis — ALLOW

   Implementation strategy: forbid raw shadow-sm/md/lg/xl/2xl (the "heavy" raw shadows that
   semantic elevations now replace). ALLOW shadow-xs (the lightest raw primitive, still
   legitimate for chips/badges/sticky-header-lift). Guard wording must make this explicit.

2. Ad-hoc card-frame combos:
     grep -nE 'rounded-(lg|xl|md).*border.*bg-(card|popover|background)'
   This catches the precise pattern Phase 28 is sweeping away. False positives possible
   on form inputs (bg-background + border) — the allowlist excludes src/components/ui
   and token pages where form inputs live, which should eliminate most. If false positives
   surface in app code, tune the regex.

3. Translucent popover regressions:
     grep -nE 'bg-(popover|background)/\d+.*backdrop-blur'
   Enforces CONTEXT lock: popovers stay opaque; translucent is header-only. Header lives
   OUTSIDE the ui/ allowlist so the guard must explicitly allow src/components/layout/header.tsx:
     EXCLUDE_FILES=("src/components/layout/header.tsx")

Allowlist (matches 27-06 pattern):
  EXCLUDE_DIRS=(
    "src/components/ui"        # shadcn primitives own their surface recipes
    "src/app/tokens"            # token reference page
    "src/components/tokens"     # token demos
  )
  EXCLUDE_FILES=(
    "src/components/layout/header.tsx"  # only legitimate translucent container
  )

Expected stragglers to close in Task 2 (based on SUMMARY deviations from Waves 2-3):
- table-footer.tsx if the outer card frame removal in 28-04 broke its border-t dependency
- data-display.tsx bare chart wrapper (if 28-05 sparklines were deferred and the wrapper remains)
- sort-dialog.tsx nested-card treatment if 28-06 chose the border-back-in fallback
- view-item.tsx, matrix-plain-table.tsx, matrix-bar-ranking.tsx, etc. — any lower-priority
  container surfaced by 28-RESEARCH line 317-326 cleanup list

The checklist is: run the guard, fix each hit (either by migrating to semantic recipe or by
verifying it belongs in the allowlist), re-run, commit.

NPM script:
  "check:surfaces": "bash scripts/check-surfaces.sh"
Keep existing "check:tokens" alongside — they're complementary, not competing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/check-surfaces.sh (POSIX grep guard)</name>
  <files>scripts/check-surfaces.sh, package.json</files>
  <action>
Read scripts/check-type-tokens.sh (the template) first; also package.json.

Create scripts/check-surfaces.sh modeled on check-type-tokens.sh. Structure:

```
#!/usr/bin/env bash
set -euo pipefail

# Phase 28 surface-token enforcement.
# Forbids ad-hoc shadow + border + bg-card container combos outside an allowlist.
# Allowlist: shadcn primitives, tokens reference page, layout/header (translucent carveout).
# Uses POSIX grep (BSD/GNU) per Phase 27-06 precedent — no ripgrep install requirement.

EXCLUDE_DIRS=(
  "src/components/ui"
  "src/app/tokens"
  "src/components/tokens"
)
EXCLUDE_FILES=(
  "src/components/layout/header.tsx"
)

FAIL=0

files_to_check() {
  find src -type f \
    \( -name '*.ts' -o -name '*.tsx' -o -name '*.js' -o -name '*.jsx' -o -name '*.mjs' -o -name '*.cjs' \) \
    -not -path 'src/components/ui/*' \
    -not -path 'src/app/tokens/*' \
    -not -path 'src/components/tokens/*' \
    -not -path 'src/components/layout/header.tsx'
}

# Check 1: raw shadow utilities that should be semantic elevations.
# Forbid shadow-sm, shadow-md, shadow-lg, shadow-xl, shadow-2xl on app-code containers.
# shadow-xs is ALLOWED (sticky-header lift, chips, badges).
echo "Checking for ad-hoc raw shadow utilities..."
RAW_SHADOW_HITS=$(files_to_check | xargs grep -nE 'shadow-(sm|md|lg|xl|2xl)([^a-zA-Z0-9-]|$)' 2>/dev/null || true)
if [ -n "$RAW_SHADOW_HITS" ]; then
  echo "$RAW_SHADOW_HITS"
  echo ""
  echo "ERROR: Found raw shadow-sm/md/lg/xl/2xl utilities. Use shadow-elevation-chrome/raised/overlay/floating." 1>&2
  echo "       shadow-xs is still allowed for chips/badges/sticky-header lifts." 1>&2
  FAIL=1
fi

# Check 2: ad-hoc card-frame combos — rounded + border + bg-(card|popover|background).
echo ""
echo "Checking for ad-hoc card-frame combos..."
CARD_FRAME_HITS=$(files_to_check | xargs grep -nE 'rounded-(lg|xl|md)[^"]*\bborder\b[^"]*bg-(card|popover|background)' 2>/dev/null || true)
if [ -n "$CARD_FRAME_HITS" ]; then
  echo "$CARD_FRAME_HITS"
  echo ""
  echo "ERROR: Found rounded+border+bg-card|popover|background combo. Use bg-surface-raised/overlay/floating + shadow-elevation-*." 1>&2
  FAIL=1
fi

# Check 3: translucent popover regressions — bg-*/NN + backdrop-blur outside header.
echo ""
echo "Checking for translucent popover regressions..."
TRANSLUCENT_HITS=$(files_to_check | xargs grep -nE 'bg-(popover|background)/[0-9]+[^"]*backdrop-blur' 2>/dev/null || true)
if [ -n "$TRANSLUCENT_HITS" ]; then
  echo "$TRANSLUCENT_HITS"
  echo ""
  echo "ERROR: Found translucent popover/dropdown. Popovers stay opaque; translucent variant is header-only." 1>&2
  FAIL=1
fi

if [ "$FAIL" -eq 1 ]; then
  exit 1
fi

echo ""
echo "Surface tokens enforced — no ad-hoc shadow/card-frame combos outside allowlist."
```

Make executable: `chmod +x scripts/check-surfaces.sh`.

Update package.json scripts to add:
  "check:surfaces": "bash scripts/check-surfaces.sh"
(Keep existing check:tokens; they complement each other.)

Run the guard. If it exits 0 on first try, Task 2 is a no-op check. If it surfaces hits, Task 2 closes them.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && test -x scripts/check-surfaces.sh && grep -q 'check:surfaces' package.json && bash scripts/check-surfaces.sh; echo "Exit: $?"</automated>
Expected eventually: Exit 0 (after Task 2 closes any hits). On initial run, may exit 1 with listed hits to remediate in Task 2.
  </verify>
  <done>
- scripts/check-surfaces.sh exists, is executable, matches the check-type-tokens.sh template
- package.json has the check:surfaces npm script alongside check:tokens
- Guard runs against src/ tree and reports violations or clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Close any DS-17 stragglers surfaced by the guard (cleanup sweep)</name>
  <files>depends on guard output — likely includes some of: src/components/table/table-footer.tsx, src/components/data-display.tsx, src/components/table/sort-dialog.tsx, src/components/views/view-item.tsx, src/components/cross-partner/matrix-bar-ranking.tsx, src/components/cross-partner/matrix-plain-table.tsx, src/components/cross-partner/matrix-heatmap.tsx</files>
  <action>
Run scripts/check-surfaces.sh. For EACH hit reported:

1. Read the hit's file:line to understand context.
2. Decide the fix category:
   - **Container is a primary surface** — migrate to semantic recipe:
       rounded-lg bg-surface-raised shadow-elevation-raised (or overlay/floating per level)
   - **Container is a CHIP/BADGE/PILL** — leave as-is but drop the shadow-sm/md (chips stay flat or on shadow-xs)
   - **Container is a FORM CONTROL (input, button, trigger)** — allowed to keep border + bg-background; these aren't primary surfaces. If the guard flags one and the allowlist (src/components/ui) already covers it, something is wrong with the EXCLUDE_DIRS — fix the guard.
   - **Legitimate exception** — add to EXCLUDE_FILES in the guard (with rationale comment), not inline.

3. Edit the file per the fix category. Do NOT add new shadow stacking; do NOT hand-roll opacity.

4. Re-run the guard. Repeat until exit 0.

Known candidate cleanup sites based on 28-RESEARCH:317-326 and likely Wave 2-3 deviations:
- table-footer.tsx: if 28-04 removed the outer card frame, table-footer's `border-t bg-card` may now float awkwardly. Resolution: drop bg-card, keep border-t (semantic top-edge between body and footer). No shadow.
- data-display.tsx:497: bare chart wrapper `shrink-0 px-2 pt-2 space-y-2` — if 28-05 wrapped charts individually, this wrapper is fine; if it relies on the old bg-card wrapper, align.
- matrix-*.tsx mini-chart shells (`bg-muted`): leave if they're small data-viz leaves, not primary surfaces. Guard shouldn't catch bg-muted alone; if it does, tune Check 2 regex.
- sort-dialog.tsx: if 28-06 chose the border-back-in fallback for nested cards, guard won't complain because border alone + no shadow passes.
- view-item.tsx:15: `rounded-md px-2 hover:bg-accent/50` — list-item hover, not a surface; passes guard.

If Task 2 modifies more than 5 files, that's a signal the sweep needed another plan. Document in SUMMARY and stop at a reasonable boundary — file a gap closure plan for the rest.

After all hits are closed, run `bash scripts/check-surfaces.sh` one final time and confirm exit 0.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && bash scripts/check-surfaces.sh</automated>
Expected: exit 0, final echo line "Surface tokens enforced ...". Also `npm run build` still passes.
  </verify>
  <done>
- scripts/check-surfaces.sh exits 0 on the current tree
- Any container surfaced by the guard has been migrated (or added to EXCLUDE_FILES with comment)
- No new shadow stacking
- Build still green
  </done>
</task>

</tasks>

<verification>
- scripts/check-surfaces.sh exists and is executable (chmod +x applied)
- package.json "check:surfaces" script entry exists
- bash scripts/check-surfaces.sh exits 0
- npm run build passes
- grep count of raw shadow-sm|md|lg|xl|2xl in src/ (excluding allowlist) == 0
- grep count of rounded+border+bg-(card|popover|background) in src/ (excluding allowlist) == 0
- grep count of bg-(popover|background)/\d+.*backdrop-blur in src/ (excluding header) == 0
</verification>

<success_criteria>
- DS-17 satisfied: all surface treatments consistent; no remaining 3-different-card-style regressions
- Enforcement guard runs <1s on src/ tree
- npm run check:surfaces passes on clean tree
- CI wiring (Vercel build step / pre-commit hook / GitHub Actions) is a one-line user-owned change — plan DOES NOT enable it (same boundary Phase 27-06 respected per STATE.md)
- Phase 28 can be CLOSED after this plan ships
</success_criteria>

<output>
Create `.planning/phases/28-surfaces-and-elevation/28-08-SUMMARY.md` documenting:
- Final scripts/check-surfaces.sh contents (or link to final version)
- List of files modified in Task 2 (cleanup sweep) with rationale for each
- Any files added to EXCLUDE_FILES with comment explaining why
- Final guard output (clean run)
- Decision record: CI wiring deferred to user (one-line change), matching Phase 27-06 precedent
- Phase 28 close-out signal: DS-11 through DS-17 all now have at least one plan's implementation + the guard preventing regression
</output>
