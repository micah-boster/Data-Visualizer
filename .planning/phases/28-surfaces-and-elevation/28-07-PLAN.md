---
phase: 28-surfaces-and-elevation
plan: 07
type: execute
wave: 3
depends_on: ["28-01", "28-02"]
files_modified:
  - src/components/layout/app-sidebar.tsx
  - src/app/layout.tsx
  - src/components/ui/sidebar.tsx
autonomous: false
requirements: [DS-16]

must_haves:
  truths:
    - "Sidebar renders on surface-base; main content renders on surface-raised — depth cue is bg differential only"
    - "No right-edge shadow on sidebar; no vertical divider line"
    - "Main content area has no outer shadow wrapper (SidebarInset's md:shadow-sm removed)"
    - "Header, sidebar, and content form a clean three-surface composition: chrome (translucent over raised), ground (base), floating content (raised)"
  artifacts:
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Sidebar no longer using variant=\"inset\" (Option B per research recommendation)"
    - path: "src/app/layout.tsx"
      provides: "<main> element directly on bg-surface-raised"
      contains: "bg-surface-raised"
    - path: "src/components/ui/sidebar.tsx"
      provides: "SidebarInset inset-variant styling cleaned (shadow-sm dropped per CONTEXT)"
  key_links:
    - from: "src/components/layout/app-sidebar.tsx"
      to: "Sidebar component without variant=\"inset\""
      via: "Sidebar collapsible=\"icon\" (no variant prop)"
      pattern: "<Sidebar collapsible"
    - from: "src/app/layout.tsx"
      to: "bg-surface-raised on main"
      via: "<main className> edit"
      pattern: "<main[^>]*bg-surface-raised"
    - from: "src/components/ui/sidebar.tsx"
      to: "SidebarInset inset variant without shadow-sm"
      via: "line ~310 className edit"
      pattern: "peer-data-\\[variant=inset\\]"
---

<objective>
Implement Option B from 28-RESEARCH Pitfall 6 / Open Question 1: switch `app-sidebar.tsx` OFF `variant="inset"`, apply `bg-surface-raised` directly to the `<main>` element in `layout.tsx`, and clean the now-unused shadow-sm from the SidebarInset inset variant in `ui/sidebar.tsx`. This gives the CONTEXT-locked "content floats on chrome rail" three-surface composition: sidebar = base (ground), main content = raised (floating), header = translucent-over-raised (glass chrome).

Purpose: shadcn's `variant="inset"` wraps main content in `md:rounded-xl md:shadow-sm` — solves a different architectural problem (rounded-corner inset panel) than what CONTEXT wants (pure bg-differential depth, no shadow). Option B fully honors CONTEXT's "separation mechanism: background differential only." Option A (keep variant=inset but drop shadow-sm) preserves rounded corners but feels like a nested panel, contradicting CONTEXT's "sidebar-as-ground" metaphor. Research recommendation is Option B unless pilot visual testing prefers Option A.

Output: Clean three-surface composition. Header glass sits above content-raised sits beside sidebar-base. No rounded-corner panel wrapping main. No drop shadow between sidebar and content.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-surfaces-and-elevation/28-CONTEXT.md
@.planning/phases/28-surfaces-and-elevation/28-RESEARCH.md
@.planning/phases/28-surfaces-and-elevation/28-01-PLAN.md
@.planning/phases/28-surfaces-and-elevation/28-02-PLAN.md

@src/components/layout/app-sidebar.tsx
@src/app/layout.tsx
@src/components/ui/sidebar.tsx

<interfaces>
Current state (28-RESEARCH):
  app-sidebar.tsx:34 — `<Sidebar variant="inset">`
  layout.tsx:42-47:
    <SidebarInset>
      <Header />
      <main className="flex-1 overflow-x-hidden p-2 md:p-3">{children}</main>
    </SidebarInset>
  ui/sidebar.tsx:210 — default Sidebar branch uses bg-sidebar which Phase 26 mapped to var(--surface-base)
  ui/sidebar.tsx:245 — floating variant (unused by app): rounded-lg shadow-sm ring-1
  ui/sidebar.tsx:310 — SidebarInset: md:peer-data-[variant=inset]:m-2 md:peer-data-[variant=inset]:rounded-xl md:peer-data-[variant=inset]:shadow-sm

Option B edits (28-RESEARCH Example 6, research recommendation):
  1. app-sidebar.tsx:34
       <Sidebar variant="inset">  ->  <Sidebar collapsible="icon">
     (drop variant prop; collapsible stays the same as today)

  2. layout.tsx:44
       <main className="flex-1 overflow-x-hidden p-2 md:p-3">
     ->
       <main className="flex-1 overflow-x-hidden p-2 md:p-3 bg-surface-raised">

  3. ui/sidebar.tsx:310 SidebarInset
       drop the three inset-variant utility classes:
         md:peer-data-[variant=inset]:m-2
         md:peer-data-[variant=inset]:rounded-xl
         md:peer-data-[variant=inset]:shadow-sm
     After Option B, SidebarInset is a pass-through main wrapper — keep any non-inset
     layout classes (flex, flex-col, w-full, etc.) unchanged.

Interaction with 28-02:
  Header has `bg-surface-translucent` (color-mix of surface-raised at 80%/82%). When content
  scrolls under the header, the blurred bg-surface-raised content is visible. Header is
  distinct because of transparency + rim highlight + blur, not bg-color contrast. This plan
  adding bg-surface-raised to <main> preserves the "content floats on chrome rail" metaphor.
  If pilot visual testing in both modes shows the header visually merges with the content
  (Pitfall 7), revisit the header translucent opacity in a 28-02 gap closure, not here.

Option A fallback (documented, not executed by default):
  If Option B reads wrong visually, the pilot SUMMARY may recommend flipping to Option A:
    - Keep variant="inset" in app-sidebar.tsx
    - Edit ui/sidebar.tsx:310 to drop only `md:shadow-sm` (keep rounded-xl + m-2)
    - layout.tsx:44 main already gets rounded-xl via SidebarInset; no bg-surface-raised needed
  If switching to Option A, the plan's checkpoint becomes the decision point.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch sidebar off variant="inset" (Option B primary edit)</name>
  <files>src/components/layout/app-sidebar.tsx</files>
  <action>
Read app-sidebar.tsx. Locate the `<Sidebar>` element currently at line ~34 with `variant="inset"`.

Edit: remove only the `variant="inset"` prop. Preserve `collapsible="icon"` and any other props.

After edit:
  <Sidebar collapsible="icon">
    {/* existing children unchanged */}
  </Sidebar>

The default Sidebar branch in ui/sidebar.tsx:210 uses `bg-sidebar` which Phase 26-01 re-mapped to `var(--surface-base)`. No additional surface class needed on the Sidebar itself — the surface-base token carries the "ground" cue per CONTEXT.

Do not modify children (sidebar content, navigation items, saved views list, etc.).
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && ! grep -q 'variant="inset"' src/components/layout/app-sidebar.tsx && grep -q '<Sidebar' src/components/layout/app-sidebar.tsx && echo OK</automated>
Then `npm run build` passes.
  </verify>
  <done>
- variant="inset" removed from app-sidebar.tsx
- Sidebar component still renders (grep confirms presence)
- collapsible="icon" preserved
- Children untouched
- Build green
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply bg-surface-raised to main + clean SidebarInset inset-variant classes</name>
  <files>src/app/layout.tsx, src/components/ui/sidebar.tsx</files>
  <action>
Read both files.

In layout.tsx (line ~44 per research; locate via grep for `<main`):
- Find the <main> element currently: `<main className="flex-1 overflow-x-hidden p-2 md:p-3">`
- Update to: `<main className="flex-1 overflow-x-hidden p-2 md:p-3 bg-surface-raised">`
- Preserve any ref, id, role attributes.

In ui/sidebar.tsx (line ~310):
- Locate the SidebarInset component's className string that contains `peer-data-[variant=inset]` utilities.
- Remove the three inset-variant classes:
    md:peer-data-[variant=inset]:m-2
    md:peer-data-[variant=inset]:rounded-xl
    md:peer-data-[variant=inset]:shadow-sm
- Keep non-inset-variant classes in the SidebarInset className (flex layout, data attrs, etc.).
- Note: leaving these utilities in would be a NO-OP since variant=inset is no longer applied (Task 1 dropped it), but cleaning them removes dead code and prevents accidental re-application.

Also spot-check ui/sidebar.tsx:245 (floating variant, unused per 28-RESEARCH):
- If `group-data-[variant=floating]:shadow-sm group-data-[variant=floating]:ring-1` remains dead code, LEAVE IT. Cleanup of unused variants is 28-08's cleanup-sweep territory, not this plan's scope.

Do not rename any exported component; do not change component APIs.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DATA VISUALIZER" && grep -E '<main[^>]*bg-surface-raised' src/app/layout.tsx && ! grep -q 'peer-data-\[variant=inset\]:shadow-sm' src/components/ui/sidebar.tsx && echo OK</automated>
Then `npm run build` passes.
  </verify>
  <done>
- <main> in layout.tsx has bg-surface-raised applied
- SidebarInset inset-variant utilities (m-2, rounded-xl, shadow-sm) removed
- Floating variant left alone (dead code cleanup out of scope)
- Build green
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — three-surface composition</name>
  <action>Confirm sidebar (base) + content (raised) + header (translucent) form a clean three-surface composition in both light and dark modes. See how-to-verify below.</action>
  <what-built>Three-surface composition per Option B: sidebar on surface-base (ground), main content on surface-raised (floating), header translucent-over-raised (glass). Sidebar no longer uses variant="inset"; main content no longer has rounded-corner panel wrapper or shadow-sm.</what-built>
  <how-to-verify>
1. `npm run dev`. Open the dashboard at desktop viewport (sidebar visible).
2. In light mode:
   - Sidebar reads as the "ground" layer (slightly warm/beige surface-base).
   - Main content is visibly brighter/lighter (surface-raised, Phase 26's conventional "raised-is-lighter" in light mode).
   - Header is translucent — content scrolling underneath is visible through the glass.
   - There is NO rounded-corner panel wrapping the main content.
   - There is NO visible shadow between sidebar and content (pure bg-differential depth).
3. In dark mode:
   - Sidebar reads darker (surface-base darker in dark mode per Phase 26 inversion).
   - Main content is lighter than sidebar (surface-raised is lighter than surface-base in dark mode, per Phase 26-01 intentional inversion).
   - Header's translucency + rim highlight still separates it from content (Pitfall 7 check).
4. Collapse the sidebar to icon mode. Confirm:
   - Transition still works (collapsible="icon" preserved).
   - Main content reflows; bg-surface-raised continues across the new space.
5. Resize to mobile viewport; confirm no layout regressions.

If the three-surface composition reads wrong (e.g., content blends into sidebar in dark mode, or the loss of rounded corners on main feels unfinished), describe it. The SUMMARY may recommend a Plan 28-08 follow-up or an Option A switch.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues observed.</resume-signal>
</task>

</tasks>

<verification>
- grep 'variant="inset"' src/components/layout/app-sidebar.tsx returns 0
- grep 'bg-surface-raised' src/app/layout.tsx >= 1 (on <main>)
- grep 'peer-data-\[variant=inset\]:shadow-sm' src/components/ui/sidebar.tsx returns 0
- npm run build passes
- Three-surface user verification complete in both modes
</verification>

<success_criteria>
- DS-16 satisfied: sidebar + content depth separation is bg-differential only, no shadow, no divider
- Content pane reads as floating on chrome (CONTEXT metaphor)
- Header glass integration preserved from 28-02
- No rounded-corner panel around main content (Option B aesthetic)
- If visual testing pushes back, Option A switch path documented in SUMMARY for a follow-up plan
</success_criteria>

<output>
Create `.planning/phases/28-surfaces-and-elevation/28-07-SUMMARY.md` documenting:
- Confirmation Option B was chosen (or deviation to Option A with rationale)
- Final app-sidebar.tsx Sidebar JSX snippet
- Final <main> className in layout.tsx
- SidebarInset className before/after
- Both-mode three-surface visual verification notes (explicit attention to Pitfall 7)
- Any Deviations: e.g., header blends with content in dark mode -> flagged for 28-01/28-02 gap closure
- Any Deviations: e.g., sidebar-base looks too close to content-raised in dark mode -> note for Phase 31 polish
</output>
