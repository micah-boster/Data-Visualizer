---
phase: 33-accessibility-audit
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - src/components/toolbar/unified-toolbar.tsx
  - src/components/toolbar/save-view-popover.tsx
  - src/components/toolbar/filter-popover.tsx
  - src/components/anomaly/anomaly-toolbar-trigger.tsx
  - src/components/columns/sort-dialog.tsx
  - src/components/layout/app-sidebar.tsx
  - src/components/navigation/breadcrumb-trail.tsx
  - src/components/charts/collection-curve-chart.tsx
  - src/components/cross-partner/trajectory-chart.tsx
  - src/components/cross-partner/comparison-matrix.tsx
  - src/components/charts/root-sparkline.tsx
  - src/components/query/query-command-dialog.tsx
  - src/components/table/data-table.tsx
  - src/components/table/draggable-header.tsx
  - tests/a11y/axe-baseline.spec.ts
requirements:
  - A11Y-02
autonomous: true

must_haves:
  truths:
    - "Every icon-only <Button size=\"icon\"> in src/ carries an aria-label (or sr-only span, or visible text); tooltips are no longer the only accessible name"
    - "All four chart wrappers (CollectionCurveChart, CrossPartnerTrajectoryChart, PartnerComparisonMatrix, RootSparkline) expose role=\"img\" + aria-label describing the chart"
    - "Toolbar toggle buttons (charts visibility, heatmap on/off) expose aria-pressed reflecting current state"
    - "Breadcrumb active segment carries aria-current=\"page\"; sidebar active saved view carries aria-current=\"page\""
    - "Sortable column headers expose aria-sort (ascending / descending / none)"
    - "Native <select> in sort-dialog carries aria-label; checkboxes and inputs have programmatic labels"
    - "axe-core rules button-name / link-name / aria-required-attr / role-img-alt / aria-valid-attr-value are all passing on the four core routes × both themes"
  artifacts:
    - path: "src/components/toolbar/unified-toolbar.tsx"
      provides: "All 6+ icon-only toolbar buttons (query, charts-toggle, columns, sort, export, filter) with aria-label + aria-pressed where applicable"
      contains: "aria-label"
    - path: "src/components/charts/collection-curve-chart.tsx"
      provides: "ChartContainer with role=\"img\" aria-label describing chart content"
      contains: "aria-label"
    - path: "src/components/navigation/breadcrumb-trail.tsx"
      provides: "Active segment marked aria-current=\"page\""
      contains: "aria-current"
    - path: "src/components/table/data-table.tsx"
      provides: "Sortable column <th> elements expose aria-sort"
      contains: "aria-sort"
    - path: "tests/a11y/axe-baseline.spec.ts"
      provides: "Plan 01 fixme markers for ARIA-category rules REMOVED; suite runs these assertions live"
  key_links:
    - from: "src/components/toolbar/unified-toolbar.tsx"
      to: "Plan 01 baseline.json button-name rule"
      via: "Each icon-only Button gets aria-label; Tooltip stays for sighted users"
      pattern: "aria-label="
    - from: "src/components/charts/**"
      to: "axe-core role-img-alt / svg-img-alt rules"
      via: "ChartContainer wrapper carries role=\"img\" aria-label"
      pattern: "role=\"img\""
    - from: "tests/a11y/axe-baseline.spec.ts"
      to: "package.json scripts.check:a11y"
      via: "After ARIA sweep, rerun `npm run check:a11y` → ARIA-category rules are zero; fixme markers for those rules removed"
---

<objective>
Close the ARIA-label, role, and state-attribute gaps surfaced by the Plan 01 baseline. This is the highest-volume, most mechanical plan in the phase — icon-only buttons alone are expected to account for ~30+ baseline hits (RESEARCH §Current A11Y State + Pitfall 5).

Purpose: axe-core's `button-name`, `link-name`, `role-img-alt`, `aria-valid-attr-value`, and `aria-required-attr` rules all fire on missing or incorrect accessible names / roles / state. Plan 02 resolves them at the markup layer without touching focus logic (Plan 03) or token contrast (Plan 04).

Output:
- Every icon-only `<Button size="icon">` in the allowlisted files below carries an `aria-label` (tooltip coexists for sighted users)
- Four chart wrappers carry `role="img" aria-label="..."`
- Toggle buttons carry `aria-pressed`
- Breadcrumb + sidebar active items carry `aria-current="page"`
- Sortable column headers carry `aria-sort`
- Sort-dialog's native `<select>` carries `aria-label`; its up/down/remove buttons carry labels
- Plan 01 fixme markers for ARIA-category rules are removed from `tests/a11y/axe-baseline.spec.ts`
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-accessibility-audit/33-CONTEXT.md
@.planning/phases/33-accessibility-audit/33-RESEARCH.md
@.planning/phases/33-accessibility-audit/33-01-SUMMARY.md
@.planning/phases/33-accessibility-audit/baseline.json
@AGENTS.md

<!-- Key interface references -->

From 33-RESEARCH.md §Current A11Y State — icon-only buttons WITHOUT aria-label (tooltip only):
- src/components/toolbar/unified-toolbar.tsx:141 (Sparkles query trigger)
- src/components/toolbar/unified-toolbar.tsx:156 (charts toggle — ALSO needs aria-pressed)
- src/components/toolbar/unified-toolbar.tsx:188 (columns)
- src/components/toolbar/unified-toolbar.tsx:205 (sort trigger)
- src/components/toolbar/unified-toolbar.tsx:247 (export)
- src/components/toolbar/save-view-popover.tsx:71 (save view trigger)
- src/components/anomaly/anomaly-toolbar-trigger.tsx:52 (anomaly trigger)
- src/components/columns/sort-dialog.tsx:154 (move up), :161 (move down), :170 (remove sort)
- src/components/layout/app-sidebar.tsx (SidebarMenuAction icons for edit/delete view/list)

From 33-RESEARCH.md §Recharts pattern:
```tsx
<ChartContainer role="img" aria-label={`Collection curves for ${partnerName}: recovery rate vs. collection month, ${curves.length} batches`}>
  <LineChart data={pivotedData}>...</LineChart>
</ChartContainer>
```

From AGENTS.md type-token rule:
- Any className touched must honor the 6 named type tokens. `sr-only` is allow-listed. Do NOT introduce raw text-xs/text-sm.

From Recharts 3.8.0 (verified node_modules/recharts/types/chart/CartesianChart.d.ts):
- accessibilityLayer?: boolean — RESEARCH Open Q#4 flagged for user decision.
  Decision for Plan 02: enable `accessibilityLayer={true}` on CollectionCurveChart ONLY as a pilot.
  If Plan 02 human-verify shows no axe regression and no visible UI change, keep.
  If any issue surfaces, remove in a follow-up edit and document in SUMMARY.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Icon-only button aria-label sweep (toolbar + sidebar + sort-dialog)</name>
  <files>
    src/components/toolbar/unified-toolbar.tsx
    src/components/toolbar/save-view-popover.tsx
    src/components/toolbar/filter-popover.tsx
    src/components/anomaly/anomaly-toolbar-trigger.tsx
    src/components/columns/sort-dialog.tsx
    src/components/layout/app-sidebar.tsx
  </files>
  <action>
For each icon-only `<Button size="icon">` (or raw `<button>` with only an icon child) in the listed files, add an `aria-label` describing the action. Keep existing Tooltip wrappers — aria-label + tooltip coexist (tooltip for sighted, aria-label for SR).

Concrete sites and copy (from RESEARCH §Current A11Y State + 33-RESEARCH.md §Pitfall 5):

1. **unified-toolbar.tsx**
   - `:141` Sparkles query → `aria-label="Ask a question about your data"`
   - `:156` Charts toggle → `aria-label="Toggle charts"` + `aria-pressed={chartsVisible}` (pass through existing state prop)
   - `:188` Columns → `aria-label="Manage columns"`
   - `:205` Sort trigger → `aria-label="Manage sort order"` (`aria-pressed` NOT applicable — it opens a dialog, not a toggle)
   - `:247` Export → `aria-label="Export data"`

2. **save-view-popover.tsx:71** → `aria-label="Save current view"`

3. **filter-popover.tsx** — review trigger button for same pattern; `aria-label="Manage filters"` or similar.

4. **anomaly-toolbar-trigger.tsx:52** → `aria-label={`Show anomalies (${count})`}` — interpolate current anomaly count into the label for SR user context. Mark badge span `aria-hidden="true"` since its content is already in the label.

5. **sort-dialog.tsx**:
   - `:154` Move up → `aria-label="Move sort up"`
   - `:161` Move down → `aria-label="Move sort down"`
   - `:170` Remove sort → `aria-label={`Remove sort on ${columnName}`}`
   - Native `<select>` on `:128` → add `aria-label="Sort column"` (or wire `<Label htmlFor>` if easier)

6. **app-sidebar.tsx** (SidebarMenuAction icon children for saved views + partner lists):
   - Edit view: `aria-label={`Edit saved view ${view.name}`}`
   - Delete view: `aria-label={`Delete saved view ${view.name}`}`
   - Edit list: `aria-label={`Edit partner list ${list.name}`}`
   - Delete list: `aria-label={`Delete partner list ${list.name}`}`
   - Active sidebar items (current view / current list): also add `aria-current="page"` on the SidebarMenuButton when `isActive`.

**Pitfall 8 (type-token compliance):** If any aria-label edit happens alongside a className change, ensure no raw text-xs/text-sm is introduced. `sr-only` allowed. Run `npm run check:tokens` after this task before committing.

**Do NOT** wrap visible text children in sr-only — if the button has a text label visible on screen, aria-label is redundant and in some cases wrong (label override). Only add aria-label where the button is ICON-ONLY.
  </action>
  <verify>
    <automated>npm run check:tokens && npm run check:a11y 2>&1 | tee /tmp/a11y-task1.log && ! grep -E "button-name|aria-required-attr" /tmp/a11y-task1.log</automated>
  </verify>
  <done>All listed icon buttons have aria-label; type-token guard green; axe-core button-name + aria-required-attr rules zero critical/serious on core routes</done>
</task>

<task type="auto">
  <name>Task 2: Chart aria-label + role="img" + toggle/sort/current state attributes</name>
  <files>
    src/components/charts/collection-curve-chart.tsx
    src/components/cross-partner/trajectory-chart.tsx
    src/components/cross-partner/comparison-matrix.tsx
    src/components/charts/root-sparkline.tsx
    src/components/navigation/breadcrumb-trail.tsx
    src/components/table/data-table.tsx
    src/components/table/draggable-header.tsx
  </files>
  <action>
1. **Chart aria-label + role="img"** — wrap each chart's top-level `<ChartContainer>` or the containing `<div>`:
   - `collection-curve-chart.tsx` → `role="img" aria-label={`Collection curves for ${partnerName || 'all partners'}: recovery rate over months on book (${curves.length} batches)`}`
     - **Optional pilot**: set `accessibilityLayer={true}` on the `<LineChart>` inside (RESEARCH Open Q#4).
     - Rerun check:a11y. If no new violations AND no visible UI regression, KEEP.
     - If any issue surfaces, remove and document in 33-02-SUMMARY.md.
   - `trajectory-chart.tsx` → `role="img" aria-label={`Cross-partner trajectory: ${metricLabel} across batches (${rankedPartners.length} partners)`}`
   - `comparison-matrix.tsx` → `role="img" aria-label={`Partner comparison matrix: ${partners.length} partners × ${metrics.length} metrics`}`
   - `root-sparkline.tsx` → if decorative-only with sibling data representation, mark `aria-hidden="true"`. If it's the only representation of its data, `role="img" aria-label="..."`. Pick one based on current behavior — the sibling data table covers the data contract so `aria-hidden="true"` on decorative sparklines is acceptable and simpler.

2. **Breadcrumb active segment** (`breadcrumb-trail.tsx:72-79` span for current-segment):
   - Add `aria-current="page"` to the current segment's rendered element (the one without an `<a href>` / `onClick`).
   - Add `data-breadcrumb-current` data attribute on the active segment's element — Plan 03 uses this selector for focus restoration after drill.

3. **Table aria-sort** (`data-table.tsx` + `draggable-header.tsx`):
   - For every sortable `<th>`, add `aria-sort={sortState === 'asc' ? 'ascending' : sortState === 'desc' ? 'descending' : 'none'}` keyed off the existing TanStack sort state.
   - Non-sortable columns: `aria-sort="none"` OR omit. Be consistent — don't mix.

4. **Sidebar active saved view** (already covered in Task 1 for sidebar-menu; double-check coverage is complete including partner-lists-sidebar-group if touched).

5. **aria-pressed on charts toggle** — already covered in Task 1 (`unified-toolbar.tsx:156`).

6. Remove any `test.fixme` markers in `tests/a11y/axe-baseline.spec.ts` whose annotation pointed at a rule in categories: button-name, link-name, role-img-alt, aria-required-attr, aria-valid-attr-value, aria-valid-attr. Keep fixme markers for focus-order, color-contrast (those are Plans 03 / 04).
  </action>
  <verify>
    <automated>npm run check:tokens && npm run check:a11y 2>&1 | tee /tmp/a11y-task2.log && ! grep -E "role-img-alt|aria-current|aria-pressed" /tmp/a11y-task2.log</automated>
  </verify>
  <done>Four charts carry role=\"img\" + aria-label (or aria-hidden if decorative); breadcrumb uses aria-current=\"page\" + data-breadcrumb-current; sortable columns expose aria-sort; ARIA-category fixme markers removed from axe-baseline.spec.ts</done>
</task>

<task type="auto">
  <name>Task 3: Dialog/Sheet modal verification + input label + skip-to-content link</name>
  <files>
    src/components/query/query-command-dialog.tsx
    src/components/columns/sort-dialog.tsx
    src/components/ui/sheet.tsx
    src/app/layout.tsx
  </files>
  <action>
1. **Dialog/Sheet `modal` prop verification** (prevents Plan 03 dependency — sets the DOM state axe audits):
   - `query-command-dialog.tsx:42` `DialogPrimitive.Root` → add explicit `modal={true}` prop. Inline comment: "A11Y-03: focus trap contract (Base UI Dialog modal)".
   - `ui/sheet.tsx` — confirm the root `DialogPrimitive.Root` used internally passes `modal={true}` by default or explicitly. If the shadcn Sheet wrapper doesn't pass it through, add an internal `modal={true}` default.
   - `sort-dialog.tsx` and other Sheet consumers: no edit needed if the Sheet root propagates modal. Verify by reading one consumer.
   - **Reasoning:** RESEARCH Pitfall 4 — Base UI default is NOT officially locked; setting it explicitly removes ambiguity for Plan 03's focus-trap assertions.

2. **`<select>` + input labels** (covered some in Task 1 sort-dialog; sweep the rest):
   - `sort-dialog.tsx` native `<select>` → aria-label (already in Task 1)
   - `save-view-popover.tsx` Input for view name → verify `<Label htmlFor>` association OR add aria-label
   - `create-list-dialog.tsx` (via partner-lists/attribute-filter-bar.tsx) checkbox → verify each has an associated `<Label>` or aria-label. If gaps exist, add.
   - `AttributeFilterBar` multi-select composed with Popover+Checkbox — add aria-label to each Checkbox inside the popover listing.

3. **Skip-to-content link** (optional, RESEARCH §global chrome — axe `bypass` rule lands MODERATE, so below our cutoff, but easy win):
   - Add in `src/app/layout.tsx` inside `<body>` before the `<main>`:
```tsx
<a
  href="#main"
  className="sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-4 focus:z-50 focus-glow bg-surface-raised px-inline py-inline rounded-md text-body"
>
  Skip to content
</a>
```
   - Ensure `<main>` has `id="main"`.
   - This uses sr-only + focus-visible utility reveal; honors type-token rule (text-body, px-inline, rounded-md from semantic spacing).

4. Final sweep: `npm run check:a11y`. Expect Plan 02's ARIA category rules all green. Focus + contrast categories still showing fixme'd tests — those are Plan 03 + 04.

5. Commit. 33-02-SUMMARY.md should enumerate:
   - Total icon buttons relabeled (from baseline count)
   - `accessibilityLayer` pilot decision (kept / reverted)
   - Any surprising gaps found during sweep
   - axe rule-level delta: before (from baseline.json) vs after
  </action>
  <verify>
    <automated>npm run check:tokens && npm run check:a11y 2>&1 | tee /tmp/a11y-task3.log && ! grep -E "label|bypass" /tmp/a11y-task3.log</automated>
  </verify>
  <done>Dialog/Sheet consumers set modal={true} explicitly; inputs/selects have programmatic labels; skip-to-content link present in layout; ARIA-category rules zero on check:a11y; Plan 02 fixme markers for ARIA rules removed</done>
</task>

</tasks>

<verification>
1. `npm run check:a11y` — axe violations in the button-name / link-name / role-img-alt / aria-required-attr / aria-valid-attr-value / aria-pressed / aria-current / aria-sort categories are zero on all 4 core routes × 2 themes.
2. `npm run check:tokens` — green (no raw text-xs/text-sm introduced).
3. `npm run check:surfaces`, `check:components`, `check:motion`, `check:polish` — all still green (Plan 02 changes are ARIA markup, not surface/typography).
4. Sighted smoke: keyboard-focus the 6+ previously-unlabeled toolbar buttons with VoiceOver (or macOS Accessibility Inspector) → each announces its action clearly.
5. Chart wrapper `role="img"` + aria-label present on all 4 chart types; sibling data table still serves as data source for SR users (CONTEXT contract preserved).
</verification>

<success_criteria>
- A11Y-02 fully ratified: every interactive element in the audited scope exposes correct ARIA labels/roles; axe-core reports zero critical/serious hits in ARIA rule categories on 4 routes × 2 themes
- Tooltip-only labeling antipattern (RESEARCH Pitfall 5) fully eliminated — Tooltip coexists with aria-label, doesn't replace it
- Toggle buttons (charts visibility) expose aria-pressed; breadcrumb + sidebar active items expose aria-current; sortable columns expose aria-sort
- accessibilityLayer pilot decision documented (kept / reverted)
- Plan 03's focus-management work lands on a correctly-labeled DOM (precondition satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/33-accessibility-audit/33-02-SUMMARY.md` covering: total aria-label additions by file, accessibilityLayer pilot outcome, rule-level delta vs Plan 01 baseline.json, any out-of-scope gaps found (to park in deferred ideas).
</output>
