---
phase: 02-core-table-and-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/columns/config.ts
  - src/lib/columns/presets.ts
  - src/lib/columns/widths.ts
  - src/lib/columns/definitions.ts
  - src/hooks/use-data.ts
  - src/app/api/data/route.ts
  - src/types/data.ts
  - package.json
autonomous: true
requirements: [TABL-01, TABL-06]

must_haves:
  truths:
    - "All 61 Snowflake columns are defined in the column config system with correct types and labels"
    - "Column presets (Finance, Outreach, All) are defined with curated 8-12 column subsets"
    - "Identity columns (partner, batch, account type, dates) appear in every preset"
    - "API fetches all columns in a single request regardless of which preset is active"
    - "TanStack Table and TanStack Virtual packages are installed"
  artifacts:
    - path: "src/lib/columns/config.ts"
      provides: "Expanded ColumnConfig with all 61 columns, types, and labels"
      contains: "COLUMN_CONFIGS"
    - path: "src/lib/columns/presets.ts"
      provides: "Named column preset definitions (finance, outreach, all)"
      exports: ["PRESETS", "DEFAULT_PRESET"]
    - path: "src/lib/columns/widths.ts"
      provides: "Default column widths by data type"
      exports: ["WIDTH_BY_TYPE"]
    - path: "src/lib/columns/definitions.ts"
      provides: "TanStack Table ColumnDef[] builder from COLUMN_CONFIGS"
      exports: ["buildColumnDefs"]
    - path: "src/hooks/use-data.ts"
      provides: "Updated hook that fetches all columns"
  key_links:
    - from: "src/lib/columns/presets.ts"
      to: "src/lib/columns/config.ts"
      via: "imports COLUMN_CONFIGS for column keys"
      pattern: "import.*COLUMN_CONFIGS.*config"
    - from: "src/lib/columns/definitions.ts"
      to: "src/lib/columns/config.ts"
      via: "builds ColumnDef from ColumnConfig"
      pattern: "ColumnDef.*ColumnConfig"
---

<objective>
Expand the column configuration system from 5 starter columns to all 61 Snowflake columns, define column presets (Finance, Outreach, All), create TanStack Table ColumnDef builders, and update the API/hook to always fetch all columns.

Purpose: The table component (Plan 02) needs column definitions, preset visibility maps, and full data to render. This plan creates all the data contracts.
Output: Complete column infrastructure, installed table dependencies, API returning all columns.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-table-and-performance/02-CONTEXT.md
@.planning/phases/02-core-table-and-performance/02-RESEARCH.md
@.planning/phases/01-setup-and-snowflake-infrastructure/01-02-SUMMARY.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/lib/columns/config.ts:
```typescript
export interface ColumnConfig {
  key: string;           // Snowflake column name (uppercase)
  label: string;         // Display name
  type: 'text' | 'currency' | 'percentage' | 'count' | 'date' | 'number';
  defaultVisible: boolean;
  nullDisplay: string;   // Always em dash
}
export const COLUMN_CONFIGS: ColumnConfig[];
export const DEFAULT_COLUMNS: string[];
export const ALLOWED_COLUMNS: Set<string>;
```

From src/types/data.ts:
```typescript
export interface DataResponse {
  data: Record<string, unknown>[];
  meta: {
    rowCount: number;
    fetchedAt: string;
    columns: string[];
  };
  schemaWarnings?: {
    missing: string[];
    unexpected: string[];
  };
}
```

From src/hooks/use-data.ts:
```typescript
export function useData(columns?: string[]): UseQueryResult<DataResponse>;
```

From src/app/api/data/route.ts:
```typescript
// Currently selects DEFAULT_COLUMNS (5 cols) when no columns param provided
// Needs to select ALL columns instead
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install TanStack Table + Virtual and expand column config to all 61 columns</name>
  <files>
    package.json
    src/lib/columns/config.ts
    src/lib/columns/presets.ts
    src/lib/columns/widths.ts
  </files>
  <action>
1. Install dependencies:
   ```bash
   pnpm add @tanstack/react-table @tanstack/react-virtual
   ```

2. Expand `src/lib/columns/config.ts` to define all 61 columns from the Snowflake `agg_batch_performance_summary` table. Since the exact schema is unknown, the executor MUST first discover the full column list by one of:
   - Running the app locally and checking the schema validation warnings (the `unexpected` array lists unconfigured columns)
   - Or querying the API endpoint and examining the `schemaWarnings.unexpected` field in the response
   - Or checking server logs for schema validator output

   For each discovered column, add a ColumnConfig entry with:
   - `key`: Exact Snowflake column name (UPPERCASE)
   - `label`: Human-readable label (e.g., "GROSS_COLLECTED" -> "Gross Collected", "CONTACT_RATE_PCT" -> "Contact Rate %")
   - `type`: Infer from column name -- columns with BALANCE/REVENUE/COLLECTED/FEE/COST -> 'currency', columns with RATE/PCT -> 'percentage', columns with COUNT/NUM -> 'count', columns with DATE -> 'date', columns with NAME/TYPE/STATUS -> 'text', otherwise 'number'
   - `defaultVisible`: false for all new columns (presets handle visibility)
   - `nullDisplay`: '\u2014' (em dash) for all

   Add an `identity` boolean field to ColumnConfig for columns that should appear in every preset (PARTNER_NAME, BATCH_NAME, ACCOUNT_TYPE, and any date columns like BATCH_DATE or PLACEMENT_DATE). Update the interface accordingly.

   Keep ALLOWED_COLUMNS and DEFAULT_COLUMNS exports working (DEFAULT_COLUMNS can become all column keys since the API will fetch everything).

3. Create `src/lib/columns/widths.ts`:
   ```typescript
   export const WIDTH_BY_TYPE: Record<string, number> = {
     text: 180,
     currency: 130,
     percentage: 90,
     count: 100,
     date: 120,
     number: 110,
   };
   export const IDENTITY_WIDTH = 160; // Pinned identity columns
   ```

4. Create `src/lib/columns/presets.ts`:
   Define at least 3 named presets as `Record<string, VisibilityState>` (where VisibilityState is `Record<string, boolean>` from TanStack Table):
   - **Finance**: identity columns + placed balance, gross collected, net revenue, collection rate, and related financial metrics (8-12 total)
   - **Outreach**: identity columns + contact rates, payment counts, unique payers, penetration metrics (8-12 total)
   - **All**: every column visible

   Each preset MUST include all identity columns (where `identity: true` in config).
   Export: `PRESETS`, `PRESET_NAMES` (ordered array for tab bar), `DEFAULT_PRESET` (= 'finance').

   The executor should use domain judgment to assign columns to presets based on column names. Financial terms (balance, collected, revenue, fee, cost, rate when related to money) go to Finance. Outreach terms (contact, call, payment count, payer, penetration, attempt) go to Outreach.
  </action>
  <verify>
    pnpm exec tsc --noEmit && echo "TypeScript compiles" && node -e "const p = require('./package.json'); console.log('@tanstack/react-table:', p.dependencies['@tanstack/react-table']); console.log('@tanstack/react-virtual:', p.dependencies['@tanstack/react-virtual'])"
  </verify>
  <done>
    - @tanstack/react-table and @tanstack/react-virtual are in package.json dependencies
    - COLUMN_CONFIGS contains entries for all Snowflake columns (should be ~61, exact count depends on schema discovery)
    - 3 presets defined (Finance, Outreach, All) with 8-12 columns each (plus All with everything)
    - WIDTH_BY_TYPE maps each column type to a pixel width
    - TypeScript compiles with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ColumnDef builder and update API/hook to fetch all columns</name>
  <files>
    src/lib/columns/definitions.ts
    src/hooks/use-data.ts
    src/app/api/data/route.ts
    src/types/data.ts
  </files>
  <action>
1. Create `src/lib/columns/definitions.ts`:
   Build a function `buildColumnDefs()` that converts COLUMN_CONFIGS into TanStack Table `ColumnDef<Record<string, unknown>>[]`. For each ColumnConfig:
   - `id`: config.key
   - `accessorKey`: config.key
   - `header`: config.label
   - `size`: Use WIDTH_BY_TYPE[config.type] or IDENTITY_WIDTH for identity columns
   - `minSize`: 60
   - `maxSize`: 400
   - `enableSorting`: true
   - `meta`: { type: config.type, identity: config.identity } (for use by renderer)

   Do NOT add cell renderers yet (formatting is Phase 3). The default `flexRender` will show raw values.
   Export the built array as `columnDefs` (call buildColumnDefs at module scope) and export `buildColumnDefs` for testing.

2. Update `src/app/api/data/route.ts`:
   - When no `columns` param is provided (or `columns=*`), select ALL columns from ALLOWED_COLUMNS instead of just DEFAULT_COLUMNS. Change the fallback from `DEFAULT_COLUMNS` to `Array.from(ALLOWED_COLUMNS)`.
   - Keep the validation logic for explicit column requests.
   - This ensures the client gets all 61 columns in one fetch regardless of preset.

3. Update `src/hooks/use-data.ts`:
   - Remove the `columns` parameter entirely. The hook should always fetch all columns (no query param needed since the API defaults to all).
   - Simplify: just fetch `/api/data` with no params.
   - Keep the queryKey as `['data']` (no column dependency since we always fetch all).

4. Update `src/types/data.ts` if needed -- the DataResponse interface should remain the same since the shape doesn't change, only the number of columns returned.
  </action>
  <verify>
    pnpm exec tsc --noEmit && echo "TypeScript compiles"
  </verify>
  <done>
    - buildColumnDefs() produces a ColumnDef for every column in COLUMN_CONFIGS
    - API route returns all columns when no columns param is provided
    - useData() fetches all columns without requiring a columns argument
    - TypeScript compiles with no errors
  </done>
</task>

</tasks>

<verification>
1. `pnpm exec tsc --noEmit` -- no TypeScript errors
2. `pnpm build` -- production build succeeds
3. Column config has entries matching Snowflake schema (check COLUMN_CONFIGS.length >= 5, ideally ~61)
4. Presets reference only column keys that exist in COLUMN_CONFIGS
</verification>

<success_criteria>
- TanStack Table and Virtual installed
- All Snowflake columns configured with types and labels
- 3 column presets defined (Finance, Outreach, All)
- ColumnDef array ready for TanStack Table consumption
- API returns all columns by default
- useData() fetches without column parameter
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-table-and-performance/02-01-SUMMARY.md`
</output>
