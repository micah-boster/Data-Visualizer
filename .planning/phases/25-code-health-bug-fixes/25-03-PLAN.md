---
phase: 25-code-health-bug-fixes
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/components/data-display.tsx
  - src/components/table/data-table.tsx
  - src/components/filters/filter-empty-state.tsx
  - docs/KNOWN-ISSUES.md
autonomous: false
requirements: [HEALTH-01]

must_haves:
  truths:
    - "Setting a dimension filter at root (e.g. PARTNER_NAME or ACCOUNT_TYPE) reduces the number of partner rows displayed in the table"
    - "With a root-level filter active, the chart and KPIs reflect only the filtered subset (filter-before-aggregate)"
    - "Drilling into a partner while a root filter is active preserves the filter as a persistent constraint on downstream computations"
    - "When the filter matches zero rows, the table shows the standard empty state with a 'No rows match the filter' hint and a one-click 'Clear filter' action"
    - "KI-07 is marked Resolved in docs/KNOWN-ISSUES.md"
  artifacts:
    - path: "src/components/data-display.tsx"
      provides: "filteredRawData memo threaded through summary rows, CrossPartnerProvider, sparklines, and KPIs"
      contains: "filteredRawData"
    - path: "src/components/filters/filter-empty-state.tsx"
      provides: "Empty state with 'No rows match the filter' hint and Clear filter action"
      contains: "Clear filter"
  key_links:
    - from: "src/components/data-display.tsx"
      to: "buildPartnerSummaryRows"
      via: "filteredRawData memo"
      pattern: "buildPartnerSummaryRows\\(filtered"
    - from: "rootSummaryRows (empty due to filter)"
      to: "FilterEmptyState render"
      via: "guard before table render at root"
      pattern: "FilterEmptyState"
---

<objective>
Plan C (behavior change): Fix the v2 root-level dimension filter bug. Apply filters UPSTREAM of aggregation so table rows, chart data, and KPIs all reflect the filtered dataset consistently. Preserve the filter as a persistent constraint when drilling into partners.

Purpose: Close HEALTH-01 / KI-07 — the v2 regression that the user specifically called out as a daily-driver pain point.
Output: `filteredRawData` memo in `data-display.tsx` threaded through ~5 downstream consumers; empty-state handling when filter matches zero rows; KI-07 closed.

**IMPORTANT — pre-execution checkpoint on test infrastructure.** The repo has no test runner installed (25-RESEARCH.md "Validation Architecture" confirms). CONTEXT.md explicitly says do NOT absorb test-infrastructure setup into this phase but also calls for a regression test for KI-07. Task 1 of this plan is a decision checkpoint. If the user elects to install vitest, that work lands as a separate Wave 0 task (NOT in this plan). If the user elects to ship test-free, Task 4's `<automated>` becomes a visual-verification note.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-code-health-bug-fixes/25-CONTEXT.md
@.planning/phases/25-code-health-bug-fixes/25-RESEARCH.md
@./AGENTS.md
@src/components/data-display.tsx
@src/components/table/data-table.tsx
@src/components/filters/filter-empty-state.tsx
@src/lib/columns/root-columns.ts
@src/hooks/use-filter-state.ts

<interfaces>
Root cause (from 25-RESEARCH.md "KI-07 deep-dive"):
- KNOWN-ISSUES.md mis-locates the bug in `use-filter-state.ts`. That hook is correct; DO NOT touch it.
- Real bug: at root level, the table renders `rootSummaryRows` = `buildPartnerSummaryRows(allData)` (one row per partner). All non-identity columns on summary rows have `enableColumnFilter: false` (see `src/lib/columns/root-columns.ts:45`). Filter columns BATCH and ACCOUNT_TYPE don't even exist on summary rows — they exist on the raw batch rows in `data.data`.
- Fix shape: apply `dimensionFilters` to the raw `data.data` BEFORE `buildPartnerSummaryRows` aggregates.

Surfaces to touch (ordered from 25-RESEARCH.md):
1. `src/components/data-display.tsx` — introduce `filteredRawData` memo; thread through 5 downstream consumers:
   - `rootSummaryRows` memo (lines ~666-683) switches from `allData`/`data.data` to `filteredRawData`
   - `CrossPartnerProvider` `allRows` prop — feed `filteredRawData` (currently `data.data`)
   - `SidebarDataPopulator` partner list — feed `filteredRawData`
   - Root-level sparkline — feed `filteredRawData`
   - KPI computation (`computeKpis` input) — feed `filteredRawData`
2. `src/components/data-display.tsx` — `tableData` memo (line ~175) currently filters by `drillState.partner` at partner level. Extend so that at ALL drill levels, `tableData` starts from `filteredRawData` so the root filter cascades.
3. `src/components/table/data-table.tsx:172` — currently passes `columnFilters` (dimensionFilters) into `useDataTable` only when `isRoot`. With the upstream fix, this pass-through is redundant at root and can stay or be removed — prefer: leave the pass-through in place to avoid collateral damage. Filter columns are already not on summary rows, so the pass-through is a no-op.
4. `src/components/filters/filter-empty-state.tsx` — already exists with "No results match your filters." copy. Update to match CONTEXT.md's "No rows match the filter." hint, OR keep existing copy (near-identical). Confirm the component already provides a one-click "Clear filter(s)" action; if not, add one.
5. `src/components/table/data-table.tsx` — empty-state detection: `!hasFilteredRows && hasActiveFilters && isRoot` (current guard at line ~326). With the upstream fix, `rootSummaryRows` may be empty directly → need an `effectiveData.length === 0 && hasActiveFilters` check that renders `<FilterEmptyState />` instead of the table.

Filter predicate shape (from research example, adapt to actual column types):
```tsx
const filteredRawData = useMemo(() => {
  if (!data?.data) return [];
  if (dimensionFilters.length === 0) return data.data;
  return data.data.filter((row) =>
    dimensionFilters.every((cf) => {
      const value = row[cf.id];
      return value != null && String(value) === String(cf.value);
    }),
  );
}, [data?.data, dimensionFilters]);
```

Note: `cf.value` may be a single value or an array (TanStack supports both). Handle both cases — if array, check membership; if scalar, check equality.

Cascade semantics edge case (25-RESEARCH.md Open Question 4): User sets `PARTNER_NAME=Acme` at root, then drills into Acme. The filter is redundant but idempotent — applying it to already-Acme-filtered data is a no-op. No special handling needed.

File overlap with Plan B: Plan B adds `<SectionErrorBoundary>` wrappers in `data-display.tsx`. Plan C threads a new memo through the same file. They touch different logical parts of the file (B: JSX wrappers; C: memos + prop threading). After Plan B commits, re-locate memos by searching for `useMemo` blocks and JSX consumers by component name. Wave 2 ordering ensures Plan B (and Plan A) commit first.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Test infrastructure decision for regression coverage</name>
  <decision>How should we verify the KI-07 fix — with an automated regression test (requires installing vitest), or visual-verification-only (no test runner install)?</decision>
  <context>
Repo has NO test runner (no vitest/jest/playwright, no `*.test.*` or `*.spec.*` files anywhere in src/). CONTEXT.md says "don't absorb test-infrastructure setup into this phase" AND "per-fix targeted regression tests for KI-07" — these are in tension when no infra exists.

KI-07 was a silent v2 regression that stayed hidden for a full release cycle. A regression test would catch it if it reappears during future refactors. Visual-only verification is faster now but carries forward-looking risk.
  </context>
  <options>
    <option id="option-a">
      <name>Install vitest + testing-library (Wave 0 task) and write a filter-predicate regression test</name>
      <pros>
- Catches future regressions of the same bug
- Sets up test infra that future phases can use (low marginal cost after this)
- Honors CONTEXT.md's "per-fix targeted regression tests" decision
      </pros>
      <cons>
- Adds ~4-6 new deps to package.json
- 1 extra plan task (or a separate micro-plan) for vitest.config.ts + tests/setup.ts + package.json script
- Contradicts CONTEXT.md's "don't absorb test-infra setup into this phase" boundary
      </cons>
    </option>
    <option id="option-b">
      <name>Ship test-free — rely on manual browser verification and document the gap</name>
      <pros>
- Honors CONTEXT.md's test-infra boundary strictly
- Ships faster; no dep additions
- Manual visual verification is what CONTEXT.md allows as the fallback
      </pros>
      <cons>
- No automated guard against re-regression
- Future Claude instances will have to re-verify by hand
- The specific predicate logic (filter-before-aggregate) is exactly the kind of thing tests catch
      </cons>
    </option>
  </options>
  <resume-signal>Select: option-a (install vitest), option-b (visual-only), or ask a question</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement filteredRawData memo and thread through downstream consumers</name>
  <files>
src/components/data-display.tsx
src/components/table/data-table.tsx
src/components/filters/filter-empty-state.tsx
  </files>
  <action>
Step 1 — read `src/components/data-display.tsx` end-to-end. Identify the current locations of:
- `data?.data` access points (the raw batch rows)
- `rootSummaryRows` memo (research says ~666-683)
- `tableData` memo (research says ~175)
- `CrossPartnerProvider` mount, `SidebarDataPopulator` mount, any sparkline mount, any KPI computation (`computeKpis`) call

If Plan B has already committed, the line numbers will have shifted; use symbol search not line numbers.

Step 2 — add the `filteredRawData` memo. Place it AFTER `data` and `dimensionFilters` are available in the component's closure, BEFORE the first downstream consumer:

```tsx
const filteredRawData = useMemo(() => {
  const rows = data?.data;
  if (!rows) return [];
  if (!dimensionFilters || dimensionFilters.length === 0) return rows;
  return rows.filter((row) =>
    dimensionFilters.every((cf) => {
      const value = (row as Record<string, unknown>)[cf.id];
      if (value == null) return false;
      // TanStack filter values can be scalars or arrays — handle both
      if (Array.isArray(cf.value)) {
        return cf.value.some((v) => String(v) === String(value));
      }
      return String(cf.value) === String(value);
    }),
  );
}, [data?.data, dimensionFilters]);
```

Step 3 — thread `filteredRawData` through the 5 downstream consumers:
- `rootSummaryRows` memo: change input from `allData`/`data.data` to `filteredRawData`. Update the dep array accordingly (`drillState.level`, `filteredRawData`).
- `CrossPartnerProvider` `allRows` prop: change from `data.data` to `filteredRawData`.
- `SidebarDataPopulator`: change the partner list source from `data.data` to `filteredRawData`.
- Any root-level sparkline data prop: switch to `filteredRawData`.
- KPI computation: input to `computeKpis` (or equivalent) becomes `filteredRawData`.

Step 4 — extend `tableData` memo so drill cascades preserve the root filter. Today the memo filters by `drillState.partner` at partner level using `data.data`. Change the source to `filteredRawData`:
```tsx
const tableData = useMemo(() => {
  const source = filteredRawData;
  if (drillState.level === 'root') return source;
  if (drillState.level === 'partner' && drillState.partner) {
    return source.filter((r) => r.PARTNER_NAME === drillState.partner);
  }
  // ... preserve existing batch-level logic, substituting source for data.data
}, [filteredRawData, drillState]);
```
Read the current tableData memo carefully and keep its existing shape; just substitute `filteredRawData` for `data.data`.

**Before modifying `tableData`, check if Plan D has committed.** If it has, the dep array should already use `filteredRawData` and `drillState` as object refs (not sub-properties like `drillState.level`, `drillState.partner`). Preserve that shape when threading `filteredRawData` — use `[filteredRawData, drillState]`, NOT `[filteredRawData, drillState.level, drillState.partner]`. This preserves Plan D's KI-12 Site 1 fix which eliminated stale-closure risk by using object identity in the dep array.

Step 5 — empty-state handling. In `src/components/table/data-table.tsx` (or wherever the root table render decision is made), add a guard:
```tsx
if (effectiveData.length === 0 && hasActiveFilters && isRoot) {
  return <FilterEmptyState onClearFilters={clearAll} />;
}
```
If a similar guard already exists at line ~326, extend it to also fire when `effectiveData` (summary rows) is empty due to upstream filtering — not only when TanStack's filtered row model is empty.

Step 6 — update `FilterEmptyState` copy to match CONTEXT.md. Current copy per research is "No results match your filters." Change primary line to "No rows match the filter." Confirm the component has a one-click "Clear filter" button that calls the `onClearFilters` prop. If not, add one.

Step 7 — DO NOT touch `src/hooks/use-filter-state.ts`. It has three deliberate `eslint-disable-next-line react-hooks/exhaustive-deps` (KI-09) that must not be regressed. The hook is correct; the bug is downstream.

Step 8 — DO NOT touch `src/lib/columns/root-columns.ts` to enable `enableColumnFilter` on summary-row columns. That would route filtering through TanStack at root, which CONTEXT.md explicitly rejects. The fix route is filter-before-aggregate.

Escalation: if threading `filteredRawData` through any consumer turns out to change observable behavior in a way beyond "fewer rows shown" (e.g. chart reference lines shift unexpectedly, KPI aggregates don't match expectations) STOP and report. Do not try-then-revert.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit &amp;&amp; pnpm lint 2>&amp;1 | grep -E "(error|warning)" | wc -l</automated>
  </verify>
  <done>
- `filteredRawData` memo exists in `data-display.tsx` and is used by rootSummaryRows, CrossPartnerProvider, SidebarDataPopulator, sparklines, KPI computation, and tableData
- `FilterEmptyState` shows "No rows match the filter" hint with a Clear filter action
- Empty-state guard fires when root filter matches zero partners
- TypeScript clean, lint no worse than baseline
- `use-filter-state.ts` and `root-columns.ts` untouched
- KI-12 Site 1 dep-array preserved — `tableData` dep array uses `filteredRawData` and `drillState` as object refs, not sub-properties (preserves Plan D's KI-12 fix)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of filter behavior across scenarios</name>
  <what-built>
- `filteredRawData` memo in `data-display.tsx`
- Threaded through rootSummaryRows, CrossPartnerProvider, SidebarDataPopulator, sparklines, KPIs, tableData
- Empty-state guard wired to FilterEmptyState with updated copy
  </what-built>
  <how-to-verify>
Run `pnpm dev` and test each scenario:

1. **Root filter reduces rows.** At root, apply a PARTNER_NAME filter for a known partner. Confirm:
   - Table shows ONE row (that partner), not all partners.
   - Chart (CrossPartnerTrajectoryChart) shows only that partner's trajectory.
   - KPI cards reflect only that partner's aggregates (not the full portfolio).
   - Sidebar partner list may or may not reflect the filter — confirm this matches user expectation. If the sidebar is a navigation control, it should show ALL partners regardless of filter; if it's a data-scoped list, it should reflect the filter. Check the existing behavior intent and preserve it.

2. **ACCOUNT_TYPE or BATCH filter works.** Apply a filter on a column that DOESN'T exist on summary rows (per research, ACCOUNT_TYPE is one). Confirm the table still reduces rows because filtering now happens on raw batch rows upstream. Verify chart and KPIs match.

3. **Drilldown cascade.** With a root filter active (say PARTNER_NAME=Acme), drill into Acme. Confirm:
   - The partner detail page still shows Acme's data.
   - Any batch-level filters continue to work.
   - Drilling back to root preserves the filter (it's in the URL).

4. **Zero-match empty state.** Apply a filter that matches zero rows (e.g. a PARTNER_NAME that doesn't exist). Confirm:
   - Table shows the FilterEmptyState card with "No rows match the filter" hint.
   - A "Clear filter" button is visible and clicking it removes the filter and restores the table.
   - No crash, no broken layout.

5. **Existing drill filters unchanged.** With no root filter, drill into a partner and apply a BATCH filter. Confirm this still works as before (no regression).

6. **use-filter-state.ts unchanged.** `git diff src/hooks/use-filter-state.ts` should be empty. Same for `src/lib/columns/root-columns.ts`.

If ANY scenario fails, STOP and report. Especially if KPI values look wrong — that's the filter-before-aggregate logic misaligned.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 scenarios pass, or describe which scenario failed and what you saw</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Write regression test (conditional on Task 1 decision) and close KI-07</name>
  <files>
docs/KNOWN-ISSUES.md
(conditionally) tests/data-display.filter.test.tsx or src/components/__tests__/data-display.filter.test.tsx
  </files>
  <action>
Part A — IF Task 1 chose option-a (vitest installed):
- Write a targeted regression test for the filter predicate. Minimum coverage:
  - With `dimensionFilters = []`, `filteredRawData` equals `data.data` by reference.
  - With a filter on a column that exists on raw rows (e.g. PARTNER_NAME=Acme), `filteredRawData` returns only rows where PARTNER_NAME is Acme.
  - With a filter on a column that DOESN'T exist on summary rows (ACCOUNT_TYPE), `filteredRawData` still filters — proving the upstream-application.
  - With a filter matching zero rows, `filteredRawData.length === 0`.
- Test file location follows the user's preference captured in Task 1. Default: `tests/data-display.filter.test.tsx`.
- Run `pnpm test` and confirm green.

Part B — IF Task 1 chose option-b (visual-only):
- Skip test authoring.
- In the Summary output for this plan, note explicitly: "Regression verified visually per user decision in Task 1 checkpoint; no automated test added."

Part C — in BOTH cases, close KI-07 in `docs/KNOWN-ISSUES.md`:

Append a resolution block to the KI-07 entry:
```markdown
**Resolved:** 2026-04-XX in Phase 25 Plan C. Root cause was filter-before-aggregate: at root, `rootSummaryRows = buildPartnerSummaryRows(data.data)` operated on unfiltered raw rows while filter columns either had `enableColumnFilter: false` or didn't exist on summary rows. Fix: introduced `filteredRawData` memo in `data-display.tsx` that applies dimensionFilters to raw data; threaded through rootSummaryRows, CrossPartnerProvider, SidebarDataPopulator, sparklines, KPIs, and tableData. Filter now cascades into drilldowns as a persistent constraint. Zero-match case renders `FilterEmptyState` with a one-click Clear filter action. Also: updated the KI-07 File(s) line — real site is `src/components/data-display.tsx`, not `src/hooks/use-filter-state.ts`.
```

Update the Summary table counts and UI/UX category count.

Commit: land this doc update in the SAME commit as the code fix. If visual verification happened in Task 3 without issues, the code + doc commit is one atomic change.
  </action>
  <verify>
    <automated>grep -A 2 "KI-07" docs/KNOWN-ISSUES.md | grep -c "Resolved"</automated>
  </verify>
  <done>
- KI-07 marked Resolved in KNOWN-ISSUES.md with accurate root-cause note
- Summary counts reconciled
- If vitest was chosen: regression test file exists and passes
- If visual-only: decision documented in the plan summary
  </done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` — clean
- `pnpm lint` — no new warnings
- `pnpm build` — completes
- Visual: all 6 scenarios in Task 3 pass
- If vitest installed: `pnpm test` — green
- `docs/KNOWN-ISSUES.md` KI-07 — Resolved block present with correct root-cause file reference
- `git diff src/hooks/use-filter-state.ts` — empty
- `git diff src/lib/columns/root-columns.ts` — empty
</verification>

<success_criteria>
- HEALTH-01 closed: root filter correctly reduces table rows AND chart AND KPIs
- Filter cascades into drilldowns as persistent constraint
- Zero-match case shows FilterEmptyState + Clear filter action
- No regression of existing drill-level filtering
- `use-filter-state.ts` and `root-columns.ts` untouched
- KI-07 marked Resolved in same commit as the fix
</success_criteria>

<output>
After completion, create `.planning/phases/25-code-health-bug-fixes/25-03-SUMMARY.md` with:
- Final answer to Task 1 decision (option-a or option-b)
- Diff summary for data-display.tsx: before/after count of `data.data` references and `filteredRawData` references
- List of downstream consumers now fed from `filteredRawData` (with file:line)
- Confirmation of Task 3 visual scenarios (which passed, any deviations)
- Confirmation that `use-filter-state.ts` and `root-columns.ts` were NOT modified
- Regression test file path (if applicable)
- Confirmation that KI-07 is closed
</output>
