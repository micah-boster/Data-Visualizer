---
phase: 25-code-health-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/charts/collection-curve-chart.tsx
  - src/components/charts/pivot-curve-data.ts
  - src/components/columns/column-picker-sidebar.tsx
  - src/components/data-display.tsx
  - src/components/table/sort-dialog.tsx
  - src/components/table/table-body.tsx
  - src/hooks/use-suggested-prompts.ts
  - docs/KNOWN-ISSUES.md
autonomous: true
requirements: [HEALTH-06]

must_haves:
  truths:
    - "pnpm lint reports zero @typescript-eslint/no-unused-vars warnings across the 7 flagged files"
    - "pnpm build completes with no new warnings introduced by import removal"
    - "KI-22 is marked resolved in docs/KNOWN-ISSUES.md"
  artifacts:
    - path: "src/components/charts/collection-curve-chart.tsx"
      provides: "Chart component without unused BatchAnomaly import"
    - path: "src/components/data-display.tsx"
      provides: "Orchestrator with dataUpdatedAt removed from destructuring"
    - path: "docs/KNOWN-ISSUES.md"
      provides: "KI-22 entry closed"
      contains: "Resolved"
  key_links:
    - from: "pnpm lint"
      to: "the 7 flagged files"
      via: "@typescript-eslint/no-unused-vars rule"
      pattern: "no-unused-vars"
---

<objective>
Plan A (safe cleanups): Remove 7 unused imports/symbols flagged by KI-22. Purely mechanical; no behavior change. Lowest-risk plan — ships first to reduce lint noise and clear the file diffs from the other parallel plans.

Purpose: Close HEALTH-06 and shrink the lint surface before the bigger refactors land.
Output: 7 files cleaned of unused symbols; KNOWN-ISSUES.md KI-22 closed; lint clean.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-code-health-bug-fixes/25-CONTEXT.md
@.planning/phases/25-code-health-bug-fixes/25-RESEARCH.md
@docs/KNOWN-ISSUES.md

<interfaces>
Inventory from 25-RESEARCH.md (KI-22 section, line numbers approximate — confirm via grep before deleting):

| File | Unused symbol | Notes |
|------|---------------|-------|
| src/components/charts/collection-curve-chart.tsx | `BatchAnomaly` (line 15 import) | Type import, no references |
| src/components/charts/pivot-curve-data.ts | `_metric` param (line 105) | Check ESLint config — underscore prefix may be allowed. If allowed, LEAVE with a comment. If not, remove. |
| src/components/columns/column-picker-sidebar.tsx | `IDENTITY_COLUMNS` (line 22 import) | Imported, not referenced |
| src/components/data-display.tsx | `dataUpdatedAt` (line 113, destructured) | Destructured from hook result, never read |
| src/components/table/sort-dialog.tsx | `config` (line 119 local var) | Assigned, never read after assignment |
| src/components/table/table-body.tsx | `useRef` | Import-level unused |
| src/hooks/use-suggested-prompts.ts | `KpiAggregates`, `TableState`, `Updater` | Type imports unused |

Verification tool: `pnpm lint` — flags these via `@typescript-eslint/no-unused-vars`. Run before and after.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline lint and confirm flagged symbols are still unused</name>
  <files>none (read-only verification)</files>
  <action>
Run `pnpm lint` from repo root and capture the output. Confirm that each of the 7 sites listed in the interfaces table above produces a `@typescript-eslint/no-unused-vars` warning (or equivalent). If any site is now USED (code has moved since research), remove it from this plan's scope and note in the summary.

For `_metric` in pivot-curve-data.ts specifically: check `eslint.config.mjs` for the `argsIgnorePattern` rule. If `^_` is allowed (standard `eslint-config-next` default IS to allow `_` prefix), then `_metric` is intentional — leave it and add a brief inline comment `// eslint-ignore-next-line intentional unused param — underscore prefix` is NOT needed; the prefix itself is the signal. Document the decision in the task result.

Do NOT modify any files in this task — just baseline and confirm.
  </action>
  <verify>
    <automated>pnpm lint 2>&1 | grep -E "(no-unused-vars|BatchAnomaly|IDENTITY_COLUMNS|dataUpdatedAt|KpiAggregates|TableState|Updater|useRef)" | wc -l</automated>
  </verify>
  <done>Baseline lint output captured; 7 sites confirmed still unused (or decision recorded for any that were promoted to used). `_metric` decision (fix vs leave) explicitly recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the 7 unused imports/symbols</name>
  <files>
src/components/charts/collection-curve-chart.tsx
src/components/charts/pivot-curve-data.ts
src/components/columns/column-picker-sidebar.tsx
src/components/data-display.tsx
src/components/table/sort-dialog.tsx
src/components/table/table-body.tsx
src/hooks/use-suggested-prompts.ts
  </files>
  <action>
For each file, remove ONLY the unused symbol(s) identified in Task 1. Do not touch anything else in the file — no reformatting, no reordering imports, no "while I'm here" changes.

Specific changes:
1. `src/components/charts/collection-curve-chart.tsx` — remove `BatchAnomaly` from the import list on line ~15. If it's the only symbol in that import, remove the entire import statement.
2. `src/components/charts/pivot-curve-data.ts` — if Task 1 determined `_metric` must be removed (underscore prefix NOT allowed by lint config), remove the parameter. If underscore prefix IS allowed, skip this file.
3. `src/components/columns/column-picker-sidebar.tsx` — remove `IDENTITY_COLUMNS` from the import list on line ~22.
4. `src/components/data-display.tsx` — remove `dataUpdatedAt` from the destructuring on line ~113. Confirm the surrounding destructured object is still syntactically valid.
5. `src/components/table/sort-dialog.tsx` — remove the `config` local variable declaration/assignment on line ~119 and any dead computation that built it.
6. `src/components/table/table-body.tsx` — remove `useRef` from the React import.
7. `src/hooks/use-suggested-prompts.ts` — remove `KpiAggregates`, `TableState`, `Updater` from the type imports.

After each file: TypeScript should still compile. Run `pnpm lint` after all 7 edits — zero `no-unused-vars` warnings should remain in these files.

Do NOT introduce a new import style, ordering, or whitespace convention. Diff should be pure deletions.
  </action>
  <verify>
    <automated>pnpm lint 2>&1 | grep -c "no-unused-vars" | tr -d ' '</automated>
  </verify>
  <done>All flagged symbols removed. `pnpm lint` shows zero `no-unused-vars` warnings in the 7 files. `pnpm build` passes (or if not run here, at minimum `pnpm tsc --noEmit` is clean).</done>
</task>

<task type="auto">
  <name>Task 3: Close KI-22 in KNOWN-ISSUES.md</name>
  <files>docs/KNOWN-ISSUES.md</files>
  <action>
Locate the KI-22 entry in `docs/KNOWN-ISSUES.md`. Append a resolution block at the bottom of the entry in the format established by the research doc:

```markdown
**Resolved:** 2026-04-XX in Phase 25 Plan A. Unused imports/symbols removed from the 7 flagged files. Verified via `pnpm lint` (zero no-unused-vars warnings across these files).
```

Use today's date (2026-04-16). Do NOT remove the original KI-22 text — append the resolution block so historical context is preserved.

Also update the Summary table counts at the top of the file: decrement the total count and the matching category row (KI-22 is in the Architecture category per the doc). If Plans B/C/D in this phase commit first and already updated the counts, reconcile rather than overwrite.

This update lands in the SAME commit as the import removals (Task 2) — do not batch.
  </action>
  <verify>
    <automated>grep -A 2 "KI-22" docs/KNOWN-ISSUES.md | grep -c "Resolved"</automated>
  </verify>
  <done>KI-22 entry has a Resolved block with today's date and Phase 25 Plan A reference. Summary table totals reconciled.</done>
</task>

</tasks>

<verification>
Final checks before this plan is marked complete:
- `pnpm lint` — zero `no-unused-vars` warnings across the 7 flagged files (sample command: `pnpm lint 2>&1 | grep no-unused-vars`)
- `pnpm build` — completes successfully with no new warnings
- `docs/KNOWN-ISSUES.md` — KI-22 shows a Resolved block
- Git diff — pure deletions in the 7 source files, plus the docs update. No reformatting noise.
</verification>

<success_criteria>
- HEALTH-06 closed (all 7 unused imports removed)
- Zero new lint warnings introduced
- KI-22 marked Resolved in KNOWN-ISSUES.md in the same commit as the code changes
</success_criteria>

<output>
After completion, create `.planning/phases/25-code-health-bug-fixes/25-01-SUMMARY.md` with:
- Files changed with brief diff summary
- Lint baseline before/after counts
- Note the `_metric` decision (removed vs left with underscore convention)
- Confirmation that KI-22 is closed
</output>
